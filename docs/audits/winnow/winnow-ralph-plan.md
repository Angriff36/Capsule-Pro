# Ralph Wiggum Remediation Plan

**Target:** Angriff36/Capsule-Pro
**Integrity score:** 47/100
**Summary:** Feature claims exceed implementation evidence. 1432 finding(s) across 373 file(s). 591 feature claim(s) extracted; 35 appear unimplemented or mocked.

## Priorities

Work top-to-bottom. Do not skip. Each task must pass its acceptance check before moving on.

### missing features

1. **organization/team management** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:62`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

2. **organization",** (multitenant)
   - Source: `apps/app/app/(authenticated)/analytics/activity-feed/page.tsx:15`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

3. **organization** (multitenant)
   - Source: `apps/app/app/(authenticated)/analytics/activity-feed/page.tsx:38`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

4. **organization to view the multi-location** (multitenant)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:122`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

5. **workspace** (multitenant)
   - Source: `apps/app/app/(authenticated)/command-board/page.tsx:107`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

6. **workspace to get into the work** (multitenant)
   - Source: `apps/app/app/(authenticated)/inventory/page.tsx:87`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

7. **workspace is a focused tool** (multitenant)
   - Source: `apps/app/app/(authenticated)/inventory/page.tsx:110`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

8. **workspace"** (multitenant)
   - Source: `apps/app/app/(authenticated)/inventory/page.tsx:112`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

9. **organization ID found</p>** (multitenant)
   - Source: `apps/app/app/(authenticated)/kitchen/page.tsx:14`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

10. **organization found</p>** (multitenant)
   - Source: `apps/app/app/(authenticated)/scheduling/requests/page.tsx:54`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

11. **Organization ID"** (multitenant)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:1107`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

12. **Organization ID{" "}** (multitenant)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:1211`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

13. **organization preferences, integrations, and access controls** (multitenant)
   - Source: `apps/app/app/(authenticated)/settings/page.tsx:18`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

14. **workspace and their roles** (multitenant)
   - Source: `apps/app/app/(authenticated)/settings/team/page.tsx:55`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

15. **organization management, you'll need to create a multi-tenancy schema** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:79`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

16. **organization: string;** (multitenant)
   - Source: `apps/app/app/(unauthenticated)/sign/contract/[token]/contract-signing-client.tsx:73`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

17. **organization,** (multitenant)
   - Source: `apps/app/app/(unauthenticated)/sign/contract/[token]/contract-signing-client.tsx:121`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

18. **organization}</p>** (multitenant)
   - Source: `apps/app/app/(unauthenticated)/sign/contract/[token]/contract-signing-client.tsx:207`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

19. **organization}** (multitenant)
   - Source: `apps/app/app/(unauthenticated)/sign/contract/[token]/contract-signing-client.tsx:474`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

20. **organization info** (multitenant)
   - Source: `apps/app/app/(unauthenticated)/sign/contract/[token]/page.tsx:132`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

21. **organization={tenant?** (multitenant)
   - Source: `apps/app/app/(unauthenticated)/sign/contract/[token]/page.tsx:183`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

22. **organization" logic as Auth** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/authjs.md:182`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

23. **organization() // if you want to use organization plugin** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/better-auth.md:92`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

24. **organization, check [organization plugin](https://better-auth** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/better-auth.md:355`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

25. **organization management like Clerk, this guide includes a multi-tenancy schema pattern to im** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:40`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

26. **Organization {** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:84`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

27. **organization Organization @relation(fields: [organizationId], references: [id], onDelete: Ca** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:102`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

28. **organization from user metadata** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:174`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

29. **organization management](#8-implement-organization-management)** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:413`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

30. **organization = await database** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:425`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

31. **organization;** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:443`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

32. **organization management](#8-implement-organization-management)[9** (multitenant)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:604`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

33. **organization management** (multitenant)
   - Source: `docs/llm/next-forge/docs/packages/authentication.md:36`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

34. **organization ID from the authenticated user is used as the Svix application UID, which is cr** (multitenant)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:44`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

35. **workspace:*",** (multitenant)
   - Source: `packages/auth/package.json:15`
   - Verdict: missing (0/100)
   - Why: No implementation evidence found.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

### surface only features

36. **CSV Export** (export)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/components/export-button.tsx:138`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/app/app/(authenticated)/events/[eventId]/components/export-button.tsx) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

37. **roles and permissions</li>** (rbac)
   - Source: `apps/app/app/(authenticated)/kitchen/team/page.tsx:76`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

38. **role-based access control** (rbac)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:40`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

39. **RBAC Enforcement** (rbac)
   - Source: `docs/audits/pass-11-auth-middleware.md:81`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

40. **RBAC architecture:**** (rbac)
   - Source: `docs/audits/pass-11-auth-middleware.md:83`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

41. **RBAC enforced through manifest runtime policy system** (rbac)
   - Source: `docs/audits/pass-11-auth-middleware.md:84`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

42. **RBAC on 22 Non-Manifest Routes**** (rbac)
   - Source: `docs/audits/pass-11-auth-middleware.md:88`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

43. **CSV export (not API-based)** (export)
   - Source: `docs/audits/pass-11-auth-middleware.md:184`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/app/app/(authenticated)/events/[eventId]/components/export-button.tsx) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

44. **RBAC on 22 routes | Non-manifest routes using `requireTenantId()` | Add role checks** (rbac)
   - Source: `docs/audits/pass-11-auth-middleware.md:408`
   - Verdict: surface_only (35/100)
   - Why: Feature surface exists (apps/api/__tests__/api/ably/auth.integration.test.ts) but no deep implementation evidence.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

### partial features

45. **Payment Form Component** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:2`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

46. **payments** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:4`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

47. **billing:checkout_started", {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:91`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

48. **payment** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:98`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

49. **payments", {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:99`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

50. **payment");** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:116`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

51. **payment = await response** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:119`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

52. **billing:checkout_completed", {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:121`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

53. **payment (in real implementation, this would call the payment gateway)** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:128`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

54. **payments/${payment** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:129`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

55. **payment method, create tokenized record** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:141`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

56. **payment-methods", {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:143`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

57. **payment:", error);** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:158`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

58. **Payment</h2>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:166`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

59. **Payment Amount</Label>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:179`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

60. **Payment Method */}** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:201`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

61. **Payment Method</Label>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:203`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

62. **payment method for future use** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:261`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

63. **Payment of ${new Intl** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/components/payment-form-client.tsx:288`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

64. **billing:invoice_viewed", {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/invoices/invoices-client.tsx:57`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

65. **payments",** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:71`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

66. **payments and reconciliation progress** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:73`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

67. **payments, and your ledger structure in one** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:208`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

68. **payments">Review payments</Link>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:227`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

69. **payment record{paymentCount === 1 ? "" : "s"}** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:253`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

70. **billing activity for the current tenant** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:271`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

71. **billing record** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:279`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

72. **payments across invoices, with method and completion status** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:328`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

73. **payment activity"** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:330`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

74. **payments recorded yet** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:335`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

75. **payment) => (** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/page.tsx:339`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

76. **Payment List Client Component** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:2`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

77. **payment records with filtering and actions** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:4`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

78. **Payment {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:37`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

79. **Payment[];** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:53`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

80. **payments, setPayments] = useState<Payment[]>([]);** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:83`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

81. **payments?${params}`);** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:106`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

82. **payments");** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:113`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

83. **payments/export");** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:126`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

84. **payments-${format(new Date(), "yyyy-MM-dd")}** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:131`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

85. **payment: Payment) => {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:138`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

86. **payment: Payment) =>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:185`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

87. **payment: Payment) => (** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:218`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

88. **payment)}** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:231`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

89. **payments found</p>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:260`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

90. **payments/new">** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:265`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

91. **Payments</h1>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:280`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

92. **payment{total !== 1 ? "s" : ""}** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:282`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

93. **payments/new")}>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:290`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

94. **payment[col** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/components/payment-list-client.tsx:355`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

95. **payment record** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:3`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

96. **payments against invoices with validation** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:4`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

97. **payments, create, accounting, form** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:6`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

98. **Payment method is required");** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:99`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

99. **payment"** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:121`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

100. **Payment recorded successfully");** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:125`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

101. **payment", {** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:128`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

102. **payments">** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:142`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

103. **payment record against an invoice** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:151`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

104. **Payment Details</CardTitle>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:159`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

105. **payment information** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:161`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

106. **Payment Method *</Label>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:196`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

107. **stripe, square"** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:247`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

108. **payments">Cancel</Link>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/new/page.tsx:267`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

109. **Payments</DisplayHeading>** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/page.tsx:144`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

110. **payment records, see what has cleared,** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/page.tsx:146`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

111. **payments with invoice, client, event, and settlement status** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/page.tsx:205`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

112. **payment records"** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/page.tsx:207`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

113. **payments have been recorded for this tenant yet** (payments)
   - Source: `apps/app/app/(authenticated)/accounting/payments/page.tsx:212`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

114. **Subscription error:",** (payments)
   - Source: `apps/app/app/(authenticated)/administrative/chat/components/admin-chat-client.tsx:659`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

115. **metrics: ClientLTVMetrics;** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/components/metrics-cards.tsx:13`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

116. **metrics, className }: MetricsCardsProps) {** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/components/metrics-cards.tsx:27`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

117. **metrics** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/components/metrics-cards.tsx:31`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

118. **metrics-cards";** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/components/clv-dashboard.tsx:10`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

119. **metrics,** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/components/clv-dashboard.tsx:45`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

120. **metrics={metrics} />** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/components/clv-dashboard.tsx:57`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

121. **dashboard";** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/page.tsx:5`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

122. **metrics = await getClientLTVMetrics();** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/page.tsx:8`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

123. **metrics={metrics} />;** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/page.tsx:11`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

124. **Analytics"** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/clients/page.tsx:17`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

125. **metrics, setMetrics] = useState<EventProfitabilityMetrics | null>(** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/events/components/profitability-dashboard.tsx:32`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

126. **analytics/events/profitability?period=${selectedPeriod}`** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/events/components/profitability-dashboard.tsx:54`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

127. **metrics) {** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/events/components/profitability-dashboard.tsx:88`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

128. **metrics will appear as events are completed** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/events/components/profitability-dashboard.tsx:432`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

129. **Metrics Section */}** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/events/components/profitability-dashboard.tsx:439`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

130. **Metrics</CardTitle>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/events/components/profitability-dashboard.tsx:504`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

131. **analytics";** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/finance/FinanceAnalyticsPageClient.tsx:25`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

132. **analytics** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/finance/FinanceAnalyticsPageClient.tsx:66`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

133. **payments: {** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/events/page.tsx:138`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

134. **payment) => sum + Number(payment** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/events/page.tsx:161`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

135. **dashboard data** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:36`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

136. **KPI {** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:50`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

137. **metrics: LocationMetrics;** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:97`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

138. **KPI[];** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:119`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

139. **kpi) => kpi** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:212`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

140. **kpi) =>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:220`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

141. **kpi** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:221`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

142. **Dashboard** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:272`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

143. **kpi) => (** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/multi-location-dashboard-client.tsx:400`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

144. **dashboard-client";** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:5`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

145. **Analytics Unavailable</h1>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:81`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

146. **analytics data** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:83`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

147. **metrics = locationMetrics** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:326`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

148. **metrics) metrics** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:327`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

149. **metrics]) => {** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:407`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

150. **metrics: {** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/multi-location/page.tsx:699`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

151. **Analytics</MonoLabel>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/page.tsx:50`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

152. **Analytics unavailable</DisplayHeading>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/page.tsx:51`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

153. **analytics data right now** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/page.tsx:53`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

154. **analytics/clients">Client analytics</Link>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/page.tsx:401`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

155. **analytics/financial">Financial detail</Link>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/page.tsx:404`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

156. **metrics"** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/page.tsx:429`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

157. **analytics right now** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/kitchen/page.tsx:71`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

158. **analytics page could not read tenant data** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/kitchen/page.tsx:77`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

159. **payments,{" "}** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:289`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

160. **payments, and ledger structure** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:380`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

161. **payments"** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:385`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

162. **Payments dashboard</div>** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:387`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

163. **dashboard</div>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:387`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

164. **analytics/events"** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:394`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

165. **analytics</div>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:396`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

166. **payment activity** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:489`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

167. **payment records tied back to invoices, clients, and events** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:492`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

168. **payments dashboard** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:499`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

169. **Payment</TableHead>** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:507`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

170. **payments found for this tenant yet** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:522`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

171. **dashboard tabs                               */** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/components/vega-chart.tsx:341`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

172. **payment activity for this** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/sales/page.tsx:234`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

173. **payments recorded for this tenant** (payments)
   - Source: `apps/app/app/(authenticated)/analytics/sales/page.tsx:285`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

174. **dashboard-skeleton";** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-wrapper.tsx:5`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

175. **dashboard to keep heavy libraries (@react-pdf/renderer, xlsx, recharts)** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-wrapper.tsx:7`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

176. **dashboard uses browser-only** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-wrapper.tsx:8`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

177. **dashboard-client")** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-wrapper.tsx:12`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

178. **kpi-card";** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-client.tsx:38`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

179. **metrics = useMemo(() => {** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-client.tsx:555`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

180. **metrics ? (** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-client.tsx:803`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

181. **metrics against the CALCS_Funnel** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-client.tsx:1388`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

182. **dashboard will appear after you upload an Excel workbook or CSV** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/sales/sales-dashboard-client.tsx:1457`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

183. **metrics = metricsByEmployee** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/page.tsx:243`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

184. **Analytics / Staff</MonoLabel>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/page.tsx:314`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

185. **analytics built from roster, timekeeping, assignment,** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/page.tsx:317`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

186. **dashboard shell** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/page.tsx:415`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

187. **metrics, setMetrics] = useState<EmployeePerformanceMetrics | null>(** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/components/employee-performance-dashboard.tsx:32`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

188. **analytics/staff/employees/${employeeId}?period=${selectedPeriod}`** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/components/employee-performance-dashboard.tsx:48`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

189. **analytics/staff/summary?period=${selectedPeriod}`** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/components/employee-performance-dashboard.tsx:54`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

190. **Metrics Analysis** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/components/employee-performance-dashboard.tsx:574`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

191. **Metrics by Role</CardTitle>** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/components/employee-performance-dashboard.tsx:579`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

192. **metrics over time** (dashboard)
   - Source: `apps/app/app/(authenticated)/analytics/staff/components/employee-performance-dashboard.tsx:651`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

193. **Webhooks",** (webhooks)
   - Source: `apps/app/app/(authenticated)/components/sidebar.tsx:76`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

194. **Analytics-aware UserButton wrapper** (dashboard)
   - Source: `apps/app/app/(authenticated)/components/tracked-user-button.tsx:9`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

195. **metrics, and render PageCanvas layout** (dashboard)
   - Source: `apps/app/app/(authenticated)/contracts/[contractId]/page.tsx:5`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

196. **metrics, contract terms,** (dashboard)
   - Source: `apps/app/app/(authenticated)/contracts/[contractId]/contract-detail-client.tsx:4`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

197. **Payment terms** (payments)
   - Source: `apps/app/app/(authenticated)/contracts/[contractId]/contract-detail-client.tsx:742`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

198. **metrics and SLA tracking** (dashboard)
   - Source: `apps/app/app/(authenticated)/contracts/[contractId]/contract-detail-client.tsx:817`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

199. **Payment Terms: </span>** (payments)
   - Source: `apps/app/app/(authenticated)/crm/clients/[id]/components/tabs/contact-info-tab.tsx:575`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

200. **Payment Terms</CardTitle>** (payments)
   - Source: `apps/app/app/(authenticated)/crm/clients/[id]/components/tabs/financial-tab.tsx:108`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

201. **Billing Contact** (payments)
   - Source: `apps/app/app/(authenticated)/crm/clients/[id]/components/tabs/contacts-tab.tsx:272`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

202. **Billing** (payments)
   - Source: `apps/app/app/(authenticated)/crm/clients/[id]/components/tabs/contacts-tab.tsx:351`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

203. **Billing",** (payments)
   - Source: `apps/app/app/(authenticated)/crm/clients/[id]/components/tabs/preferences-tab.tsx:64`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

204. **Payment Terms (days)** (payments)
   - Source: `apps/app/app/(authenticated)/crm/clients/new/page.tsx:356`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

205. **analytics/clients/actions/get-client-ltv";** (dashboard)
   - Source: `apps/app/app/(authenticated)/crm/page.tsx:32`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

206. **billing and reporting splits** (payments)
   - Source: `apps/app/app/(authenticated)/crm/segmentation/page.tsx:287`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

207. **automated variance tracking and** (automation)
   - Source: `apps/app/app/(authenticated)/cycle-counting/page.tsx:56`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

208. **dashboard (Connection string → Pooled) and ensure the project is not** (dashboard)
   - Source: `apps/app/app/(authenticated)/error.tsx:64`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

209. **billing and communication",** (payments)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/components/event-setup-checklist.tsx:75`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

210. **AI-powered insights (summary, tasks, suggestions)** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client.tsx:14`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

211. **queue</p>** (background)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/ai-insights-panel.tsx:219`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

212. **AI-powered features** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/lazy-ai-insights-panel.tsx:9`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

213. **automated follow-up tasks for this event** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/index.tsx:1468`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

214. **billing: <DollarSign className="h-4 w-4" />,** (payments)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:40`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

215. **automated-followups/list?eventId=${eventId}`** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:69`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

216. **automated-followups/commands/generate",** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:84`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

217. **automated-followups/commands/complete",** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:106`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

218. **automated-followups/commands/skip",** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:127`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

219. **Automated follow-up tasks for client management** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:164`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

220. **automated post-event tasks** (automation)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/follow-ups/page.tsx:189`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

221. **metrics, and line items",** (dashboard)
   - Source: `apps/app/app/(authenticated)/events/budgets/[budgetId]/page.tsx:49`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

222. **Dashboard"** (dashboard)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/page.tsx:62`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

223. **queue";** (background)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:67`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

224. **queue"** (background)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:736`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

225. **Queue** (background)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:739`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

226. **Queue"}** (background)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:1067`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

227. **Queue list** (background)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:1213`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

228. **AI-powered alerts based on equipment usage, maintenance history,** (automation)
   - Source: `apps/app/app/(authenticated)/kitchen/equipment/equipment-page-client.tsx:706`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

229. **Subscription error: ${String(error)}`** (payments)
   - Source: `apps/app/app/(authenticated)/kitchen/production-board-realtime.tsx:141`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

230. **queue when coming back online** (background)
   - Source: `apps/app/app/(authenticated)/kitchen/waste/mobile/page.tsx:137`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

231. **Queue for later sync** (background)
   - Source: `apps/app/app/(authenticated)/kitchen/waste/mobile/page.tsx:180`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

232. **analytics and reduction opportunities** (dashboard)
   - Source: `apps/app/app/(authenticated)/kitchen/waste/page.tsx:64`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

233. **queue" | "history">("queue");** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:195`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

234. **Queue state** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:197`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

235. **queue");** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:254`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

236. **queue") {** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:312`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

237. **queue")}** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:532`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

238. **Queue Tab */}** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:550`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

239. **queue" && (** (background)
   - Source: `apps/app/app/(authenticated)/payroll/approvals/page.tsx:551`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

240. **Automated email sequences for client onboarding, event follow-ups, and nurture campaigns** (automation)
   - Source: `apps/app/app/(authenticated)/marketing/page.tsx:27`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

241. **Analytics",** (dashboard)
   - Source: `apps/app/app/(authenticated)/marketing/page.tsx:33`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

242. **analytics/sales",** (dashboard)
   - Source: `apps/app/app/(authenticated)/marketing/page.tsx:36`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

243. **analytics — drive growth across every channel** (dashboard)
   - Source: `apps/app/app/(authenticated)/marketing/page.tsx:49`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

244. **Payment Finalized",** (payments)
   - Source: `apps/app/app/(authenticated)/payroll/runs/[runId]/components/approval-history-timeline.tsx:73`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

245. **Payment",** (payments)
   - Source: `apps/app/app/(authenticated)/payroll/runs/[runId]/components/approval-workflow-panel.tsx:105`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

246. **payment processing** (payments)
   - Source: `apps/app/app/(authenticated)/payroll/runs/[runId]/components/approval-workflow-panel.tsx:107`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

247. **Payment Complete",** (payments)
   - Source: `apps/app/app/(authenticated)/payroll/runs/[runId]/components/approval-workflow-panel.tsx:113`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

248. **payments have been processed** (payments)
   - Source: `apps/app/app/(authenticated)/payroll/runs/[runId]/components/approval-workflow-panel.tsx:115`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

249. **Payment Terms</span>** (payments)
   - Source: `apps/app/app/(authenticated)/procurement/vendor-contracts/[id]/page.tsx:262`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

250. **Payment Terms</Label>** (payments)
   - Source: `apps/app/app/(authenticated)/procurement/vendor-contracts/page.tsx:365`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

251. **metrics={** (dashboard)
   - Source: `apps/app/app/(authenticated)/scheduling/availability/components/availability-client.tsx:347`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

252. **Payment Terms:{" "}** (payments)
   - Source: `apps/app/app/(authenticated)/procurement/vendors/[id]/page.tsx:489`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

253. **Queue</h1>** (background)
   - Source: `apps/app/app/(authenticated)/scheduling/requests/page.tsx:156`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

254. **analytics } from "@repo/analytics";** (dashboard)
   - Source: `apps/app/app/(authenticated)/search/page.tsx:3`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

255. **automated email is triggered** (automation)
   - Source: `apps/app/app/(authenticated)/settings/email-workflows/[id]/page.tsx:226`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

256. **Automate email notifications based on event triggers** (automation)
   - Source: `apps/app/app/(authenticated)/settings/email-workflows/components/email-workflows-client.tsx:135`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

257. **email notifications based on event triggers** (notifications)
   - Source: `apps/app/app/(authenticated)/settings/email-workflows/components/email-workflows-client.tsx:135`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 16 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

258. **automated email workflows to send notifications when events** (automation)
   - Source: `apps/app/app/(authenticated)/settings/email-workflows/components/email-workflows-client.tsx:240`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

259. **automated email trigger** (automation)
   - Source: `apps/app/app/(authenticated)/settings/email-workflows/new/page.tsx:127`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

260. **Webhook Secret"** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:424`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

261. **webhook-secret">** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:535`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

262. **Webhook Secret{" "}** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:536`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

263. **webhook-secret"** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:540`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

264. **webhooks to push real-time events to external services** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/page.tsx:13`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

265. **webhooks-client";** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/page.tsx:1`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

266. **Webhook,** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:52`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

267. **webhook?: { name: string; url: string };** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:114`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

268. **Webhook form dialog** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:225`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

269. **webhook: WebhookConfig | null;** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:259`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

270. **webhook) {** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:266`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

271. **webhook** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:268`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

272. **webhook, open]);** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:280`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

273. **webhook ? "Edit Webhook" : "New Webhook"}</DialogTitle>** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:303`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

274. **webhook endpoint** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:307`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

275. **webhooks"** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:328`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

276. **webhook ? "(unchanged)" : "Optional"}** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:341`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

277. **webhook ? "Update" : "Create"}** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:447`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

278. **webhooks");** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:460`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

279. **webhooks, setWebhooks] = useState<WebhookConfig[]>([]);** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:463`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

280. **webhooks ?? []);** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:501`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

281. **webhooks/delivery-logs?limit=50&offset=${offset}`** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:513`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

282. **webhooks/dlq?limit=50&unresolved=true&offset=${offset}`** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:534`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

283. **webhooks/${editingWebhook** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:580`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

284. **webhooks", {** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:585`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

285. **Webhook updated" : "Webhook created");** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:592`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

286. **webhook`,** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:610`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

287. **webhooks/${wh** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:617`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

288. **Webhook ${next}`);** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:623`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

289. **webhook",** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:635`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

290. **Webhook deleted");** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:646`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

291. **webhooks/dlq/${entry** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:666`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

292. **webhooks/retry", {** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:710`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

293. **webhooks** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:729`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

294. **webhooks…** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:742`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

295. **Webhooks</DisplayHeading>** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:754`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

296. **webhooks to push real-time events to your** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:756`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

297. **Metrics */}** (dashboard)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:778`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

298. **Webhooks</MetricLabel>** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:781`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

299. **Queue</MetricLabel>** (background)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:793`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

300. **webhooks">** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:802`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

301. **Webhook className="mr-1** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:807`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

302. **Webhooks tab ---- */}** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:816`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

303. **Webhook className="mx-auto h-10 w-10 text-muted-foreground/40" />** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:820`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

304. **webhooks configured** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:822`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

305. **webhook to receive real-time event notifications** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:825`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

306. **Queue tab ---- */}** (background)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:1035`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

307. **Webhook</TableHead>** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:1056`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

308. **webhook?** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:1078`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

309. **webhook={editingWebhook}** (webhooks)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:1141`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

310. **SMS Notifications** (notifications)
   - Source: `apps/app/app/(authenticated)/settings/notifications/page.tsx:11`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 16 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

311. **queue the action** (background)
   - Source: `apps/app/app/(authenticated)/staff/mobile/timeclock/page.tsx:340`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

312. **queue indicator */}** (background)
   - Source: `apps/app/app/(authenticated)/staff/mobile/timeclock/page.tsx:570`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

313. **metrics, reviews, and development tracking** (dashboard)
   - Source: `apps/app/app/(authenticated)/staff/page.tsx:34`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

314. **AI-powered coverage** (automation)
   - Source: `apps/app/app/(authenticated)/staffing/layout.tsx:35`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

315. **AI-powered staffing recommendations for events",** (automation)
   - Source: `apps/app/app/(authenticated)/staffing/recommendations/page.tsx:9`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

316. **AI-powered staffing recommendations based on event details** (automation)
   - Source: `apps/app/app/(authenticated)/staffing/recommendations/staffing-recommendations-client.tsx:82`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

317. **AI-powered suggestions with action handling, and event summary generation** (automation)
   - Source: `apps/app/app/(authenticated)/tools/ai/ai-client.tsx:4`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

318. **AI-powered recommendations** (automation)
   - Source: `apps/app/app/(authenticated)/tools/ai/ai-client.tsx:423`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

319. **AI-powered event summary** (automation)
   - Source: `apps/app/app/(authenticated)/tools/ai/ai-client.tsx:623`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

320. **AI-powered task plan** (automation)
   - Source: `apps/app/app/(authenticated)/tools/ai/bulk-task-generator.tsx:650`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 1 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

321. **dashboard coming soon")}>** (dashboard)
   - Source: `apps/app/app/(authenticated)/warehouse/receiving/page.tsx:259`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

322. **Webhooks</h1>** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/page.tsx:8`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

323. **Webhook {** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:35`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

324. **webhooks" | "delivery-logs" | "dlq";** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:89`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

325. **Webhook Dialog** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:221`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

326. **Webhook | null;** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:231`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

327. **webhooks/${editing** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:304`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

328. **webhook");** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:315`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

329. **Webhook "${name** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:320`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

330. **webhook"** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:327`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

331. **Webhook" : "Create Webhook"}</h2>** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:343`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

332. **webhook configuration"** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:346`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

333. **webhook endpoint"}** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:347`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

334. **Webhook"}** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:549`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

335. **webhook: Webhook | null;** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:568`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

336. **webhook)) {** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:571`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

337. **Webhook Configuration</p>** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:581`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

338. **Webhooks state** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:706`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

339. **webhooks, setWebhooks] = useState<Webhook[]>([]);** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:707`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

340. **Webhook | null>(null);** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:730`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

341. **webhooks/delivery-logs?limit=50"** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:776`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

342. **webhooks/dlq?limit=50&unresolved=true"** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:800`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

343. **Webhook ${newStatus === "active" ? "activated" : "paused"}`** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:863`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

344. **webhooks/${confirmAction** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:883`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

345. **webhooks/dlq/${confirmAction** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:909`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

346. **Webhooks</p>** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1023`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

347. **webhooks", label: "Webhooks" },** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1075`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

348. **Queue" },** (background)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1077`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

349. **WEBHOOKS TAB ====== */}** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1095`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

350. **webhooks" && (** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1096`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

351. **Webhooks</h2>** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1100`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

352. **webhook to receive event notifications** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1174`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

353. **webhooks are triggered** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1397`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

354. **Queue</h2>** (background)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1495`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

355. **queue is empty** (background)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1535`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

356. **webhook delivery and monitoring</p>** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1637`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

357. **webhook delivery** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1654`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

358. **webhook permanently** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1658`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

359. **Queue for manual review</li>** (background)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1668`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

360. **webhook={detailWebhook}** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1709`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

361. **webhook "${confirmAction** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1725`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

362. **Webhook Status"** (webhooks)
   - Source: `apps/app/app/(dev-console)/dev-console/webhooks/webhooks-client.tsx:1740`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

363. **metrics and supplier performance trends** (dashboard)
   - Source: `apps/app/app/(authenticated)/warehouse/receiving/reports/page.tsx:170`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

364. **metrics will appear** (dashboard)
   - Source: `apps/app/app/(authenticated)/warehouse/receiving/reports/page.tsx:230`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

365. **Analytics wrapper around Clerk's <SignIn>** (dashboard)
   - Source: `apps/app/app/(unauthenticated)/sign-in/[[...sign-in]]/sign-in-with-analytics.tsx:9`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

366. **Analytics wrapper around Clerk's <SignUp>** (dashboard)
   - Source: `apps/app/app/(unauthenticated)/sign-up/[[...sign-up]]/sign-up-with-analytics.tsx:9`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

367. **stripe/route** (payments)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/better-auth.md:331`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

368. **webhooks/stripe/route** (webhooks)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/better-auth.md:331`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

369. **dashboard')) {** (dashboard)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:243`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

370. **dashboard');** (dashboard)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:290`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

371. **dashboard';** (dashboard)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:491`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

372. **subscription } } = supabase** (payments)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:543`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

373. **subscription** (payments)
   - Source: `docs/llm/next-forge/docs/migrations/authentication/supabase.md:547`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

374. **Webhooks](#webhooks)** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/authentication.md:46`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

375. **webhooks to handle authentication events and you can send these to your application** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/authentication.md:48`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

376. **webhooks locally, so you'll have to test them in a staging environment** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/authentication.md:56`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

377. **Webhooks](#webhooks)[Email Templates](#email-templates)[Local Development](#local-develo** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/authentication.md:60`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

378. **webhooks from other services** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:36`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

379. **webhook handlers for several key services** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:38`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

380. **Payment Events](#payment-events)** (payments)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:40`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

381. **Payment events](/en/packages/payments) are handled in the `POST /webhooks/payments` rou** (payments)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:42`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

382. **webhooks/payments` route in the `api` app** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:42`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

383. **Stripe CLI to forward webhooks to your local server** (payments)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:44`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

384. **webhooks locally, we've configured the Stripe CLI to forward webhooks to your local serv** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:44`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

385. **webhooks/auth` route in the `api` app** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:48`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

386. **webhook events you need in your Clerk project settings** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:50`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

387. **webhooks is to notify your application when a database record is created, updated, or de** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:58`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

388. **cron jobs or other methods** (background)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:58`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

389. **Webhooks](https://supabase** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:60`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

390. **Payment Events](#payment-events)[Authentication Events](#authentication-events)[Local D** (payments)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:64`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

391. **webhooks/inbound** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/inbound.md:66`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

392. **Cron Jobs** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:34`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

393. **cron jobs feature to trigger Next** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:38`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

394. **webhooks and other such tasks isolated from our UI** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:46`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

395. **cron jobs to any app, we use the `api` app to keep our background jobs, webhooks and** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:46`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

396. **cron` folder** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:48`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

397. **cron/[your-function]/route** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:50`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

398. **cron/keep-alive",** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:73`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

399. **cron jobs](#how-vercel-triggers-cron-jobs)** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:80`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

400. **cron job, Vercel makes an HTTP GET request to your project's production deployment U** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:82`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

401. **cron/keep-alive** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:85`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

402. **dashboard or just hit the endpoint with a standard HTTP request** (dashboard)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:90`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

403. **cron job manually, you can either do so in the Vercel dashboard or just hit the endp** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:90`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

404. **cron jobs](#how-vercel-triggers-cron-jobs)[Triggering functions manually](#triggerin** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:100`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

405. **cron** (background)
   - Source: `docs/llm/next-forge/docs/packages/cron.md:102`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

406. **webhook security)** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:3`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

407. **payments, packages/webhooks)** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:8`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

408. **webhooks)** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:8`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

409. **webhook event drops, cross-tenant data access in forecasts, and a payload injection in** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:19`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

410. **webhooks(** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:29`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

411. **Cron Auth Accepts Spoofable Header**** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:39`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

412. **Cron routes verify `Authorization: Bearer ${CRON_SECRET}`, but `cron/inventory-audit** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:40`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

413. **Webhook Routes Lack Signature Verification**** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:43`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

414. **webhook/route** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:44`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

415. **webhook signature** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:45`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

416. **webhooks/supplier-catalog` correctly uses HMAC-SHA256 with `timingSafeEqual` — this is t** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:46`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

417. **webhook now has Resend HMAC-SHA256 signature verification with replay protection** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:48`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

418. **payments, accounting/invoices, logistics/dispatch, inventory/reorder-suggestions, etc** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:89`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

419. **Payments (`packages/payments/`)** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:307`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

420. **Stripe** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:309`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

421. **Stripe Key in Client Bundle Risk**** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:311`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

422. **payments/index** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:312`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

423. **Stripe secret key from entering client bundles** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:313`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

424. **payments/ai** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:317`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

425. **Stripe key, granting access to the entire account** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:318`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

426. **Webhook Secret**** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:321`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

427. **payments/keys** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:322`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

428. **webhook endpoints exist without this env var, signature verification is skipped, allowi** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:323`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

429. **Webhooks (`packages/webhooks/`)** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:325`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

430. **webhooks/lib/svix** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:330`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

431. **webhook deliveries** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:353`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

432. **webhook drops | `packages/webhooks/lib/svix** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:365`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

433. **Webhook payload injection | `packages/webhooks/lib/svix** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:366`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

434. **Webhook signature missing | `collaboration/notifications/email/webhook`, `sms/webhook`** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:374`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

435. **Stripe webhook secret | `packages/payments/keys** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:375`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

436. **webhook secret | `packages/payments/keys** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:375`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

437. **webhook retry | `packages/webhooks/lib/svix** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:400`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

438. **webhook receivers, cron)** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:407`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

439. **cron)** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:407`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

440. **Stripe API Version** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:540`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

441. **Stripe deprecates this version, the integration may silently break or receive unexpect** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:543`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

442. **payments/` (entire package)** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:547`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

443. **checkout** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:548`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

444. **webhook handler processes `checkout** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:548`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

445. **Dashboard are never reflected in the application** (dashboard)
   - Source: `docs/audits/pass-11-auth-middleware.md:548`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

446. **Webhook Handler Does Full User Scan** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:550`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

447. **payments/route** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:552`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

448. **webhooks/payments/route** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:552`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

449. **webhooks/keys** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:562`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

450. **payments/`, `packages/webhooks/`** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:579`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

451. **webhooks/`** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:579`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

452. **payments) and data synchronization (supplier connectors), this is a significant reliabil** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:580`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

453. **payments/` | Add handlers for `charge** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:597`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

454. **Webhook Security Deep-Dive** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:604`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

455. **webhook receiver deep-audit, route-level auth re-verification, integration services re-** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:607`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

456. **webhook as the "gold standard" without verifying the conditional signature check** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:608`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

457. **Webhook Security** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:618`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

458. **Webhook Body Round-Trip Breaks Signature Verification** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:639`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

459. **webhooks/auth/route** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:641`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

460. **webhook handler reads the body via `request** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:642`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

461. **Stripe webhook handler at `apps/api/app/webhooks/payments/route** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:643`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

462. **webhook handler at `apps/api/app/webhooks/payments/route** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:643`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

463. **webhooks may be rejected, causing user creation/update events to be silently lost** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:644`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

464. **Webhook Signature Check Bypassed by Omitting Header** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:647`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

465. **webhooks/supplier-catalog/route** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:649`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

466. **Webhook Body** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:655`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

467. **webhook body is logged: `log** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:658`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

468. **webhook performs a raw SQL `$queryRaw` lookup by `email_id` (Resend ID) without any `te** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:665`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

469. **Cron Endpoints** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:679`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

470. **cron/inventory-audit/route** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:682`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

471. **cron/idempotency-cleanup/route** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:683`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

472. **cron/email-reminders/route** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:684`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

473. **cron/contract-expiration-alerts/route** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:685`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

474. **webhook-retry/route** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:686`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

475. **cron/webhook-retry/route** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:686`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

476. **cron endpoints have `verifyCronAuth()` functions that return `true` when `CRON_SECRE** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:687`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

477. **cron endpoints return 503 when `CRON_SECRET` is not set (follow the `inventory-audit** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:689`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

478. **Cron Retry Uses Timing-Unsafe Bearer Token Comparison** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:705`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

479. **Stripe Event Returned in Webhook Response** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:728`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

480. **Webhook Response** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:728`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

481. **Stripe event object is returned: `NextResponse** (payments)
   - Source: `docs/audits/pass-11-auth-middleware.md:731`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

482. **webhook signatures are correct** — HMAC-SHA256 with timestamp-prefixed payload, standar** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:750`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

483. **webhook body round-trip) |** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:762`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

484. **webhook log), AE2-A06 (unscoped email webhook) |** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:763`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

485. **Webhook body round-trip | **TIER 0** | Replace `request** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:773`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

486. **webhook log | **TIER 1** | Remove `body` from `log** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:775`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

487. **webhook | **TIER 1** | Add HMAC signature verification (as A-03 recommends) AND add `te** (webhooks)
   - Source: `docs/audits/pass-11-auth-middleware.md:776`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

488. **Cron timing-unsafe comparison | **TIER 3** | Replace string comparison with `crypto** (background)
   - Source: `docs/audits/pass-11-auth-middleware.md:780`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

489. **webhooks to your users using Svix** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:36`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

490. **webhooks to your users using [Svix](https://www** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:38`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

491. **Webhooks are automatically enabled by the existence of the `SVIX_TOKEN` environment vari** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:40`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

492. **webhook](#send-a-webhook)** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:48`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

493. **webhook, simply use the `send` function from the `@repo/webhooks` package:** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:50`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

494. **webhooks } from '@repo/webhooks';** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:53`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

495. **webhook endpoints](#add-webhook-endpoints)** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:62`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

496. **webhooks subscriptions** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:64`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

497. **dashboard](https://docs** (dashboard)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:64`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

498. **webhooks` and use the returned URL in an `iframe` on your dashboard** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:66`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

499. **webhooks` that you can use as a starting point** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:78`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

500. **webhook](#send-a-webhook)[Add webhook endpoints](#add-webhook-endpoints)** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:82`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

501. **webhooks/outbound** (webhooks)
   - Source: `docs/llm/next-forge/docs/packages/webhooks/outbound.md:84`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

502. **Webhook System** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:1`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

503. **webhooks for specific entity types and events (create, update, delete)** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:7`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

504. **webhook payloads to external URLs with custom data formats** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:8`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

505. **webhook deliveries with exponential backoff** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:9`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

506. **webhook delivery attempts and results** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:10`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

507. **webhook authentication (API keys, signatures)** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:11`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

508. **webhooks per integration** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:12`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

509. **webhook handling (receiving webhooks from external systems)** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:15`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

510. **Webhook payload transformation or mapping UI** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:16`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

511. **Webhook testing or debugging tools** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:17`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

512. **Webhook analytics or reporting** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:18`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

513. **analytics or reporting** (dashboard)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:18`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

514. **Webhook payloads must never include sensitive data without proper authorization** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:21`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

515. **Webhook deliveries must never be lost; failed deliveries must retry** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:22`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

516. **Webhook URLs must never be called without proper authentication if configured** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:23`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

517. **Webhook retries must never continue indefinitely; must have max retry limit** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:24`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

518. **Webhook deliveries must never block main application operations** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:25`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

519. **Webhook payloads must never be sent to invalid or unreachable URLs without error handli** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:26`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

520. **webhook for event updates → webhook created and enabled** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:29`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

521. **webhook payload sent to configured URL** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:30`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

522. **Webhook delivery fails → retry scheduled with backoff** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:31`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

523. **webhook logs → shows delivery attempts and results** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:32`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

524. **webhook → no payloads sent while disabled** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:33`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

525. **Webhook retry limit reached → delivery marked as failed, logged** (webhooks)
   - Source: `specs/webhook-outbound-integrations_TODO/webhook-outbound-integrations.md:34`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

526. **Dashboard, not in code** (dashboard)
   - Source: `planning/auth-implementation.md:68`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

527. **Dashboard → User & Authentication → Social Connections → GitHub**** (dashboard)
   - Source: `planning/auth-implementation.md:69`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

528. **Webhook Handler Verification** (webhooks)
   - Source: `planning/auth-implementation.md:73`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

529. **webhook handler exists at `/webhooks/clerk` | 10 min |** (webhooks)
   - Source: `planning/auth-implementation.md:81`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 11 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

530. **Dashboard config, not a code change | — |** (dashboard)
   - Source: `planning/auth-implementation.md:83`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 5 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

531. **payments are due according to the payment schedule outlined in the proposal** (payments)
   - Source: `apps/api/app/api/events/contracts/[id]/pdf/route.tsx:155`
   - Verdict: partial (60/100)
   - Why: Found real evidence but also 51 mock/slop signal(s) nearby.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

### implemented features

532. **live in the `agents/` folder** (realtime)
   - Source: `.codex/skills/.system/skill-creator/references/openai_yaml.md:3`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

533. **Live ledger accounts for the current tenant, pulled directly from** (realtime)
   - Source: `apps/app/app/(authenticated)/accounting/chart-of-accounts/page.tsx:76`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

534. **Real-time updates from leadership and operations** (realtime)
   - Source: `apps/app/app/(authenticated)/administrative/chat/components/admin-chat-client.tsx:963`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

535. **Live" : "Connecting"}** (realtime)
   - Source: `apps/app/app/(authenticated)/administrative/chat/components/admin-chat-client.tsx:967`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

536. **Live staffing gaps across locations",** (realtime)
   - Source: `apps/app/app/(authenticated)/administrative/overview-boards/page.tsx:390`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

537. **real-time updates and filtering capabilities** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/components/activity-feed-client.tsx:5`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

538. **real-time updates** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/components/activity-feed-client.tsx:134`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

539. **Live event budget, invoicing, and report status for the latest 12** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/events/page.tsx:199`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

540. **live tenant data** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/events/page.tsx:262`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

541. **Live recipes",** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/kitchen/page.tsx:263`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

542. **live prep, waste, and** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/kitchen/page.tsx:291`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

543. **live recipe catalog behind kitchen** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/kitchen/page.tsx:492`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

544. **Live receivables, cash collection, and budget variance for the current** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/finance/page.tsx:261`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

545. **Live pipeline, proposal, invoice, and payment activity for this** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/sales/page.tsx:234`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

546. **Live staff analytics built from roster, timekeeping, assignment,** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/staff/page.tsx:317`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

547. **live work recently** (realtime)
   - Source: `apps/app/app/(authenticated)/analytics/staff/page.tsx:340`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

548. **streaming" || status === "submitted";** (realtime)
   - Source: `apps/app/app/(authenticated)/components/ai-assistant/ai-assistant-panel.tsx:31`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

549. **Live pipeline snapshot from CRM proposals** (realtime)
   - Source: `apps/app/app/(authenticated)/crm/pipeline/page.tsx:186`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

550. **real-time state updates via server action mutations** (realtime)
   - Source: `apps/app/app/(authenticated)/crm/proposals/proposals-page-client.tsx:5`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

551. **live and each segment links into a filtered client list** (realtime)
   - Source: `apps/app/app/(authenticated)/crm/segmentation/page.tsx:239`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

552. **live-now"** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:69`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

553. **live-now") && !isRelatedLive) {** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:287`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

554. **live-now")}** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:699`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

555. **live-now") ? "default" : "outline"** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:702`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

556. **Live now** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:706`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

557. **Live"** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:987`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

558. **Live for ${formatDuration(relatedTimeSince)}`** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-explorer.tsx:1045`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

559. **Live readiness across guests, tasks, and inventory** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/event-overview-card.tsx:161`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

560. **Live";** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/index.tsx:89`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

561. **Live for ${fmt(now** (realtime)
   - Source: `apps/app/app/(authenticated)/events/[eventId]/event-details-client/index.tsx:296`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

562. **real-time conflict detection** (realtime)
   - Source: `apps/app/app/(authenticated)/events/components/guest-management.tsx:3`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

563. **live-now" | "starting-soon" | "high-capacity" | "sold-out";** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:68`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

564. **live" | "finished";** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:69`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

565. **live: {** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:89`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

566. **Live",** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:90`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

567. **live-now") && !event** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:492`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

568. **Live service visibility, timeline control, and rapid actions** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:712`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

569. **Live operations** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:929`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

570. **Live for ${formatDuration(now** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:955`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

571. **Live ${formatDuration(now** (realtime)
   - Source: `apps/app/app/(authenticated)/events/kitchen-dashboard/kitchen-dashboard-client.tsx:1225`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

572. **Real-time temperature monitoring and probe management** (realtime)
   - Source: `apps/app/app/(authenticated)/kitchen/iot/iot-page-client.tsx:196`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

573. **Real-time temperature monitoring, probe management, and IoT alerts",** (realtime)
   - Source: `apps/app/app/(authenticated)/kitchen/iot/page.tsx:10`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

574. **real-time connection** (realtime)
   - Source: `apps/app/app/(authenticated)/kitchen/production-board-realtime.tsx:50`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

575. **real-time connection"** (realtime)
   - Source: `apps/app/app/(authenticated)/kitchen/production-board-realtime.tsx:53`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

576. **real-time is optional** (realtime)
   - Source: `apps/app/app/(authenticated)/kitchen/production-board-realtime.tsx:99`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

577. **Live" : "Paused"}** (realtime)
   - Source: `apps/app/app/(authenticated)/kitchen/recipes/components/menu-card.tsx:112`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

578. **Live tracking",** (realtime)
   - Source: `apps/app/app/(authenticated)/logistics/page.tsx:25`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

579. **Real-time GPS tracking for active deliveries and arrival ETAs** (realtime)
   - Source: `apps/app/app/(authenticated)/logistics/page.tsx:27`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

580. **Real-time tracking for active deliveries** (realtime)
   - Source: `apps/app/app/(authenticated)/logistics/tracking/page.tsx:337`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

581. **real-time tracking here** (realtime)
   - Source: `apps/app/app/(authenticated)/logistics/tracking/page.tsx:407`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

582. **Live Data</h3>** (realtime)
   - Source: `apps/app/app/(authenticated)/logistics/tracking/page.tsx:552`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

583. **live open-shift bids** (realtime)
   - Source: `apps/app/app/(authenticated)/scheduling/page.tsx:444`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

584. **Live view of shift starts and currently uncovered slots** (realtime)
   - Source: `apps/app/app/(authenticated)/scheduling/page.tsx:634`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

585. **live connection or sync is required** (realtime)
   - Source: `apps/app/app/(authenticated)/settings/integrations/page.tsx:1775`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

586. **real-time events to external services** (realtime)
   - Source: `apps/app/app/(authenticated)/settings/page.tsx:13`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

587. **real-time events to your** (realtime)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:756`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

588. **real-time event notifications** (realtime)
   - Source: `apps/app/app/(authenticated)/settings/webhooks/webhooks-client.tsx:825`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

589. **Live coverage across every location** (realtime)
   - Source: `apps/app/app/(authenticated)/staffing/page.tsx:156`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

590. **real-time events for that tenant** (realtime)
   - Source: `docs/audits/pass-11-auth-middleware.md:76`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

591. **live keys** (realtime)
   - Source: `docs/audits/pass-11-auth-middleware.md:313`
   - Verdict: implemented (90/100)
   - Why: Evidence in apps/api/__tests__/api/ably/auth.integration.test.ts: Realtime subscription/socket present.
   - Acceptance: implement real backend path, wire UI, add integration test that fails without the feature.

## Definition of done

- Every claimed feature has real server code, validation, error handling, and a test that exercises a non-mocked path.
- No Math.random metrics or static chart arrays on dashboards.
- Webhook routes verify signatures.
- Env vars referenced in code or removed from .env.example.
- No `return { ok: true }` shortcuts in routes claiming to do work.
