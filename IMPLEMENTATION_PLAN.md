# Manifest Entity Interconnection — Implementation Plan

_Last updated: 2026-06-13 (re-verified against current source via parallel code audit; corrections folded in inline)_

> **Second verification pass (2026-06-13):** a 6-agent parallel re-audit re-confirmed the highest-leverage claims against live source. All checks held: the `payload.result.*` no-op class (reactions.manifest:12-151, file is 196 lines), `EventCreated→BattleBoard` correct/DONE (line 164 `payload.result.id` valid on a create), the `ProcessInvoicePayment` saga vs. Payment→Invoice reaction **double-apply risk** (payment-rules.manifest:195-205 + reactions.manifest:12), unregistered inventory-listener stubs (`event-listeners.ts:29-32,59` exist; never registered in `manifest-runtime-factory.ts` — only prep-list-seed + prep-inventory-demand middleware wired at factory:534-543), the empty `InventoryTransaction` ledger (3 direct-Prisma producers, zero reaction), `manifest:audit-reaction-payloads` absent from `manifest:ci` (it routes through `manifest:audit:strict`, which omits it), the 4-entry `reaction-payload-baseline.json` (all StaffMember→TrainingAssignment + the `StaffMemberFirstShiftScheduled` orphan), the duplicate `StaffMemberCreated` event (staff-member-rules:85 vs training-module-sel-rules:4, incompatible topics/fields), SmsAutomationRule activate/deactivate routes calling generic `update` (events never emit), missing `CalendarEntry`/`ForecastEntry` entities, the 4 orphan Event-lifecycle events (Guest/Dish/Cancelled/Finalized — zero consumers), zero `realtime`/`async`/`role…extends` declarations, the single aggregate computed (`Recipe.hasVersion`, 1 of 45 hasMany), opt-in idempotency (factory:496) + wired outbox (factory:563), flagProvider in apps/api only, and all CSV-importer + 5 unregistered-write bypass claims. **No contradictions surfaced; only the role-check file count below was off.**

## Executive summary

The goal is a fully interconnected domain where Manifest itself dictates how every entity propagates its effects to related entities — so that creating, updating, or transitioning any entity correctly fans out to calendar, CRM, kitchen prep, inventory demand, staffing, payroll, finance, and notifications without hand-written glue. Today the wiring is sparse and partly broken: 20 reactions exist, but a multi-agent audit confirms that **11 of those 20 (~55%) silently no-op** because they read `payload.result.*` off MUTATE commands (where `result` is the last mutate's scalar value, not the entity instance), and only **two domain middleware** (prep-list-seed, prep-inventory-demand) cover the 1:N fan-out that reactions structurally cannot do. Whole domains are islands — integrations (SMS automation), versioning (VersionedEntity/EntityVersion/VersionApproval), and knowledge base emit events that nothing consumes; payroll/labor, staffing, and finance pipelines require manual hops at every stage. The biggest leverage points, in order: (1) fix the `payload.result.*` no-op reaction class — it unblocks payment→invoice, contract→event, lead→deal, proposal→event, maintenance→equipment, shipment→inventory in one pass; (2) wire the core event-lifecycle propagations (EventCreated/Confirmed/Cancelled/Finalized, EventStaffAssigned, EventDish/guest-count changes) via reactions + a small set of new middleware; (3) connect the many self-contained child→parent rollups (computed aggregates) and orphan events. Schema/store codegen (Phases 2b/4/5/6) is explicitly **out of scope** here and tracked in `manifest/task_plan.md`.

A recurring pattern surfaced by verification: **many planned reactions have no valid target command or event field yet**. Several MUTATE-emitter events (`LeadConvertedToClient`, `ProposalAccepted`, `TrainingAttemptSubmitted`) already carry the needed fields as direct payload entries — the fix is to stop reading `payload.result.*` and read `payload.<field>`. But many others require authoring a missing command (`VersionedEntity.setCurrentVersion`, `ScheduleShift.flag`, `Dish.clearDefaultContainer`, `AdminChatThread.recordLastMessage`, `Schedule.incrementShiftCount`, `TimeEntry.applyEdit`) or even a missing entity (`CalendarEntry`, `ForecastEntry`, `EventFinancialSummary`) **before** the reaction can be declared. Each such prerequisite is now called out inline.

## Guiding principles

- **Governed writes only.** All domain mutations execute through `RuntimeEngine.runCommand` via the canonical dispatcher. Direct `prisma.*` create/update/delete/upsert/raw-SQL on governed entities is a bypass and must be registered or removed.
- **Reactions for 1:1, middleware for 1:N.** A reaction resolves exactly one target instance. Any "one parent → many children" fan-out must be a runtime middleware (precedent: `prep-list-seed-middleware`, `prep-inventory-demand-middleware`).
- **Fix producers, not generated files.** Correct `.manifest` source, command params, event payloads, and reaction resolves — never patch generated IR/route/client artifacts.
- **No reaction without a target.** Before declaring a reaction, confirm (a) the target command exists with the params you intend to pass, and (b) the source event payload actually carries every referenced field. If either is missing, author the command/field/entity in the same PR.
- **One retirement/migration per PR.** Each bypass removal, reaction fix, or new propagation lands as its own small, verifiable change with its own conformance test.

---

## P0 — Broken interconnection (fix first)

The `payload.result.*` reaction no-op class is the single highest-leverage defect. For every MUTATE command, the engine sets `result` to the last mutate's scalar value, so `payload.result.<field>` is `undefined` and the reaction silently no-ops. Fix by reading the field directly off the emitted event payload (`payload.<field>`) — adding the field as an explicit command param / event field where it is not currently emitted — or `payload._subject.*` for the source instance. Each fix must be paired with a conformance test asserting the target instance is resolved and the downstream mutation occurs.

> **⚠ VERIFIED ENGINE-SEMANTICS CORRECTION (2026-06-13, from `@angriff36/manifest@2.4.2` `dist/manifest/runtime-engine.js:2291-2351` + parser:863-865 + spec `semantics.md:392-398`):** the per-item claims below that several reactions are "pure expression fixes (`payload.result.*` → `payload.*`) with **no new command params needed**" are **WRONG**. The emitted payload is built as **`{ ...commandInput, result }` only** — a reaction can see *exactly*: (a) the source command's **input params**, (b) `result` (the last action's value: an **instance** only for `create`/auto-create commands, otherwise the last mutate's **scalar**), and (c) `payload._subject.id` (the source instance id, engine-stamped from `instanceId`). **Declared event fields are NEVER auto-populated from `self.*`** — a bare `emit X` does not copy the entity's fields into the payload, and there is **no `emit X { ... }` payload-block syntax** (parser grammar rejects it). Consequence: any reaction that needs a source-entity field which is *not* a command input param **cannot** be fixed by an expression change. The correct mechanisms are: **(1)** add the needed field as a command **input param** *and* update the caller to supply it (only sensible when the caller already holds the value, e.g. the proposal-respond route has the full proposal); or **(2)** convert the propagation to **middleware**, which has store access and can load the `_subject` entity to read its fields (architecturally cleaner — avoids polluting a command's contract with its own entity's fields). The codebase already uses the correct mutate-source pattern at `reactions.manifest:185-195` (`EventConfirmed run PrepList.create` resolves `payload._subject.id`, **not** `payload.result.id`). **Re-triage each P0 item below under these rules before implementing — do not ship the "expression-only" fix as described.** Per-item param impact (verified): Lead→Deal needs `companyName`+`estimatedValue` added to `convertToClient` (caller does NOT currently fetch the lead); Proposal→Event needs `clientId`+`proposalNumber`+`title`+`total` added to `accept` (the respond route DOES hold the proposal); Training needs `attemptNumber`+`passThresholdPercent` added to the 3 submit commands (`passThresholdPercent` is read from `self.`, not a param). All are param-addition-or-middleware, not one-liners.

> **Verification nuance (2026-06-13 audit):** for the Payment/Contract items below, the *event* declaration (e.g. `PaymentProcessed` at payment-rules.manifest:152-158, `PaymentRefunded` at :160-165, `ContractSigned` at event-contract-rules.manifest:249-255) already lists the needed fields (`invoiceId`/`amount`/`paymentId`/`eventId`). But the emitting command does **not** take them as params, so the runtime has nothing to populate the declared field with at emit time — confirm whether the value can be sourced from `payload._subject.*` (the field exists on the entity) before adding a command param. The Lead/Proposal/Training items genuinely carry the values in the payload already and need only the `payload.result.* → payload.*` expression fix.

- [x] **Fix Payment→Invoice payment application** — DONE (2026-06-14). `on PaymentProcessed run Invoice.applyPayment`. `Payment.process` is a MUTATE taking only `gatewayTransactionId`, so `payload.result.invoiceId`/`payload.result.amount` were both `undefined` — silent no-op (payments marked COMPLETED but invoices never credited; the payments route's `status !== "DRAFT"` heuristic masked it for already-sent invoices). **Per the verified engine-semantics correction, the prior "add `invoiceId`/`amount` as params" framing was WRONG**: `invoiceId`/`amount` are the Payment's OWN fields, not `process` input params, and declared event fields are not auto-populated from `self.*` — so no reaction can read them (same shape as Lead→Deal / Contract→Event). **Fixed as MIDDLEWARE**: removed the dead reaction from `reactions.manifest`; added `manifest/runtime/src/middleware/payment-processed-invoice-apply-middleware.ts` (registered in `manifest-runtime-factory.ts`), which loads the processed Payment via `_subject.id`, reads `self.invoiceId`/`self.amount`, and dispatches governed `Invoice.applyPayment` — only when guard-safe (payment COMPLETED, invoice ∈ {SENT,VIEWED,OVERDUE,PARTIALLY_PAID}, 0 < amount ≤ amountDue; over-pay/DRAFT skipped so the route's pre-existing `markAcceptedNotApplied` fallback handles them). Idempotency key per payment. Conformance test: `manifest/runtime/src/__tests__/payment-processed-invoice-apply-middleware.test.ts` (5 tests: invoice credited; DRAFT skip; over-pay skip; IR no longer carries the reaction or the saga). `manifest:audit-reaction-payloads` no longer lists this reaction; api+app+runtime typecheck green; runtime suite 193 pass. Mechanism: middleware (replaces reaction).
- [x] **Coordinate the ProcessInvoicePayment saga vs. the Payment→Invoice reaction — double-apply risk** — DONE (2026-06-14). The `ProcessInvoicePayment` saga (was payment-rules.manifest:195-205) was **DORMANT** — nothing in `apps/` passed `"ProcessInvoicePayment"` to the generic saga executor (`/api/manifest/sagas/[saga]`), and the generated client/hooks carry no reference. It was also incompatible with the propagation design the payments routes actually use: its `processPayment` step emits `PaymentProcessed`, which the new middleware turns into an `applyPayment`, so a wired saga would apply twice (its own `applyToInvoice` step + the middleware). **Resolution (inverts the plan's earlier "keep the saga" preference — surfaced as a Rule 7 decision):** the middleware owns the path; the dormant conflicting saga was REMOVED from `payment-rules.manifest`. The route's `Payment.markAcceptedNotApplied` state already covers the non-atomic failure case the saga's compensation aimed at. Regression-locked by the conformance test asserting the IR carries no `ProcessInvoicePayment` saga. Mechanism: saga removal.
- [x] **Fix Payment→Invoice refund** — DONE (2026-06-14). `on PaymentRefunded run Invoice.recordRefund`. `Payment.refund(refundAmount, reason)` / `partialRefund(...)` are MUTATE commands, so `resolve payload.result.invoiceId` and `paymentId: payload.result.id` were both `undefined` — silent no-op (refunds debited the payment but the invoice books never moved; the refund route `apps/api/.../accounting/payments/[id]/route.ts:373-384` relies on this propagation, it does not credit the invoice itself). **Per the verified engine-semantics correction, the prior "add `invoiceId` as a param" framing was WRONG**: `invoiceId` is the Payment's OWN field, not a refund param (declared event fields are not auto-populated from `self.*`), so no reaction can read it. (The refund AMOUNT *is* a command input param, so it does ride the payload.) **Fixed as MIDDLEWARE** (symmetric counterpart of the apply leg): removed the dead reaction from `reactions.manifest`; added `manifest/runtime/src/middleware/payment-refunded-invoice-record-middleware.ts` (registered in `manifest-runtime-factory.ts`), which loads the refunded Payment via `_subject.id`, reads `self.invoiceId`, takes `refundAmount` from the command input, and dispatches governed `Invoice.recordRefund` — guard-safe (skips when the invoice was never credited or the refund exceeds `amountPaid`). Idempotency key per payment. **TWO prerequisite defects found + fixed at test time, both making the target command DEAD:** (1) `Payment.refund`/`partialRefund` guarded `self.isRefundable` — a COMPUTED, which the runtime does not resolve inside guards ([[P2 systemic finding]]) — so the guard silently evaluated undefined and the command could never fire; inlined to `self.status == "COMPLETED" and self.refundedAt == null`. (2) `Invoice.recordRefund` mutates `status -> PARTIALLY_PAID`, but the runtime does not exempt no-op self-transitions (notes §21) and `PARTIALLY_PAID`'s `to` list lacked itself while `PAID` was terminal — so recordRefund's transition was rejected in BOTH realistic refund states; added the `PARTIALLY_PAID` self-loop (also fixes repeat partial `applyPayment`) and `PAID -> PARTIALLY_PAID`. Conformance test: `manifest/runtime/src/__tests__/payment-refunded-invoice-record-middleware.test.ts` (6 tests: IR regression-lock the reaction is gone + transitions present; partial-paid refund credited back; partialRefund; PAID-invoice reversal; over-refund skip). `manifest:audit-reaction-payloads` no longer lists this reaction; api+runtime typecheck green; runtime suite 199 pass. Mechanism: middleware (replaces reaction) + guard inlining + transition fix.
- [x] **Fix Collections→Invoice payment** — DONE (2026-06-14). `on CollectionPaymentRecorded run Invoice.applyPayment`. `CollectionCase.recordPayment(amount, paymentId, paymentDate)` is a MUTATE (last mutate `lastActivityAt = now()`), so the prior reaction's `resolve payload.result.invoiceId` was a silent no-op (collections recorded the recovery but the AR books never moved — the collections route `apps/api/.../accounting/collections/cases/[id]/route.ts` only calls `recordPayment` and relies on this propagation, it does not credit the invoice itself). **The prior framing ("add `paymentId` to the event, resolve `payload.invoiceId`") was WRONG** per the verified engine-semantics correction at the top of P0: `invoiceId` is the **CollectionCase's OWN field** (collections-rules.manifest:11), NOT a `recordPayment` param, and declared event fields are never auto-populated from `self.*` — so no reaction can read it (`payload.invoiceId` is `undefined` too). `amount`/`paymentId` ARE genuine `recordPayment` params (so they ride the payload), but `invoiceId` is unreachable. **Fixed as MIDDLEWARE** (direct sibling of the payment apply/refund legs): removed the dead reaction from `reactions.manifest` and added `manifest/runtime/src/middleware/collection-payment-recorded-invoice-apply-middleware.ts` (registered in `manifest-runtime-factory.ts` after the payment legs), which loads the CollectionCase via `_subject.id`, reads `self.invoiceId`, takes `amount`/`paymentId` from the command input, and dispatches governed `Invoice.applyPayment` — guard-safe (skips DRAFT/PAID/overpay, matching `applyPayment`'s `status in [SENT,VIEWED,OVERDUE,PARTIALLY_PAID]` + `paymentAmount <= amountDue` guards). **No double-apply risk:** a collection payment is NOT a `Payment.process`, so the PaymentProcessed apply leg never fires for this path; the route only calls `recordPayment`. Idempotency key uses the case's strictly-decreasing post-payment `outstandingAmount` (or `paymentId` when present) so a re-emitted event dedups but sequential payments do not collide. Conformance test: `manifest/runtime/src/__tests__/collection-payment-recorded-invoice-apply-middleware.test.ts` (4 tests: IR regression-lock the reaction is gone; recorded payment credits the invoice + `PaymentApplied` bubbles up; DRAFT skip; over-pay skip while the case still records). `manifest:audit-reaction-payloads` no longer lists this reaction (EXIT=0, 0 new); runtime suite 215 pass; runtime+api typecheck green. Mechanism: middleware (replaces reaction).
- [x] **Fix Contract→Event confirmation** — DONE (2026-06-14). `on ContractSigned run Event.confirm`. `EventContract.sign()` takes **no params** (event-contract-rules.manifest:95-102) and is a MUTATE (`mutate status="signed"`), so the emitted payload is `{result: <signedAt scalar>}` — the prior reaction's `resolve payload.result.eventId` was a silent no-op. **The prior framing ("add `eventId` as a `sign` param/event field and resolve `payload.eventId`") was WRONG** per the verified engine-semantics correction at the top of P0: `eventId` is the EventContract's OWN field, not a `sign` input param, and declared event fields are NOT auto-populated from `self.*` — so NO reaction can read it. Adding it as a param would pollute the command contract AND the only caller (contract-status route `apps/api/app/api/events/contracts/[id]/status/route.ts`, `buildPayload: () => ({})`) holds only the contract id, not the eventId. **Fixed as MIDDLEWARE** (mechanism (2), the engine-cleaner path for entity-owned fields): removed the dead reaction from `reactions.manifest` and added `manifest/runtime/src/middleware/contract-signed-event-confirm-middleware.ts` (registered in `manifest-runtime-factory.ts`), which loads the signed EventContract via `_subject.id`, reads `self.eventId`, and dispatches governed `Event.confirm(userId:"system")` — but ONLY when the linked Event is still `draft` (guard-safe + idempotent: an already-confirmed/cancelled event is skipped, not error-spammed; matches `Event.confirm`'s `guard self.status == "draft"`). **Gotcha hit + locked in the test ([[manifest-createinstance-constraint-bootstrap]]):** `Event.confirm`'s `mutate status` persists via the engine's `updateInstance`, which re-validates Event's ENTITY-level `block` constraints (`validEventType`/`validTitle`/…, event-rules.manifest:61-64) against the merged row and **silently returns `undefined` (no persist) while the command still emits `EventConfirmed`** — so a conformance test seeding a partial Event (missing `eventType`) saw `EventConfirmed` fire yet the row stay `draft`. The seed must satisfy every entity-level block constraint (real Events always have `eventType` — `create` defaults it to "general"). Conformance test: `manifest/runtime/src/__tests__/contract-signed-event-confirm-middleware.test.ts` (IR regression-lock that the broken reaction is gone + runtime proof the linked draft Event transitions to `confirmed`, the already-past-draft skip, and the no-eventId skip). `manifest:audit-reaction-payloads` no longer lists this reaction; runtime (188) + api typecheck green. Mechanism: middleware (replaces reaction).
- [x] **Fix Maintenance→Equipment (×2)** — DONE (2026-06-14). `on MaintenanceWorkOrderCompleted run Equipment.recordMaintenance` and `…run Equipment.updateStatus`, both resolving `payload.result.equipmentId`. `MaintenanceWorkOrder.completeWork(totalCost, partsUsed, notes, userId)` is a MUTATE (last mutate `completedAt = now()`), so `payload.result.equipmentId` was `undefined` — both reactions silently no-op'd (completed work orders never touched the equipment record). **The prior "add `equipmentId` as a `completeWork` param" framing was WRONG** per the verified engine-semantics correction at the top of P0: `equipmentId` is the **MaintenanceWorkOrder's OWN field** (equipment-rules.manifest:212), NOT a `completeWork` param, and declared event fields are never auto-populated from `self.*` — so no reaction can read it; and the only callers (generated client/hooks) hold just the work-order id. **Fixed as MIDDLEWARE** (sibling of the shipment-receive leg): removed both dead reactions from `reactions.manifest` and added `manifest/runtime/src/middleware/maintenance-completed-equipment-record-middleware.ts` (registered in `manifest-runtime-factory.ts` after the shipment middleware), which loads the completed MaintenanceWorkOrder via `_subject.id`, reads `self.equipmentId`, and dispatches the governed `Equipment.recordMaintenance` (maintenanceDate from the work order's `completedAt`; cost/performer from the genuine `totalCost`/`userId` params with work-order fallbacks; workOrderType mapped down to recordMaintenance's `preventive|corrective|emergency` set). **Rule-7 collapse: only ONE command is dispatched** — `recordMaintenance` itself mutates `status = "active"`, subsuming the redundant `updateStatus -> "active"` reaction; dispatching both would trip `updateStatus`'s `newStatus != self.status` guard once active. **Engine gotcha hit + fixed in source ([[create-command-transition-self-loop-bug]] / [[mutate-persist-dropped-by-block-constraints]]):** because `MaintenanceWorkOrderCreated -> maintenance` is unwired, equipment is typically ALREADY active when a work order completes, so `recordMaintenance`'s `status = "active"` is a no-op self-transition — which the engine does not exempt, silently dropping the WHOLE update (incl. the date refresh). Added an `active -> active` self-loop to Equipment's transition list (same pattern as the payment-refund `PARTIALLY_PAID` self-loop); blast radius is recordMaintenance only (updateStatus's guard already forbids same-status). Idempotency key per work order. Conformance test: `manifest/runtime/src/__tests__/maintenance-completed-equipment-record-middleware.test.ts` (4 tests: IR regression-lock both reactions are gone; already-active equipment gets dates stamped + stays active [proves the self-loop]; maintenance→active reset; no-equipmentId skip). `manifest:audit-reaction-payloads` no longer warns on these two reactions (EXIT=0, 0 new); runtime suite 219 pass; runtime+api typecheck green. Mechanism: middleware (replaces 2 reactions) + transition self-loop.
- [x] **Fix Shipment→Inventory restock** — DONE (2026-06-14). `on ShipmentItemReceived run InventoryItem.restock`. `ShipmentItem.updateReceived(quantityReceived, quantityDamaged, condition, conditionNotes, userId)` is a MUTATE (shipment-rules.manifest:218-246), so the prior reaction's `resolve payload.result.itemId` was a silent no-op (received shipments never restocked inventory). **The prior framing ("add `itemId`/`unitCost` as params, resolve `payload.itemId`") was WRONG** per the verified engine-semantics correction at the top of P0: `itemId` and `unitCost` are the **ShipmentItem's OWN fields** (shipment-rules.manifest:173/178), NOT `updateReceived` params, and declared event fields are never auto-populated from `self.*` — so no reaction can read them (`payload.itemId` is `undefined` too, not just `payload.result.itemId`). Only `quantityReceived`/`userId` are genuine params that ride the payload. **Fixed as MIDDLEWARE** (mechanism (2)): removed the dead reaction from `reactions.manifest` and added `manifest/runtime/src/middleware/shipment-item-received-inventory-restock-middleware.ts` (registered in `manifest-runtime-factory.ts` after the inventory-movement ledger middleware), which loads the ShipmentItem via `_subject.id`, reads `self.itemId`/`self.unitCost`, and dispatches governed `InventoryItem.restock(quantity=payload.quantityReceived, costPerUnit=self.unitCost, userId)` — guard-safe (skips when nothing received or the linked item is missing). **Corruption fix (the plan's `costPerUnit: 0` warning):** `restock` unconditionally `mutate unitCost = costPerUnit`, so the old hardcoded 0 would have zeroed `InventoryItem.unitCost` on every receipt; the middleware uses the shipment line's real `unitCost`, and when that is 0/missing it **PRESERVES the item's existing unitCost** (loaded from the InventoryItem store) rather than clobbering a known cost. The nested `restock` emits `InventoryRestocked`, which the inventory-movement ledger middleware (just shipped) records as a `receipt` `InventoryTransaction` — closing the receive→stock→ledger chain. Idempotency key per received line (`shipment-restock:tenant:shipmentItemId`) so re-receiving cannot double-count. Conformance test: `manifest/runtime/src/__tests__/shipment-item-received-inventory-restock-middleware.test.ts` (4 tests: IR regression-lock the reaction is gone; restock by received qty valued at shipment unitCost + `InventoryRestocked` bubbles up; **unitCost preserved (no $0 corruption) when the line carries no cost**; zero-received skip). `manifest:audit-reaction-payloads` no longer warns on this reaction; runtime suite 211 pass; runtime+api typecheck green; no IR/schema/route change beyond the reaction removal. Mechanism: middleware (replaces reaction).
- [x] **Fix Lead→Deal creation** — DONE (2026-06-14). `on LeadConvertedToClient run Deal.create`. `Lead.convertToClient` is a MUTATE (last mutate `status="won"`), so `payload.result.companyName/estimatedValue` and `resolve payload.result.id` were all `undefined`. **The prior framing ("event already carries the fields; expression-only fix; no new params") was WRONG** per the verified engine-semantics correction at the top of P0: the emitted payload is `{...commandInput, result}` only — declared event fields are NOT auto-populated from `self.*`, so a reaction can NEVER read `companyName/estimatedValue` (they are the **Lead's own fields**, not `convertToClient` input params). Adding them as params would pollute `Lead.convertToClient`'s contract with its own entity's fields, and the **only caller** (CRM UI `marketing/leads/[leadId]/lead-detail-client.tsx:78` → generic dispatcher) supplies just `{id}` — it does not hold them. **Fixed as MIDDLEWARE** (mechanism (2), "architecturally cleaner"): removed the dead reaction from `reactions.manifest` and added `manifest/runtime/src/middleware/lead-converted-deal-create-middleware.ts` (registered in `manifest-runtime-factory.ts`), which loads the converted Lead via `_subject.id` and dispatches governed `Deal.create` (title = companyName→contactName→"Untitled Deal" so the `title != ""` guard passes; value = estimatedValue; stage "new"/probability 25; idempotent — skips if a Deal already exists for the lead). **Two engine gotchas hit + documented in the test:** (1) `Lead.convertToClient` mutates status→"won", which the Lead state machine only allows from **"proposal"** (lead-rules.manifest:39) — seed/caller must be in `proposal`, not `qualified`; (2) for a middleware-dispatched **create**, the new id goes in the command **body** (`id:`), NOT as `instanceId` in the dispatch options — passing `instanceId` returns success but never persists the row (mirrors prep-list-seed's item create). Conformance test: `manifest/runtime/src/__tests__/lead-converted-deal-create-middleware.test.ts` (IR regression-lock that the broken reaction is gone + runtime proof the Deal is created with the lead's company/value, the contactName fallback, and idempotency). `manifest:audit-reaction-payloads` no longer lists this reaction (3 fewer warnings); runtime+api typecheck green; full runtime suite (184) + crm-extended (74) pass. Mechanism: middleware (replaces reaction).
- [x] **Fix Proposal→Event creation** — DONE (2026-06-14). `reactions.manifest` `on ProposalAccepted run Event.create`. `Proposal.accept` is a MUTATE (last mutate `acceptedAt = now()`), so `payload.result.*` and `resolve payload.result.id` were all `undefined`. **Two corrections to the prior framing, both verified:** (1) the event "already carries" claim was WRONG per the engine-semantics correction — declared event fields are NOT auto-populated; `clientId/proposalNumber/title/total` had to be added as **command input params** on `accept` (pass-through, not mutated) and supplied by the only caller (`apps/api/.../public/proposals/[token]/respond/route.ts`, which holds the proposal). (2) the resolve fix is **`resolve null`** (engine auto-generates the new Event id — the repo idiom at `EventCreated→BattleBoard.create`), NOT `uuid()`. **SECOND DEFECT found at test time (create-target bootstrap-constraint class):** the reaction hardcoded `eventType: ""`, but `Event.create` enforces entity-level `block` constraint `validEventType` (and `validTitle`) at create-bootstrap *before* the command's `eventType→"general"` coalescing mutate runs ([[manifest-createinstance-constraint-bootstrap]]) — so the Event.create would still fail (silently, since reaction errors are logged-and-swallowed, leaving `accept` ok). Fixed by threading the proposal's `eventType` (param) and coalescing blank→non-empty in the route (title→"Accepted Proposal", eventType→"general"). Conformance test: `manifest/runtime/src/__tests__/proposal-accepted-event-create-reaction.test.ts` (IR regression-lock on resolve=null + `payload.*` not `payload.result.*`, plus runtime proof that accept creates the Event with the proposal's client/number/title/eventType/budget and emits ProposalAccepted+EventCreated). `manifest:audit-reaction-payloads` no longer lists this reaction; api+runtime typecheck green. **Lesson for the sibling create-fanout reaction (Lead→Deal, still open):** check `Deal.create` for bootstrap block-constraints that the reaction's placeholder params would trip — the `payload.result.*` fix alone is necessary but not sufficient. Mechanism: command-param add + reaction + route.
- [x] **Fix EventCreated→BattleBoard eventId** — `reactions.manifest:159-169` `on EventCreated run BattleBoard.create` with `eventId: payload.result.id`. `Event.create` IS a create command, so `payload.result.id` is valid and `BattleBoard.create` (battle-board-rules.manifest:63-76) takes `eventId`. Boards are not orphaned by this path. — DONE: live code is already correct (this was never broken). Remaining work is only CI-gate coverage, tracked under "Audit-gate the whole class".
- [x] **Fix Training attempt recording** — DONE (2026-06-14). `on TrainingAttemptSubmitted run TrainingAttempt.create`. The three emitters (`submitPassingAttempt`/`submitFailedAttempt`/`submitFinalFailedAttempt`) are all MUTATE commands ending on `mutate managerReviewRequired = <bool>`, so the engine's emitted payload `{...commandInput, result}` carries `result` = that boolean scalar — `payload.result.attemptCount`/`passThresholdPercent`/`managerReviewRequired` were all `undefined`, so no attempt ledger row was ever recorded. **The prior framing ("the event carries `attemptNumber`/`passThresholdPercent` directly; change `payload.result.*` → `payload.*`") was WRONG** per the verified engine-semantics correction at the top of P0: declared event fields are NEVER auto-populated from `self.*`, and `attemptNumber` (= `self.attemptCount + 1`), `passThresholdPercent`, and `managerReviewRequired` are the **TrainingAssignment's OWN fields**, NOT submit-command params (the submit params are only `assignmentId/attemptId/moduleId/staffMemberId/scorePercent/answersJson`) — so no reaction, even reading `payload.*`, can see them. **Fixed as MIDDLEWARE** (sibling of Lead→Deal): removed the dead reaction from `training-module-rules.manifest`; added `manifest/runtime/src/middleware/training-attempt-submitted-record-middleware.ts` (registered in `manifest-runtime-factory.ts`), which loads the just-mutated TrainingAssignment via `_subject.id`, reads `self.attemptCount` (post-increment → this attempt's number), `self.passThresholdPercent`, and `self.managerReviewRequired`, derives `passed = scorePercent >= threshold`, takes `attemptId/moduleId/staffMemberId/scorePercent/answersJson` from the command input, and dispatches the governed `TrainingAttempt.create` (create id in the body, NOT instanceId). Idempotent per `attemptId` (the TrainingAttempt id) — a re-emitted submit event skips the dedupe guard. Conformance test: `manifest/runtime/src/__tests__/training-attempt-submitted-record-middleware.test.ts` (4 tests: IR regression-lock the reaction is gone; passing submission records attempt #1 with passed=true; failing submission records passed=false; idempotent skip). `manifest:audit-reaction-payloads` EXIT=0 (0 new, 0 warnings); runtime suite 223 pass; runtime+api typecheck green. Mechanism: middleware (replaces reaction).
- [x] **Fix StaffMemberCreated→TrainingAssignment (baselined) + resolve duplicate event definition** — DONE (2026-06-14). `on StaffMemberCreated run TrainingAssignment.create` (was training-module-sel-rules.manifest:471) read `payload.staffMemberId/firstShiftAt/dueAt`. Per the verified engine-semantics correction at the top of P0: `staffMemberId` is a **COMPUTED** (`self.id`, staff-member-rules.manifest:23), NOT a `StaffMember.create` param; `firstShiftAt`/`dueAt` are not knowable at staff-create time (the first shift is scheduled later) and are not params either — so all three were `undefined` at runtime and NO onboarding assignment was ever auto-created. The reaction ALSO passed `staffRole: payload.role`, but `TrainingAssignment.create` guards `staffRole == "staff"` while `StaffMember.role` defaults to "server", so the create would have been blocked even if the field were reachable. **Duplicate-event root cause confirmed + resolved:** `StaffMemberCreated` was defined twice — staff-member-rules.manifest:85 (topic `staff.staff-member.created`, the one `StaffMember.create` emits) and a stub at training-module-sel-rules.manifest:4 (topic `staff.member.created`) that existed only to give the reaction firstShiftAt/dueAt fields; the stub is **removed** so the IR now carries exactly ONE `StaffMemberCreated` (verified). **Fixed as MIDDLEWARE** (sibling of Lead→Deal / training-attempt): removed the dead reaction and added `manifest/runtime/src/middleware/staff-member-created-training-assignment-middleware.ts` (registered in `manifest-runtime-factory.ts`), which reads the new StaffMember id from `_subject.id`, pins `staffRole` to the literal "staff" (so the create guard passes), and dispatches the governed `TrainingAssignment.create` with the SEL module constants + `dueDateReviewNeeded=true` (firstShiftAt/dueAt intentionally omitted — pinned later by the still-open StaffMemberFirstShiftScheduled→applyFirstShiftDueDate path). Idempotent per (tenant, staffMemberId, SEL module); create-id in the body NOT instanceId ([[lead-deal-middleware-create-fanout]]). Conformance test: `manifest/runtime/src/__tests__/staff-member-created-training-assignment-middleware.test.ts` (4 tests: IR regression-lock the reaction is gone; IR-lock StaffMemberCreated defined exactly once; runtime proof the assignment is created with employeeId=staffMemberId/staffRole="staff"/SEL module/dueDateReviewNeeded=true even when the staff role is "server"; idempotent skip). Drained 3 of 4 baseline entries; `check-reaction-payloads --strict` EXIT=0. Also fixed a stale brittle `reactions.length >= 11` floor in `reactions-conformance-runtime.test.ts` (the count legitimately shrinks as each no-op reaction becomes middleware). runtime suite 227 pass; runtime+api typecheck green. Mechanism: event de-dup + middleware (replaces reaction). **Remaining for the baseline drain to zero: the StaffMemberFirstShiftScheduled orphan below.**
- [x] **Emit StaffMemberFirstShiftScheduled (orphan reaction, no emitter)** — DONE (2026-06-14). `on StaffMemberFirstShiftScheduled run TrainingAssignment.applyFirstShiftDueDate` had **no emitter** — the lone remaining `reaction-payload-baseline.json` entry (`:: orphan-event`). **The plan's "emit + reaction" framing was WRONG** per the verified engine-semantics correction at the top of P0: `applyFirstShiftDueDate` resolves its target via `guard assignmentId == self.id` (the assignment id is NOT on any shift payload), the natural emitter `ScheduleShift.create` is a MUTATE whose payload `{...commandInput, result}` carries only `employeeId`/`shiftStart` (no `assignmentId`/`dueAt`), and "first shift" is a STATEFUL fact a declarative reaction cannot express. The orphan reaction was also internally broken (`resolve payload.staffMemberId` would load the assignment by `id == staffMemberId`, but the assignment's id ≠ staffMemberId — its `employeeId` field = staffMemberId). **Fixed as MIDDLEWARE**: removed the orphan reaction AND its now-dead `StaffMemberFirstShiftScheduled` event from source; added `manifest/runtime/src/middleware/schedule-shift-first-shift-due-date-middleware.ts` (registered in `manifest-runtime-factory.ts` after the staff-created leg), which on `ScheduleShiftCreated` reads `employeeId`/`shiftStart` from the payload, finds the employee's open SEL onboarding `TrainingAssignment` that still needs its due date pinned (`dueDateReviewNeeded == true`, status ∈ {assigned,in_progress,overdue}), and dispatches governed `applyFirstShiftDueDate(firstShiftAt = dueAt = shiftStart)` — training is due by the shift it must precede. **First-shift-only idempotency comes free:** the command's own `mutate dueDateReviewNeeded = false` clears the flag, so a LATER shift finds no match and no-ops (no shift-history scan). Identity: matches `assignment.employeeId == shift.employeeId` (both are the StaffMember id in the SEL flow); diverging ids no-op safely. Conformance test: `manifest/runtime/src/__tests__/schedule-shift-first-shift-due-date-middleware.test.ts` (5 tests: IR regression-lock the orphan reaction is gone + the dead event removed; first shift pins dueDate + `TrainingAssignmentDueDateSet` bubbles up; second shift does NOT re-pin; different-employee no-op). **`reaction-payload-baseline.json` drained to ZERO** (`check-reaction-payloads --strict` EXIT=0, baseline of 0); runtime suite 232 pass; runtime+api typecheck green; `manifest:audit:strict` clean. Mechanism: event de-dup removal + middleware (replaces orphan reaction).
- [x] **Fix the two no-op SmsAutomationRule routes** — DONE (2026-06-13). `apps/api/app/api/smsautomationrule/activate|deactivate/route.ts` now invoke the dedicated `activate`/`deactivate` commands (sms-automation-rules.manifest:70-85) instead of generic `update`, so `SmsAutomationRuleActivated/Deactivated` emit and the activation transition guard (`isActive == false`/`== true`) is enforced (the old `update` path also risked clobbering other fields via its full mutate set). No IR change (commands already existed). Coverage added: `apps/api/__tests__/communications/sms-automation-activate.test.ts` (7 tests, locks the contract that the route MUST call the dedicated command, never `update`). Note: still **no consumer** of these events yet — the SMS-as-reaction-target middleware is the open P1 item under "Integrations & versioning"; this fix is its prerequisite. Mechanism: command-variant correction.
- [x] **InventoryConsumed/Wasted/Restocked/Adjusted → InventoryTransaction.create** — DONE (2026-06-14). Every `InventoryItem` movement (`consume`/`waste`/`restock`/`adjust`) emitted its event but **nothing created the ledger row**, so the `InventoryTransaction` store was structurally defined yet effectively EMPTY for day-to-day kitchen movements — only the 3 direct-Prisma routes (purchase-orders complete, stock-levels adjust, cycle-count finalize) ever wrote it, starving valuation/par-reorder/audit of a movement trail. **Fixed as ONE MIDDLEWARE** (`manifest/runtime/src/middleware/inventory-movement-transaction-middleware.ts`, exported via the barrel, registered in `manifest-runtime-factory.ts` after the payment legs), NOT 4 reactions / 4 middleware / the listener stubs. **Why middleware, not the "reaction (or listener registration)" the plan suggested (Rule-7 refinement):** the ledger row needs a `unitCost` for valuation, which for consume/waste/adjust is the **InventoryItem's OWN field** (loaded from the store) — not a movement command param, and declared event fields are never auto-populated from `self.*` — so a reaction's payload structurally cannot value the row; and the SIGNED ledger delta (consume/waste store `−quantity`, restock/adjust `+quantity`) cannot be negated in a reaction's params. The `event-listeners.ts` `onInventory*` stubs sit OUTSIDE the Manifest lifecycle (constitution prefers governed dispatch), so they were rejected too — they remain unwired utilities. **Key correctness findings (locked in the test):** (1) `InventoryTransaction.transactionType` is gated by the entity-level block `validTransactionType in ["receipt","issue","adjustment","transfer","waste","return"]`, so the movement→type map uses the **enum members, NOT the command names**: consume→`issue`, waste→`waste`, restock→`receipt`, adjust→`adjustment` (the direct-Prisma producers' `"purchase"` would FAIL this gate — they bypass it). (2) Signed deltas match the existing producers (receipt +, adjustment signed). (3) `restock` alone carries `costPerUnit` in its payload (a command param) → valued from the payload; the others load `item.unitCost`. (4) The movement events carry no `tenantId`/`itemId` field → middleware resolves itemId from `event.subject?.id` and tenantId from the runtime context. (5) Middleware-dispatched create id goes in the body, not `instanceId` ([[lead-deal-middleware-create-fanout]]). Idempotency key is per-row (each movement is a distinct, real append-only ledger entry — dedup here would be a bug). Conformance test: `manifest/runtime/src/__tests__/inventory-movement-transaction-middleware.test.ts` (5 tests: IR regression-lock that no movement→InventoryTransaction.create reaction exists + runtime proof that consume/waste/restock/adjust each record exactly one ledger row with the correct enum type, signed quantity, and valuation). Runtime suite 207 pass; runtime+api typecheck green; no IR/schema/route change so `manifest:audit-reaction-payloads` + drift gates are unaffected. Mechanism: middleware.
- [x] **PrepListFinalized reserves but PrepListCompleted never consumes** — DONE (2026-06-14). `prep-inventory-demand-middleware.ts` reserves inventory on `PrepListFinalized` via `InventoryItem.reserve` (bumps `quantityReserved`; wired at manifest-runtime-factory.ts), but `PrepListCompleted` (prep-list-rules.manifest:143 event / :138 `markCompleted`) had no consumer — reserved quantities were permanently stranded (`quantityReserved` only ever grew; `quantityOnHand` never dropped for prep; `quantityAvailable` = onHand − reserved bled down). **Fixed as MIDDLEWARE** (1:N fan-out over PrepListItem rows — a reaction cannot resolve the set): added `manifest/runtime/src/middleware/prep-list-completed-consume-middleware.ts` (exported from the `middleware/index.ts` barrel; registered in `manifest-runtime-factory.ts` right after the demand middleware), which on `PrepListCompleted` loads the prep list's `PrepListItem` rows and dispatches a governed `InventoryItem.consume(quantity=scaledQuantity, lotId=prepListId)` per item. **Key IR finding:** `consume` is the correct single command — its mutates decrement BOTH `quantityOnHand` (physical usage) AND `quantityReserved` (`self.quantityReserved > quantity ? self.quantityReserved - quantity : 0`, reservation release) in one call — so no separate `releaseReservation` is needed; one dispatch reverses the reserve and books real consumption. Items whose `scaledQuantity` exceeds on-hand are surfaced via `onDiagnostic` (consume's `blockInsufficientStock`) and skipped, not fatal — the rest still consume. Idempotency key per item (`prep-consume:${tenantId}:${prepListId}:consume:${itemId}`). The `event-listeners.ts` `onPrepListCompleted` stub remains an unwired external-consumer utility (NOT the fix). **Gotcha hit + locked in the test ([[mutate-persist-dropped-by-block-constraints]]):** PrepList's bare invariant `positiveTotalTime: self.totalEstimatedTime >= 0` silently dropped `markCompleted`'s `status` mutate when the seed omitted `totalEstimatedTime` (undefined ≥ 0 = false) while the event still fired — the seed must satisfy every entity-level block constraint. Conformance test: `manifest/runtime/src/__tests__/prep-list-completed-consume-middleware.test.ts` (3 tests: 2-item fan-out draws down on-hand + releases reservations + 2 `InventoryConsumed` bubble up + PrepList→completed; insufficient-stock item skipped while the rest consume; IR regression-lock that no `PrepListCompleted→InventoryItem.consume` reaction exists). Runtime suite 202 pass; runtime typecheck green; `manifest:audit-reaction-payloads` OK (unaffected — no IR reaction added). Mechanism: middleware. (Was P0: reservations leak forever.)
- [x] **Audit two more candidate no-op reactions** — DONE (2026-06-14): both are SOUND, no fix needed. `on TrainingCompleted run StaffTrainingSignal.recordSchedulabilityGranted` and `on TrainingWaived run …` read `payload.staffMemberId/moduleId/assignmentId`. Although their emitters (`submitPassingAttempt` for TrainingCompleted; `waive` for TrainingWaived) are MUTATE commands, those three fields ARE genuine **input params** of the emitting commands (verified: `submitPassingAttempt(assignmentId, attemptId, moduleId, staffMemberId, …)`, `waive(assignmentId, moduleId, staffMemberId, reason, …)`), so they ride the `{...commandInput, result}` payload and resolve correctly — this is NOT the `payload.result.*` no-op class. The `on TrainingWaived run StaffTrainingSignal.recordWaiverLogged` reaction (`reason: payload.reason`) is likewise sound (`reason` is a `waive` param). Confirmed by `manifest:audit-reaction-payloads` (these three are not flagged as errors or warnings). Mechanism: audit (no change).
- [~] **Audit-gate the whole class** — PARTIAL (2026-06-13). CI wiring **DONE**: `manifest:audit-reaction-payloads` (`check-reaction-payloads.mjs --strict`, package.json:157) is now appended to the `manifest:audit:strict` umbrella (package.json:174), which `manifest:ci` (package.json:91) already runs — so a NEW orphan reaction or a NEW non-param `payload.X` reference now fails CI (verified `EXIT=0` today: 4 errors, 0 new vs baseline of 4; 32 warnings are the still-broken mutate-`payload.result.*` reactions). Closes the asymmetry where the non-strict `manifest:audit` (line 173) included this check but the strict umbrella did not. **REMAINING:** (a) ~~drain `reaction-payload-baseline.json`~~ **DONE 2026-06-14 — baseline drained to ZERO** (the last entry, the StaffMemberFirstShiftScheduled orphan, was resolved via schedule-shift-first-shift-due-date middleware; `check-reaction-payloads --strict` now runs against an empty baseline, so ANY new orphan/non-param `payload.X` fails CI with nothing grandfathered in); (b) the `payload.result.*`-on-mutate cases are classified as WARNINGS (not gated) by design — they shrink only as each reaction is fixed, not by promoting them to baselined errors. With the baseline empty, this item is effectively COMPLETE for the orphan/param subclass. Mechanism: CI gate. Prevents regression of the param/orphan no-op subclass.

---

## ✅ RESOLVED (2026-06-14): `@angriff36/manifest` 2.5.0 computed-in-guard compile blocker

**Status: RESOLVED — `pnpm manifest:compile` is GREEN; the IR is regenerated with 2.5.0 and the tree is unblocked. All IR-touching increments can land again.**

**Resolution (2026-06-14):** inlined every computed-property reference inside `guard`/`constraint`/`:block`/`:warn` expressions with the computed's own stored-prop formula (transitively resolved, parenthesized for precedence). Done via a deterministic one-off transform (faithful by construction — substitutes each entity's exact computed formula, scoped by string/comment-aware brace-tracked entity boundaries, verified against the actual `entity` declarations before applying). **Actual scope: 121 references across 34 files** — NOT 130/35. The "130" over-counted: it grepped `guard self.is*`-style lines without distinguishing **computed** refs (rejected) from **stored `property is*`** refs (legal). Confirmed examples of the over-count: `kitchen-extended-rules` (`TemperatureProbe`/`IotAlertRule`/`BulkCombineRule` all have `property isActive: boolean = true`, not a computed → 7 non-violations) and `prep-list-rules` (`PrepList.isActive` is a stored property → 2 non-violations). The compiler agrees: green after exactly the 121 computed inlines.

**Verification (all green):** `pnpm manifest:compile` (210 entities, 1048 commands), runtime suite **350/350 pass** (no regression from making 121 dead guards live — the change only *enables* commands that were wrongly `guard_failed`), `pnpm manifest:schema:check` no drift (guards/constraints don't project to the Prisma schema), `pnpm manifest:validate` valid, `pnpm manifest:validate-ai` 100/100. The committed IR (`kitchen.ir.json` + all shards) is regenerated with 2.5.0 — untouched-entity shards changed only in provenance (`compilerVersion 2.4.2→2.5.0`); the 34 edited entities' shards carry the real inlined guards.

**Newly-live (previously INERT) commands** — making these guards live is the correctness win: `StaffMember.deactivate`/`updateProfile`/`changeRole` (`self.isActive` → `self.status == "active"`) now fire, so `StaffMemberDeactivated` emits and the already-committed `staff-member-deactivated-unassign-event-staff-middleware` (commit `05ddf9533`) is no longer dead. The same revival applies across CallPlanningSession, EventPlanningDraft, ProposalDraft, Lead, Invoice, Equipment, TimeEntry, etc. **Follow-up for the [[computeds-dead-in-guards-and-constraints]] memory:** under 2.5.0 this class is now a hard COMPILE error (not a silent dead guard), so the gate is self-enforcing going forward — no new computed-in-guard can land.

<details><summary>Original blocker writeup (historical)</summary>

**Status: BLOCKER — `pnpm manifest:compile` fails, so the IR cannot be regenerated and `manifest:ci` drift gates cannot pass. No IR-touching increment can land until this is resolved.**

A concurrent loop bumped `@angriff36/manifest` **2.4.2 → 2.5.0** (installed at `node_modules/@angriff36/manifest` + every `apps/*/package.json` + `pnpm-lock.yaml`, all uncommitted but `pnpm install`-consistent). **2.5.0's compiler now HARD-REJECTS computed-property references inside `guard` / `:block` / `:warn` / `constraint` expressions** — what was a silent dead-guard under 2.4.2 (`computed → undefined → guard always fails`, see [[computeds-dead-in-guards-and-constraints]]) is now a compile error: `Guard on command 'X' references computed property 'Y', which is not available during guard evaluation. Inline the formula or use a stored property.`

- **Scope: 130 violations across 35 source files** (full scan 2026-06-14). The compiler aborts on the FIRST offending file (`ai/call-planning-session-rules.manifest`), so it is **all-or-nothing**: every one of the 130 must be inlined before `compile` succeeds.
- The committed `manifest/ir/kitchen.ir.json` is therefore **stale 2.4.2 output** (merged file is clean/unchanged; ~100 shards are dirty from the concurrent loop but the merged IR was never regenerated because compile fails). The **346 runtime tests still pass** on the stale IR + 2.5.0 runtime — they do not exercise the dead guards.
- **Consequence — many already-shipped commands/middleware are INERT**: any command whose guard references a computed silently never fires. Confirmed example: `StaffMember.deactivate` (`guard self.isActive`) returns `guard_failed` for every active staff member, so `StaffMemberDeactivated` never emits and the already-committed `staff-member-deactivated-unassign-event-staff-middleware` (commit `05ddf9533`) can never run. The same dead-guard sits on `StaffMember.updateProfile` and `changeRole`. This pattern recurs across the 35 files (CallPlanningSession, EventPlanningDraft, ProposalDraft, Lead, Invoice, Equipment, TimeEntry, …).

**Fix (a dedicated initiative, NOT a P1 propagation):** inline every computed-in-guard with the computed's own formula, **transitively** (a guard using a computed that itself references another computed must inline both). The 2.5.0 compiler is the verifier — iterate `pnpm manifest:compile` until green, then confirm the **346-test runtime suite** still passes and `pnpm manifest:schema:check` is drift-free. This is exactly the inlining the [[computeds-dead-in-guards-and-constraints]] memory has recommended; doing it makes 130 currently-dead guards **live** (a large correctness win) AND unblocks the pipeline. **Do it as one coherent pass in an isolated worktree** to avoid colliding with the concurrent-loop churn (100 dirty IR shards + uncommitted Phase-7 accessor-config + the 2.5.0 dep bump are all in the working tree). Affected files (violation counts):

```
ai/call-planning-session-rules.manifest (9)        ai/event-planning-draft-rules.manifest (20)
ai/proposal-draft-rules.manifest (20)              core/container-rules.manifest (1)
crm/client-interaction-rules.manifest (2)          crm/lead-rules.manifest (5)
events/battle-board-rules.manifest (1)             events/event-budget-rules.manifest (3)
events/event-import-workflow.manifest (1)          events/event-rules.manifest (1)
finance/budget-rules.manifest (1)                  finance/invoice-rules.manifest (1)
finance/payment-method-rules.manifest (1)          integrations/version-control-rules.manifest (4)
inventory/cycle-count-rules.manifest (2)           inventory/inventory-extended-rules.manifest (12)
inventory/inventory-rules.manifest (2)             kitchen/dish-rules.manifest (1)
kitchen/kitchen-extended-rules.manifest (7)        kitchen/kitchen-task-rules.manifest (1)
kitchen/prep-comment-rules.manifest (4)            kitchen/prep-list-rules.manifest (2)
kitchen/prep-task-rules.manifest (3)               operations/equipment-rules.manifest (4)
operations/facilities-all-rules.manifest (2)       operations/logistics-all-rules.manifest (5)
operations/work-order-rules.manifest (2)           platform/api-key-rules.manifest (3)
platform/user-rules.manifest (1)                   quality/qa-rules.manifest (1)
quality/waste-entry-rules.manifest (1)             staff/labor-budget-rules.manifest (1)
staff/schedule-rules.manifest (1)                  staff/staff-logistics-extended-rules.manifest (1)
staff/time-entry-rules.manifest (4)
```

</details>

## Pre-existing infra issues observed (2026-06-14, during the CollectionWrittenOff→Invoice.writeOff increment)

- [x] **`@repo/manifest-runtime/flag-provider` subpath was unexported** — FIXED. `apps/api/lib/manifest-runtime.ts:15` imports `createEnvFlagProvider` from `@repo/manifest-runtime/flag-provider`, but the runtime package's `exports` map had no `./flag-provider` entry (source + dist both existed) → `pnpm --filter api typecheck` failed with TS2307. Added `"./flag-provider": "./src/flag-provider.ts"` to `manifest/runtime/package.json` exports (same `./name → ./src/name.ts` pattern as every sibling subpath). api typecheck now green.
- [x] **`manifest:ci` references an undefined `manifest:schema:check` script** — RESOLVED (verified 2026-06-14). Stale plan entry: HEAD `package.json` already defines `"manifest:schema:check": "node manifest/scripts/check-schema-drift.mjs"` (line 79) and `manifest:ci` (line 91) references it — the alias landed in a prior commit, so `manifest:ci` no longer errors at this step. (The drift it gates was the real remaining problem — next item.)
- [x] **Committed IR-derived schema artifacts are STALE** — DONE (2026-06-14, commit `de610631a`). The committed `generated-schema.prisma` + `prisma-options.generated.json` were ~350 lines stale vs the IR (`check-schema-drift.mjs` reported drift `prisma-options.generated.json` 11760→12474, `generated-schema.prisma` 7837→7677), so `pnpm manifest:schema:check` (now defined + wired into `manifest:ci`) failed with DRIFT DETECTED — making `manifest:ci` RED on the drift step. Regenerated both via the production generators (`pnpm manifest:schema:full` = `derive-prisma-options.mjs` + `generate-full-schema.mjs`); `prisma validate` passes and the gate is now in sync (committed == regenerated, GATE_EXIT=0). Committed as its own dedicated 2-file change (no propagation bundled). **Note (shared-tree):** the session's working tree carried 171 dirty files (concurrent loop) incl. uncommitted Phase-7 accessor-config edits to `package.json` referencing the still-untracked `manifest/scripts/{check-accessor-config,generate-entity-accessor}.mjs` — left untouched/uncommitted; only the 2 regenerated artifacts were staged.

---

## P1 — Missing core propagations

### Event lifecycle

- [ ] **EventCreated → Calendar** — **prerequisite:** `CalendarEntry` entity does not exist in any manifest source; author it first. Then `on EventCreated run CalendarEntry.create` (`eventId/date/title` from `payload.result.*`, valid since `Event.create` is a create). Mechanism: entity + reaction. **NOTE (2026-06-14): higher-risk than the rest of P1** — authoring a new durable entity = new Prisma table + a `db:dev`/`db:deploy` MIGRATION on the live dev DB + unconfirmed product intent (possible overlap with existing calendar UI). Deferred in favor of the no-migration reaction/middleware items until the migration scope is confirmed. **Dead-computed-guard audit follow-up:** the `Lead.update` fix (Proposal→Lead item below) found that computeds evaluate `undefined` inside command GUARDS (not just :block/:warn constraints per the Station finding) → any command `guard self.<computed> …` is dead. A repo-wide grep of command guards for `self.is*`/`self.has*`/computed names is warranted; inline the stored-prop equivalent where found.
- [x] **EventCreated → CRM activity** — DONE (2026-06-14). Booking an event for a client now logs a CRM touchpoint on the client's timeline. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `ClientInteraction.create` REQUIRES a non-empty `employeeId` (command `guard employeeId != ""` AND the entity-level `validEmployeeId` block constraint, client-interaction-rules.manifest:34,70), and `EventCreated` carries no creator field — `Event.create` has no `userId`/`createdBy` param and declared event fields are never auto-populated from `self.*`, so `payload.employeeId` is structurally `undefined` and a pure reaction could never satisfy the create guard (it would fail and be logged-and-swallowed → zero interactions). The event id is also not reachable via `payload.eventId` (that declared field isn't auto-populated either — the working `EventCreated→BattleBoard` reaction reads `payload.result.id`). **Fixed as MIDDLEWARE** (sibling of Lead→Deal): added `manifest/runtime/src/middleware/event-created-client-interaction-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the proposal-line-item leg), which on `EventCreated` reads `clientId`/`title` off the payload (both are `Event.create` params, so they ride `{...commandInput}`), the event id from `event.subject?.id`, and the `employeeId` from the runtime context's acting user (who booked the event = the correct CRM attribution; `"system"` fallback so the non-empty guard always passes), then dispatches the governed `ClientInteraction.create` (type `note`, subject `New event booked: <title>`, `correlationId = eventId`, create id in the body NOT instanceId). **Skips** clientless events (clientId defaults to `""` on Event → no CRM client; also avoids `warnNoClientOrLead`) and is **idempotent** per event (dedup on an existing interaction with `correlationId == eventId` + a per-event dispatch idempotencyKey). **KNOWN LIMITATION (documented, not silent):** the dispatch runs as the same actor and is subject to `ClientInteraction`'s policy (`user.role in [sales, sales_rep, sales_manager, manager, admin]`); event booking is a sales/manager action so the common case passes, but a non-sales actor's event yields a `create` diagnostic + skip (the runtime has no per-call identity override — elevating it would be a governance change, out of scope). Conformance test: `manifest/runtime/src/__tests__/event-created-client-interaction-middleware.test.ts` (4 tests: IR regression-lock no such reaction exists; client booking logs a `note` interaction attributed to the actor + `ClientInteractionCreated` bubbles up; clientless event skips; idempotent when one already exists). No IR/reaction change → audit gates unaffected; runtime suite 244 pass; runtime+api typecheck green. Mechanism: middleware. **Note for the sibling Event-lifecycle items:** `EventCreated → Calendar` (CalendarEntry entity) and the demand/staffing/forecasting middleware remain open.
- [ ] **EventCreated/Confirmed → demand / staffing estimate / forecasting / missing-info Tasks** — new **middleware** (1:N) deriving inventory demand rows, `EventStaff` slots from guestCount+eventType, a `ForecastEntry`, and `AdminTask` for missing fields. **Prerequisite:** `ForecastEntry` entity does not exist — author it first. Note the existing `prep-inventory-demand` middleware hooks `PrepListFinalized`, **not** EventCreated, so demand-at-booking timing (per AGENTS.md) still requires this new middleware. Mechanism: entity + middleware.
- [x] **Event update sync → BattleBoard** — DONE (2026-06-14). `EventUpdated`/`EventDateUpdated`/`EventLocationUpdated` now re-sync every linked battle board's inherited snapshot via runtime middleware, retiring the imperative `syncBattleBoardsForEvent()` server-action helper. **Why middleware, not a reaction (confirmed):** a board is 1:N by `eventId` (a reaction resolves exactly ONE target), AND the snapshot fields (`eventDate`/`clientId`/`guestCount`/`venueName`/`venueAddress`/`locationId`) are the **Event's OWN fields**, not the update commands' input params — and the engine payload is `{...commandInput, result}` only (declared event fields are never auto-populated from `self.*`), so the partial `EventUpdated`/`EventDateUpdated`/`EventLocationUpdated` payloads cannot carry the full snapshot. **Fixed as MIDDLEWARE**: `manifest/runtime/src/middleware/event-updated-board-sync-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the event-created-client-interaction leg, before the cancel cascade). It runs `after-emit`, loads the just-updated Event from the store via `event.subject?.id` (the row already reflects the update), filters `BattleBoard` rows by `tenantId+eventId` (skipping soft-deleted), and dispatches `BattleBoard.syncFromEvent` per board with the authoritative current values. **NO idempotency key by design:** `syncFromEvent` MUST re-run on every update (a static key would suppress all syncs after the first) and is naturally idempotent (overwrites the snapshot). Retired: removed both imperative callers (`events/actions.ts`, `events/[eventId]/event-mutation-actions.ts`) and DELETED `events/actions/sync-battle-boards.ts` — the new middleware also covers the `updateDate`/`updateLocation` paths the helper never did. Conformance test: `manifest/runtime/src/__tests__/event-updated-board-sync-middleware.test.ts` (4 tests: IR regression-lock no Event-update→BattleBoard reaction; EventLocationUpdated full-snapshot sync to linked boards only [unrelated + soft-deleted boards untouched, `BattleBoardSyncedFromEvent` ×2]; EventDateUpdated pushes new date; no-boards no-op). Runtime suite 254 pass; runtime+app typecheck green. Mechanism: middleware (replaces imperative helper).
- [x] **Event update sync → CateringOrder venue snapshot** — DONE (2026-06-14). `EventLocationUpdated` now re-syncs the venue on every ACTIVE catering order linked to the event, via a new `CateringOrder.syncVenue` command + a sibling middleware to the BattleBoard leg. **Two corrections to the plan's framing, both verified against source:** (1) the "+city/state/zip/contact" parenthetical is **impossible** — the Event entity owns ONLY `venueName`/`venueAddress` (plus `locationId`/`venueId`, which CateringOrder lacks); `venueCity/State/Zip/ContactName/ContactPhone` are **caller-supplied on the order** (catering-order-rules.manifest documents this on `create`, because the Event has no such fields), so syncing them would blank caller-owned data. `syncVenue(venueName, venueAddress)` therefore syncs exactly the two fields the Event authoritatively owns (Rule 7). (2) the middleware fans out to **ACTIVE orders only** (orderStatus ∈ draft/confirmed/in_progress), skipping delivered/completed/cancelled — a delivered order is physical history (it shipped to the venue it then held) and re-syncing would falsify the record; the one deliberate divergence from the board leg (boards have no terminal record). **Mechanism: command + middleware** (orders are 1:N by eventId → a single-target reaction can't reach the set; mirrors `event-updated-board-sync-middleware`). New command `CateringOrder.syncVenue` (guard `self.eventId != ""`, mutates venueName/venueAddress, emits new `CateringOrderVenueSynced`); new `manifest/runtime/src/middleware/event-location-catering-sync-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the board-sync leg, before the cancel cascade) loads the updated Event after-emit and dispatches `syncVenue` per active linked order. No idempotency key (sync must re-run on every relocation; the unconditional mutate is naturally idempotent). **Known limitation (documented):** each dispatch runs as the relocating actor, subject to CateringOrder's default policy (event_coordinator/catering_manager/manager/admin) — the common case, but a lower-privilege actor's update yields a policy-skip + diagnostic. Trigger is **`EventLocationUpdated` only** (the dedicated `Event.updateLocation` venue path). Conformance test `event-location-catering-sync-middleware.test.ts` (4 tests: IR lock no such reaction; `syncVenue` exists in IR; relocation re-syncs the 3 active orders while terminal/other-event/soft-deleted stay stale AND caller-owned city/state/zip/contact are preserved; no-active-orders no-op). Runtime suite 265 pass; runtime+api typecheck green; `manifest:validate`/`validate-ai` 100/`schema:check` no-drift/`audit-reaction-payloads` 0/0 all green. Mechanism: command + middleware leg.
- [x] **EventGuestCountUpdated → PrepList / inventory / budget** — DONE (2026-06-14). Changing an event's guest count now proportionally rescales its draft prep. `batchMultiplier` was hardcoded to 1 at the `EventConfirmed run PrepList.create` seed and each `PrepListItem.scaledQuantity` was derived once — nothing recomputed either on guest-count change, so bumping 120→240 silently left the kitchen prepping for 120 (food-quantity correctness defect). **Fixed as MIDDLEWARE** for TWO independent reasons (a reaction could not): (1) 1:N fan-out (one Event → many PrepLists → many PrepListItems); (2) the rescale is a RATIO (new/old) and the OLD count is unreachable from a reaction — `updateGuestCount` does `mutate guestCount = newGuestCount` then emits `{...commandInput, result}`, and the declared `oldGuestCount` event field is never auto-populated from `self.*`, so by any `after-emit` consumer the stored row already holds the new count. Added `manifest/runtime/src/middleware/event-guest-count-prep-rescale-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` with the kitchen/prep group). **Key engine technique (new in this repo):** the middleware runs on TWO hooks — the engine threads ONE `evalContext` through every hook of a `runCommand` and only overwrites `evalContext.self` with the post-mutation instance AFTER the action loop (runtime-engine.js:1914 build / 1060 contextPatch-merge / 2265-2268 self-overwrite), so it captures `self.guestCount` (still OLD) on `before-guard` via `contextPatch`, then on `after-emit` reads `payload.newGuestCount` (a genuine input param) + the stashed old count, computes `ratio = new/old`, and fans out `PrepList.updateBatchMultiplier(batchMultiplier*ratio)` + `PrepListItem.updateQuantity(scaledQuantity*ratio)` per row (baseQuantity left intact — it is the guest-independent recipe base). **SCOPE: draft prep lists only** — `updateBatchMultiplier` guards `status=="draft"` and finalized/completed lists are locked for execution; guest counts change during planning while draft, so this covers the real case without disturbing in-flight service. Idempotency keys embed `newGuestCount` (re-delivered event dedups; a later genuine change re-applies, ratio chaining off the new old=previous count stays correct). Ratio==1 is a clean no-op. Conformance test: `manifest/runtime/src/__tests__/event-guest-count-prep-rescale-middleware.test.ts` (5 tests: IR regression-lock no such reaction; double → batchMultiplier+scaledQuantity ×2 with base untouched + `PrepListBatchMultiplierUpdated`/`PrepListItemUpdated` bubble up; halve → ×0.5; draft-only [finalized + other-event lists untouched]; unchanged count no-op). No IR/source/generated change → audit + drift gates unaffected; runtime suite 278 pass; runtime+api typecheck green. Mechanism: middleware. **NOTE:** the original line also mentioned "inventory demand / budget" — deferred: inventory demand-at-booking is a separate open item (EventCreated/Confirmed → demand middleware) and no event-level budget recompute target exists yet; this item delivers the prep-quantity rescale, which was the core defect.
- [x] **EventDishCreated/QuantityUpdated → PrepList & inventory** — DONE (2026-06-14). Adding a dish to a confirmed event, or changing a dish's serving count, now keeps the event's draft prep list in step. The prep list is derived ONCE at seed time (`prep-list-seed-middleware` on the `EventConfirmed run PrepList.create` shell); `EventDishCreated` (event-dish-rules.manifest) and `EventDishQuantityUpdated` had ZERO consumers — so a dish added after the seed never reached the kitchen, and a re-portioned dish never re-scaled its ingredient demand (silent missing-mise-en-place / food-quantity defect). **Fixed as MIDDLEWARE** (`manifest/runtime/src/middleware/event-dish-prep-sync-middleware.ts`, barrel-exported; registered in `manifest-runtime-factory.ts` right after the guest-count rescale leg). **Why middleware not a reaction (three blockers):** (1) 1:N fan-out (one dish change → many PrepLists → many PrepListItems); (2) the new ingredient rows are DERIVED across a cross-store walk (EventDish→Dish→RecipeVersion→RecipeIngredient→Ingredient→InventoryItem) the DSL cannot express; (3) `EventDishQuantityUpdated` carries only `quantityServings` (the lone `updateQuantity` param) — `eventId` is the EventDish's OWN field, never auto-populated onto the payload, so the dish must be LOADED. **Key correctness technique — RE-DERIVE + RECONCILE, not per-dish incremental:** the seed AGGREGATES ingredients across all of an event's dishes by inventory-item id (a shared ingredient sums contributions; only the first dish's id is recorded on the row), so a naive single-dish add would double-count or mis-attribute. The middleware instead re-derives the FULL current demand via the shared `deriveSeedLines` (exported from the seed middleware — single source of truth, no duplication) from the authoritative post-mutation dish set (so NO old/new ratio is needed, unlike the guest-count leg), then reconciles per draft list: a derived ingredient with no row → `PrepListItem.create`; a changed quantity → `PrepListItem.updateQuantity`; unchanged → no-op (no spurious event). **Preserves the guest-count rescale:** the reconcile target is `derivedScaled × currentBatchMultiplier` (NOT the raw seed scale), so adding a dish to a guest-rescaled (`batchMultiplier ≠ 1`) list does not wipe the rescale; `baseQuantity` (guest-independent recipe base) comes straight from the derivation. **SCOPE — draft lists only** (mirrors the guest-count leg: finalized/completed lists are locked for execution; an unseeded event has no draft list → clean no-op, the seed picks the dish up at confirm). Idempotency: structural (reconcile diffs current vs target before dispatching) plus per-row keys. Conformance test `manifest/runtime/src/__tests__/event-dish-prep-sync-middleware.test.ts` (5 tests: IR regression-lock no such reaction; a dish added after the seed creates its ingredient rows + leaves Dish A untouched + bubbles `PrepListItemCreated`; a serving-count change re-scales the existing rows; a prior guest-count rescale is preserved on new rows; a finalized list is never touched). No IR/source/generated change → audit + drift gates unaffected; runtime suite 287 pass; runtime+api typecheck green. Mechanism: middleware (+ shared `deriveSeedLines` export). **`EventDishRemoved` leg spun out below.**
- [x] **EventDishRemoved → prune PrepListItem demand** — DONE (2026-06-14). Removing a dish from a confirmed event now retracts the ingredient demand it carried from the draft prep lists, completing the EventDish→PrepList sync triad (add/re-scale/remove). `EventDishRemoved` (event-dish-rules.manifest, `EventDish.remove` soft-deletes via `deletedAt`) had no consumer, so a removed dish's mise-en-place lingered on the kitchen's prep list forever (over-prep / waste). **No DB migration needed** (the plan's "source + IR recompile" call was correct): `prep_list_items` already carries a `deleted_at` column and `PrepListItem` uses the generic store, which soft-deletes via `deletedAt` and filters reads — the gap was purely that the IR/DSL never declared the property or a `remove` command. **Source/IR:** added `property deletedAt` + `computed isDeleted` + `command remove(reason, userId)` (guards `self.deletedAt == null`, mutates `deletedAt = now()`, emits new `PrepListItemRemoved`) to PrepListItem (prep-list-rules.manifest), recompiled the IR; the IR-derived `generated-schema.prisma` picked up the one `deletedAt` line (schema:check back in sync) and the generated `prep-list-items/list` read route now filters `deletedAt: null`. **Middleware:** extended `event-dish-prep-sync-middleware` rather than adding a new one — it ALREADY re-derives the full demand and reconciles (create/update), so removal is just the inverse: an ingredient ONLY the removed dish demanded drops out of the re-derived set and is pruned (`PrepListItem.remove`), while a SHARED ingredient stays in the set at its reduced quantity and is decremented (existing update path), never pruned. **Two engine gotchas handled:** (1) once `remove` soft-deletes the EventDish the store filters it out AND `EventDishRemoved`'s payload does not carry `eventId` (engine payload is `{...commandInput, result}` only), so the dish's `eventId`/`tenantId` are captured on a `before-guard` hook (live `evalContext.self`) and read back on `after-emit` via `contextPatch` — the same two-hook technique as the guest-count rescale ([[middleware-premutation-capture-two-hook]]); the middleware now declares `hooks: ["before-guard", "after-emit"]`. (2) Prune touches ONLY derived rows (non-empty `recipeVersionId`, the seed/sync fingerprint) so a hand-added prep row is never collaterally removed; soft-deleted rows are skipped so a re-emit can't double-remove. **SCOPE — draft lists only** (mirrors the add/re-scale leg; finalized/completed lists are locked for execution). Idempotency key per (tenant, event, prepList, item). Conformance test: extended `event-dish-prep-sync-middleware.test.ts` (now 10 tests: +IR-lock that `PrepListItem.remove` exists; +orphan prune soft-deletes the row & bubbles `PrepListItemRemoved`; +shared ingredient decremented not pruned; +hand-added row preserved; +finalized list untouched on removal). Runtime suite 292 pass; runtime+api typecheck green; `manifest:validate`/validate-ai 100/`schema:check` in-sync/audit-reaction-payloads 0/0/route+parent-context audits clean. Mechanism: command + middleware (extends the existing leg). **EventDish→PrepList sync triad now COMPLETE.**
- [x] **EventCancelled → cascade cleanup** — DONE (2026-06-14). `EventCancelled` (event-rules.manifest, `Event.cancel(reason)`) had ZERO consumers — cancelling an event left every downstream commitment live (staff rostered, catering orders open, prep lists active, draft/sent invoices billable, collection cases chasing money for a dead event), each unwound by hand or silently rotting into bad labor/inventory/AR data. **Fixed as MIDDLEWARE** (each leg is a 1:N fan-out by `eventId` — a single-target reaction cannot resolve the set): `manifest/runtime/src/middleware/event-cancelled-cascade-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the event-created-client-interaction leg). On `EventCancelled` it loads each child entity from the store, filters by `tenantId + eventId`, and dispatches the governed terminal command per row — all guard-safe + idempotent (a child is only dispatched when its current status still satisfies the target guard, so a re-emit skips already-handled rows): **EventStaff.unassign** (status ∈ assigned/confirmed), **CateringOrder.cancel** (orderStatus not completed/cancelled — note the status field is `orderStatus`), **PrepList.cancel** (status != completed/cancelled), **Invoice.voidInvoice** (status ∈ DRAFT/SENT/VIEWED/OVERDUE **and amountPaid == 0** — money-bearing invoices are protected; they need a refund/credit, not a void), **CollectionCase.close** (status ∈ ACTIVE/IN_PROGRESS/DISPUTED, param is `resolution`). **Inventory reservation release** is handled by a decoupled sibling middleware (next bullet) that the dispatched `PrepList.cancel` auto-chains into (the engine's middleware dispatch re-enters `runCommand`, factory:804-808), so it is NOT duplicated in the cascade. **KNOWN LIMITATION (documented, not silent):** every dispatched leg runs as the SAME actor who cancelled the event, subject to that target's policy — CateringOrder.cancel / Invoice.voidInvoice / CollectionCase.close are role-restricted (catering_manager/finance/manager/admin), so a lower-privilege canceller gets a policy-denied + skipped leg with a diagnostic (the runtime has no per-call identity override). **PREREQUISITE BUG FOUND + FIXED (dead-target class):** `Invoice.voidInvoice` mutates `status = "VOID"` and `writeOff` mutates `status = "WRITE_OFF"`, but the Invoice transition list targeted `"VOIDED"`/`"WRITTEN_OFF"` — which matched NOTHING (not the commands, the API status union `accounting/invoices/{types,validation}.ts`, the `[id]` route guard `status === "VOID"`, the financial-reports filters, or the generated Prisma enum), so **both commands were DEAD** (the engine rejected every call: "Invalid state transition 'SENT' -> 'VOID'"). Fixed the transition list (invoice-rules.manifest:159-171) to the canonical `"VOID"`/`"WRITE_OFF"`, recompiled the IR, and fixed the now-reachable dead UI badge key (`invoices-client.tsx` `VOIDED` → `VOID` + added `WRITE_OFF`). Conformance test: `manifest/runtime/src/__tests__/event-cancelled-cascade-middleware.test.ts` (IR regression-lock no `EventCancelled` reaction exists; full cascade cancels all 5 child types + releases the prep-list reservation via the chain; money-bearing invoices NOT voided + already-terminal children skipped). Runtime suite 250 pass; runtime+api typecheck green. Mechanism: middleware (×1) + transition fix. **Inventory leg via the sibling below.**
- [x] **PrepListCancelled → release inventory reservations** — DONE (2026-06-14, the inventory leg of the EventCancelled cascade + a standalone leak fix). The `prep-inventory-demand` middleware RESERVES stock on `PrepListFinalized`; the consume middleware releases it on `PrepListCompleted`; but CANCELLATION had no consumer, so a finalized-then-cancelled prep list stranded its reserved stock forever (`quantityReserved` only grew → `quantityAvailable` bled down, items looked perpetually unavailable — the same P0 leak class, on the cancel path). **Fixed as MIDDLEWARE** (symmetric sibling of prep-list-completed-consume; 1:N fan-out over PrepListItem rows + needs the PrepList's `eventId` which is not a `cancel` param): `manifest/runtime/src/middleware/prep-list-cancelled-release-reservation-middleware.ts` (barrel-exported; registered right after the consume middleware). On `PrepListCancelled` it loads the PrepList (for `eventId`/`tenantId`), loads its PrepListItem rows, and per item whose linked InventoryItem still holds a reservation (`quantityReserved > 0` — skips never-reserved/draft-list items so no spurious `InventoryReservationReleased` events) dispatches governed `InventoryItem.releaseReservation(quantity = scaledQuantity, eventId, userId)`. Fixes the leak on EVERY prep-cancel path (standalone or event-driven) and is what releases the inventory an event's prep lists held when the cascade above cancels them. Idempotency key per (tenant, prepList, item). Conformance test: `manifest/runtime/src/__tests__/prep-list-cancelled-release-reservation-middleware.test.ts` (IR regression-lock; finalized-list cancel releases each item's reservation while on-hand is untouched; no spurious release for a never-reserved item). Mechanism: middleware.
- [ ] **EventFinalized → finance/inventory/followup** — release reserved inventory, finalize `EventBudget`, generate/finalize `Invoice`, create post-event `EventFollowup`/`ClientInteraction`. — PARTIAL: the `FinalizeEventWithReporting` saga (event-rules.manifest:614-627) already covers `EventProfitability.recalculate` + `EventSummary.create`; the inventory/budget/invoice/followup legs remain unwired (`EventFollowup` exists at events-extended-rules.manifest:517 but is unconnected). Mechanism: reaction + middleware.
- [ ] **EventProfitability auto-actuals** — feed `actualRevenue` from `Invoice.amountPaid`, `actualFoodCost` from approved `WasteEntry`, `actualLaborCost` from `TimeEntry`; trigger `recalculate` (event-rules.manifest:362) on `EventBudgetFinalized`/`PaymentApplied`. Both `PaymentApplied` (invoice-rules.manifest:186) and `EventBudgetFinalized` (event-budget-rules.manifest:134) are **orphan events** with no consumer. Mechanism: computed + reaction.

### Staffing, payroll & labor

- [~] **EventStaffAssigned → schedule/profile/calendar/payroll/notify** — PARTIAL (2026-06-14): the **notify leg is DONE**; schedule/profile/calendar/payroll legs remain. **`EventStaffAssigned → Notification.create` DONE** — rostering a staff member onto an event now notifies that person (the most direct staffing hand-off). `EventStaffAssigned` (event-staff-rules.manifest:148, emitted by BOTH `EventStaff.assign` AND the auto-bootstrap `create`) had ZERO consumers (verified: no reaction, middleware, or factory registration), so `/notifications` was blind to event staffing. **Mechanism: MIDDLEWARE** (not the plan's bare "reaction"): although the recipient `staffMemberId` IS an `assign`/`create` param (so a 1:1 reaction could resolve it), a *useful* notification names the event ("assigned to Smith Wedding as Lead Server") and the event **title is the Event's OWN field** — never on the `EventStaffAssigned` payload (engine payload is `{...commandInput, result}`; declared event fields are not auto-populated from `self.*`), so the leg must LOAD the Event (same pattern as deal-lifecycle's DealAssigned→Notification). Added `manifest/runtime/src/middleware/event-staff-assigned-notify-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the event-location-catering leg). On `EventStaffAssigned` it loads the persisted EventStaff row via `_subject.id` for authoritative `staffMemberId`/`eventId`/`role` (robust to the dual emitter's differing payloads; payload fallback if the row is unreachable), opportunistically loads the Event for its title, and dispatches governed `Notification.create(recipientEmployeeId = staffMemberId, notificationType "event_staff_assigned", correlationId = eventId, create id in BODY not instanceId)` — generic-message fallback when the Event row is missing so the notify is never blocked. Idempotency key `event-staff-notify:{tenant}:{eventStaffId}:{staffMemberId}` (the create/assign double-emit on one row dedups via the opt-in idempotency store; not unit-asserted per the repo's opt-in-idempotency convention [[deal-lifecycle-propagation-middleware]]). **KNOWN LIMITATIONS (documented, not silent):** (1) recipient = `StaffMember` id used as `Notification.recipientEmployeeId` (a `User`/employee id) — follows the app's existing unenforced `staffMemberId == employeeId` join convention; (2) the dispatch runs as the assigning actor, subject to `Notification.create`'s default `manager/admin` policy → a lower-privilege assigner (e.g. event_coordinator) yields a policy-denied diagnostic + skip (no per-call identity override; same class as deal-assign / event-created-client-interaction). Conformance test `manifest/runtime/src/__tests__/event-staff-assigned-notify-middleware.test.ts` (4 tests: IR regression-lock no such reaction; assign notifies the staff member with an event-named title/role-aware body + `NotificationCreated` bubbles up; missing-Event generic-message fallback still notifies; two distinct assignments each notify their own recipient). **NO IR/source/migration change** → audit + drift gates unaffected; runtime suite 365 pass; runtime+api typecheck green. Mechanism: middleware. **REMAINING legs (deferred, each its own increment):** `ScheduleShift.create` from the assignment's call times, calendar call-time, and the `EventProfitability` labor-estimate update (the last is blocked on the same no-rate/ambiguous-target issues as the LaborBudget legs above).
- [~] **EventStaffCheckedOut → TimeEntry / labor actuals** — BLOCKED, prerequisites scoped 2026-06-14 (do not attempt as-is). Two independent blockers found by source-verified investigation: **(1) the TimeEntry.addEntry leg has a create-bootstrap gap.** TimeEntry has NO `create` command — its creation path is `addEntry` (time-entry-rules.manifest:80). The engine only auto-instantiates a row when `commandName === "create"` (verified: `@angriff36/manifest` `dist/manifest/runtime-engine.js:2082` `shouldAutoCreateInstance = commandName === 'create' && !!options.entityName && !options.instanceId`; for any other command `instance = options.instanceId ? getInstanceRaw(...) : autoCreatePreparedData`, and `autoCreatePreparedData` is undefined for non-create commands — line 2097-2099). So dispatching `addEntry` from middleware/reaction would target a NON-EXISTENT instance and persist nothing. Wiring this requires FIRST authoring an explicit `TimeEntry.create` command (per the plan's "no reaction without a target — author the command in the same PR" rule), which overlaps semantically with the existing `addEntry`/`clockIn` and needs design review. **(2) the LaborBudget.recordActual leg is unsound** — confirmed BLOCKED ([[client-interaction-overdue-notify-middleware]] memory): LaborBudget is location+period-scoped with NO `eventId` (labor-budget-rules.manifest), there is no hourly-rate field to compute labor cost from EventStaff/StaffMember, and `recordActual(actualSpend)` SETS `actualSpend` (does not increment) so accumulation + the location/period→budget match are both ambiguous. **(3) identity caveat (not itself blocking):** `EventStaff.staffMemberId` (FK→StaffMember) is used as a `TimeEntry.employeeId` (FK→User/employees) only by an unenforced convention — the existing staff page joins `event_staff.staffMemberId = employees.id` via raw SQL with a comment admitting "no Prisma relation between EventStaff and User"; defensible (the app already relies on it) but worth a formal `StaffMember.userId` someday. **Resume plan:** ship only the TimeEntry leg after adding a `TimeEntry.create` command (design-reviewed), as MIDDLEWARE (load the checked-out EventStaff via `_subject.id` for `checkedInAt`/`checkedOutAt`/`staffMemberId`, since `EventStaffCheckedOut` carries only `eventStaffId`/`eventId`/`staffMemberId`/`checkedOutAt`). Leave the LaborBudget leg out. Mechanism: command (TimeEntry.create) + middleware.
- [x] **TimecardEditApproved → TimeEntry correction** — DONE (2026-06-14). **prerequisite (now satisfied):** `TimeEntry` had no command to apply a correction (only clockIn/clockOut/addEntry/softDelete at time-entry-rules.manifest:32-112). Add `TimeEntry.applyEdit(clockIn, clockOut, breakMinutes)` first, then `on TimecardEditApproved` (time-entry-rules.manifest:173) `run TimeEntry.applyEdit`. Mechanism: command + reaction. **VERIFIED 2026-06-14 (scoped, NOT yet implemented — next clean candidate):** the gap is real and high-value — `TimecardEditRequest.approve(userId)` only `mutate status = "approved"` + emits `TimecardEditApproved`; the corrected values `requestedClockIn`/`requestedClockOut`/`requestedBreakMinutes` (the request's OWN fields, NOT approve params) NEVER reach the `TimeEntry`, so approved edits are silently lost and payroll/labor uses uncorrected hours. **NO double-apply** (confirmed): neither the governed approve nor the non-governed `apps/api/app/api/timecards/bulk/route.ts` writes the corrected clock values back to the `TimeEntry` (the bulk route only UPSERTS pending edit requests + bulk-stamps `approvedBy/approvedAt`). **Mechanism is command + MIDDLEWARE, not reaction** (the corrected values + `timeEntryId` are the request's OWN fields). **NO migration** — `applyEdit` reuses existing `TimeEntry.clockIn/clockOut/breakMinutes` (plain mutate, guard `deletedAt == null`, no transition). Middleware loads the approved request via `_subject.id`, COALESCES each requested value against the current TimeEntry value (a null request field must not blank a real clock time), and dispatches `applyEdit` to `request.timeEntryId`. **Implemented as command + MIDDLEWARE:** added `TimeEntry.applyEdit(clockIn, clockOut, breakMinutes)` (plain mutate, guard `deletedAt == null`, no transition, no migration) + new `TimeEntryEdited` event to `time-entry-rules.manifest`; recompiled IR. The clock-time COALESCE lives in the COMMAND (`mutate clockIn = clockIn != null ? clockIn : self.clockIn`, same for clockOut — the established mutate-ternary idiom at call-planning-session-rules.manifest:53), so `applyEdit` is self-protecting (a partial edit can never blank a real clock time even if called directly with nulls) and the middleware stays a thin pass-through — a refinement over the plan's "middleware coalesces" wording (middleware no longer needs to load the TimeEntry just to coalesce). `requestedBreakMinutes` is `int=0` (never null → no migration to nullable) so the break value applies through. New `manifest/runtime/src/middleware/timecard-edit-approved-time-entry-apply-middleware.ts` (registered in `manifest-runtime-factory.ts`) filters `TimecardEditApproved`/`TimecardEditRequest`/`approve`, loads the approved request via `_subject.id`, reads `self.timeEntryId` + the requested values, guard-safe loads the TimeEntry (skips deleted/missing rather than firing a swallowed guard failure), and dispatches the governed `TimeEntry.applyEdit`. Idempotency key per request (`approve` is single-shot anyway: `status == "pending"` guard + pending→approved transition). Conformance test `timecard-edit-approved-time-entry-apply-middleware.test.ts` (4 tests: IR-lock the applyEdit command exists; corrected window+break applied + `TimeEntryEdited` bubbles up; null clock-in coalesces to the existing value; deleted-entry skip). runtime suite 315 pass; runtime typecheck green; `manifest:audit-reaction-payloads` (0 errors) + `manifest:schema:check` (no drift) unaffected (commands/events don't project to the Prisma schema). Mechanism: command + middleware. The NOTED BYPASS below (`timecards/bulk` direct Prisma writes) remains a separate tracked migration.
  - **NOTED BYPASS (constitution §9, document-not-fix this PR):** `apps/api/app/api/timecards/bulk/route.ts` performs direct Prisma writes to governed entities outside the runtime — `tx.timeEntry.updateMany` (approve sets `approvedBy/approvedAt`; reject sets `notes`; exception flags append to `notes`) and `tx.timecardEditRequest.upsert` (creates pending edit requests). It is a transport route owning governed mutations (should route through the dispatcher / `TimecardEditRequest` + `TimeEntry` commands). Separate migration; track here.
- [!] **TimeEntryClockedOut/Added → LaborBudget.recordActual** — **BLOCKED as specified (verified 2026-06-14); needs 3 prerequisite fixes first.** (1) **No pay rate on `TimeEntry`** — the entity (time-entry-rules.manifest:5-31) stores `employeeId/locationId/clockIn/clockOut/breakMinutes` and computeds `totalHours`, but NO `hourlyRate`/`payRate`/`grossPay`. Labor COST (hours×rate) cannot be derived from TimeEntry's own fields or the event payloads — the rate lives on the raw `employees` table (`apps/api/lib/staff/labor-budget.ts:514` reads `e.hourly_rate`), which is not a Manifest entity reachable via `storeProvider`. (2) **Ambiguous target** — `LaborBudget` (labor-budget-rules.manifest:5-21) has `locationId`+`periodStart`+`periodEnd` but NO FK from TimeEntry; the existing imperative matcher (`labor-budget.ts:606-676 getApplicableBudgets`) returns **0-N** budgets per (location, date) — location-specific OR tenant-wide, possibly overlapping — and the Manifest store API exposes only `getById(id)`, no predicate query by location+date-range, so middleware cannot resolve the single target. (3) **`recordActual(actualSpend)` is a full SET** (`mutate actualSpend = actualSpend`, line 95), not an increment — calling it per-TimeEntry would clobber the accumulated total; there is no `addToActual(delta)` and no atomic read-modify-write in the command model (race under concurrent clock-outs). **Prerequisites before this is viable:** add `hourlyRate`/`laborCost` to TimeEntry (or expose Employee as a governed entity), add an `addToActual(delta)` increment command to LaborBudget, and either add a deterministic TimeEntry→LaborBudget FK or accept the fan-out-to-all-matching semantics in a middleware with a real predicate-query store. Mechanism: schema + command + middleware (re-scope).
- [!] **TimecardApproved → PayrollLineItem.create** — **BLOCKED / NOT A CLEAN TARGET (verified 2026-06-14).** The emitter is `TimecardApproval.approve(approvedBy, notes)` (staff-logistics-extended-rules.manifest:45-55, MUTATE), and the `TimecardApproval` entity (lines 10-86) stores ONLY `employeeId` + `payrollRunId` + approval-workflow fields — it holds **zero pay-calculation data** (no hours, no rates, no grossPay). But `PayrollLineItem.create` (line 113) requires **10 params**: `payrollRunId, employeeId, grossPay, netPay, totalDeductions, hoursWorked, hoursRegular, hoursOvertime, rateRegular, rateOvertime`. A middleware could load `employeeId`/`payrollRunId` from the approval, but the 8 pay-calc params are unreachable — they would have to be re-derived by scanning `TimeEntry` rows and **replicating payroll-engine math**, or defaulted to `0` (garbage line items). **Worse: a governed create path ALREADY exists** — `apps/api/lib/payroll/manifest-payroll-data-source.ts:207` (`savePayrollRecords`) dispatches `PayrollLineItem.create` from the payroll engine's calculated `PayrollRun.process` flow. Wiring `TimecardApproved → PayrollLineItem.create` would DOUBLE-CREATE line items (approval path + payroll-run path) for the same employee/run. **Correct design (deferred):** payroll line items belong to the calculated `PayrollRun.process` flow, not to per-timecard approval; if approval should *gate* payroll-run processing, that is a `TimecardApproved → PayrollRun` state-guard relationship, not a line-item create. Re-scope before attempting. Mechanism: ~~reaction~~ → none viable as specified.
- [x] **PayrollRunPaid → lock period / close tip pool / notify** — DONE (2026-06-15): all three legs shipped (lock leg 2026-06-14; close-TipPool + notify legs 2026-06-15). **`PayrollRunPaid → close TipPool(s) + notify employees` DONE (2026-06-15)** — added `manifest/runtime/src/middleware/payroll-run-paid-cascade-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the lock leg), one file with both remaining legs fanning out off the same `PayrollRunPaid` event. **Both legs are middleware, not reactions** (markPaid() takes no params → payload `{...commandInput, result}` is a `paidAt` scalar): (1) close leg reaches the pools via the `PayrollRun.payrollPeriodId → PayrollPeriod.id ← TipPool.periodId` chain (TipPool has NO direct `payrollRunId` FK; payrollPeriodId is the run's OWN field → load run via `_subject.id`, then scan TipPool store by periodId — 1:N), guard-mirrors `close()`'s `status in [allocated, distributed]` so open/closed pools skip cleanly; (2) notify leg collects the DISTINCT `employeeId`s on the run's `PayrollLineItem` rows (scan by `payrollRunId`, 1:N) and dispatches `Notification.create` per employee (id in BODY not instanceId). Legs are independent (a run with no period still notifies; a run with no line items still closes pools); idempotent per pool (`...:close`) / per (run, employee). **PREREQUISITE DEAD-COMMAND FIX (the crux):** `TipPool.close()` guards `status in ["allocated","distributed"]` but the FSM only declared `distributed → closed` — so close() was DEAD from `allocated` (engine rejected the undeclared transition; same dead-target class as Invoice VOID / CollectionCase WRITTEN_OFF). Added `allocated → closed` to the FSM (staff-logistics-extended-rules.manifest) + recompiled IR (incidental compiler bump 2.5.0→2.5.1; unrelated shards changed only in provenance, contentHash stable). **KNOWN LIMITATION (documented):** each dispatch runs as the actor who marked the run paid, subject to `Notification.create`/`TipPool.close`'s manager/admin policy — markPaid is itself a finance/admin action so the common path aligns; a lower-privilege actor gets a policy-denied diagnostic + skip (no per-call identity override). Conformance test `manifest/runtime/src/__tests__/payroll-run-paid-cascade-middleware.test.ts` (4 tests: IR-lock no PayrollRunPaid reaction + close() reaches closed from BOTH allocated and distributed [pins the FSM fix] + Notification.create/PayrollLineItem exist; happy path closes allocated+distributed pools + notifies emp-A once despite 2 line items + emp-B + TipPoolClosed/NotificationCreated bubble; guard-safe open/closed pools untouched; tenant/run/period-scoped + independent legs). Verified: runtime suite 392 pass (75 files); runtime+api typecheck clean; `manifest:validate` valid / `schema:check` no-drift (transitions don't project) / `audit-reaction-payloads` 10 checked 0/0. Mechanism: middleware (one file, two legs) + transition (dead-command) fix.

  **`PayrollRunPaid → PayrollPeriod.lock` DONE (2026-06-14, historical — the lock leg)** — once a payroll run is marked PAID, its pay period is now LOCKED so paid payroll becomes immutable (no reopen, no retroactive time-entry edits into an already-paid period — closing a real financial-integrity hole; the period otherwise stayed `closed`/`open` and editable). `PayrollRunPaid` (payroll-rules.manifest:353, emitted by `PayrollRun.markPaid()`) had ZERO consumers. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `markPaid()` takes NO params and is a MUTATE (last mutate `paidAt = now()`), so the emitted payload `{...commandInput, result}` carries only a `paidAt` scalar; the period to lock is `PayrollRun.payrollPeriodId` — the **run's OWN field** (payroll-rules.manifest:175), NOT a `markPaid` param, and declared event fields are never auto-populated from `self.*` — so a reaction reading `payload.payrollPeriodId` is a silent no-op. **Fixed as MIDDLEWARE** (direct sibling of `payment-plan-completed-collection-case-resolve`): `manifest/runtime/src/middleware/payroll-run-paid-period-lock-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the timecard-edit leg, in the staffing/payroll group), which on `PayrollRunPaid` loads the PayrollRun via `_subject.id`, reads `self.payrollPeriodId`, and dispatches the governed `PayrollPeriod.lock` (no-param mutate; id in body AND instanceId). **Guard-safe:** `PayrollPeriod.lock()` guards `self.status == "closed"` (FSM closed→locked only), so the middleware only dispatches for a `closed` period — an already-`locked` period (a second run paid against it) or a still-`open` period (paid before being closed — anomalous) is skipped via `onDiagnostic`, never a swallowed transition failure. Idempotent per period (`payroll-run-paid-lock:{tenant}:{periodId}:lock`; locking is terminal). **NO IR/source/migration change** (both `PayrollRunPaid` and `PayrollPeriod.lock` already exist) → reaction-payload + schema-drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/payroll-run-paid-period-lock-middleware.test.ts` (4 tests: IR regression-lock no `PayrollRunPaid` reaction + both source FSMs allow the path [markPaid approved→paid, lock closed→locked]; paid run locks its closed period; still-open period left untouched [guard-safe]; no-payrollPeriodId clean skip). Verified: runtime suite 369 pass; runtime+api typecheck exit 0; `manifest:audit-reaction-payloads` 10 checked / 0 errors / 0 warnings. Mechanism: middleware. **REMAINING legs NOW DONE (2026-06-15)** — close-TipPool (the `PayrollRun.payrollPeriodId → PayrollPeriod.id ← TipPool.periodId` chain) + notify-employees (PayrollLineItem fan-out) shipped in `payroll-run-paid-cascade-middleware.ts`; see the 2026-06-15 writeup at the top of this item. The whole `PayrollRunPaid` item is now closed.
- [x] **OpenShiftClaimed → ScheduleShift.create** — DONE: both legs shipped (ScheduleShift.create 2026-06-15; `Schedule.shiftCount` reconciliation 2026-06-15). Claiming an open shift now materializes the real roster shift. `OpenShiftClaimed` (staff-logistics-extended-rules.manifest:1180, emitted by `OpenShift.claim`) had ZERO consumers — the claim flipped the open shift to `claimed` + stamped `claimedBy` but produced NO `ScheduleShift`, so a claimed shift never appeared on the roster / labor reports / the employee's schedule (a manager re-created it by hand). **The plan's prerequisite-(2) "the event carries only scheduleId/claimedBy → params unsuppliable" was a REASON to use MIDDLEWARE, not a blocker:** `OpenShift.claim(claimedBy)` is a MUTATE whose payload `{...commandInput, result}` carries only `claimedBy`; the shift's `scheduleId`/`role`/`shiftStart`/`shiftEnd` are the OpenShift's OWN fields (declared event fields are never auto-populated from `self.*`), so a reaction can't read them — but a middleware LOADS the OpenShift via `_subject.id`. **Second wrinkle solved:** `ScheduleShift.create` (schedule-rules.manifest:159) requires a `locationId` that is NOT one of its declared params and is NOT on the OpenShift — it lives on the parent `Schedule`; the middleware loads the Schedule (via the open shift's `scheduleId`) and body-seeds `locationId` (the production POST `apps/api/app/api/staff/shifts/route.ts` likewise body-seeds it — the create-bootstrap seeds required-but-non-param fields from the body, same as the CollectionCase.create middleware). Added `manifest/runtime/src/middleware/open-shift-claimed-create-schedule-shift-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the staff-deactivated-unassign leg). Field map: employeeId←claimedBy (both `User` ids), scheduleId/shiftStart/shiftEnd/role←OpenShift, locationId←Schedule. Guard-safe + idempotent: mirrors `create`'s `shiftEnd > shiftStart` guard + the `validShiftTimes` invariant (skips an invalid/missing window), skips when the parent schedule has no `locationId` (required field), and dedups against an existing matching non-deleted ScheduleShift (tenant+schedule+employee+start+end) plus a stable idempotencyKey (`claim` is single-shot via the open→claimed FSM anyway). No double-apply: the open-shift detail route is GET-only and the claim flows through the canonical dispatcher; nothing else creates a shift on claim. **KNOWN LIMITATION (documented):** the create runs as the claiming actor, subject to `ScheduleShift`'s `manager/admin` policy; `OpenShift.claim` also admits `hr_admin`/`payroll_admin`, whose creates are policy-denied + skipped with a diagnostic (no per-call identity override). **NO IR/source/migration change** (both the trigger event and `ScheduleShift.create` already exist) → reaction-payload + schema-drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/open-shift-claimed-create-schedule-shift-middleware.test.ts` (4 tests: IR regression-lock no `OpenShiftClaimed→ScheduleShift` reaction; claim creates the shift for the claimer with the schedule's location + shift window/role + `ScheduleShiftCreated` bubbles up; location-less schedule → no shift; idempotent — a matching roster shift is not duplicated). Verified: runtime suite 428 pass (83 files); runtime+api typecheck exit 0. Mechanism: middleware. **`Schedule.shiftCount` reconciliation DONE (2026-06-15)** — closes the unmaintained-counter gap (also flagged on the time-off shift-cleanup leg below). `Schedule.shiftCount` is a STORED int read by `Schedule.approve`'s `guard self.shiftCount > 0` and `Schedule.release`'s `blockNoShifts:block self.shiftCount > 0`, but NO ScheduleShift create/remove path (governed command or the manager-create route) ever moved it past its create-time 0 — so both gates were STARVED: a schedule with real shifts could neither be approved nor published (the same dead-counter deadlock class as the Proposal `lineItemCount` send-block). **Fixed as a RECOMPUTE middleware + a new `Schedule.syncShiftCount(count)` command** (mirroring `Station.syncTaskCount`, NOT the plan's `increment`/`decrement` pair): added `manifest/runtime/src/middleware/schedule-shift-count-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the open-shift-claimed leg). On `ScheduleShiftCreated`/`ScheduleShiftRemoved` it resolves the affected `scheduleId` (a create param on the payload; the remove payload carries no `scheduleId` — declared event fields are never auto-populated from `self.*` — so the soft-deleted row is loaded via `_subject.id` to recover it), recomputes the TRUE count (= non-deleted `ScheduleShift` rows for that schedule), and dispatches the absolute, idempotent `Schedule.syncShiftCount(count)` ONLY when the stored count drifted. **Why recompute, not delta:** a +/- delta cannot decrement on remove (no `scheduleId` on the payload) and would leak; recompute is correct on every path AND HEALS drift (out-of-band/importer writes are picked up on the next governed event since the count reads the real store state). Authored `command syncShiftCount(count: int)` (plain mutate `shiftCount = count`, guard `count >= 0`, emits new `ScheduleShiftCountSynced`) + recompiled the IR (1053→1054 commands); **NO migration** (`shiftCount` already exists) and commands/events don't project so `schema:check` stays in-sync. **KNOWN LIMITATION (documented):** the sync runs as the acting actor under `Schedule`'s `manager`/`admin` policy; a `kitchen_lead` (admitted by `ScheduleShift`'s LeadsCanManageShifts) gets a policy-denied diagnostic + skip — manager/admin (the common path) always passes. Conformance test `manifest/runtime/src/__tests__/schedule-shift-count-middleware.test.ts` (5 tests: IR lock — `syncShiftCount` exists + no ScheduleShift→Schedule reaction; **deadlock fix end-to-end** — a 0-shift draft cannot be approved, creating a shift syncs the count + `ScheduleShiftCountSynced` bubbles + approval then succeeds; multi-shift true count; removal decrements + re-locks approval when the last shift is removed; **drift heal — a stale `shiftCount` of 9 is corrected to the true 1, proving absolute recompute not a delta**). Verified: runtime suite 433 pass (84 files); runtime+api typecheck exit 0; `manifest:validate` valid / `schema:check` no drift / `audit-reaction-payloads` 10 checked 0/0. Mechanism: command + middleware.
- [x] **TimeOffRequestApproved → conflicting shift cleanup** — DONE (2026-06-14). Approving an employee's time-off request now removes that employee's scheduled shifts that fall inside the approved window. `TimeOffRequestApproved` (time-off-request-rules.manifest:155) had ZERO consumers, so approving PTO changed only the request's own status and left every conflicting `ScheduleShift` live — the schedule showed the employee both on approved leave AND rostered to work (a double-booking a human had to clean up by hand). **Pure runtime MIDDLEWARE, NO IR/source change** (both the trigger event and `ScheduleShift.remove` already exist in IR — same shape as the SchedulePublished leg; chosen deliberately given the in-flight shared working tree). Middleware (not a reaction) for two structural reasons: (1) 1:N fan-out (one approved request → MANY conflicting shifts; a reaction resolves exactly ONE target); (2) the fields needed to find the conflicts (`employeeId`/`startDate`/`endDate`) are the TimeOffRequest's OWN fields, NOT `approve` params — the `TimeOffRequestApproved` payload carries only `{requestId, processedBy, processedAt}` (engine payload is `{...commandInput, result}`; declared event fields are never auto-populated from `self.*`), so the request must be LOADED via `_subject.id` and the shifts QUERIED by employee + date. Added `manifest/runtime/src/middleware/time-off-approved-shift-cleanup-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the SchedulePublished leg). On `TimeOffRequestApproved` it loads the request, reads employee + whole-day-floored `[startDate, endDate]` range, scans `ScheduleShift` for that tenant+employee whose window OVERLAPS the range (`shiftStart < midnight-after-endDate AND shiftEnd >= startDate`; missing `shiftEnd` treated as a point at `shiftStart`), and dispatches governed `ScheduleShift.remove(userId=processedBy)` per conflict. Soft-deleted shifts are filtered out before dispatch so a re-delivered event is a clean no-op; per-(request, shift) idempotency key; every skip reported via `onDiagnostic`. **Dates:** `startDate`/`endDate` are date strings, `shiftStart`/`shiftEnd` are epoch-ms datetimes — both floored/coerced to epoch ms (flooring to a multiple of 86_400_000 = UTC midnight) for a clean whole-day comparison. **DEFERRED — restore-on-cancel (documented, not silent):** cancelling an approved request does NOT bring removed shifts back; it needs (1) a new `ScheduleShift.restore` command (mutate `deletedAt = null` → IR/source change) AND (2) a removal-provenance marker so a cancel handler can tell which soft-deleted shifts THIS request removed vs ones a manager deleted on purpose — restoring by employee+date alone would resurrect deliberately-deleted shifts. Tracked as a follow-up. **KNOWN LIMITATION (documented):** each `remove` dispatch runs as the approving actor, subject to `ScheduleShift`'s `manager/admin` policy; the common manager/admin approval passes, but `approve`'s own policy also admits `hr_admin`/`payroll_admin`, whose removes are policy-denied + skipped with a diagnostic (no per-call identity override). Conformance test `manifest/runtime/src/__tests__/time-off-approved-shift-cleanup-middleware.test.ts` (4 tests: IR regression-lock no such reaction; in-window shifts removed while day-before/day-after survive + `ScheduleShiftRemoved` bubbles up; another employee's same-day shift untouched; already-soft-deleted shift left as-is = idempotent). Runtime suite 346 pass; runtime+api typecheck green. Mechanism: middleware. **NOTE:** ~~`Schedule.shiftCount` is not decremented when a shift is removed~~ **RESOLVED 2026-06-15** — the new `schedule-shift-count-middleware` (see the OpenShiftClaimed item above) now reconciles `Schedule.shiftCount` on every governed `ScheduleShift` create/remove via a recompute, so the removals this leg performs correctly drive the count down too.
- [~] **StaffMemberDeactivated → unassign future work** — **EventStaff leg ALREADY IMPLEMENTED + committed** (commit `05ddf9533`): `manifest/runtime/src/middleware/staff-member-deactivated-unassign-event-staff-middleware.ts` + conformance test, registered in `manifest-runtime-factory.ts`, unassigns the staff member's `assigned`/`confirmed` EventStaff rows. **NOW LIVE (verified 2026-06-15)** — the previously-noted INERT blocker is RESOLVED. The ⛔ 2.5.0 computed-in-guard migration inlined `StaffMember.deactivate`'s guard to `guard (self.status == "active")` (staff-member-rules.manifest:56, confirmed in source; same inline on updateProfile/changeRole), so `deactivate` now emits `StaffMemberDeactivated` and the committed middleware fires end-to-end. The conformance test (`staff-member-deactivated-unassign-event-staff-middleware.test.ts`) drives the REAL `deactivate` command through the engine and asserts `StaffMemberDeactivated` emits + the assigned/confirmed assignments flip to `unassigned` (4 tests, passing) — a test that could only pass once the guard stopped evaluating the dead computed. So the **EventStaff leg of this item is DONE**. The `ScheduleShift` leg is correctly OUT (its `employeeId belongsTo User`, a different id-space than `StaffMember` — identity mismatch); the `TimecardEditRequest`-cancel leg still needs a new `cancel` command. Mechanism: (migration to un-dead the guard) — the middleware already exists.
- [x] **SchedulePublished → notify staff** — DONE (2026-06-14). Publishing a staff schedule (`Schedule.release`, approved/draft → published) now notifies every employee who has a shift on it. `SchedulePublished` (schedule-rules.manifest:280) had ZERO consumers, so the most time-sensitive scheduling event reached no one and `/notifications` was blind to it. **Pure runtime MIDDLEWARE, NO IR/source change** (chosen deliberately given the in-flight IR working-tree state — [[concurrent-loop-shared-tree]]): both the trigger event AND `Notification.create` (notification-rules.manifest:31) already exist in IR. Middleware (not a reaction) for two reasons: (1) 1:N fan-out (one Schedule → many `ScheduleShift` rows → one Notification per distinct employee — a reaction resolves exactly ONE target); (2) the recipients are NOT on the payload — `SchedulePublished` carries only `{scheduleId, scheduleDate, shiftCount, publishedBy, publishedAt}` (the engine payload is `{...commandInput, result}` only; declared event fields are never auto-populated from `self.*`), so the employee ids must be QUERIED off `ScheduleShift` by `scheduleId`. Added `manifest/runtime/src/middleware/schedule-published-notify-staff-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the schedule-shift-first-shift leg), which on `SchedulePublished` loads the schedule's ACTIVE shifts (skips soft-deleted), collects DISTINCT employee ids (an employee with 2 shifts is notified once), and dispatches governed `Notification.create(recipientEmployeeId, notificationType="schedule_published", title, body, correlationId=scheduleId)` per recipient (create id in the body NOT instanceId, [[lead-deal-middleware-create-fanout]]). **Idempotency key embeds `publishedAt`** (`schedule-notify:{tenant}:{scheduleId}:{publishedAt}:{employeeId}`) so a re-delivered identical event dedupes while a genuine re-publish (reopen → release, new `publishedAt`) re-notifies — the correct behaviour when the schedule changed. **KNOWN LIMITATION (documented):** each dispatch runs as the publishing actor, subject to Notification's `manager/admin` policy — which ALWAYS aligns here since `Schedule.release` itself requires manager/admin (so the common path never trips it). Conformance test: `manifest/runtime/src/__tests__/schedule-published-notify-staff-middleware.test.ts` (4 tests: IR regression-lock no such reaction exists; publish notifies each distinct employee exactly once [emp with 2 shifts → 1 notification] + `NotificationCreated` bubbles up; soft-deleted shift's employee not notified; only the published schedule's employees notified, not other schedules'). Runtime suite 311 pass; runtime typecheck green; no IR/source/generated change → audit + drift gates unaffected. Mechanism: middleware.
- [~] **Fix `BudgetAlert` parent + auto-create** — PARTIAL (2026-06-15): the **auto-create leg is DONE**; the relationship-target fix remains. **`LaborBudgetActualRecorded (over target) → BudgetAlert.create` DONE** — recording an actual that exceeds the budget target now opens a governed budget alert, so an over-budget labor period no longer stays silent until a human spots the number. `LaborBudgetActualRecorded` (labor-budget-rules.manifest:231) had ZERO consumers (the app-side `createBudgetAlert` helper at `apps/api/lib/staff/labor-budget.ts:681` is exported but **never called** anywhere — and its `alertType` enum `threshold_80/90/100/exceeded` doesn't even match the governed `BudgetAlert.create` guard `["threshold_warning","threshold_exceeded","overage","critical"]` — so there is NO existing alert path and thus NO double-apply hazard). **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `LaborBudget.recordActual(actualSpend)` is a MUTATE, so the emitted payload `{...commandInput, result}` carries only the `actualSpend` param — NOT the budget's OWN `budgetTarget` needed to decide "is this over budget?" (declared event fields like `LaborBudgetActualRecorded.tenantId` are never auto-populated from `self.*`). **Fixed as MIDDLEWARE** (sibling of `invoice-overdue-collection-case-create`): `manifest/runtime/src/middleware/labor-budget-actual-recorded-alert-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the invoice-written-off leg) loads the LaborBudget via `_subject.id`, compares post-mutation `actualSpend > budgetTarget`, and only then dispatches the governed `BudgetAlert.create(alertType="overage", utilization=round(actual/target*100,2), message)` (create id in BODY not instanceId). **Scope = over target only** (the "(over target)" the plan calls for); the common under-budget recording is a quiet no-op (no alert, no log spam); threshold-warning tiers (80%/90%) are a deferred follow-on. **Idempotent**: at most one UNRESOLVED `overage` alert per budget (scan BudgetAlert store), so recording actuals repeatedly while over budget never piles up duplicates; once that alert is resolved a later overage opens a fresh one; stable idempotencyKey keyed on the recorded spend is a second backstop. Skips a non-positive `budgetTarget` via `onDiagnostic`. **Policy aligns** (both `recordActual` and `BudgetAlert.create` require finance/finance_manager/manager/admin → the recording actor can always dispatch; no per-call identity override needed). **NO IR/source/migration change** (both the event and `BudgetAlert.create` already exist) → reaction-payload + schema-drift gates unaffected. Biome-complexity kept in budget by extracting `processRecordedBudgetEvent` (same pattern as the logistics-route leg). Conformance test `manifest/runtime/src/__tests__/labor-budget-actual-recorded-alert-middleware.test.ts` (5 tests: IR regression-lock no `LaborBudgetActualRecorded→BudgetAlert.create` reaction; over-budget recording opens an `overage` alert with the right utilization + `BudgetAlertCreated` bubbles up; under-budget = no alert; idempotent while an unresolved overage exists; a resolved prior alert allows a fresh one). Verified: runtime suite 424 pass (82 files); runtime+api typecheck exit 0; `manifest:audit-reaction-payloads` 10 checked 0/0; `manifest:schema:check` no drift. Mechanism: middleware. **REMAINING — the relationship-target fix:** `BudgetAlert.belongsTo budget: EventBudget with budgetId` (labor-budget-rules.manifest:178) is the WRONG parent — `BudgetAlert` lives in the LaborBudget file and `budgetId` is a LaborBudget FK; change `EventBudget` → `LaborBudget` (and drop EventBudget's now-orphaned `alerts` back-relation). Deferred from this increment because it touches the IR-derived Prisma relation (schema-drift-gate surface) whereas the auto-create leg is pure-runtime; ship it as its own small correctness increment. Mechanism (remaining): relationship fix.

### Finance & collections

- [x] **InvoiceMarkedOverdue → CollectionCase.create** — DONE (2026-06-14). `InvoiceMarkedOverdue` (invoice-rules.manifest:213, emitted by `Invoice.markOverdue()`) had ZERO consumers: an overdue invoice never entered an AR-recovery workflow — the only path to a CollectionCase was the manual POST route (`accounting/collections/cases/route.ts`), so delinquent debt silently accumulated until someone opened a case by hand. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `markOverdue()` takes NO params and is a MUTATE (last mutate `overdueSince = now()`), so the emitted payload is `{...commandInput, result}` = just a timestamp scalar; every field a collection case needs (`clientId`/`eventId`/`total`/`amountDue`/`invoiceNumber`) is the **Invoice's OWN field**, NOT a `markOverdue` param, and declared event fields (`InvoiceMarkedOverdue.invoiceId`/`.invoiceNumber`/`.daysOverdue`) are NEVER auto-populated from `self.*` — so no reaction could read them. **Fixed as MIDDLEWARE**: `manifest/runtime/src/middleware/invoice-overdue-collection-case-create-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the collection-payment leg), which on `InvoiceMarkedOverdue` loads the Invoice via `_subject.id`, reads its client/event/amounts, and dispatches the governed `CollectionCase.create`. Idempotent: skips when a case already exists for the invoice (mirrors the route's 409 guard — scans the CollectionCase store by `invoiceId`, non-deleted) + a stable `idempotencyKey`; guard-safe: skips a zero-total invoice (would fail CollectionCase's `amount_positive` constraint). clientName is resolved opportunistically from the Client store (businessName→name→displayName→contactName→""). **PREREQUISITE COMMAND AUTHORED (the crux):** `CollectionCase` had NO explicit `create` command, so `engine.runCommand("create", …, {entityName:"CollectionCase"})` (the middleware dispatch path) returned `Command 'create' not found` — unlike Deal/ClientInteraction whose middleware works because they DO declare an explicit `create`. The manual route only worked because `runManifestCommandCore` special-cases create-bootstrap. Added an explicit `command create(invoiceId, clientName, originalAmount, outstandingAmount)` to collections-rules.manifest + a new `CollectionCaseCreated` event. **Two gotchas folded in:** (1) `status` is deliberately NOT a param/mutate — it carries transitions, the create seed defaults it to "ACTIVE", and mutating it to "ACTIVE" would be a rejected no-op self-transition ([[create-command-transition-self-loop-bug]]); the engine's auto-create still seeds the full instance from the body and generates the id. (2) Params are kept MINIMAL (only collection-specific, non-parent-inferable fields) so the parent-context audit stays clean — the FK-mirrored fields (invoiceNumber/eventId/clientId) and coincidental name-collisions (metadata/assignedTo) are NOT params; the caller supplies them in the body, where a body value always wins over parent-context inheritance ([[parent-context-over-inheritance-metadata-fence]]; resolver `ALWAYS_EXCLUDED` also fences `status`). Conformance test: `manifest/runtime/src/__tests__/invoice-overdue-collection-case-create-middleware.test.ts` (4 tests: IR regression-lock no such reaction; an overdue invoice opens an ACTIVE/MEDIUM case carrying the invoice's debt + client/event/number + resolved clientName; idempotent — no second case when one exists; zero-total invoice skipped). Verified: runtime suite 273 pass; parent-context audit 6/6; runtime+api typecheck exit 0; `manifest:validate` ok / validate-ai 100 / schema:check no-drift / audit-reaction-payloads 0/0. Mechanism: command + middleware.
- [x] **Invoice reaches zero → markAsPaid** — DONE (2026-06-14). `Invoice.applyPayment` (invoice-rules.manifest:94-102) UNCONDITIONALLY mutates `status = "PARTIALLY_PAID"`, even when `paymentAmount == amountDue` (a full payment, which drives `amountDue` to exactly 0) — so an invoice paid in full via the apply path was stranded at PARTIALLY_PAID with `amountDue = 0` and NEVER reached PAID. `Invoice.markAsPaid()` (line 104) and the transitions to PAID already existed, but nothing called markAsPaid; fully-settled invoices kept showing as owed and AR/collections kept chasing them. **The plan's "Mechanism: reaction + computed + guard" was WRONG** per the verified engine-semantics correction at the top of P0: whether the balance is now zero depends on the Invoice's OWN post-mutation `amountDue`/`status` — NOT on `applyPayment`'s input params, and the declared `PaymentApplied` fields (`remainingBalance`, …) are NEVER auto-populated from `self.*` (the engine emits `{ ...commandInput, result }` only). So a reaction structurally cannot read the remaining balance to decide the close, and no `amountDue`-computed conversion was needed (the stored prop is correct post-mutation). **Fixed as MIDDLEWARE**: removed nothing from source (no IR change); added `manifest/runtime/src/middleware/invoice-fully-paid-mark-paid-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the invoice-overdue leg), which on `PaymentApplied` loads the Invoice via `_subject.id`, reads the authoritative post-mutation `status`/`amountDue`, and dispatches governed `Invoice.markAsPaid` when `amountDue <= 0` and `status != "PAID"`. **The re-trigger concern is solved by SCOPE, not a source guard:** `markAsPaid` ALSO emits `PaymentApplied` (line 108), but the handler filters `command.name === "applyPayment"`, so markAsPaid's own PaymentApplied never re-enters the middleware — no cycle (belt+suspenders: it also skips `status == "PAID"`, which both prevents a rejected `PAID -> PAID` no-op self-transition and makes a stray re-emit a clean no-op; plus a stable idempotencyKey). A PARTIAL payment (`amountDue > 0`) is the common path and is a quiet no-action (no log spam); only anomalies + the close are diagnosed. Mutate-dispatch idiom: id in body AND instanceId. Conformance test: `manifest/runtime/src/__tests__/invoice-fully-paid-mark-paid-middleware.test.ts` (4 tests: IR regression-lock no PaymentApplied→markAsPaid reaction; full payment → PAID + paidAt + amountDue 0; partial → stays PARTIALLY_PAID; partial-then-final → PAID). Verified: runtime suite 282 pass; runtime+api typecheck green; `manifest:audit-reaction-payloads` 0/0 (no IR change); `manifest:schema:check` no drift. Mechanism: middleware.
- [~] **ContractSigned → RevenueRecognitionSchedule.create; Invoice paid → schedule complete; InvoiceWrittenOff → RevenueRecognitionSchedule.cancel** — wire revenue recognition to the invoice lifecycle. **`InvoiceWrittenOff → RevenueRecognitionSchedule.cancel` DONE (2026-06-14)** — when an invoice is written off as uncollectable, every revenue-recognition schedule still recognizing revenue against it is now CANCELLED, so the books stop accruing earned revenue on dead debt and no PENDING/IN_PROGRESS schedule dangles forever. `InvoiceWrittenOff` (invoice-rules.manifest:231) had ZERO consumers and `RevenueRecognitionSchedule.cancel` (revenue-recognition-rules.manifest:172) was unreachable from the invoice lifecycle. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `Invoice.writeOff(reason, writeOffAmount)` is a MUTATE (last mutate `amountDue = 0`) → payload `{...commandInput, result}` carries only a scalar; the schedule(s) to cancel are identified by `RevenueRecognitionSchedule.invoiceId` — the **SCHEDULE's OWN field on the related child entity**, NOT a writeOff param — and it is a **1:N fan-out** (one invoice may have several schedules) a single-target reaction cannot do. **Fixed as MIDDLEWARE** (`manifest/runtime/src/middleware/invoice-written-off-revrec-cancel-middleware.ts`, barrel-exported; registered in `manifest-runtime-factory.ts` right after the invoice-fully-paid leg) — combines the scan-by-invoiceId pattern of `invoice-overdue-collection-case-create` with the dispatch-mutate pattern of `collection-written-off-invoice-write-off`: on `InvoiceWrittenOff` it resolves `invoiceId` from `_subject.id`, scans the `RevenueRecognitionSchedule` store for that tenant+invoiceId, filters to CANCELLABLE states (PENDING/IN_PROGRESS/PAUSED — mirroring `cancel`'s guard + FSM so COMPLETED/CANCELLED are clean skips, not swallowed guard failures), and dispatches the governed `cancel(reason)` per match (id in body AND instanceId for the mutate; `reason` is a genuine writeOff param so it rides the payload and is forwarded to the schedule's audit trail). Idempotent per schedule (`invoice-writeoff-revrec:{tenant}:{scheduleId}:cancel`; an invoice writes off once → terminal). **NO IR/source/migration change** (cancel + InvoiceWrittenOff + the PENDING/IN_PROGRESS/PAUSED→CANCELLED transitions all already existed) → audit + drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/invoice-written-off-revrec-cancel-middleware.test.ts` (4 tests: IR regression-lock no InvoiceWrittenOff reaction + the schedule FSM allows CANCELLED from all three cancellable states; write-off cancels the in-progress schedule + invoice reaches WRITE_OFF; 1:N fan-out cancels PENDING+IN_PROGRESS while leaving COMPLETED untouched; a different-invoice schedule is not touched). Verified: runtime suite 329 pass; runtime+api typecheck exit 0; `manifest:audit-reaction-payloads` 0/0/0. Mechanism: middleware. **REMAINING (the other two legs, both NOT clean — verified 2026-06-14):** (1) **`ContractSigned → RevenueRecognitionSchedule.create` is BLOCKED by missing data** — the schedule REQUIRES `invoiceId` (required prop + guard) and `totalAmount > 0`, but `EventContract` (event-contract-rules.manifest) carries NEITHER an invoice link NOR any monetary value; a recognition schedule semantically belongs to an **Invoice** (the entity even declares `belongsTo invoice` + required `invoiceId`), and at contract-sign time the invoice often does not exist yet. The natural trigger is invoice-side (e.g. on first `InvoiceSent`/`applyPayment`), not contract-signing — needs a design decision, not a one-liner. (2) **"Invoice paid → schedule complete" is semantically questionable** — revenue recognition (earning revenue over a service period) is deliberately decoupled from cash collection (an invoice can be paid in full while recognition is still mid-schedule), so auto-completing a schedule on payment would mis-state recognized revenue; deferred pending an accounting-owner decision. Today recognition is created only via the POST route (accounting/revenue-recognition/schedules/route.ts:82). Mechanism: middleware (1 leg done; 2 legs need design).
- [~] **PaymentProcessed → CollectionCase.markResolved; CollectionWrittenOff → Invoice.writeOff; PaymentPlanCompleted → CollectionCase.markResolved** — close the loops between payments, collections, and invoice status. — (corrected: the event is `CollectionWrittenOff`, not `CollectionCaseWrittenOff`; it is emitted by `CollectionCase.writeOff` at collections-rules.manifest:206-214). **`CollectionWrittenOff → Invoice.writeOff` DONE (2026-06-14)** — closes the OTHER end of the collections↔invoice loop (symmetric counterpart of the shipped `InvoiceMarkedOverdue → CollectionCase.create`): writing off a case now also marks the underlying invoice WRITE_OFF so AR stops carrying dead debt as owed. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `CollectionCase.writeOff(amount, reason, approvedBy)` is a MUTATE (last mutate `closedAt = now()`) → payload `{...commandInput, result}` is a timestamp scalar; the invoice to write off is `CollectionCase.invoiceId` — the case's OWN field, NOT a writeOff param, and declared event fields (`CollectionWrittenOff.invoiceId`) are never auto-populated from `self.*` → no reaction can read it. **Fixed as MIDDLEWARE** (`manifest/runtime/src/middleware/collection-written-off-invoice-write-off-middleware.ts`, barrel-exported; registered in `manifest-runtime-factory.ts` right after the collection-payment-recorded leg), which loads the CollectionCase via `_subject.id`, reads `self.invoiceId`, and dispatches the governed `Invoice.writeOff` for the invoice's FULL remaining `amountDue` (the whole balance is the loss; independent of the case-side `amount` residual). Guard-safe (skips non-OVERDUE/PARTIALLY_PAID and zero-due invoices, mirroring `Invoice.writeOff`'s guards) + idempotent per case (static key; a case is written off once = terminal). **PREREQUISITE DEAD-COMMAND BUG FOUND + FIXED (the crux):** `CollectionCase.writeOff` mutates `status = "WRITTEN_OFF"` but the CollectionCase FSM listed NO transition target of `"WRITTEN_OFF"` from any state → the engine rejected EVERY call ("Invalid state transition … -> WRITTEN_OFF") before the mutate ran, so the command was 100% DEAD despite the live route at `accounting/collections/cases/[id]` exposing it (the route test mocks `runManifestCommand`, so it never caught the dead transition). This is the exact dead-target class already fixed once for `Invoice` (VOIDED/WRITTEN_OFF → VOID/WRITE_OFF). Added `"WRITTEN_OFF"` as a transition target from ACTIVE/IN_PROGRESS/LEGAL (collections-rules.manifest) + recompiled the IR. Conformance test (`collection-written-off-invoice-write-off-middleware.test.ts`, 4 tests): IR-lock no `CollectionWrittenOff` reaction + the FSM now allows WRITTEN_OFF from all three writeOff-guard states; happy path proves the case reaches WRITTEN_OFF (FSM fix) AND the invoice is written off (status WRITE_OFF, amountDue 0); paid/terminal invoice skipped while the case still writes off; no-invoiceId clean skip. Verified: runtime suite 296 pass; runtime typecheck 0; `manifest:validate` ok / `validate-ai` 100/100 / `audit-reaction-payloads` 0/0/0. Mechanism: transition (dead-command) fix + middleware. **`PaymentPlanCompleted → CollectionCase.markResolved` DONE (2026-06-14)** — when a collection payment plan is paid off and marked COMPLETED, its case now leaves the dunning pipeline instead of sitting ACTIVE forever chasing a settled debt. `PaymentPlanCompleted` (collections-rules.manifest:538) had ZERO consumers. **The plan's "the event carries `caseId` directly" claim was WRONG** per the engine-semantics correction at the top of P0: `CollectionPaymentPlan.markCompleted()` takes NO params (collections-rules.manifest:379) → the emitted payload `{...commandInput, result}` is just a `completedAt` scalar; the case to resolve is `CollectionPaymentPlan.collectionCaseId` (the plan's OWN field, line 331), never auto-populated onto the event — so a reaction reading `payload.caseId` is a silent no-op. **Fixed as MIDDLEWARE** (`manifest/runtime/src/middleware/payment-plan-completed-collection-case-resolve-middleware.ts`, barrel-exported; registered in `manifest-runtime-factory.ts` right after the CollectionWrittenOff leg), which loads the plan via `_subject.id`, reads `self.collectionCaseId`, and dispatches the governed `CollectionCase.markResolved`. **GUARD-SAFE (the crux):** `markResolved` guards `self.outstandingAmount <= 0.01` (line 131) and the FSM only reaches RESOLVED from ACTIVE/IN_PROGRESS/LEGAL/DISPUTED — the middleware mirrors BOTH, so a case that is already terminal, or whose balance is NOT yet settled (a completed plan that under-paid), is skipped with a diagnostic rather than wrongly resolved (resolving residual debt would erase real money owed). The case balance is driven by `CollectionCase.recordPayment` as real payments land, so a properly-paid plan drives outstanding→0 and resolves; an under-paid plan does not. Idempotent per case (static key; a plan completes once). No IR/source change → audit + drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/payment-plan-completed-collection-case-resolve-middleware.test.ts` (4 tests: IR-lock no PaymentPlanCompleted reaction + both source FSMs allow the path; settled plan → case RESOLVED w/ resolvedAt/closedAt; residual-debt plan → case stays ACTIVE [guard-safety can't regress to wrongful resolution]; no-collectionCaseId clean skip). Verified: runtime suite 300 pass; runtime+api typecheck exit 0. Mechanism: middleware. **REMAINING: the `PaymentProcessed → CollectionCase.markResolved` leg — DEFERRED (RISKY-COORDINATION).** A direct invoice payment (`Payment.process → Invoice.applyPayment`) credits the INVOICE's `amountDue` but never decrements the *case's* `outstandingAmount`, so `markResolved`'s `outstandingAmount <= 0.01` guard cannot pass on that path. It needs case-balance mirroring from invoice payments first (e.g. a `PaymentApplied/InvoicePaid → CollectionCase.recordPayment` leg that scans the case store by `invoiceId`, as the InvoiceMarkedOverdue middleware already does) — a separate design decision, not a clean one-liner. Mechanism: middleware (blocked on case-balance mirroring).
- [ ] **Make `RevenueRecognitionSchedule.recognize` atomic** — `accounting/revenue-recognition/schedules/[id]/route.ts:142-204` chains 3 sequential `runCommand` calls (create line → recognizeAmount → completeIfFullyRecognized) with no compensation; partial failure leaves a dangling RECOGNIZED line. Note the line is created pre-recognized via `status: 'RECOGNIZED'` (line 153), bypassing the governed `RevenueRecognitionLine.recognize` transition. Replace with one governed command or a saga with compensation that routes through the status transition. Mechanism: command/saga.

### CRM pipeline

- [x] **Proposal lifecycle → Lead status** — DONE 2026-06-14 (`proposal-lifecycle-lead-status-middleware.ts`). `on ProposalCreated/Sent → Lead.status="proposal"`, `ProposalAccepted → "won"`, `ProposalRejected → "lost"`. **Mechanism corrected: MIDDLEWARE, not reaction** (Rule 7). The plan said reaction, but (a) `leadId` is the Proposal's OWN field and `send/accept/reject` take no `leadId` param → unreachable by a reaction; (b) `Lead.update` (lead-rules.manifest:88) is a FULL-FIELD mutate guarded by `contactName != ""` → a reaction passing only `{leadId,status}` would blank fields + trip the guard, so the Lead must be LOADED and its fields re-passed; (c) the Lead FSM gates each transition (proposal⇐qualified, won⇐proposal, lost⇐{contacted,qualified,proposal}) and self-transitions aren't exempt → the middleware reads current status and skips illegal/already-advanced/converted leads cleanly. **Prerequisite bug fixed in the same change:** `Lead.update`'s `guard self.isConverted == false` was DEAD — computeds don't resolve inside guards (they evaluate `undefined`, so `undefined == false` rejected EVERY update; Lead.update was unusable for ALL callers incl. the lead-edit UI). Inlined to the stored-prop form `self.convertedToClientId == "" or self.convertedAt == null` (same pattern as Proposal.send's isDraft inline, IMPLEMENTATION_PLAN P0). Conformance test: 7 cases (each transition, full-field-preserve, FSM-skip, no-leadId, IR-has-no-reaction lock). Verified: runtime 261 pass, IR validate, reaction-payloads 0, schema:check no-drift, api typecheck 0.
- [x] **DealClosed → Lead status mirror; DealAssigned → Notification** — DONE (2026-06-14). `DealClosed` (deal-rules.manifest:176) and `DealAssigned` (deal-rules.manifest:169) had ZERO consumers: closing a deal won/lost never reflected back onto the originating Lead's pipeline status (CRM funnel froze at "proposal"), and assignees were never notified (silent work hand-off). **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: (1) the Lead to mirror is `Deal.leadId` — the deal's OWN field; `close(status)` takes only `status` as a param, so the emitted payload `{...commandInput, result}` carries `status` but NOT `leadId` (declared event fields are never auto-populated from `self.*`), AND `Lead.update` (lead-rules.manifest:88) is a FULL-FIELD mutate guarded by `contactName != ""` + `isConverted == false`, so a reaction passing only `{leadId,status}` would blank the lead's other fields and trip the guard. (2) `DealAssigned → Notification.create` needs the deal's `title` (its OWN field, not an `assign` param). **Fixed as ONE MIDDLEWARE** (`manifest/runtime/src/middleware/deal-lifecycle-propagation-middleware.ts`, barrel-exported; registered in `manifest-runtime-factory.ts` right after the proposal-lifecycle-lead-status leg — siblings in the CRM group). On `DealClosed` it loads the Deal via `_subject.id`, reads the authoritative post-mutation `status` (won/lost) + `leadId`, loads the Lead, and dispatches the governed `Lead.update` with all fields re-passed (only `status` changed) — **FSM-gated** (mirrors the Lead FSM: `→won` only from `proposal`, `→lost` only from `{contacted,qualified,proposal}`; lead-rules.manifest:36-39) so already-won/converted/archived/earlier-funnel leads are skipped cleanly (self-transitions are NOT exempt → an out-of-state dispatch would be a swallowed failure). On `DealAssigned` it loads the Deal (for `title`), reads `assignedTo` (an `assign` param, on the payload; falls back to the loaded deal's field), and dispatches the governed `Notification.create` (type `deal_assigned`, `correlationId = dealId`; create id in the body NOT instanceId, randomUUID). Idempotency keys: `deal-lead:tenant:leadId:outcome` (close mirror) and `deal-notify:tenant:dealId:recipient` (a re-emit dedups; a reassignment to a DIFFERENT person notifies anew). **KNOWN LIMITATION (documented, not silent):** each dispatch runs as the acting actor; `Notification.create`'s default policy is `manager/admin` (notification-rules.manifest:30) while `Deal.assign` admits `sales*` too, so a sales_rep's assignment yields a policy-denied diagnostic + skip on the notify leg (no per-call identity override — same class as the event-created-client-interaction leg). Conformance test: `manifest/runtime/src/__tests__/deal-lifecycle-propagation-middleware.test.ts` (7 tests: IR regression-lock no Deal*→Lead.update/Notification.create reaction; won→lead "won" + DealClosed/LeadUpdated bubble; lost→"lost" with fields preserved; converted-lead skip [guard-safe]; no-leadId skip; assign→Notification row for the assignee correlated to the deal; reassignment notifies the new recipient). No IR/source/generated change → audit + drift gates unaffected; runtime suite 307 pass; runtime+api typecheck green. Mechanism: middleware (one file, two legs). **DealAssigned policy elevation + DealAbandoned→Lead mirror left as follow-ons.**
- [ ] **ProposalAccepted → Invoice.create** — generate a draft/deposit invoice on acceptance (carry `proposalId`). The only `on ProposalAccepted` reaction today routes to `Event.create` (reactions.manifest:133). **Coordination risk:** `Event.create` already cascades to BattleBoard + (via confirm) PrepList; adding `Invoice.create` on the same event without sequencing can leave Event+BattleBoard without an Invoice (or vice versa) on partial failure — wrap the ProposalAccepted fan-out in a saga. Mechanism: reaction (saga-coordinated).
- [x] **ProposalLineItem add/remove → Proposal.lineItemCount (functional deadlock)** — DONE (2026-06-14). `lineItemCount` was a plain `int = 0`, set to 0 on create and never changed, while `Proposal.send`'s gate `constraint blockNoLineItems:block self.hasLineItems` required it — so **no proposal could be sent**. **Both the plan's suggested fixes were WRONG given verified engine semantics:** (a) a `count_of(self.lineItems)` aggregate would leave the gate dead (computeds don't resolve inside constraints — see the systemic finding below); (b) increment/decrement **reactions** can't do the remove leg (`ProposalLineItem.remove(userId)` takes no `proposalId` — it's the line item's OWN field, and declared event fields are never auto-populated from `self.*`, so `resolve payload.proposalId` is the P0 no-op class). **Fixed as MIDDLEWARE + stored-prop inlining + two new commands:** (1) inlined the gate to `self.lineItemCount > 0` (stored prop); (2) added `Proposal.incrementLineItemCount`/`decrementLineItemCount` commands (kept `lineItemCount` a STORED int, not an aggregate); (3) `manifest/runtime/src/middleware/proposal-line-item-count-middleware.ts` (registered in factory; barrel-exported) listens `ProposalLineItemCreated`/`Removed`, resolves the parent from `payload.proposalId` (create) or by loading the line item for `self.proposalId` (remove), and dispatches the governed increment/decrement (idempotent per line item per direction). **SECOND DEADLOCK uncovered + fixed (the crux):** even with the count fixed, `Proposal.send`'s `guard self.isDraft` is ALSO dead — computeds don't resolve in guards either (the `:block` constraint runs BEFORE guards, which had masked it). The earlier probe that "showed isDraft passing" was reaching the constraint, not the guard. Inlined `guard self.isDraft → self.status == "draft"`. While in the file, inlined the rest of the dead computed-referencing guards/constraints (the same one-line defect was silently disabling enforcement across the whole Proposal FSM): `Proposal.update` (`guard self.isDraft` — update was entirely unusable), `accept` (`blockExpired:block !self.isExpired` — expired proposals were acceptable), `withdraw` (`blockAlreadyAccepted:block !self.isAccepted` + `blockAlreadyWithdrawn:block !self.isWithdrawn`). Computeds kept for read projections; just no longer referenced in guards/constraints. Conformance test: `manifest/runtime/src/__tests__/proposal-line-item-count-middleware.test.ts` (8 tests: IR lock that the gate reads the stored prop; IR lock that NO Proposal guard/constraint references a computed [covers the whole FSM]; both new commands exist; no count reaction in IR; count-0 send blocked; count>0 send succeeds; add→increment / remove→decrement; decrement floors at 0; `Proposal.update` now works on a draft). runtime suite 521 pass; runtime+api typecheck green; `manifest:ci` gates green (validate/validate-ai score 100/doctor/check/schema:check/audit:strict). Mechanism: middleware + 2 commands + constraint/guard inlining (×5).
- [~] **ClientArchived → close open Deals/Proposals; ClientInteractionEscalated/Overdue → Notification** — PARTIAL. **`ClientInteractionMarkedOverdue → Notification.create` DONE (2026-06-14).** The orphan overdue event (client-interaction-rules.manifest:237) now notifies the assignee. **The plan's "reactions for the 1:1 notifications" was WRONG** per the verified engine-semantics correction at the top of P0: `markOverdue()` takes NO input params (line 149), so the emitted `{...commandInput, result}` payload carries no entity fields (declared event fields are never auto-populated from `self.*`) — `payload.employeeId`/`subject`/`tenantId` are all `undefined`. The recipient (`employeeId`), `subject`, and `tenantId` are the interaction's OWN fields, and `Notification.create` guards `recipientEmployeeId != ""`/`title != ""`, so a reaction passing undefineds would fail the create guard + be logged-and-swallowed. **Fixed as MIDDLEWARE** (sibling of `deal-lifecycle`'s DealAssigned→Notification leg): `manifest/runtime/src/middleware/client-interaction-overdue-notify-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` in the CRM group after deal-lifecycle), which on `ClientInteractionMarkedOverdue` loads the interaction via `_subject.id`, reads `self.employeeId`/`self.subject`/`self.tenantId`, and dispatches governed `Notification.create(type "interaction_overdue", correlationId=interactionId, create id in BODY not instanceId)`. Guard-safe (skips missing/soft-deleted/no-assignee with diagnostics, never silent); idempotency key per (interaction, recipient); `markOverdue`'s FSM (open|scheduled→overdue) means it cannot fire twice anyway. **KNOWN LIMITATION (documented):** dispatch runs as the marking actor, subject to `Notification.create`'s manager/admin policy — a sales-rep-marked overdue yields a policy-denied diagnostic + skip (no per-call identity override; cron/admin overdue sweeps pass). Conformance test: `manifest/runtime/src/__tests__/client-interaction-overdue-notify-middleware.test.ts` (3 tests: IR regression-lock no such reaction; overdue→assignee notified with correct recipient/type/correlation/title + `NotificationCreated` bubbles; event-name scoped — `escalate` does NOT create an overdue notification). No IR/reaction change → audit gates unaffected (`manifest:audit-reaction-payloads` EXIT=0); runtime suite 338 pass; runtime+api typecheck green. ****`ClientInteractionEscalated → Notification.create` DONE (2026-06-14)** — the near-identical sibling of the overdue leg, escalating a follow-up now notifies the **escalation TARGET** (`escalatedTo`), not the original assignee — the deliberate behavioral difference (escalation hands the item to a new owner, who needs the signal). `ClientInteractionEscalated` (client-interaction-rules.manifest:247) was an orphan event. **Mechanism: MIDDLEWARE** (not a reaction): although `escalatedTo`/`reason` ARE `escalate(escalatedTo, reason)` params (so they ride the `{...commandInput, result}` payload), the notification also needs the interaction's OWN `subject`/`tenantId`, which are never auto-populated onto the event payload — so the middleware LOADS the interaction via `_subject.id` (same mechanism as the overdue sibling). Recipient is read from the loaded `interaction.escalatedTo` (escalate mutates it) with the `escalate` param as fallback; `reason` (payload param) rides into the notification body. `manifest/runtime/src/middleware/client-interaction-escalated-notify-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the overdue leg). notificationType `interaction_escalated`, `correlationId = interactionId`, create id in the BODY not instanceId; guard-safe (skips missing/soft-deleted/no-target with diagnostics); idempotency key per (interaction, recipient) so a re-emit dedups while a re-escalation to a DIFFERENT person notifies anew. **KNOWN LIMITATION (documented):** the dispatch runs as the escalating actor, subject to `Notification.create`'s manager/admin policy — a sales-rep escalation yields a policy-denied diagnostic + skip (no per-call identity override; same class as the overdue/deal-assign legs). **NO IR/source/migration change** (both `escalate`/`ClientInteractionEscalated` and `Notification.create` already exist in IR) → audit + drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/client-interaction-escalated-notify-middleware.test.ts` (3 tests: IR regression-lock no such reaction; escalation notifies the TARGET [not the assignee] with the right type/correlation/subject-title/reason-body + `NotificationCreated` bubbles; event-name scoped — markOverdue does NOT create an escalated notification). Runtime suite 357 pass; runtime+api typecheck green. Mechanism: middleware. **STILL OPEN:** (a) `ClientArchived → close open Deals/Proposals` **— DEFERRED (reversibility hazard, verified 2026-06-14).** Only the **Proposal** leg is even feasible (`Deal` has no `clientId` field, keyed by `leadId` only, so deals cannot be queried by archived client), BUT it should NOT be implemented as a terminal cascade: `Client.archive` is **REVERSIBLE** (`Client.reactivate` sets `deletedAt = null`, client-rules.manifest:129) while `Proposal.withdraw` is **TERMINAL** (no FSM transition out of `withdrawn`; proposal-rules.manifest:58-60). Cascading an irreversible withdraw off a reversible parent state is the exact anti-pattern this codebase defers (vendor-suspend, dish-eightySix) — a reactivated client's proposals could not be brought back. Proposal has no reversible "hold"/"suspend" command, so this leg is blocked until either (i) archive is reclassified as permanent, or (ii) a reversible Proposal hold + restore-on-reactivate path is authored; (b) `ClientInteractionMarkedOverdue`/`Escalated` could also feed an `AdminTask` if desired. Mechanism: middleware (overdue + escalate legs done) + middleware (remainder).
- [ ] **LeadCreated/Updated → re-score** — `on LeadUpdated run Lead.recalculateScore`. **Prerequisite:** no `Lead.recalculateScore` command exists; scoring lives in `crm/scoring/calculate/route.ts` as a manual POST sweep that writes `score`/`scoreBreakdown` via `database.lead.updateMany` (route.ts:144-208) to columns **not declared in the Lead manifest entity**. Add `score`/`scoreBreakdown` as governed properties + a `recalculateScore` command, then wire the reaction. Mechanism: command + property + reaction.

### Kitchen, inventory & procurement

- [ ] **PurchaseOrderReceived → InventoryItem.restock (1:N)** — middleware fanning out over `PurchaseOrderItem`; create `InventoryTransaction(type=purchase)` per line and update `InventorySupplier.openPOCount`. — PARTIAL: an explicit TODO marks this open (inventory/purchase-orders/[id]/complete/route.ts:12) and raw Prisma writes remain. **Prerequisite:** `InventorySupplier.openPOCount` (inventory-supplier-rules.manifest:18) is set to 0 on create with no governed mutate command — add `InventorySupplier.incrementOpenPOCount`/decrement before the middleware can update it. Mechanism: command + middleware.
- [x] **InventoryStock ↔ InventoryItem sync** — DONE (2026-06-14). `InventoryStock.adjust(delta, reason)`→`InventoryStockAdjusted` and `recount(newQuantity, countedBy)`→`InventoryStockRecounted` (inventory-extended-rules.manifest:116/134) mutate ONLY the per-location stock row; nothing propagated to the aggregate `InventoryItem.quantityOnHand`, so the item total (used by valuation/par/reorder/"how much do we have?" reads) silently diverged from the sum of its locations after any location adjustment or recount. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction: the target item is `InventoryStock.itemId` — the stock row's OWN field (inventory-extended-rules.manifest:78), NOT an adjust/recount param — and the event's declared `inventoryItemId` field is never auto-populated from `self.*` (event decl :924/:935), so a reaction's payload structurally cannot carry the item id. **Fixed as MIDDLEWARE** `manifest/runtime/src/middleware/inventory-stock-sync-item-middleware.ts` (registered in `manifest-runtime-factory.ts` right after the inventory-movement ledger leg), which reads `self.itemId` off the InventoryStock instance and dispatches governed `InventoryItem.adjust(quantity=delta, reason, userId="system:stock-sync")`. **recount needs the two-hook pre-mutation capture** ([[middleware-premutation-capture-two-hook]]): `recount` does `mutate quantityOnHand = newQuantity` before emitting, so the OLD on-hand (needed for `delta = newQuantity − old`) is gone by after-emit — captured on `before-guard` via `contextPatch` (same pattern as the guest-count rescale). `delta` for the adjust leg IS a genuine param so it rides the payload directly. Zero net delta (e.g. a recount confirming the count) is skipped (no item mutation, no spurious ledger row). The propagated `InventoryItem.adjust` emits `InventoryAdjusted`, which the inventory-movement ledger middleware records as an `adjustment` `InventoryTransaction` — so a location adjustment flows item-total→ledger in one governed chain. **No IR/source change** (the events already exist; this is a pure runtime consumer) so drift/reaction-payload gates are unaffected. Conformance test: `manifest/runtime/src/__tests__/inventory-stock-sync-item-middleware.test.ts` (4 tests: IR regression-lock no such reaction exists; adjust mirrors signed delta onto the item total; recount mirrors the new−old delta [proves the before-guard capture]; no-op recount leaves the item total untouched). Runtime suite 325 pass; runtime+api typecheck green. Mechanism: middleware.
- [ ] **VarianceReportApproved → apply adjustment** — `on VarianceReportApproved` (cycle-count-rules.manifest:529) `run InventoryItem.adjust` + `InventoryTransaction.create(type=adjustment)`. — PARTIAL: the cycle-count finalize route (`inventory/cycle-count/sessions/[id]/finalize/route.ts`) already calls governed `InventoryTransaction.create` + `InventoryItem.adjust` + `VarianceReport.approve` inline via `runManifestCommandCore`, so the adjustment fires on that path — but the **declarative reaction** is still missing (variance approved by any other route/path won't propagate). Mechanism: reaction.
- [ ] **CycleCountSessionFinalized → AuditSchedule.recordRun + ReorderSuggestion/ForecastInput** — middleware fan-out per record on `CycleCountSessionFinalized` (cycle-count-rules.manifest:445). The event carries only sessionId/tenantId/locationId/userId/finalizedAt, so the middleware must query `CycleCountRecord` by sessionId to produce per-record dispatches. `AuditSchedule.recordRun` exists (inventory-extended-rules.manifest:858). Mechanism: middleware. **BLOCKED — the AuditSchedule.recordRun leg has no session↔schedule link (verified 2026-06-15):** `AuditSchedule` (inventory-extended-rules.manifest:785) has NO `locationId`/scope field (only id/tenantId/name/frequency/time/createdBy/nextRunAt/lastRunAt/isActive) and `CycleCountSession` has no `auditScheduleId`, so there is no way to know WHICH schedule a finalized session fulfills — `recordRun`-on-all-active would wrongly advance unrelated cadences (a freezer count advancing the dry-goods schedule). `recordRun(nextRunAt)` also requires a next-run date = cadence math from `frequency`, which is mechanical, but the matching is the blocker. Needs a session→schedule link (an `auditScheduleId` on the session, or a `locationId`/scope on AuditSchedule) before it is implementable without inventing semantics (constitution §10). `isActive` here IS a stored property, so `recordRun`'s guard is fine. The ReorderSuggestion/ForecastInput legs are separately under-specified.
- [ ] **ReorderSuggestionAccepted → PurchaseRequisition.create; InventoryForecastApproved → ReorderSuggestion** — events at inventory-extended-rules.manifest:1006/1177; `PurchaseRequisition.create` at procurement-requisition-rules.manifest:74. No reactions. Mechanism: reaction (+middleware where 1:N). **SCOPED 2026-06-14 — NOT clean as-is:** `PurchaseRequisition.create` GUARD-requires `requiredBy: datetime` (procurement-requisition-rules.manifest:74-77), and also wants `locationId`/`department`/`priority` — none of which are derivable from `ReorderSuggestion` (inventoryItemId/suggestedQuantity/reason only) or `InventoryItem`. `requiredBy` is procurement policy (lead-time + urgency), not inventory data; auto-defaulting it (e.g. `now()+3d`) would invent business semantics (constitution §10). Viable only after a tenant procurement-default policy (requiredBy lead time + default location) exists, OR if `create`'s `requiredBy` guard is relaxed. Defer until that prerequisite is decided.
- [~] **InventoryTransfer received → stock movement (both levels)** — PARTIAL (2026-06-14): the **stock-movement leg is DONE**; the `TransferDiscrepancyFlagged → VarianceReport.create` leg + `partialReceive` remain. **Shipped:** `manifest/runtime/src/middleware/inventory-transfer-received-stock-movement-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the inventory-stock-sync leg). Verified first that NOTHING consumed `TransferReceived`/`TransferShipped` (no middleware/reaction), so the transfer status machine walked to `received` but the physical `InventoryStock` rows NEVER moved — per-location balances went permanently stale. On `TransferReceived` (inventory-transfer-rules.manifest:163, from `receive`) it loads the transfer via `_subject.id`, reads its OWN `fromLocationId`/`toLocationId` (NOT `receive` params — declared event fields are never auto-populated from `self.*`, so this MUST be middleware, not a reaction), fans out over the `InventoryTransferItem` rows (getAll+filter by tenantId+transferId), and per line dispatches `InventoryStock.adjust(delta=−qty)` on the source-location row then `(+qty)` on the destination row. **Books the whole move at RECEIPT** — the single source of truth, since nothing decrements the source at `ship`. **The plan's "adjust both InventoryStock AND InventoryItem" was refined (Rule-7):** the middleware adjusts ONLY the per-location `InventoryStock`; the aggregate `InventoryItem` total is mirrored AUTOMATICALLY by the existing stock-sync middleware (each `InventoryStockAdjusted` → `InventoryItem.adjust`), so source(−qty)+dest(+qty) cancel to **net-zero** — which is the CORRECT semantic (a transfer redistributes on-hand across locations; it does not change the total owned). For net-zero to hold the destination leg must always be an `adjust` (mirrored); when the destination has no stock row yet it is **bootstrapped at quantity 0 then adjusted +qty** (a direct `create(qty)` emits `InventoryStockCreated`, which the sync middleware does NOT mirror → would leave the item total net −qty). The new row's `unitId` (int) is copied from the source row (the transfer item's own `unitId` is a string). **Consistency guards (fail-loud, no half-booking):** the source leg goes first; if the source row is missing OR the `adjust` block `(quantityOnHand+delta) >= 0` rejects an oversell, the destination leg is skipped and an `onDiagnostic` fires — never phantom stock at the destination. Idempotency: `receive` is one-way (`status=="in_transit"`→received, cannot run twice); dispatch keys are deterministic per (transfer,item,location,direction) as defence-in-depth. Conformance test: `manifest/runtime/src/__tests__/inventory-transfer-received-stock-movement-middleware.test.ts` (4 tests: IR regression-lock NO TransferReceived→InventoryStock reaction; both-rows-exist move with item total UNCHANGED; missing-destination bootstrap-at-0-then-credit with unitId copied + still net-zero; insufficient-source → destination NOT credited). runtime suite 361 pass (67 files); runtime typecheck exit 0; no IR/schema/migration change. Mechanism: middleware. **REMAINING:** (a) **`TransferDiscrepancyFlagged` (line 183) → `VarianceReport.create`** — a separate leg; (b) **`partialReceive`** (`TransferPartiallyReceived`, line 175) carries a `receivedItems` JSON subset with possibly-different quantities and keeps the transfer `in_transit` → distinct semantics, a separate increment; (c) chained ledger rows are `adjustment`-typed (we dispatch `InventoryStock.adjust`, not a direct `transfer`-typed `InventoryTransaction`) — faithful but verbose; a future leg could add transfer-typed rows.
- [~] **Dish/Ingredient lifecycle propagation** — PARTIAL (2026-06-14): the **DishDeactivated** leg is DONE; the eightySix + ingredient-recall legs remain. **DishDeactivated → prune open downstream work** shipped as `manifest/runtime/src/middleware/dish-deactivated-prune-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the event-dish-prep-sync leg). On `DishDeactivated` (dish-rules.manifest:292) it fans out by `dishId` across three entities and dispatches the EXISTING governed retirement command on each open row: `PrepTask.cancel(reason, canceledBy)` for non-terminal tasks (transitions open/pending/in_progress→canceled), `PrepListItem.remove(reason, userId)` + `EventDish.remove(reason, userId)` for non-removed rows. **MIDDLEWARE not reaction** (1:N fan-out across PrepTask/PrepListItem/EventDish keyed by the dish's OWN id `_subject.id`; a reaction resolves one target) — and per the engine-semantics correction the declared `DishDeactivated` fields (`dishId`/`tenantId`/`name`) are NOT auto-populated, so dishId comes from `_subject.id`, tenantId from loading the Dish, and only `reason`/`userId` (deactivate params) ride the payload. Guard-safe (only rows each target command's guard accepts are touched → no swallowed guard failures) + idempotent (re-delivery finds nothing open; per-(dish,row) idempotency key). **SCOPED — `DishDeactivated` only, NOT `DishEightySixed`** (a deliberate correct-over-convenient split, since the plan grouped them): an 86 is a TRANSIENT, REVERSIBLE pull-from-service (`Dish.reinstate`, dish-rules.manifest:252) with NO restore-on-reinstate provenance, so blanket irreversible cancel/soft-delete would strip the dish from FUTURE events for a same-day stockout and `reinstate` could not bring them back (same restore-on-cancel prerequisite class as the time-off shift-cleanup middleware). Deactivate is permanent — full cleanup matches the command's own `warnActiveMenu` intent. No IR/source/schema change → audit + drift gates unaffected. Conformance test: `manifest/runtime/src/__tests__/dish-deactivated-prune-middleware.test.ts` (4 tests: IR regression-lock no DishDeactivated→{PrepTask,PrepListItem,EventDish} reaction; IR-lock the eightySix prune leg stays UNWIRED; deactivate cancels open tasks + removes items/event-dishes while leaving terminal/already-removed/other-dish rows untouched + downstream events bubble up; no-downstream no-op). runtime suite 354 pass; runtime+api typecheck green. Mechanism: middleware. **REMAINING:** (a) **DishEightySixed leg** — needs date-scoping to the affected service window + a restore-on-reinstate path (Dish.reinstate→re-add) before irreversible pruning is safe; (b) **`IngredientRecallFlagged` → quarantine `InventoryItem` DONE (2026-06-14)** — a supplier recall (`Ingredient.flagRecall`, ingredient-rules.manifest:178) now pulls the recalled ingredient's linked physical stock from inventory, closing a food-safety gap where the recall flagged only the Ingredient row (isRecalled/isActive) while its `InventoryItem` stock stayed live and visible in lists/par-reorder/"what do we have". **MIDDLEWARE not reaction:** `flagRecall` is a MUTATE so the payload is `{...commandInput, result}` (last-mutate scalar), and the item is `Ingredient.inventoryItemId` — the ingredient's OWN field (ingredient-rules.manifest:19), NOT a flagRecall param, never auto-populated onto the event — so no reaction can read the FK; the middleware LOADS the recalled Ingredient via `_subject.id` and reads `self.inventoryItemId`. `manifest/runtime/src/middleware/ingredient-recalled-quarantine-inventory-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the dish-deactivated-prune leg) dispatches the governed `InventoryItem.softDelete` (the entity's ONLY availability-removing command — sets `deletedAt`, after which inventory reads filter the row out). Guard-safe + idempotent: `softDelete` guards `self.deletedAt == null`, so an already-pulled item is skipped (no swallowed guard failure) and an unlinked ingredient is a clean no-op; idempotency key per (tenant, ingredient, item). **NO IR/source/migration change** (both `flagRecall`/`IngredientRecallFlagged` and `InventoryItem.softDelete` already exist) → reaction-payload/schema-drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/ingredient-recalled-quarantine-inventory-middleware.test.ts` (4 tests: IR regression-lock no `IngredientRecallFlagged → InventoryItem` reaction; recall pulls the linked item [deletedAt set + `InventoryItemDeleted` bubbles up] while the ingredient goes recalled/inactive; already-deleted item skipped [guard-safe]; unlinked ingredient = clean no-op). runtime suite 373 pass; runtime+api typecheck green; `manifest:audit-reaction-payloads` 0/0/0. Mechanism: middleware. **KNOWN LIMITATIONS (documented, not silent):** (1) there is NO governed `InventoryItem.restore`/undelete, so the symmetric `clearRecall → restore` leg cannot be wired yet (deferred — needs IR) — acceptable for a food-safety recall, where hard-pulling contaminated stock is the conservative action and re-stocking after a cleared recall is a deliberate manual act; (2) `softDelete` hides the item from inventory READS but `consume`/`reserve`/`waste` do not yet guard `deletedAt`, so a direct command could still mutate a pulled item — hardening those guards is a separate IR change. **STILL OPEN:** flag active `PrepListItem` (has `ingredientId`) needs a NEW `PrepListItem.flagForRecall` command (NEEDS-IR — `remove` is a permanent soft-delete, semantically wrong for a recall hold); the `EventDish`/`PurchaseOrderItem` ingredient legs are SCHEMA-BLOCKED (EventDish has no `ingredientId`; PurchaseOrderItem.itemId → InventoryItem, not Ingredient). (c) **DishEightySixed leg** still needs date-scoping/restore prereqs. Mechanism (remaining): command + middleware (PrepListItem flag) + date-scoping/restore prereqs for 86.
- [x] **PrepTask lifecycle → Station task-count reconciliation** — DONE (2026-06-14). `Station.currentTaskCount` is the STORED prop the capacity computeds (`isAtCapacity`/`capacityRemaining`/…) AND the `assignTask` `blockFull`/`warnNearCapacity` constraints read to enforce capacity — but NOTHING in the runtime ever moved it. **The plan's claims were both wrong** (verified): the command is `assignTask`/`removeTask`, NOT `removeTask` alone, and counts did NOT "only increment" — `assignTask`/`removeTask` have ZERO reaction/middleware callers (only the generated client + read-only projections that compute their own display `taskCount`), so the governed count sat at its create-time 0 and the recently-shipped Station capacity computeds/constraints (P2 below) were correct in form but **inert in practice** (a capacity-3 station would never block a 4th task). **The plan's "Mechanism: reaction" was also WRONG**: a reaction resolves one target instance, but this fans out across ALL of a tenant's stations and derives occupancy from a cross-entity PrepTask scan. **Fixed as MIDDLEWARE with a new `Station.syncTaskCount(count)` command** — a RECOMPUTE, not +1/-1 deltas, which is the crux: `PrepTask.unclaim`/`release` CLEAR `stationId` in the same mutate that re-opens the task, so by the after-emit hook a delta middleware has lost the station to decrement (and the partial event payload doesn't carry it — declared event fields are never auto-populated from `self.*`), leaking the count upward forever. Instead `manifest/runtime/src/middleware/prep-task-station-count-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` after the prep-list-cancelled leg) listens on PrepTaskClaimed/Completed/Canceled/Unclaimed/Released/Reassigned, recomputes every station's true occupancy (= `in_progress` PrepTasks pointing at it) for the tenant, and dispatches the absolute, idempotent `Station.syncTaskCount(count)` ONLY for stations whose stored count drifted (so it never dispatches a no-op; no idempotencyKey by design — sync must re-run on every drift and is naturally idempotent). New `syncTaskCount` is absolute + free of the capacity block so it corrects drift in either direction; the legacy `assignTask`/`removeTask` deltas are left in place (unused, harmless). **KNOWN LIMITATION (documented):** each sync runs as the acting user; Station's `default policy` admits kitchen_staff/kitchen_lead/inventory_manager/manager/admin (the prep-task actors), so the common path passes — a lower-privilege actor's prep-task event yields a policy-skip + diagnostic per station. Conformance test: `manifest/runtime/src/__tests__/prep-task-station-count-middleware.test.ts` (4 tests: IR-lock syncTaskCount exists + NO PrepTask→Station reaction; claim increments to the TRUE count [2 tasks → 2; unrelated station stays 0]; complete decrements; **unclaim resets to 0 despite the cleared stationId — proving recompute > delta**). runtime suite 269 pass; runtime+api typecheck green; `manifest:validate`/`schema:check` no-drift/`audit-reaction-payloads` 0/0 green; no generated-client/route regen needed (the command is middleware-dispatched only, matching the CateringOrder.syncVenue sibling). Mechanism: command + middleware. **This activates the P2 Station capacity work below end-to-end.** (Was: "PrepTask completion → Station.removeTask", Mechanism: reaction.)
- [ ] **RecipeVersion cost propagation** — `on RecipeVersionCostUpdated` (recipe-rules.manifest:468) `run Dish.updatePricing` (1:N middleware over dishes); derive `RecipeVersion.totalCost` from ingredients. Mechanism: middleware + computed. **SCOPED 2026-06-14 — feasible (1:N middleware: filter Dish by `tenantId + recipeId`, dispatch `Dish.updatePricing(pricePerPerson=current, costPerPerson=…, userId="system")`) BUT one semantic caveat to resolve first:** `RecipeVersionCostUpdated` carries `costPerYield` (cost per recipe yield-unit), while `Dish.costPerPerson` is per-person-serving — there is NO modeled `servingsPerYield`/portioning link between them, so mapping `costPerYield → costPerPerson` directly would invent an equivalence (constitution §10). Resolve by either (a) mirroring the EXACT formula the existing out-of-Manifest cost path (`apps/api/.../recipe-costing.ts`) already uses to push recipe cost onto dishes, or (b) adding a portioning field. The `RecipeVersion.totalCost` computed is a separable follow-on (cost is currently materialized via `updateCosts`, not computed) — defer it. `updatePricing` must pass `pricePerPerson` too (it mutates both); load each Dish's current price and preserve it.
- [~] **Kitchen QA/IoT → CorrectiveAction (disambiguate the two entities)** — PARTIAL (2026-06-14). **QA-check leg DONE:** `QACheckFailed → QACorrectiveAction.create` shipped as MIDDLEWARE (`manifest/runtime/src/middleware/qa-check-failed-corrective-action-middleware.ts`, registered in `manifest-runtime-factory.ts` after the ingredient-recall leg). `QACheckFailed` was a complete ORPHAN (zero reaction/middleware/factory consumer; `qACheckFail` is never called in `apps/**` outside the generated client) even though `QACheck.fail`'s own comment says "callers should open a QACorrectiveAction" — so failed quality checks recorded the failure and dropped it (no tracked remediation). Middleware, NOT a reaction: `fail` is a MUTATE (payload `{...commandInput, result}` = the `completedAt` scalar), and the corrective action's `relatedCheckId` is the QACheck id (`_subject.id`) while the dispatch `tenantId` is the check's OWN field — neither is a `fail` param and declared event fields are never auto-populated from `self.*` → the leg LOADS the check via `_subject.id` for tenant/checkType/location. Field sourcing: `description` = inspector `notes` (a `fail` param, on payload) else generated "<checkType> check failed at <location>"; `assignedTo` = `inspector` (a `fail` param, guarded non-empty); `severity` = "high" (satisfies the create guard; per-checkType refinement deferred); `dueDate` = now + 3-day window (REQUIRED create param — only `relatedCheckId` is `optional` — passed as epoch ms). Idempotency key per check. NO IR/schema change. Conformance test `manifest/runtime/src/__tests__/qa-check-failed-corrective-action-middleware.test.ts` (4: IR regression-lock no reaction; failure opens a linked corrective action for the inspector + `CorrectiveActionCreated` bubbles; blank-notes generated-description fallback; `complete()`-not-`fail` clean no-op). Runtime suite 396 pass; runtime typecheck green; `check-reaction-payloads --strict` EXIT=0. **REMAINING (IoT/temperature legs — REJECTED for this increment, double-apply hazard):** `on IoTAlertCreated(critical) run CorrectiveAction.create` + `on TemperatureReadingCreated(out-of-range) run IoTAlert.create` — `apps/api/app/api/kitchen/iot/readings/route.ts` ALREADY creates `IoTAlert` rows inline via `runManifestCommand`, and `CorrectiveAction.create` needs `actionNumber` (counter query) + `sourceId`; middleware on those without route surgery would double-create. Original spec for those legs: `on IoTAlertCreated(critical) run CorrectiveAction.create`; `on TemperatureReadingCreated(out-of-range) run IoTAlert.create`. **Two distinct entities exist and must be targeted explicitly:** `CorrectiveAction` (kitchen-extended-rules.manifest:325, slug `kitchen.correctiveaction.created`) for the IoT/temperature path, and `QACorrectiveAction` (qa-rules.manifest:102, slug `kitchen.qa.corrective.created`) for the QA-check path. `TemperatureReadingCreated → IoTAlert.create` requires a **middleware** to look up the matching `IotAlertRule` (kitchen-extended-rules.manifest:171) before deciding whether to create an alert — not a pure reaction. Mechanism: reaction + middleware.

### Operations & logistics

- [x] **LogisticsRouteStarted/Completed → Driver/Vehicle status** — DONE (2026-06-14). The route lifecycle now keeps fleet availability in lockstep, the sibling of the shipped dispatch leg: `LogisticsRouteStarted → Driver.setOnRoute + Vehicle.setInUse` (busy), `LogisticsRouteCompleted`/`LogisticsRouteCancelled → Driver.setAvailable + Vehicle.setAvailable` (free). **NO IR change** — reuses the four status commands added for the dispatch leg. **Mechanism: MIDDLEWARE** (`manifest/runtime/src/middleware/logistics-route-driver-vehicle-status-middleware.ts`, barrel-exported; registered in `manifest-runtime-factory.ts` right after the dispatch leg): although the declared `LogisticsRouteStarted` event lists `driverId`/`vehicleId`, `start()`/`complete()`/`cancel()` take NO params so the engine payload `{...commandInput, result}` never carries them (declared event fields are never auto-populated from `self.*`) — the middleware loads the route via `_subject.id` and reads its OWN `driverId`/`vehicleId` uniformly across all three legs. **PRECEDENCE DECISION (the plan's open "decide precedence before wiring both"):** a `LogisticsDispatch` belongs to a route (`routeId` required) and carries its own driver/vehicle, so a fleet member can be committed to BOTH an in-progress route and an in-transit dispatch. Resolution that needs NO change to the shipped dispatch middleware (surgical): the busy leg is unconditional + FSM-guard-safe; the **free legs free the driver/vehicle ONLY when no OTHER active commitment remains** — no `assigned`/`in_transit` `LogisticsDispatch` and no other `in_progress` `LogisticsRoute` references them (scan both stores). So route completion never frees a driver whose dispatch is literally mid-delivery; the dispatch sibling's own `deliver`/`fail` frees them. Guard-safe + idempotent (FSM guards make repeats no-ops; per route+entity+command idempotency key). Conformance test `manifest/runtime/src/__tests__/logistics-route-driver-vehicle-status-middleware.test.ts` (6 tests: IR-lock no `LogisticsRoute*→Driver/Vehicle` reaction + the 4 status commands exist; start→on_route+in_use; complete→free both; cancel→free both; **PRECEDENCE — complete does NOT free the fleet while a dispatch is still in_transit**; guard-safe busy-driver skip). Verified: runtime suite 407 pass (78 files); runtime+api typecheck exit 0; biome clean; no IR/source/migration change → reaction-payload + schema-drift gates unaffected. **DEFERRED:** route↔dispatch precedence for the reverse direction is handled symmetrically by the dispatch leg's own FSM-guard skip; the `LogisticsRouteStopCompleted`/`DelayRecorded` legs do not affect fleet status. Mechanism: middleware (reuse the dispatch leg's commands). **ORIGINAL SCOPING (history):** flip `Driver`/`Vehicle` between `available`/`on_route`/`in_use`. **Prerequisite now SATISFIED (2026-06-14):** the four status commands (`Driver.setOnRoute`/`setAvailable`, `Vehicle.setInUse`/`setAvailable`) were added for the dispatch-lifecycle item below, so the route legs can now reuse them — wire `LogisticsRouteStarted → Driver.setOnRoute + Vehicle.setInUse` and `LogisticsRouteCompleted/Cancelled → …setAvailable`. `LogisticsRouteStarted` (logistics-all-rules.manifest:554) carries `driverId`/`vehicleId` as payload fields, but `LogisticsRouteCompleted`/`Cancelled` (577/583) do not, so reuse the dispatch-status middleware's load-`_subject`-and-read-own-fields pattern. Remaining: route↔dispatch overlap (a driver may be freed by route-complete while still mid-dispatch, or vice-versa) — decide precedence before wiring both. Mechanism: middleware (reuse the new commands).
- [x] **DispatchAssigned/Delivered/Failed → Driver/Vehicle status** — DONE (2026-06-14). The dispatch lifecycle now keeps fleet availability in lockstep: `LogisticsDispatchAssigned → Driver.setOnRoute + Vehicle.setInUse` (busy), `LogisticsDispatchDelivered`/`LogisticsDispatchFailed → Driver.setAvailable + Vehicle.setAvailable` (free). **Root gap fixed:** the Driver `on_route` and Vehicle `in_use` statuses were declared (validStatus constraint + transition, logistics-all-rules.manifest:22/41-43/136/139-142) but **UNREACHABLE** — no command ever mutated to them — so dispatching catering goods never showed the driver/vehicle as busy and never freed them. Added 4 additive IR commands (`Driver.setOnRoute`/`setAvailable`, `Vehicle.setInUse`/`setAvailable`) + their events; recompiled IR (1048→1052 commands). **Fixed as MIDDLEWARE, not the planned reaction** (engine-semantics rule): `deliver`/`fail` take no driver/vehicle params and `driverId`/`vehicleId` are the LogisticsDispatch's OWN fields — declared event fields are never auto-populated from `self.*`, so the delivered/failed payloads cannot carry them. `manifest/runtime/src/middleware/logistics-dispatch-driver-vehicle-status-middleware.ts` (registered in `manifest-runtime-factory.ts`) loads the dispatch via `_subject.id`, reads `self.driverId`/`self.vehicleId` uniformly across all 3 legs, and dispatches the governed status commands. Guard-safe: a driver/vehicle already busy (on another dispatch) or already free is skipped cleanly (free idempotency via the FSM guards). Conformance test: `manifest/runtime/src/__tests__/logistics-dispatch-driver-vehicle-status-middleware.test.ts` (5 tests: IR carries the 4 new commands; assign→on_route+in_use; deliver→available×2; fail→available×2; guard-safe busy-driver skip). Runtime suite 401 pass; runtime+api typecheck green; validate-ai 100/100; zero schema drift. **DEFERRED:** (a) `LogisticsDispatchReassigned` — swaps driver/vehicle, needs two-hook pre-mutation capture to free the PREVIOUS pair (after-emit only sees the new ids); (b) `ShipmentDelivered`/`ShipmentCancelled → Event/LogisticsDispatch` — separate concern, and Shipment carries no driver/vehicle (procurement supplier→facility transfers, not dispatch-routed) so it is N/A to the fleet-status cascade. Mechanism: additive IR commands + middleware.
- [x] **Facilities/maintenance lifecycle** — `MaintenanceWorkOrderCreated → asset under_maintenance`; `FacilityWorkOrderCompleted → FacilityAsset.returnFromMaintenance`; `PreventiveMaintenanceSchedule completed → next MaintenanceWorkOrder`. — PARTIAL: `MaintenanceWorkOrderCompleted → Equipment.recordMaintenance + Equipment.updateStatus` is already wired (reactions.manifest:72-94). ~~`MaintenanceWorkOrderCreated` → set equipment under_maintenance~~ **DONE (2026-06-14)** — symmetric counterpart of the completed leg: opening a work order now takes its Equipment into `maintenance` status so it stops reading as bookable/schedulable; completing the work order returns it to `active` (the existing recordMaintenance leg). **Status-value correction:** Equipment has no `under_maintenance` value — its FSM set is `[active, maintenance, out_of_service, retired]` (equipment-rules.manifest:205-208); the real intent maps to the existing `"maintenance"` via the existing `Equipment.updateStatus` command (no new status, no migration). **Mechanism: MIDDLEWARE, NOT reaction** — although `equipmentId` IS a `MaintenanceWorkOrder.create` param (equipment-rules.manifest:303) so `payload.equipmentId` is reachable and a reaction was technically possible here (unlike the completed leg, where it is unreachable), middleware is chosen for GUARD-SAFETY: `updateStatus` guards `newStatus != self.status` and the FSM only allows `→ maintenance` from `active`/`out_of_service`, so a reaction would fire blindly and rely on the engine SWALLOWING the guard failure for already-in-maintenance/retired/terminal equipment (the established anti-pattern). New `manifest/runtime/src/middleware/maintenance-created-equipment-status-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the completed leg) reads `payload.equipmentId`, loads the Equipment, PRE-CHECKS its status (skips already-maintenance / retired / out_of_service-ineligible / missing cleanly via `onDiagnostic`), and dispatches governed `Equipment.updateStatus(newStatus="maintenance", reason, userId)`. Idempotency key per work order (`maintenance-open:{tenant}:{workOrderId}`). **Pure runtime addition — NO IR/source/schema change** (both the trigger event and the target command already exist in IR), so the reaction-payload / schema-drift / route gates are unaffected. Note: this also makes the completed leg's `maintenance → active` transition real (it was a no-op self-loop while open→maintenance was unwired; the self-loop stays for the active-at-completion edge case). Conformance test `manifest/runtime/src/__tests__/maintenance-created-equipment-status-middleware.test.ts` (6 tests: IR-lock no `MaintenanceWorkOrderCreated → Equipment` reaction; active→maintenance + `EquipmentStatusUpdated` bubbles up; out_of_service→maintenance; already-maintenance skip [guard-safe, no swallowed failure]; retired skip; no-equipmentId skip). Verified: runtime suite 321 pass; runtime+api typecheck 0; `manifest:audit-reaction-payloads` 0/0. Mechanism: middleware. **FacilityWorkOrder ↔ FacilityAsset maintenance status DONE (2026-06-14)** — the facility-side mirror of the Equipment legs, BOTH legs in one middleware: `FacilityWorkOrderCreated → FacilityAsset.sendToMaintenance` (operational→maintenance) AND `FacilityWorkOrderCompleted → FacilityAsset.returnFromMaintenance` (maintenance→operational). Opening a work order against an asset now takes it out of service (stops reading as bookable) and completing the order returns it; both events previously had ZERO consumers so asset status silently diverged from real availability. **Mechanism: MIDDLEWARE (not the plan's "reaction"), one file two legs** — the COMPLETED leg's `assetId` is the work order's OWN field, NOT a `complete` param (declared event fields are never auto-populated from `self.*`), so no reaction can read it (load the work order via `_subject.id`, read `self.assetId`); the CREATED leg's `assetId` IS a `create` param (reachable) but middleware is chosen for GUARD-SAFETY (`sendToMaintenance` guards `status == "operational"`) — load the asset and skip cleanly (already-maintenance/retired/sold) rather than firing blindly and relying on the engine swallowing the FSM-guard failure (same deliberate choice as the Equipment created leg). The COMPLETE leg PRESERVES the asset's existing `nextMaintenanceAt` (returnFromMaintenance unconditionally re-mutates it; pass the loaded value back rather than clobber — shipment-restock unitCost-preserve pattern). Idempotency key per work order per leg. **NO IR/source/migration change** — both trigger events + both target commands + the FacilityAsset operational↔maintenance FSM all already exist in IR → schema/route/reaction-payload/drift gates unaffected. New `manifest/runtime/src/middleware/facility-work-order-asset-status-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the Equipment created leg). Conformance test `facility-work-order-asset-status-middleware.test.ts` (6 tests: IR regression-lock no FacilityWorkOrder*→FacilityAsset reaction; open takes operational→maintenance + `FacilityAssetSentToMaintenance` bubbles; full open→start→complete cycle returns it to operational + `FacilityAssetReturnedFromMaintenance` bubbles; already-maintenance open skip [guard-safe]; no-assetId open skip; complete on an operational asset is a clean no-op). runtime suite 335 pass; runtime+api typecheck green; `audit-reaction-payloads` 0/0/0. Mechanism: middleware (one file, two legs). **`MaintenanceScheduleCompleted` → create next `MaintenanceWorkOrder` DONE (2026-06-14)** — closes the broken preventive-maintenance RECURRENCE LOOP. A `PreventiveMaintenanceSchedule` (staff-logistics-extended-rules.manifest:834) is the recurring template; its `complete(nextDueAt)` rolls the schedule forward (new `nextDueAt` + `lastCompletedAt`) and emits `MaintenanceScheduleCompleted` (line 908), but that event had ZERO consumers — nothing opened the work order for the next cycle, so once the first work order was done preventive maintenance silently stopped recurring. **Mechanism: MIDDLEWARE, not reaction** — `complete(nextDueAt)` is a MUTATE so the payload `{...commandInput, result}` carries `nextDueAt` (a genuine input param) but NOT the schedule's OWN fields the work order needs (`equipmentId`/`areaId`/`title`/`description`/`assignedTo`/`scheduleNumber`), and declared event fields are never auto-populated from `self.*`; the middleware LOADS the schedule via `_subject.id` and dispatches the EXISTING explicit `MaintenanceWorkOrder.create` (equipment-rules.manifest:303). New `manifest/runtime/src/middleware/maintenance-schedule-completed-work-order-create-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the maintenance-created-equipment-status leg) creates the next work order with `workOrderType="preventive"`/`priority="medium"`/`scheduledDate = the schedule's new nextDueAt` (status defaults to `"open"` — all pass the target's `validWorkOrderType`/`validPriority`/`validStatus` invariants by construction; create id in the BODY not instanceId). Guard-safe + idempotent: a store-scan skips when a non-terminal `preventive` work order already exists for the same asset (equipmentId, else areaId) at the same `scheduledDate`, so a re-emitted SAME completion cannot double-open the cycle, while a genuinely LATER completion (different `nextDueAt`) opens a fresh order; a stable `idempotencyKey` is a second backstop. **NO IR/source/migration change** (both the trigger event and the target create command already exist) → reaction-payload / schema-drift / route gates unaffected. **KNOWN LIMITATION (documented):** the dispatch runs as the completing actor, subject to `MaintenanceWorkOrder.create`'s policy (facility_manager/facilities_manager/manager/admin = the same set as the schedule's `complete` policy, so the common path passes; a lower-privilege actor's completion yields a policy-denied diagnostic + skip). Conformance test `manifest/runtime/src/__tests__/maintenance-schedule-completed-work-order-create-middleware.test.ts` (4 tests: IR regression-lock no `MaintenanceScheduleCompleted → MaintenanceWorkOrder.create` reaction; completion opens the next preventive work order scheduled for the new nextDueAt + `MaintenanceWorkOrderCreated` bubbles up; re-completion at the SAME due date does NOT double-open [dedupe]; an advanced due date opens a fresh order [recurrence]). Verified: runtime suite 385 pass (73 files); runtime+api typecheck exit 0. Mechanism: middleware. **Facilities/maintenance lifecycle item now COMPLETE.**
- [ ] **WorkforceOptimization/PerformancePrediction → governed action** — propagate AI optimization output to staffing/scheduling and route low-productivity/high-overtime predictions to a `Notification`/`AdminTask`. Events at workforce-ai-rules.manifest:129/142, both orphaned. Mechanism: reaction.

### Integrations & versioning

- [ ] **Generic versioning loop** — **prerequisite:** `VersionedEntity.setCurrentVersion` does not exist (only register/lock/unlock/updateName); author `setCurrentVersion(versionId: string)` first. Then `on EntityVersionCreated run VersionedEntity.setCurrentVersion`; `on VersionApprovalCompleted/Rejected run EntityVersion.approve/reject`; `on VersionApprovalRequested run Notification.create`. Verify reaction params against the event payload — `VersionApprovalCompleted` (version-control-rules.manifest:332) carries `approverId`/`comments`, not the `reviewerId` that `EntityVersion.reject` (line 140) guards on. Replace the hardcoded `versionCount = 0` (line 18) and `hasPendingApprovals = false` (line 21) with real cross-entity computeds. Mechanism: command + reaction + computed.
- [~] **SmsAutomationRule as a reaction target** — PARTIAL (2026-06-14). The `on EmailTemplateDeleted run SmsAutomationRule.deactivate` leg is **DONE** as middleware (`manifest/runtime/src/middleware/email-template-deleted-sms-rule-deactivate-middleware.ts`, registered in `manifest-runtime-factory.ts`), the direct SMS sibling of the already-shipped `email-template-deleted-deactivate-workflows-middleware`. `SmsAutomationRule.belongsTo template: EmailTemplate with templateId` (sms-automation-rules.manifest:97), so soft-deleting a template left every dependent rule ACTIVE and the SMS trigger service kept firing them against a template whose content is gone (broken/empty SMS). Middleware (not reaction) for the proven reasons: 1:N fan-out by `templateId`, and `templateId` is reachable only as `event.subject?.id` (`softDelete()` takes no params → declared event field not auto-populated from `self.*`). Guard-safe + idempotent: only ACTIVE, non-deleted rules whose `templateId` matches are toggled; custom-message-only rules (empty `templateId`) are correctly left alone. Reversible (`activate` re-enables) → safe to cascade. No IR/schema change. Conformance test: `email-template-deleted-sms-rule-deactivate-middleware.test.ts` (3 tests: fan-out deactivates dependents while sparing other-template/custom-message rules; guard-safe skip of inactive/deleted; IR regression-lock no reaction). Runtime suite 410 pass; runtime typecheck green. **REMAINING (still open):** wire business events (`EventConfirmed/ContractSigned/EventStaffAssigned/ProposalAccepted`) into SMS automation via a net-new middleware fan-out to matching active rules (the larger, separate feature — no SMS send fan-out middleware exists today). Mechanism: middleware (done leg) + middleware (remaining leg).
- [ ] **Notification on email/SMS send** — sending an email/SMS must create a governed `Notification`; currently `packages/notifications/sms-notification-service.ts:121` and `email-notification-service.ts` write only to infra `sms_logs`/`email_logs` with zero `Notification.create` calls, so `/notifications` is blind to all messaging. Mechanism: reaction/governed command.

### Core / cross-cutting orphan events

- [x] **AdminChatMessageSent → AdminChatThread.recordLastMessage** — DONE (2026-06-14). `AdminChatThread.lastMessageAt` was a DEAD column (no command ever wrote it), so admin chat thread lists could not order by recency. **Fixed as a REACTION** (the plan's "command + reaction" was correct): added `command recordLastMessage()` to AdminChatThread (crm-admin-extended-rules.manifest — `guard self.deletedAt == null`, `mutate lastMessageAt = now()`, emit new `AdminChatThreadLastMessageRecorded`) + the reaction `on AdminChatMessageSent run AdminChatThread.recordLastMessage resolve payload.threadId` (reactions.manifest). **Why a reaction works here (unlike the no-op class):** the target thread's id IS reachable from the payload — `AdminChatMessage.create(threadId, …)` takes `threadId` as a genuine INPUT PARAM, so it rides the emitted `{ ...commandInput, result }` as `payload.threadId`. **Correctness landmine avoided:** resolve on `payload.threadId` (the real create param), NOT `payload.adminChatThreadId` — that is a DECLARED event field (crm-admin-extended-rules.manifest:709) which the engine never auto-populates from `self.*`, so it would be a silent no-op AND would fail the `audit-reaction-payloads` strict gate (which is why the gate, at 10 reactions, stays at 0 errors). **No migration:** `lastMessageAt` already exists as a column (`schema:check` stayed in-sync). `recordLastMessage` mutates a no-FSM entity with no block constraints, so `updateInstance` persists cleanly; the `deletedAt == null` guard skips removed threads (reaction error logged-and-swallowed → the send still succeeds). Conformance test: `manifest/runtime/src/__tests__/admin-chat-message-thread-activity-reaction.test.ts` (4 tests: IR-lock that resolve points at the `threadId` param not `adminChatThreadId`/`result`; sending stamps `lastMessageAt` + bubbles `AdminChatThreadLastMessageRecorded`; targets ONLY the message's own thread; soft-deleted thread skipped without breaking the send). `manifest:validate`/validate-ai 100/`schema:check` in-sync/`audit-reaction-payloads` 10 checked 0/0; runtime suite 342 pass; runtime+api typecheck green. Mechanism: command + reaction.
- [~] **EmployeeCertificationExpired/Revoked → escalation** — PARTIAL (2026-06-14): the **notify-the-employee leg is DONE**; the AdminTask escalation + suspend-`EmployeeAvailability`/flag-shifts legs remain. **`EmployeeCertificationExpired/Revoked → Notification.create` DONE** — a lapsed or revoked credential now notifies the affected employee (compliance/safety). Both events (employee-certification-rules.manifest:131/138, emitted by `expire()` and `revoke(reason, revokedBy)`) had ZERO consumers (verified by repo-wide grep: only IR/shards/commands.json reference them — no reaction, middleware, or app code), so a lapse fired nothing — the employee was never told to renew and `/notifications` was blind to it. **The plan's "Mechanism: reaction" was WRONG** per the verified engine-semantics correction at the top of P0: `expire`/`revoke` are MUTATE commands → emitted payload `{...commandInput, result}` carries only a scalar; the recipient is `EmployeeCertification.employeeId` (the cert's OWN field, employee-certification-rules.manifest:9), NOT an `expire`/`revoke` param, and declared event fields are never auto-populated from `self.*` — so no reaction could read the recipient FK. **Fixed as MIDDLEWARE** (sibling of event-staff-assigned-notify): `manifest/runtime/src/middleware/employee-certification-lapsed-notify-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` in the staff/HR group after the staff-onboarding leg), which on either lapse event loads the certification via `_subject.id`, reads `self.employeeId` (+ certificationName/type/expiryDate for the message), and dispatches the governed `Notification.create(recipientEmployeeId=employeeId, notificationType="certification_expired"/"certification_revoked", correlationId=certificationId, create id in BODY not instanceId)`. The revocation `reason` IS a `revoke` param so it rides the payload and is folded into the body. Guard-safe + idempotent (single-shot FSM transitions; per-(kind,cert,employee) key); a cert with no `employeeId` or unresolvable tenant is a clean `onDiagnostic` skip. **KNOWN LIMITATIONS (documented, not silent):** (1) `employeeId` is a real `User` id (`belongsTo employee: User`), so the recipient mapping is clean — no convention gap; (2) the dispatch runs as the acting actor, subject to `Notification.create`'s default `manager/admin` policy — a revocation (manager/admin action) passes, but the scheduled `expire()` sweep must run as manager/admin (no per-call identity override) or the notify is a policy-denied diagnostic + skip (same class as deal-assign / event-staff-assigned legs). **NO IR/source/migration change** (both events + `Notification.create` already exist) → reaction-payload + schema-drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/employee-certification-lapsed-notify-middleware.test.ts` (4 tests: IR regression-lock no `EmployeeCertification*→Notification` reaction; expire notifies the employee [type/correlation/title] + `NotificationCreated` bubbles + cert→expired; revoke notifies with the reason in the body + cert→revoked; no-employee clean skip). Verified: runtime suite 381 pass (72 files); runtime+api typecheck exit 0. Mechanism: middleware. **`EmployeeCertificationExpired/Revoked → suspend EmployeeAvailability` DONE (2026-06-15)** — a lapsed/revoked credential now pulls the employee off the schedule: the middleware fans out the existing governed `EmployeeAvailability.suspend(reason)` over every active availability row for the affected employee. **MIDDLEWARE not reaction for two structural reasons** (per the engine-semantics correction at the top of P0): (1) 1:N fan-out (one lapse → MANY availability rows — recurring day-of-week slots are separate rows — a reaction resolves exactly ONE target); (2) `expire`/`revoke` are MUTATE commands whose payload is `{...commandInput, result}`, and the employee is `EmployeeCertification.employeeId` (the cert's OWN field, NOT an expire/revoke param, never auto-populated onto the event), so the leg LOADS the cert via `_subject.id` then queries `EmployeeAvailability` by `employeeId` (same load-then-scan shape as the cert notify sibling + the email-template-deactivate fan-out). `manifest/runtime/src/middleware/employee-certification-lapsed-suspend-availability-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the cert-notify leg). **SAFE to cascade because `suspend` is REVERSIBLE** (`EmployeeAvailability.reinstate`, employee-availability-rules.manifest:82) — cascading a reversible action off a lapse, so the permanent-vs-reversible hazard that defers ClientArchived→withdraw / vendor-suspend / dish-eightySix does NOT apply; there is intentionally NO restore-on-renewal leg (re-scheduling a renewed employee is a deliberate manual act, same conservative stance as ingredient-recall quarantine). **SCOPE — credential-wide, not shift-specific** (no modeled credential→shift-requirement link exists, so a lapse suspends ALL of the employee's availability — the conservative compliance default). Guard-safe + idempotent: only non-deleted, not-already-suspended rows are dispatched (mirrors `suspend`'s `deletedAt == null` + `isSuspended == false` guards → no swallowed guard failures; a re-emitted lapse finds nothing active and no-ops); per-(cert, availability) key. The revocation `reason` (a genuine `revoke` param) rides the payload and is folded into the suspend reason. **ID space is clean** — both `EmployeeCertification` and `EmployeeAvailability` `belongsTo employee: User with employeeId`, so no convention gap. **KNOWN LIMITATION (documented):** each dispatched `suspend` runs as the lapsing actor, subject to EmployeeAvailability's `hr_admin/payroll_admin/manager/admin` policy — a revocation (manager/admin action) passes; the scheduled `expire()` sweep must run as one of those roles or the suspend is a policy-denied diagnostic + skip (no per-call identity override; same class as the notify legs). **NO IR/source/migration change** (both lapse events + `suspend`/`reinstate` already exist) → reaction-payload + schema-drift gates unaffected (the conformance test regression-locks that no `EmployeeCertification*→EmployeeAvailability` reaction was added). Conformance test `manifest/runtime/src/__tests__/employee-certification-lapsed-suspend-availability-middleware.test.ts` (5 tests: IR regression-lock no such reaction; expire suspends BOTH of the employee's active rows with the cert reason while another employee's row is untouched + 2 `EmployeeAvailabilitySuspended` bubble; revoke folds the reason into the suspend reason; already-suspended/soft-deleted rows skipped [guard-safe, original reason preserved]; no-active-availability clean no-op). Runtime suite 415 pass (80 files); runtime+api typecheck green. Mechanism: middleware (reuses the existing `suspend` command). **REMAINING (deferred, each its own increment):** an `AdminTask` escalation for manager/HR follow-up (`AdminTask.create` is feasible — no sequential counter, randomUUID id — but `assignedTo` has no clean manager source to derive), and flag the employee's future `ScheduleShift` rows (needs a NEW `ScheduleShift.flag` command — does not exist).
- [~] **EmployeeAvailabilitySuspended → flag ScheduleShifts; ContainerDeactivated → Dish.clearDefaultContainer; EmailTemplateDeleted → EmailWorkflow.setActive(false); ChartOfAccountDeactivated → deactivate children** — middleware fan-outs. **`EmailTemplateDeleted → EmailWorkflow.setActive(false)` DONE (2026-06-14).** `EmailTemplateDeleted` (email-template-rules.manifest:92, emitted by `EmailTemplate.softDelete`) had ZERO consumers, so soft-deleting a template left every `EmailWorkflow` referencing it (`emailTemplateId`) ACTIVE — the trigger service kept firing those workflows against a missing template (broken/empty mail). **MIDDLEWARE not reaction** (1:N fan-out by `emailTemplateId`; `softDelete()` takes no params so the declared `templateId` event field is never auto-populated from `self.*` → templateId comes from `event.subject?.id`, like the VendorBlacklisted cascade): `manifest/runtime/src/middleware/email-template-deleted-deactivate-workflows-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the vendor-blacklist leg) loads no subject (the template is gone), scans the EmailWorkflow store by tenant+`emailTemplateId`, and dispatches the existing governed `EmailWorkflow.setActive(false)` per ACTIVE, non-deleted match. Guard-safe + idempotent: skips already-inactive workflows (no spurious `EmailWorkflowUpdated`) and soft-deleted ones (would trip `setActive`'s `deletedAt==null` guard). **SAFE to cascade because `setActive` is REVERSIBLE** — a recreated template can be re-linked + re-activated, so the permanent-vs-reversible split (vendor-suspend / dish-eightySix) does not apply (contrast the ClientArchived→withdraw-Proposals leg at line 151, deferred for exactly that hazard). Policy aligns (setActive = manager/admin/system ⊇ softDelete's manager/admin). NO IR/source/migration change → reaction-payload + schema-drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/email-template-deleted-deactivate-workflows-middleware.test.ts` (3 tests: IR regression-lock no such reaction; delete deactivates dependent active workflows + `EmailWorkflowUpdated` bubbles + other-template workflow untouched; already-inactive/soft-deleted skipped with no spurious update). Runtime suite 388 pass; runtime+api typecheck green; audit-reaction-payloads 0/0; schema:check no drift. Mechanism: middleware. **`ContainerDeactivated → Dish.clearDefaultContainer` DONE (2026-06-15).** Deactivating a container (`Container.deactivate`, core/container-rules.manifest:84) left every `Dish` whose `defaultContainerId` referenced it still pointing at the retired container, so the `belongsTo defaultContainer` relationship (dish-rules.manifest:47) resolved a dead reference and plating/packout defaults kept naming an out-of-circulation container. `ContainerDeactivated` (container-rules.manifest:137) had ZERO consumers. **Authored the prerequisite command + MIDDLEWARE** (the plan's "author the command first" — done, NO migration since `defaultContainerId` already exists): added `Dish.clearDefaultContainer(userId)` (dish-rules.manifest — plain `mutate defaultContainerId = ""`, guard `self.defaultContainerId != ""` for idempotency, emits new `DishDefaultContainerCleared`) + recompiled IR (1052→1053 commands). **MIDDLEWARE not reaction** (1:N fan-out keyed by the container's OWN id): `manifest/runtime/src/middleware/container-deactivated-dish-clear-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the dish-deactivated-prune leg) reads `containerId` from `event.subject?.id` (`deactivate(reason, userId)` takes no `containerId` param, so the declared event field is never auto-populated from `self.*` — mirrors the EmailTemplateDeleted cascade), `tenantId` from the runtime context (the event carries none), scans the Dish store for non-soft-deleted dishes whose `defaultContainerId` matches, and dispatches the governed `clearDefaultContainer` per match. **SAFE to cascade** (clearing an FK is a non-destructive, reversible cleanup — the dish is otherwise untouched; an operator can re-select the container if it is reactivated — so the permanent-vs-reversible hazard that defers dish-eightySix does not apply). Guard-safe + idempotent: inactive/86'd dishes ARE cleared (the dangling reference should reset regardless of availability state), but soft-deleted dishes and already-empty dishes are skipped (the command's `defaultContainerId != ""` guard is the backstop); per-(container, dish) idempotency key. **KNOWN LIMITATION (documented):** the dispatch runs as the deactivating actor, and Dish's default policy omits `inventory_manager` (which `Container.deactivate`'s policy admits), so an inventory_manager-triggered deactivation yields a policy-denied diagnostic + skip on the dish-clear leg (no per-call identity override). Conformance test `manifest/runtime/src/__tests__/container-deactivated-dish-clear-middleware.test.ts` (4 tests: IR regression-lock no `ContainerDeactivated→Dish` reaction; IR-lock the `clearDefaultContainer` command exists; deactivation clears every dependent dish's reference while leaving other-container dishes alone + `DishDefaultContainerCleared` bubbles up twice; guard-safe — inactive dish cleared, empty/soft-deleted/other-container dishes untouched). Verified: runtime suite 419 pass (81 files); runtime+api typecheck exit 0; `manifest:validate` valid / `schema:check` no drift (commands/events don't project) / `audit-reaction-payloads` 10 checked 0/0. **GOTCHA re-confirmed** in the test: Dish AND Container entity-level `>= 0` block constraints silently drop the mutate on persist unless the seed carries every numeric field ([[mutate-persist-dropped-by-block-constraints]]). Mechanism: command + middleware. **REMAINING legs (each its own increment) — Prerequisites:** `ScheduleShift.flag` does not exist (only create/update/swap/remove) — author it first. `ChartOfAccount.deactivate` (chart-of-account-rules.manifest:56) deactivates one record only and there is no declared hasMany over children, so child-deactivation needs both a fan-out command and the inverse relationship. Mechanism: command + middleware + reaction.
- [ ] **DocumentVersionPublished → Document.markParsed** — `Document.markParsed` (crm-admin-extended-rules.manifest:405) guards `self.status == 'parsing'`, but `DocumentVersionPublished` (document-version-rules.manifest:85) does not transition the parent Document into `parsing`, so the reaction as described would always fail the guard. Either set `parsing` in a prior step or relax the guard. Mechanism: reaction (+ guard/precursor).
- [ ] **WasteEntryApproved → event food-cost rollup** — **prerequisite:** the previously-named `EventFinancialSummary` entity does not exist. Define a governed cost-rollup target (likely a command on `EventBudget` or a cost field on `Event`) before wiring `on WasteEntryApproved` (waste-entry-rules.manifest:177). Mechanism: entity/command + reaction.
- [ ] **AI planning → CRM/Event** — `AiEventSetupSessionConfirmed → Event.create (+ AiEventSetupSession.markCreated with the new id)`; `ProposalDraftApproved → governed CRM Proposal`; link call-planner drafts to CRM `Lead`. — (corrected: the prior `EventPlanningDraftConverted → Event.create` framing was wrong — `EventPlanningDraft.convertToEvent` receives a **pre-existing** eventId (event-planning-draft-rules.manifest:215), so the Event must already exist; the real disconnected path is `AiEventSetupSessionConfirmed`, where `markCreated` is called manually by the app layer after an ungoverned Prisma create). Overall diagnosis — AI event-creation produces ids with no downstream propagation — stands. Mechanism: reaction.

### App-layer governance bypasses (break reaction propagation)

- [ ] **Route event-creating bypasses through governance** — `importer.ts`, `documents/parse`, `setup-event-completely.ts`, calendar `sync/trigger`, and the server-to-server route create Events/EventDish/PrepList via raw SQL/Prisma, so `EventCreated`/`EventDishCreated`/`PrepListCreated` never emit and BattleBoard/PrepList/seed middleware are skipped. — PARTIAL: the **PDF** importer path now uses `runManifestCommand` (importer.ts:898/917/1372) and several routes are registered in `bypasses.json:135-185`. Still ungoverned: the **CSV** importer path (insertEvent at importer.ts:525, insertEventDish:566, insertPrepTask:616 — all `$executeRaw`), `documents/parse` retained `eventDish.updateMany`/`eventImport.create` (registered bypasses.json:147-158), `calendar/sync/trigger` `providerSync.update` (registered bypasses.json:173-185), and `setup-event-completely.ts` creates EventDish/PrepList/EventContract/EventBudget via `$executeRaw` (registered bypasses.json:264-276; note its EventStaff leg at :283 IS governed via runManifestCommand). Clarification (2026-06-13 audit): the documents/parse, calendar/sync, and setup-event-completely paths are **registered** approved bypasses — "ungoverned" here means *event-suppressing* (no reaction propagation), not unregistered. The only **unregistered + ungoverned** event-creating path is the **CSV importer** ($executeRaw, no bypass entry). The **CSV vs PDF asymmetry** means CSV-imported events are invisible to BattleBoard/PrepList seed middleware. Mechanism: governed command.
- [ ] **Register the unregistered ungoverned writes** — five categories called out in the plan are **both ungoverned and absent from `bypasses.json`**: `crmScoringRule.create` (crm/scoring/route.ts:139), `proposalTemplate.updateMany` (proposals/templates/actions.ts:190,255), `administrative/trash/restore` universal delegate update/delete, `activityFeed.create/createMany` (activity-feed-service.ts:57,89 + payments/route.ts:385,444), `paymentRefundAttempt.create` (payments/[id]/route.ts:339). Route through governed commands or register them. — PARTIAL: `lead.updateMany` (crm/scoring/calculate, bypasses.json:186-198) **and** `providerSync.update` (calendar/sync/trigger, bypasses.json:173-185) are already registered — so `providerSync.update` should be struck from the unregistered list. The five categories above remain both ungoverned and unregistered. Mechanism: governed command / registration.
- [ ] **Govern the RecipeVersion / forecasting / import / chat side-effect writes** — additional direct writes to governed entities, in `baselines/direct-writes.json` but **not** `bypasses.json`, that suppress events: `recipeVersion.updateMany` (costCalculatedAt backfill — api recipe-costing.ts:275 AND app recipe-costing.ts:262, suppresses RecipeVersion.updated → no downstream cost-recalc; note bypasses.json:199-223 covers the *`recipeIngredient`*.updateMany calls in those same files, NOT these `recipeVersion` calls — the recipeVersion writes are genuinely unregistered); `inventoryForecast.create/update` + `reorderSuggestion.create` (inventory-forecasting.ts:710/693/788/733); `eventImport.create` (events/actions.ts:450, a path separate from importer.ts); `trainingCompletion.upsert` (training/complete/route.ts:159/249); `adminChatThread.update` lastMessageAt (chat messages route.ts:399); `proposalTemplate.updateMany` and `paymentRefundAttempt.create` (also above). Each suppresses its `*.created`/`*.updated` event so reactions never fire. Mechanism: governed command.

---

## P2 — Unused Manifest features worth adopting

- [ ] **Add missing inverse relationships** — Correction (2026-06-13 audit): `Event` **already declares 11 `hasMany`** (invoices, payments, budgets, eventStaff, eventGuests, eventDishes, eventFollowups, eventContracts, eventReports, eventProfitabilities, eventSummaries — event-rules.manifest:49-59); still missing are `Event hasMany` cateringOrders, battleBoards, commandBoards, EventTimeline+EventTimelineItem, waitlistEntries, automatedFollowups, TimelineTask. `Vendor`/`InventorySupplier` **already has** `hasMany items/contacts/ratings` (inventory-supplier-rules.manifest:35-37) — only `hasMany contracts` is missing. Genuinely absent: `Equipment hasMany workOrders`; `VersionedEntity hasMany versions`; `EntityVersion hasMany approvals`; `Driver hasMany routes/dispatches`. Mechanism: relationship. Without inverse sides there is no governed query path or basis for aggregate computeds.
- [ ] **Add missing belongsTo FKs** — `Event.proposalId→Proposal`, `Deal.clientId→Client`, `Invoice.proposalId`, `ClientInteraction.dealId`, `QATemperatureLog.equipmentId→Equipment`, `LogisticsRoute.eventId`, `LogisticsDispatch.shipmentId`, `EventImportWorkflow→EventImport` (note: no `EventImport` FK exists on the workflow entity, and the two model the same domain object at different lifecycle stages). None are implemented yet. Mechanism: relationship.
- [ ] **Aggregate computed rollups (with caching)** — replace manual/denormalized counters with `sum_of`/`count_of` over hasMany: `Event.totalPaidAmount = sum(invoices…) cache request`, `confirmedStaffCount/confirmedGuestCount = count_of(...)`, `EventBudget.actualSpend = sum(lineItems…)`, `Proposal.lineItemCount = count_of(self.lineItems)` (currently a drift-prone manual int — and the source of the send deadlock in P1), `Shipment.totalItems`, `Station` capacity. Only 1 of 45 hasMany relationships has any aggregate today (`Recipe.hasVersion`). Mechanism: computed + aggregate built-ins + caching.
- [ ] **Declare state transitions for status fields** — Dish (active/deactivated/eighty_six), Ingredient (active/deactivated/recalled), Station (active/inactive/under_maintenance). — (corrected: these three entities have **no `status` string field** — Dish/Ingredient use boolean `isActive`+`isEightySix`/`isRecalled`, Station uses `isActive`+`inMaintenance`. A boolean-to-string-status migration is required *first*, then transitions.) Audit every other governed status entity for runtime-enforced FSMs. Mechanism: property migration + transition.
- [ ] **Pull UI/route FSMs into Manifest** — source allowed transitions from the IR instead of the duplicated tables in `apps/api/app/api/shipments/[id]/status/route.ts:37` (`VALID_STATUS_TRANSITIONS` — note: this is an **API** route, not apps/app), `events/contracts/[id]/status/route.ts:29` + `administrative/kanban/actions.ts:27` (`STATUS_COMMAND_MAP`), and `events/constants.ts:1` (`eventStatuses`). Note `eventStatuses` includes `tentative`/`postponed` which are **not** in the Event manifest constraint (event-rules.manifest:63) — the UI offers transitions the engine rejects. Mechanism: transition + IR-derived UI.
- [ ] **Cross-entity constraints** — `EventStaff.staffMustBeActive`, `Proposal.clientMustBeActive`, `EventContract.eventMustBeConfirmedOrDraft`. None exist (EventStaff.create only checks non-empty ids). Mechanism: cross-entity constraint.
- [x] **Fix hardcoded computeds: Station.capacityRemaining / availablePercentage** — DONE (2026-06-13). Both were literal `= 0` (station-rules.manifest:33-34); now `capacityRemaining = self.capacitySimultaneousTasks - self.currentTaskCount` and `availablePercentage = percent(self.capacityRemaining, self.capacitySimultaneousTasks)` (type widened `int`→`number` to match `percent`'s float return). **But the computed fix alone did NOT make `warnNearCapacity` fire** — see the systemic finding below: the runtime does not resolve computed properties inside constraint expressions. The constraints were ALSO inlined to stored-prop expressions (`warnNearCapacity` → `(capacitySimultaneousTasks - currentTaskCount) == 1`, `blockFull` → `currentTaskCount < capacitySimultaneousTasks`), which additionally fixed `blockFull` (capacity was entirely unenforced — a capacity-3 station accepted a 5th task). Conformance test: `manifest/runtime/src/__tests__/station-capacity-computeds.test.ts` (IR regression-lock + production-engine runtime proof that the warn fires at one-slot-remaining and the block enforces capacity). Mechanism: computed + constraint inlining. **(2026-06-14 follow-up: this enforcement was still INERT in production because nothing moved the stored `currentTaskCount` — now fixed by the PrepTask lifecycle → Station task-count reconciliation middleware in P1 above, which keeps the count truthful so these constraints actually fire.)**

- [ ] **🔴 SYSTEMIC (verified 2026-06-13, RE-CONFIRMED + sharpened 2026-06-14): computed properties do NOT resolve inside constraint OR guard expressions** — any `constraint`/`guard`/`:block`/`:warn` whose condition references a `computed` property silently evaluates that operand to `undefined`, so the rule is dead. **2026-06-14 sharpenings (verified against the real `ManifestRuntimeEngine` on `Proposal`):** (1) the defect applies to **GUARDS too**, not just constraints — `guard self.isDraft` (`isDraft = self.status == "draft"`) failed even on a draft proposal (`kind: guard_failed`), while the stored-prop `guard self.status == "sent"` on `markViewed` passed. So it is NOT "simple vs compound" — it is **stored-prop (resolves) vs computed (dead)**, regardless of how trivial the computed is. (2) **Evaluation order is `:block`/`:warn` constraints BEFORE guards.** This MASKS dead computed guards behind an always-blocking computed constraint: `Proposal.send` looked "guarded fine" in a probe because its `blockNoLineItems:block self.hasLineItems` (computed, always falsy → always blocked) short-circuited before the dead `guard self.isDraft` was ever reached. Inlining the constraint to a stored prop EXPOSES the still-dead guard — fix BOTH in the same pass. Worked cases now inlined: **Station** (capacity constraints) and **all of `proposal-rules.manifest`** (send/update guards + accept/withdraw `:block` constraints — the whole Proposal FSM was silently unenforced; `Proposal.update` was entirely unusable). **Impact:** every entity with a guard/constraint referencing a computed has a dead rule, and many are masked. **Action:** scan `manifest/source/**` — for each command, collect `self.<x>` refs in `guards` + `constraints` and flag any `x` that is a `computed` of the same entity; inline the stored-prop equivalent. The conformance lock in `proposal-line-item-count-middleware.test.ts` ("NO Proposal command guard or constraint references a computed") is the reusable pattern. Mechanism: constraint/guard inlining (+ consider an upstream engine fix so guards/constraints can read computeds).
- [ ] **Adopt async commands for heavy ops** — mark `PayrollRun.process`, `EventSummary.refresh`, `InventoryForecast.create`, bulk lead-scoring as `async` (feature is fully implemented; zero `async` declarations exist in source); wire auto-emitted `…Completed`/`…Failed` events into reactions (e.g. `PayrollRunProcessCompleted` → period lock / tip-pool close). Mechanism: async command + reaction.
- [ ] **Saga-ize fragile multi-step flows** — express **BookEvent** (Contract.sign → Payment.charge → Inventory.reserve → Calendar.block → Staff.assign) and **revenue recognition** as sagas with compensation; the most business-critical booking chain has no saga today and a payment-success/inventory-failure leaves contract signed + payment charged with no rollback. Existing sagas (FinalizeEventWithReporting, ProcessInvoicePayment, FinalizeCycleCountSession, AutoGeneratePrepList, RequisitionToPurchaseOrder, InstallSelOnboardingTraining) cover sub-flows only. Mechanism: saga.
- [ ] **Approval workflows on financial/HR commands** — add staged `approval` blocks (not just role guards) to `PayrollRun.process`, `EventBudget.approve`, `EventContract.sign`, emitting `ApprovalRequested/Granted`. Today only procurement has real approval chains (procurementChain, PO managerApproval, vendor contractApproval). Note `PayrollApprovalRequested` is emitted from `PayrollApprovalHistory.create` (a log entity), not from `PayrollRun.process` — so the approval flow has no governance link to the run command. Mechanism: approval workflow.
- [ ] **Role hierarchy + hasPermission** — declare `role … extends …` (e.g. KitchenLead, CateringManager) and replace the **466 `user.role in [...]` string-list checks across 112 files** (corrected from "63 files"; second 2026-06-13 audit refined to 112 files with the bracket pattern — 286 files touch `.role` overall) with `hasPermission(action, target)`. Zero role declarations exist today; adding a role means editing hundreds of policy expressions. Mechanism: role hierarchy.
- [ ] **Scheduled/cron command triggers in Manifest** — the `schedule` DSL is **not yet implemented** in the lexer/parser/IR (planned v1.9.0). Near-term action is to emit governed events from the existing raw-Prisma polling crons (`TaskDueSoon`/`ContractExpiringSoon`/`ShiftStartingSoon`) and react, **not** to wait for the DSL. Mechanism: emitted events + reaction (see P3).
- [ ] **Transactional outbox + idempotency on the dispatcher** — PostgresOutboxStore is already wired into the engine (manifest-runtime-factory.ts:563). — PARTIAL: the remaining work is **idempotency**: `PrismaIdempotencyStore` exists but is opt-in (`if (deps.idempotency)`, factory:496) and no call site sets it, so payment/contract/PO commands have **no retry dedup** — a double-click re-executes the mutation. Wire `idempotencyKey` through generated routes and enable the store by default. Mechanism: idempotency. **(Idempotency gap is P0-severity for payments.)**
- [ ] **Realtime on live-dashboard entities** — mark `BattleBoard`, `CommandBoard`, `PrepTask`, `EventStaff`, `InventoryAlert` `realtime` so the P1 cascade reactions stream to dashboards (SSE routes + `use{Entity}Subscription` hooks are auto-generated). Zero `realtime` declarations exist; without them, fixing the 20 broken reactions and adding 30+ new ones produces data users must reload to see. This is the **primary live-dashboard enabler**, not optional rollout tooling — land it before the P1 reaction work. Mechanism: realtime.
- [ ] **Feature flags as reaction killswitches** — gate each P0/P1 reaction fix behind `flag('…')` (e.g. `flag('event-staffing-cascade')`) so a broken reaction can be enabled per-tenant during rollout. The provider works end-to-end (3 flags in use) but no flag gates any reaction. Correction (2026-06-13 audit): `createEnvFlagProvider()` **IS** injected as `deps.flagProvider` in **apps/api** (`apps/api/lib/manifest-runtime.ts:40,96`) — so existing guards (budget.early_warning, procurement.budget_management) DO fire there. The gap is **apps/app**, where no `flagProvider` injection exists (grep returns zero) — verify the app-side runtime path before relying on flags there. Mechanism: feature flags.
- [ ] **Adopt `cache session`/`cache ttl` for slowly-changing computeds** — all 52 cached computeds use `cache request` only. Time-pure or rarely-changing computeds re-evaluate every command: `Invoice.isOverdue` (invoice-rules.manifest:56) and the three revenue-recognition computeds (revenue-recognition-rules.manifest:243-245) suit `cache ttl 3600`; `Event.budgetedGrossMarginPct`/`actualGrossMarginPct` (event-rules.manifest:284/291) and `Proposal.isExpired` suit `cache session`. Mechanism: computed caching.

---

## P3 — Governance & hygiene

- [ ] **Resolve pending bypass sign-offs** — `CycleCountAuditLog` (bypasses.json:330), `ExtractedDetail` (:343), `ProposalAction` (:356) are all marked PENDING USER SIGN-OFF with no governance decision. Model as governed entity or formally accept as infra. Mechanism: governance.
- [ ] **Register/govern ungoverned infra writes (incl. webhook tables)** — `ProviderSync` (only ProviderSync has a partial entry), plus the entirely-unregistered webhook writes: `outboundWebhook.update` (cron/webhook-retry/route.ts:225), `webhookDeliveryLog.update` (cron/webhook-retry/route.ts:99/118/163 — 3 call sites), `webhookDeadLetterQueue.create` (:184), and `manifestIdempotency.deleteMany` (cron/idempotency-cleanup/route.ts:54). Also `notification_preferences`, `email_logs`/`sms_logs`. Add explicit `bypasses.json` entries + migration intent. Mechanism: governance.
- [ ] **Close IR command-param gaps that force permanent bypasses** — add `EventDish.updateServiceStyle`, `EventSummary.softDelete`, `RecipeIngredient` cost fields (adjustedQuantity/ingredientCost/costCalculatedAt), `Lead.score/scoreBreakdown`, `EventPlanningDraft.linkProposal`, `SmsAutomationRule.recordTriggered` (EmailWorkflow already has `recordTriggered` at email-workflow-rules.manifest:80; SmsAutomationRule's raw-Prisma workaround is also missing from `bypasses.json`). Each missing command is the sole reason a governed entity is written via raw SQL. Mechanism: command/param addition.
- [ ] **De-duplicate webhook auto-disable logic into a reaction** — `shouldAutoDisable` is already centralized in `@repo/notifications`, but its imperative disable-and-update call-site logic is repeated in **four** files (webhook-dispatch.ts:106, integrations/webhooks/trigger/route.ts:187, integrations/webhooks/retry/route.ts:236, cron/webhook-retry/route.ts:220 — the plan previously said three). Collapse into a single `on WebhookDeliveryFailed` reaction (none exists). Mechanism: reaction.
- [ ] **Replace polling crons with emitted events** — `cron/email-reminders/route.ts:204/352` polls `kitchenTask`/`scheduleShift`; `cron/contract-expiration-alerts/route.ts:159` polls `eventContract`. Emit `TaskDueSoon`/`ShiftStartingSoon`/`ContractExpiringSoon` (none exist) and react. Mechanism: emitted events + reaction.
- [ ] **Fix relationship/naming inconsistencies** — `FacilityArea.venueId` (facilities-all-rules.manifest:117) vs siblings' `facilityId` (:195/317/419); `PrepComment` informal `with taskId`/`with employeeId` (prep-comment-rules.manifest:27-28) and `PrepTaskPlanWorkflow` informal `with eventId` (:268) → explicit `fields/references`. (Note: the prior "duplicate CorrectiveAction kitchen vs QA" claim is partly mistaken — `CorrectiveAction` exists only in kitchen-extended-rules.manifest:325; the QA path uses the distinct `QACorrectiveAction` in qa-rules.manifest. `StaffPerformance` and `PerformanceReview` are genuinely distinct live entities, not duplicates.) Mechanism: relationship cleanup.
- [ ] **Move domain pricing/scoring/threshold logic out of UI/routes** — intake `pricingEngine` (apps/app/.../_intake/engine/pricingEngine.ts:23, client TS), CRM lead scoring (crm/scoring/calculate raw updateMany), par/reorder thresholds (SQL CASE in analytics/consolidated/route.ts:164), overtime thresholds belong in computed properties/policies/governed commands. Mechanism: computed/policy.
- [~] **Orphan-event mop-up (lower-severity cascades)** — PARTIAL. **`VendorBlacklisted → cancel open PurchaseOrders` DONE (2026-06-14).** Blacklisting a vendor for cause (`Vendor.blacklist`, vendor-rules.manifest:202) had ZERO consumers: `status="blacklisted"` is a TERMINAL ban (no transition out; `approve` guards `status != "blacklisted"`), yet every open PurchaseOrder pointing at that vendor stayed live and would still be ordered/received/paid (financial-integrity hole). **Fixed as MIDDLEWARE** (1:N fan-out — one blacklisted Vendor → many open POs by `vendorId`, which a single-target reaction cannot resolve; and `vendorId` is reachable only as `event.subject?.id`, not a `blacklist` param — declared event fields are never auto-populated from `self.*`): `manifest/runtime/src/middleware/vendor-blacklisted-cancel-purchase-orders-middleware.ts` (barrel-exported; registered in `manifest-runtime-factory.ts` right after the EventCancelled cascade). On `VendorBlacklisted` it loads the `PurchaseOrder` store, filters by tenantId + vendorId + cancellable status + not-soft-deleted, and dispatches governed `PurchaseOrder.cancel(userId=blacklistedBy, reason="Vendor blacklisted: …")` per open PO. **Scoped to BLACKLIST ONLY, NOT suspend (the crux):** `suspend → status="inactive"` is REVERSIBLE (`approve` transitions inactive→active), so irreversibly cancelling a suspended vendor's in-flight POs would be wrong if the pause is lifted — same permanent-vs-reversible split as the Dish deactivate/eightySix precedent. **Guard- AND transition-safe:** only `draft`/`submitted`/`approved`/`ordered`/`partially_received` (non-deleted) POs are cancelled; `received`/`cancelled`/`rejected` are skipped — note `rejected → cancelled` is NOT a declared transition, so dispatching cancel on a rejected PO would be a swallowed engine rejection, not a real cancel. Idempotent per (tenant, vendor, PO). **Policy aligns** (no skip class): `PurchaseOrder.cancel`'s policy (procurement_manager/manager/admin) is the SAME set as `Vendor.blacklist`'s, so the blacklisting actor can always cancel. **Out of scope (documented):** `PurchaseRequisition` has no vendor FK on its header (only items carry `suggestedVendorId`) → cannot resolve by vendorId; `InventorySupplier` also emits blacklist events but `PurchaseOrder.vendorId` links to the procurement `Vendor`, not InventorySupplier. **NO IR/source/migration change** (both `VendorBlacklisted` and `PurchaseOrder.cancel` already exist) → reaction-payload/schema-drift gates unaffected. Conformance test `manifest/runtime/src/__tests__/vendor-blacklisted-cancel-purchase-orders-middleware.test.ts` (4 tests: IR regression-lock no `VendorBlacklisted` reaction; blacklist cancels all 5 cancellable-state POs + `PurchaseOrderCancelled` ×5 bubble up while another vendor's PO is untouched; terminal/soft-deleted POs skipped [guard+transition-safe] while a genuinely-open one still cancels; SUSPEND does NOT cancel [event-name scoped]). Runtime suite 377 pass; runtime+api typecheck green; `audit-reaction-payloads` 0/0/0. Mechanism: middleware. **REMAINING:** `CateringOrderConfirmed → BudgetLineItem on the event's EventBudget`; `PurchaseOrderReceived` full-PO-close → linked Shipment/requisition; `FacilityWorkOrderCompleted → FacilityAsset maintenance-history`; `StaffTrainingSchedulingEligibilityGranted → mark StaffMember schedulable`; and `VendorSuspended` deliberately left UNwired (reversible). Mechanism: reaction/middleware.

---

## Verification

- **P0 (reaction no-op class):** `manifest/scripts/check-reaction-payloads.mjs` must report **zero new violations**, and `reaction-payload-baseline.json` must shrink to zero as each fix lands (entries only removed, never added). Each fixed reaction ships a conformance test (engine in `deterministicMode` with `emitIndex` assertions) proving the target instance resolves and the downstream mutation fires. Saga/reaction coexistence fixes (Payment→Invoice) must assert `Invoice.applyPayment` is invoked exactly once.
- **P1 (propagations):** per-propagation conformance tests assert the source event triggers the expected child command(s); for middleware (1:N) assert the correct fan-out count and consolidation behavior. Every new reaction/middleware must first prove its target command and event fields exist (no reaction without a target). Diff behavior vs `main` for each new chain.
- **Bypasses:** no new entries in `manifest/governance/bypasses.json` beyond registrations; CI bypass scan stays green; every migrated route asserts the previously-skipped event now emits (BattleBoard/PrepList chains fire for imported events, incl. the CSV path).
- **Schema/relationship/drift:** `manifest:schema:check`, `manifest-schema-parity`, and `manifest:ci` (validate / validate-ai min-score 100 / doctor) pass after each relationship or computed addition. New transitions/computeds carry IR-validator coverage.
- **Definition of done per item:** root cause fixed in producer source (not generated files), prerequisite command/field/entity authored where the verdict flagged one missing, conformance test added/strengthened to encode *why* the propagation matters, all gates green, and the change demonstrated against the running app where user-visible.

## Out of scope (tracked elsewhere)

Schema and store **code generation** (Manifest Automation Phases 2b/4/5/6 — IR-derived Prisma schema replacement, store-projection bridge, generated-client adoption) is **not** part of this plan. It is owned by `manifest/task_plan.md`. This document covers only **entity interconnection** (reactions, middleware, relationships, computeds, transitions, sagas, events/outbox). Where an interconnection fix is blocked by a missing IR command or field (P3), add the minimal command/param here — do not duplicate the codegen initiative.