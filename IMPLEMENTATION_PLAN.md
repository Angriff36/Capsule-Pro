<!-- Generated by LLM with content and structure it deems most appropriate.
     One constraint: write each task as a Markdown checkbox ("- [ ]", "- [x]" when
     done) — check_done.sh halts the build loop only when no "- [ ]" items remain,
     so a plan without checkboxes reads as already complete. -->

# Native Manifest Source Rewrite — Implementation Plan

Authoritative plan: `manifest/NATIVE-REWRITE-PLAN.md` (WS0–WS16, Phase 0 P1–P9, sequencing, standing constraints). Spec: `specs/manifest-native/native-rewrite.md`. Contract: `ACCEPTANCE_TESTS.md`.

**Counts re-verified 2026-07-11 with bare `rg` over `manifest/source/*.manifest` (reference `.bak`/`.example` excluded).** These supersede the plan document's stale numbers.

| Workstream | Metric | Plan-time | Verified 2026-07-11 |
| --- | --- | --- | --- |
| WS0 | `uuid … = ""` defaults | 188 / 69 files | **17 / 10 files** (post-accounting batch — IR-only sweep COMPLETE; was 38/16 post-events). All 17 remaining are required-not-param-seeded DEFER cases (persist-before-mutate trap), each needing a schema/fork decision. |
| WS1 | `user.role in [...]` literals | 464 | **461** |
| WS7 | `status: string` (raw, incl. command params) | 82 fields | **140 raw** / 92 `validStatus` constraints |
| WS5 | native `schedule` decls | 0 | **0** (10 crons in `apps/api/vercel.json`) |
| WS3 | `fanOut` in source | 0 | **0** |
| — | `roleAllows` sites | 12 | **12** (`_base` + accounting/payroll + accounting/time-entry) |

**Compiler:** `@angriff36/manifest` **3.4.25** (pinned in root/api/runtime/mcp-server `package.json`). ≥ 3.4.23 ⇒ trusted `from context.*` params project as optional in zod — the spec's open question is **resolved YES**.

**Per-domain distribution (WS0 uuid / WS1 role / WS7 status):**

| Domain | uuid="" | role-in | status:string |
| --- | --- | --- | --- |
| accounting | 7 DEFER | 26 | 21 |
| administrative | 1 (userId deferred) | 14 | 5 |
| events | 1 DEFER | 72 | 31 |
| knowledge-base | 1 | 3 | 1 |
| operations | 31 | 185 | 53 |
| platform | 10 | 51 | 2 |
| sales | 0 ✓ | 44 | 10 |
| tenant-team | 0 ✓ | 64 | 17 |

`operations/` is too large for one commit — sub-batch by subdirectory: kitchen (3 DEFER / role 90 — 27 DONE 2026-07-11), inventory (2 DEFER / 48 — 16 DONE 2026-07-11), procurement (0 ✓ / 23 — DONE 2026-07-11), logistics (2 / 9 — 5 nullable DONE 2026-07-11, 2 required deferred), equipment (0 ✓ / 5 — DONE 2026-07-11), facilities (0 ✓ / 7 — DONE 2026-07-11), maintenance (0 ✓ / 3 — DONE 2026-07-11).

**Cadence (from the plan):** one domain-batch = one task = one commit (`[refactor(manifest)] <domain>: <workstream> — <what>`), staged by explicit pathspec, `pnpm manifest:ci` green at every commit. Phase 2 batches also end `pnpm db:check` clean and record migrations as awaiting human deploy. WS0/WS1/WS2/WS3 are pausable mid-stream; WS7 batches must complete per-domain (half-migrated vocab is worse than none). Never hand-edit generated artifacts; never run the bare `manifest` CLI.

---

## Phase 0 — Preflight verifications (cheap; gate the workstreams; record answers in `manifest/NATIVE-REWRITE-PLAN.md`)

None are recorded as answered in the plan document's Phase-0 table. WS0 needs no preflight and can start in parallel.

- [x] **P1 — role-name case matching (gates WS1).** ✓ ANSWERED 2026-07-12 → **RESOLVED (fix applied).** `roleAllows` exact-matches `roleIndex.get(roleName)` with no case normalization; capsule injects snake_case `user.role` raw but the IR carried PascalCase → every `roleAllows` policy site denied every real user (live auth bug, hidden by the near-empty dev DB). Fix: renamed all 28 `_base` roles PascalCase→snake_case (pure rename — capabilities/inheritance unchanged; `manifest.prisma` byte-identical ⇒ IR-only, no migration). Verified: 11/11 regression test (positives resolve, negatives deny, legacy `"Manager"` denies) + IR diff confined to role names. The 461 literal sites are unaffected; new caps + drift analysis deferred to the WS1 vocabulary-freeze iteration. → AC-003 gate cleared.
- [ ] **P2 — enum end-to-end on 3.4.25 (gates WS7).** Pilot one low-traffic `status` (e.g. `EventContract.status`): source `enum` → IR → Prisma `enum` column → zod/hooks/client/metadata regen → `pnpm db:dev -- --create-only`. Record. → AC-004
- [ ] **P3 — transitions/guards vs enum members (gates WS7).** After the P2 pilot compiles, inspect IR transitions + run an FSM conformance test: do transitions compare enum members or still strings? Record whether transitions stay string-based. → AC-004
- [x] **P6 — schedule projection reality (gates WS5).** ✓ ANSWERED 2026-07-12 → **RESOLVED.** `schedule` projects to `app/api/cron/<name>/route.ts` → `runtime.runSchedule` (CRON_SECRET GET) + auto-emitted vercel.json; fits capsule convention. Of 10 crons: 2 already target a manifest command (inventory-audit, async-reactions/drain), 2 hybrid, 6 infra. WS5 unblocked. → AC-006
- [ ] **P9 — fanOut + count() conformance (gates WS3).** Port one existing cascade middleware to `fanOut` in a test IR; run it end-to-end in the capsule runtime harness. Record. → AC-005
- [ ] **P4 — OCC version-in-compound-key bug (gates WS16).** Re-run the 2026-06-18 silent-data-loss repro against a versioned entity on 3.4.25. Record. → (WS16, fork)
- [x] **P5 — read-path masking (gates WS11).** ✓ ANSWERED 2026-07-12 → **CONFIRMED INERT.** 0 `masked` fields in source today; reads bypass the runtime (constitution §10) → `masked`/`unmask when` is a no-op. WS11 is a fork (route reads through runtime, or mask at read-projection); do NOT sweep-add `masked`. → AC-008
- [x] **P7 — webhook projection reality (gates WS14).** ✓ ANSWERED 2026-07-12 → **INERT.** Upstream ships a full webhook handler (HMAC + idempotency + transform) + a nextjs route generator, BUT capsule wires zero webhook routes through the runtime (Stripe/Clerk/Sentry are hand-written). WS14 fork. → AC-008
- [x] **P8 — async command runtime (gates WS14).** ✓ ANSWERED 2026-07-12 → **PARTIAL.** Upstream `async`/JobQueue IR is inert (0 consumption). BUT capsule's OWN durable async-reaction system is live (`PostgresAsyncReactionStore` + `async-reactions/drain` worker + 9+ handlers). WS14 builds on capsule's system, not upstream `async`. → AC-008

---

## WS0 — Empty-string UUID defaults ⛔ CRITICAL (do FIRST; active create-breaking bug) → AC-001, AC-002, AC-007

Not a blind `s/= ""//`: per field, classify **nullable-vs-required** and **mutate-filled-vs-param-seeded** (the persist-before-mutate trap). **Nullable `uuid? = ""` (129/186) → drop default = IR-ONLY, NO migration** (verified 2026-07-11: compiler 3.4.25 no longer projects `@default("")` on `@db.Uuid` columns, so `manifest.prisma` is unchanged → no `db:dev`, no "awaiting deploy"; just `manifest:build` + `manifest:ci`). **Required `uuid = ""` (57/186)** set only by a rename-mutate → seed at bootstrap (rename create param to match the field, accept it as a param, or make the column nullable) — these MAY touch schema → migration, case-by-case. Nullable per batch: manifest edit → `pnpm manifest:build` → `pnpm manifest:ci` → commit. Gate: `manifest:ci` green + a **Postgres-backed create smoke-test per touched entity** (in-memory stores accept `""` and hide the bug).

- [x] Pilot: `TrainingAssignment` (done 2026-07-07 — `employeeId` required + param renamed, `assignedBy → uuid?`, migration `20260707125708_...`, full flow proven against Postgres)
- [x] WS0 · knowledge-base (1 field) — DONE 2026-07-11: `authorId: uuid? = ""` → `uuid?` (nullable → IR-only, NO migration; `manifest:ci` green, `manifest.prisma` byte-identical). Warm-up batch validated the pipeline.
- [x] WS0 · platform (9/10 IR-only) — DONE 2026-07-11: `roleId`, `entityId`, `overriddenBy` (required, param-seeded), `authorizedBy` (+4 sentinel checks → `null`), `createdById`, `supersededBy` (+`isLatest` simplify), `templateId` (+`hasTemplate`/`hasMessageOrTemplate` simplify), `emailTemplateId`, `emailTemplateTenantId`. All IR-only, no migration (`manifest:ci` green, `manifest.prisma` byte-identical). Lesson: nullable uuid fields whose `""` is a sentinel need the `==""`/`!=""` checks moved to `==null`/`!=null` in the same commit.
- [ ] WS0 · platform/api-key `createdByUserId` (required, optional-param + `: user.id` mutate fallback → needs nullable change `uuid→uuid?` → migration; separate batch)
- [x] WS0 · administrative (13/14 IR-only) — DONE 2026-07-11: dropped `= ""` from 13 fields (7 nullable + 6 required-but-param-seeded — each has a same-named create param that seeds the bootstrap INSERT, so the column stays NOT NULL with zero schema change). Fields: BoardConfig.createdBy; AdminTaskAttachment.{taskId,uploadedBy}; AdminTaskDevMeta.taskId; AdminTaskComment.{taskId,authorId}; AdminChatParticipant.threadId; AdminTaskFileRef.{taskId,refId,linkedBy}; AdminTask.{assignedTo,createdBy,sourceId}. Also fixed `admin-task-rules` `mutate sourceId = … : ""` → `: null` (the mutate fallback was writing a bad `""` into `@db.Uuid` — same WS0 bug class; null is valid for the nullable column). `manifest:ci` green; `manifest.prisma` byte-identical (no migration). `apps/app` typecheck green.
- [ ] WS0 · administrative/admin-chat-participant `userId` (1 field deferred) — required `uuid`, NO create param, filled only by `mutate userId = user.id` (context) → bootstrap INSERT writes `""` to NOT NULL `@db.Uuid`. ~~Clean IR-only fix: add a trusted create param `userId: string from context.user.id`~~ **⚠ 2026-07-11 finding: the trusted-param fix is NOT viable — it's a FORK.** Callers pass ARBITRARY userIds in the create body for multi-participant thread creation: `apps/api/app/api/administrative/chat/threads/route.ts:209` (`userId: employeeId`), `:457` (`userId: employee.id` — self), `:481` (`userId: participantId` — other), `threads/[threadId]/route.ts:81` and `threads/[threadId]/messages/route.ts:108` (`userId: options.userId`). Forcing `userId = context.user.id` would make every participant the current user and break multi-participant chat. So this field is NOT a clean drop. Options for Ryan: (a) accept a caller-supplied `userId: string` param (server-mediated today; the spoofing hole only matters via the generic dispatcher — mitigate with an execute policy there, not by freezing the value), or (b) split: keep `create` self-scoped + add an admin-only `addParticipant(userId)` command for multi-participant. Fallback: make column nullable → migration. Needs sign-off before code.
- [x] WS0 · sales (17 fields) — DONE 2026-07-11: dropped `= ""` from 17 uuid fields across 6 files (12 nullable + 5 required-but-param-seeded — each required field has a same-name create param that seeds the bootstrap INSERT, so the NOT NULL column stays satisfied). **All IR-only**: `manifest:ci` green, `manifest.prisma` byte-identical (no migration, no `db:dev`; independently re-confirmed by the prisma-drift gate), apps/app+api+web typecheck green. Sentinel moves (nullable fields whose `""` was the unset marker → `null`, in the same commit): `Lead.convertedToClientId` (the `isConverted` computed + 3 guards/constraints + the explanatory comment — load-bearing: `null != ""` would have flipped the computed true), `Lead.assignedTo` (warnHighValueUnassigned), `Client.assignedTo` (warnArchiveWithAssignment), `ClientInteraction.{clientId,leadId}` (warnNoClientOrLead); `Proposal.publicToken` (`generatePublicLink` guard `== ""` → `== null` — load-bearing: without it the guard would block first link generation); `ProposalDraft.templateId` mutate `: ""` → `: null` (the mutate was itself writing a bad `""` into `@db.Uuid` — same WS0 bug class as admin-task `sourceId`). **Side effect to expect on every WS0 IR-only batch**: dropping a *required* uuid's default tightens its generated TypeScript read type `string?` → `string` (the generator treated defaulted fields as optional; removing the default reveals the true NOT NULL shape) — correct, and no consumer broke here.
- [x] WS0 · tenant-team (19 fields) — DONE 2026-07-11: dropped `= ""` from all 19 uuid fields across 6 files (14 nullable + 5 required-but-param-seeded — each required field `TipPool.periodId`, `DisciplinaryAction.issuedBy`, `TimeOffRequest.employeeId`, `EmployeeCertification.employeeId`, `EmployeeAvailability.employeeId` has a same-name create param that seeds the bootstrap INSERT, so the NOT NULL column stays satisfied with zero schema change). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration, no `db:dev`). Load-bearing secondary fixes in the same commit: (1) `OpenShift.create` `mutate claimedBy = ""` → `null` (the create-mutate was writing a bad `""` into nullable `@db.Uuid` — same WS0 bug class); (2) `ScheduleShift.declineSwap` + `cancelSwap` `mutate swapOfferedTo = ""` → `null` (×2, same class); (3) `DeliveryRoute.start` `guard self.driverId != ""` → `!= null` (sentinel — after the drop, `null != ""` would have wrongly computed true and allowed an unassigned route to start). `pnpm check` green (26/27; the 1 failure is the pre-existing unrelated `@repo/manifest-runtime` ai-suggestions.ts TS2589/TS7006). Required-field drops tightened generated read types `string?` → `string` — zero app/web/api consumers broke. Count: 123/44 → 104/38. **tenant-team now WS0-clean (0 remaining).**
- [x] WS0 · events (23/24 IR-only) — DONE 2026-07-11: dropped `= ""` from 23 uuid fields across 9 files (20 nullable + 3 required-but-param-seeded: `BoardProjection.entityId`, `CateringOrder.customerId`, `EventGuest.eventId` — each has a same-name create param verified). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). Load-bearing sentinel simplifications in same commit (4: `event-planning-draft hasVenue`, `event-report isReviewed`, `catering-order syncVenue` guard, `battle-board syncFromEvent` guard — each dropped a now-dead `and self.<field> != ""` clause from a `!= null` check; after the drop the value is null-not-"" so `!= null` alone is correct, and the old `!= ""` would have wrongly passed on null). `pnpm check` green (26/27; the 1 failure is the pre-existing unrelated `@repo/manifest-runtime` ai-suggestions.ts TS2589/TS7006). Count: 61/25 → 38/16.
- [ ] WS0 · events DEFER (1 field) — `EventImportWorkflow.eventId` is required `uuid`, set only at workflow step 6 via `completeActivating(eventId)` (late-binding; no create param). NOT a clean IR-only drop (the bootstrap INSERT would write the default before the step-6 mutate). Leave `= ""` until a focused sub-batch decides: make nullable (matches the "not yet assigned" semantics) → migration, or accept `eventId` as a create param. Awaiting decision.
- [x] WS0 · accounting (21/28 IR-only) — DONE 2026-07-11: dropped `= ""` from 21 uuid fields across 9 files (12 nullable + 9 required-but-param-seeded: `CollectionCase.invoiceId`, `EmployeeDeduction.employeeId`, `PayrollApprovalHistory.{payrollRunId,performedBy}`, `PayrollRun.payrollPeriodId`, `BudgetAlert.budgetId`, `Invoice.eventId`, `Payment.invoiceId`, `PaymentMethod.clientId` — each verified same-name create param). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). Load-bearing sentinel fixes in same commit (3): `time-entry isApproved` (`self.approvedBy != ""` → `!= null`), `chart-of-account hasParent` (`!= ""` → `!= null`) + `isRoot` (`== ""` → `== null`) — each would have flipped wrongly post-drop. `pnpm check` green (26/27; the 1 failure is the pre-existing unrelated `@repo/manifest-runtime` ai-suggestions.ts TS2589/TS7006). Count: 38/16 → 17/10.
- [ ] WS0 · accounting DEFER (7 fields) — all required, NO same-name create param (parent-context/body-seeded only, persist-before-mutate trap): `CollectionCase.{eventId, clientId}`, `CollectionAction.collectionCaseId` (no `create` command), `CollectionPaymentPlan.collectionCaseId` (created via `createPaymentPlan`), `Payment.{eventId, clientId}`, `Invoice.clientId`. Preferred IR-only resolution per field: accept the field as an explicit create param (rename or add), or trusted `from context.*` param for audit fields, or make nullable → migration. Same class as the other DEFERs. Awaiting a focused sub-batch.
- [x] WS0 · operations/kitchen (27/30 IR-only) — DONE 2026-07-11: dropped `= ""` from 27 uuid fields across 11 files (20 nullable + 7 required-but-param-seeded: `WasteEntry.{inventoryItemId,loggedBy}`, `QACorrectiveAction.assignedTo`, `QATemperatureLog.{equipmentId,loggedBy}` [seeded via the `log(...)` command — the only create path], `AllergenWarning.eventId`, `PrepTaskPlanWorkflow.eventId` — each verified to have a same-name create/log param). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). Load-bearing secondary fixes in same commit: 4 sentinel migrations (`waste-entry hasLocation/hasEvent`, `ingredient hasInventoryItem`, `dish clearDefaultContainer guard` — all `!= ""` → `!= null`, else `null != ""` flips true) and 5 mutate-fallbacks (`prep-list completedBy` ×2, `prep-comment resolvedBy`, `ingredient inventoryItemId`, `dish defaultContainerId` — all `= ""` → `= null`, same WS0 bug class writing bad `""` into `@db.Uuid`). `pnpm check` green (26/27; the 1 failure is the pre-existing unrelated `@repo/manifest-runtime` ai-suggestions.ts TS2589/TS7006). Count: 88/34 → 61/25.
- [ ] WS0 · operations/kitchen DEFER (3 fields) — required `uuid`, not param-seeded → NOT clean IR-only drops: (1) `QACorrectiveAction.relatedCheckId` — create param is `optional` (callers omit for standalone actions) + column comment says nullable; (2) `QACorrectiveAction.escalatedTo` — no create param, set only by `escalate`; (3) `PrepTask.locationId` — `create` (line 227) has no `locationId` param, set by `updateDetails`. Preferred IR-only resolution per field: add a same-name create param (or trusted `from context.user.id` for audit-ish) / make `uuid?` where the column is already nullable (verify vs `manifest.prisma`). Same class as inventory createdById/countedById. Awaiting a focused sub-batch.
- [x] WS0 · operations/inventory (16/18 IR-only) — DONE 2026-07-11: dropped `= ""` from 16 uuid fields across 5 files (14 nullable + 2 required-but-param-seeded: `InventoryTransfer.fromLocationId`/`toLocationId`, each has a same-name create param @ line 52 that seeds the bootstrap INSERT). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). Load-bearing sentinel fix in same commit: `InventoryTransaction.hasReference` computed `self.referenceId != ""` → `!= null` (after the drop, `null != ""` would have wrongly flagged a no-reference transaction as having one). `pnpm check` green (26/27; the 1 failure is the pre-existing unrelated `@repo/manifest-runtime` ai-suggestions.ts TS2589/TS7006). Count: 104/38 → 88/34.
- [ ] WS0 · operations/inventory `CycleCountSession.createdById` + `CycleCountRecord.countedById` (2 DEFER) — both required `uuid`, NO create param, filled only by context-mutate `mutate <field> = user.id` (persist-before-mutate trap: the bootstrap INSERT writes the default before the mutate runs). NOT a clean IR-only drop. Preferred IR-only resolution: add a trusted create param `<field>: string from context.user.id` (the proven wiring-inspect pattern for actor/audit params — engine injects at bootstrap, also closes the spoofing hole) and drop the now-redundant mutate; fallback: make columns nullable → migration. Same class as platform/api-key `createdByUserId`. Awaiting a focused sub-batch (trusted-param port or nullable migration).
- [x] WS0 · operations/procurement (11 fields) — DONE 2026-07-11: dropped `= ""` from all 11 uuid fields (10 nullable + 1 required-but-param-seeded). The 1 required field `PurchaseRequisition.requestedBy` has a same-name create param (line 79) + guard (81) + mutate (87) that seeds the bootstrap INSERT, so dropping the bogus default keeps the column NOT NULL with zero schema change. Two load-bearing fixes in the same commit: (1) `approveManager` mutate `approvedBy = self.estimatedTotal >= 5000 ? "" : userId` → `: null` (the ternary wrote a bad `""` into `@db.Uuid` on the finance-escalation path — same WS0 bug class); (2) `PurchaseRequisitionItem.hasSuggestedVendor` computed `self.suggestedVendorId != ""` → `!= null` (after the drop, `null != ""` would have wrongly computed `true`, signalling a vendor that isn't there — load-bearing). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). Side effect: `requestedBy` generated read type `string?` → `string` (required-field tightening); `pnpm check` green except the pre-existing `@repo/manifest-runtime` `ai-suggestions.ts` TS2589/TS7006 (unrelated, documented below). Count: 134/47 → 123/44. **procurement now WS0-clean (0 remaining).**
- [x] WS0 · operations/logistics nullable (5 fields) — DONE 2026-07-11: dropped `= ""` from 5 nullable uuid fields (Driver.vehicleId; Shipment.{eventId, supplierId, locationId, deliveredBy}). Also fixed Shipment.create mutate fallbacks `supplierId/eventId = … : ""` → `: null` — same WS0 bug class (the fallback was writing a bad `""` into `@db.Uuid` when the optional param was null; exactly the admin-task `sourceId` pattern). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration, no `db:dev`). No in-file sentinel `""` checks on these fields (verified by full read of both logistics files) → no `"" → null` check migration needed. Read types unchanged (nullable stays `string | null`) → `pnpm check` not required. Count: 139/48 → 134/47.
- [ ] WS0 · operations/logistics `LogisticsRoute.{vehicleId, driverId}` (2 required, DEFERRED) — required `uuid`, but the create params are `optional vehicleId/driverId` and the route-create UI (`apps/app/.../logistics/routes/routes-view.tsx:120-164`) sends only `name` + `scheduledDate` at create (assignments happen later via update/dispatch). So the columns are genuinely optional at creation and MUST become nullable (`uuid → uuid?`) → migration. NOT a clean IR-only drop. Same class as platform/api-key `createdByUserId`. Awaiting a nullable-migration sub-batch (`pnpm db:dev --create-only` + human deploy); watch for `string → string | null` consumer widening + a `db:check`.
- [x] WS0 · operations/equipment (4 fields) — DONE 2026-07-11: dropped `= ""` from 4 nullable `MaintenanceWorkOrder` uuid fields (areaId, equipmentId, reportedBy, assignedTo). All IR-only (`manifest:ci` green, exit 0; `manifest.prisma` byte-identical — no migration, no `db:dev`). No in-file sentinel `""` checks on these fields (the only `!= ""` guards are on command-input params like `assignedTo`, not on `self.<field>`), so no `"" → null` check migration needed. Read types unchanged (nullable stays `string | null`) → no app-consumer type impact, `pnpm check` not required. `equipmentId` is the only field with live bug-fix value (optional create param → omitted case went from failing `""` insert to valid `null`); the other 3 were dead-default cleanup that completes the per-field sweep. AC-002 covered by the deductive IR-only proof: nullable `@db.Uuid` + `null` default is a Postgres invariant (pilot-proven 2026-07-07); column byte-identical. Count: 147/51 → 143/50.
- [x] WS0 · operations/facilities (3 fields) — DONE 2026-07-11: dropped `= ""` from 3 nullable uuid fields (FacilityArea.venueId, FacilityAsset.facilityId, FacilityAsset.areaId). All IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). Each has a required same-name create param that seeds the INSERT, so the `= ""` was dead default — pure cleanup, no runtime change, no persist-before-mutate risk. No in-file sentinel `""` checks on these fields; read types unchanged (nullable stays `string | null`). Count: 143/50 → 140/49.
- [x] WS0 · operations/maintenance (1 field) — DONE 2026-07-11: dropped `= ""` from `WorkOrder.assignedTo` (nullable uuid). IR-only (`manifest:ci` green; `manifest.prisma` byte-identical — no migration). **Real bug fix, not just cleanup**: `WorkOrder.create` does NOT set `assignedTo`, so the bootstrap INSERT previously wrote `""` to a nullable `@db.Uuid` (Postgres reject); now inserts `null` (valid). Load-bearing sentinel migration in the same commit: `computed isAssigned = self.assignedTo != ""` → `!= null` (without it, an unassigned `null` work order would compute `isAssigned = true`). Read type unchanged (`string | null`). Count: 140/49 → 139/48. This completes all fully-nullable zero-required operations subdomains (equipment/facilities/maintenance all WS0-clean).
- [ ] WS0 · verify `rg 'uuid.*= ""' manifest/source -g '*.manifest'` → 0 (AC-001) and every touched entity has a green Postgres create smoke-test (AC-002)

---

## Phase 1 — IR-only rewrites (no DB migration; per-domain batch = one commit + green `manifest:ci`)

### WS1 — Role capability migration (461 `user.role in [...]` → `roleAllows`) → AC-003, AC-007
**P1 RESOLVED 2026-07-12** — `roleAllows` now resolves snake_case (28 `_base` roles renamed PascalCase→snake_case, IR-only); the gate is cleared. Freeze the capability vocabulary first (top literal patterns → `manageAccess`/`leadAccess`/`staffAccess`/`kitchenAccess`/`hrPayrollAccess`/`eventAccess`/`financeAccess`; mint narrow caps for the long tail — no semantic widening/narrowing). Add new caps to `_base.manifest` (already has: staffAccess, leadAccess, salesAccess, financeAccess, manageAccess, adminAccess). Per site, assert the resolved role set equals the removed literal array via an `effectivePermissions` IR-diff script — fail the batch on any drift. Regen `compile · openapi` per batch.

- [ ] WS1 · freeze capability vocabulary + add missing caps to `_base.manifest` + write the `effectivePermissions` IR-diff guard script
- [ ] WS1 · operations/kitchen (90 sites)
- [ ] WS1 · events (72 sites)
- [ ] WS1 · tenant-team (64 sites)
- [ ] WS1 · platform (51 sites)
- [ ] WS1 · operations/inventory (48 sites)
- [ ] WS1 · sales (44 sites)
- [ ] WS1 · accounting (26 sites; partial — payroll/time-entry already on `roleAllows`)
- [ ] WS1 · operations/procurement (23 sites)
- [ ] WS1 · administrative (14 sites)
- [ ] WS1 · operations/logistics (9 sites)
- [ ] WS1 · operations/facilities (7 sites)
- [ ] WS1 · operations/equipment (5 sites)
- [ ] WS1 · operations/maintenance (3 sites)
- [ ] WS1 · knowledge-base (3 sites)
- [ ] WS1 · verify `rg 'user\.role in \[' manifest/source -g '*.manifest'` → 0 (AC-003)

### WS2 — Command hygiene: guard-soup → named constraints (ride-along) → AC-007
Convert bare message-less validation guards into named `constraint` blocks (stable code, `message`/`messageTemplate`, `details`); guards keep only *state* preconditions. Batch user-facing-form domains first (events, sales/crm, accounting). Replace defensive create ternary-defaulting with property defaults where persist-before-mutate allows. `overrideable` additions (capacity, credit-limit, variance) are product decisions → batch for Ryan. Regen `compile · openapi`.

- [ ] WS2 · events + sales command hygiene pass
- [ ] WS2 · accounting command hygiene pass
- [ ] WS2 · remaining domains command hygiene pass
- [ ] WS2 · collect `overrideable` candidates into a NEEDS-RYAN list (→ WS6 doc)

### WS4 — Explicit `emit` payload mapping (optional ride-along) → AC-007
Only when touching a WS2/WS3 command whose event feeds a reaction — make the emit explicit so `payload.X` is compiler-checked. No standalone sweep.

- [ ] WS4 · convert emits to explicit form opportunistically during WS2/WS3 (no dedicated gate)

### WS5 — Schedules: declare the 10 vercel.json crons as native `schedule` → AC-006, AC-007
Gated on P6. Crons: `sentry-fixer/process`, `cron/webhook-retry`, `cron/inventory-audit`, `cron/contract-expiration-alerts`, `cron/email-reminders`, `cron/idempotency-cleanup`, `cron/integration-auto-sync`, `outbox/publish`, `async-reactions/drain`, `cron/keep-alive`. Some target non-manifest routes → need a thin global `command` first. Point vercel.json at the projection-emitted route (+ app→api rewrite). Delete the hand route only after traffic proven on the generated one.

- [ ] WS5 · survey each of the 10 crons: manifest-command target vs needs-thin-command; record mapping
- [ ] WS5 · declare `schedule` for crons whose target is already a manifest command
- [ ] WS5 · author thin global commands + `schedule` for the rest
- [ ] WS5 · prove each generated route fires (manual trigger) before deleting its hand route
- [ ] WS5 · verify 10/10 crons declared as `schedule` (AC-006)

### WS3 — Reaction nativization: middleware → `on` / `fanOut` / `count()` → AC-005, AC-007
Gated on P9. Inventory `manifest-runtime-factory.ts` (69 `create*Middleware(` sites); classify per rationale comment into (a) 1:1 dispatch → `on`, (b) unconditional cascade → `fanOut`, (c) parent-count recompute → `count()`, (d) justified-TS (leave). Port one at a time: add source reaction, delete the middleware wiring **and** its test in the **same commit**, port the test to a reaction conformance test. Never run both (double-fire). Gate: `audit-reaction-payloads` + ported tests.

- [ ] WS3 · classification pass over the 69 middleware sites (a/b/c/d table)
- [ ] WS3 · port category (a) 1:1-dispatch middleware to `on` reactions (one per commit)
- [ ] WS3 · port category (b) cascade middleware to `fanOut` (one per commit)
- [ ] WS3 · port category (c) parent-count middleware to `count()` params (one per commit)
- [ ] WS3 · confirm justified-TS (d) middleware left intact with rationale (AC-005)

---

## Phase 2 — Schema-touching rewrites (batch: manifest edit → full regen → `db:dev --create-only` → review SQL → deploy is human → `db:check`)

### WS7 — Enum migration (flagship) → AC-004, AC-007
Gated on P2/P3 pilot. Per field: extract vocabulary from its `validStatus` constraint + transition table (they already agree), declare `enum XStatus { }` top-level, retype the property, **delete the now-redundant `validStatus` constraint**, keep transitions (string-based unless P3 shows enum-awareness). Pre-flight per table: `SELECT DISTINCT status` and reconcile stragglers before the enum alter. Low-write-volume domains first. Fix app-side consumer type errors per batch. (140 raw `status: string` incl. params; ~92 `validStatus` constraints scope the true closed sets — classify per field.)

- [ ] WS7 · pilot one entity end-to-end (one migration) — records P2/P3
- [ ] WS7 · knowledge-base + platform (low volume: 1 + 2)
- [ ] WS7 · administrative (5) + sales (10)
- [ ] WS7 · tenant-team (17) + accounting (21)
- [ ] WS7 · events (31)
- [ ] WS7 · operations by subdirectory (53 total; kitchen/inventory largest)
- [ ] WS7 · verify closed-set `status: string` → 0 with each batch's `validStatus` deleted (AC-004)

### WS8 — Composite unique keys → AC-007
`unique [tenantId, eventNumber]`-style (0 today). Candidates: eventNumber, invoiceNumber, sku, email-per-tenant. Verify no dup rows (`GROUP BY … HAVING count(*)>1`) then declare + migrate per entity.

- [ ] WS8 · survey natural-key candidates + dup-row check
- [ ] WS8 · declare + migrate composite uniques per entity (interleave with WS7 domain touch)

### WS9 — Referential actions (fork-gated) → AC-008
253 `belongsTo` / 12 `onDelete`. **Verify first** whether `onDelete` projects an FK (conflicts with repo no-FK convention) or is IR-metadata-only. If FK → NEEDS-RYAN fork; if metadata-only → sweep cascades for owned children.

- [ ] WS9 · verify whether manifest `onDelete` projects a Prisma FK; record verdict → if FK, write NEEDS-RYAN

### WS10 — Money precision (ride-along only) → AC-007
Bare `money` already projects `Decimal(10,2)`. Standardize explicit `money(12,2)` only when the drift audit flags it or when touching a file anyway. No standalone sweep.

- [ ] WS10 · fix explicit money precision opportunistically when a drift audit flags a mismatch

### WS11 — PII masking/encryption (fork-gated on P5) → AC-008
If P5 confirms reads bypass masking, this is a fork (route reads through runtime, or mask at read-projection) — do NOT sweep-add unenforced `masked` modifiers.

- [ ] WS11 · after P5, write NEEDS-RYAN proposal (route-through-runtime vs read-projection masking); no sweep without sign-off

---

## Phase 3 — Structural / organizational (each NEEDS-RYAN before any code) → AC-008

- [ ] WS6 · Approvals + sagas expansion — one-page candidates table (hand-coded flow location, proposed saga/approval shape, NEEDS-RYAN status); no impl without sign-off
- [ ] WS12 · `module` blocks per domain — investigate whether `compileProjectToIR` merges `module`-wrapped files with shared `_base`; pilot one domain; if `ENTITY_DOMAIN_MAP` can derive from `ir.modules`, write NEEDS-RYAN; else document as cosmetic-skip
- [ ] WS13 · Value objects — investigate projection shape (Json vs flattened columns) for repeating address/contact clusters (≥3 entities); write NEEDS-RYAN before touching DB
- [ ] WS14 · Async commands + inbound webhooks — gated on P7/P8; propose each individually as NEEDS-RYAN
- [ ] WS16 · versionProperty expansion — gated on P4; expand only after P4 proves the generic store handles versions; write NEEDS-RYAN
- [ ] WS15 · Non-goals recorded (external entity, realtime flags, new display-only computeds, retiring generate.mjs route remap) — no re-litigation

---

## Definition of done (success criteria — `ACCEPTANCE_TESTS.md` gates the loop)
- [ ] `rg 'uuid.*= ""' manifest/source -g '*.manifest'` → 0 + Postgres create smoke-test per touched entity (AC-001, AC-002)
- [ ] `rg 'user\.role in \[' manifest/source -g '*.manifest'` → 0, semantics-identical (AC-003)
- [ ] Closed-set `status: string` → 0, each `validStatus` deleted (AC-004)
- [ ] Every portable middleware ported, wiring + test moved in same commit (AC-005)
- [ ] 10/10 crons as `schedule`, generated route proven (AC-006)
- [ ] `pnpm manifest:ci` green at every commit; no new governance-allowlist entries (AC-007)
- [ ] Every Phase-3/fork item is a written NEEDS-RYAN entry, not silent code (AC-008)

---

## Pre-existing issues noticed during WS0 (unrelated to the rewrite — tracked, not ignored)

- **`pnpm check` red on `@repo/manifest-runtime` only** (noticed 2026-07-11, proven pre-existing): `manifest/runtime/src/bottleneck-detector/ai-suggestions.ts` lines 97/163/165 fail **TS2589** ("Type instantiation excessively deep") on Vercel-AI-SDK `generateObject({ schema: <complex inline zod schema> })`, and line 203 fails **TS7006** (implicit-any `s`, cascading from the unresolved `result.object`). Verified unrelated to manifest work: the file imports only `@ai-sdk/openai`, `ai`, `zod`, `@repo/observability`, `@sentry/node`, `./types` — no manifest entity/generated/store-metadata types. Surfaces now because turbo ran cold ("0 cached"). 26/27 typecheck tasks pass (app/api/web all green), so it does NOT block manifest batches. Fix needs AI-flow verification (OpenAI key). **PROVEN 2026-07-11: extracting the inline schema to a named const is INSUFFICIENT** — it dropped the errors 4→3 but the remaining TS2589 still fires at the `generateObject` CALL SITE (132/198) itself; the depth source is the `OBJECT` generic inferred from the zod schema, not the inline definition. A real fix needs one of: a hand-written output type (`generateObject<Output>({ schema: schema as z.ZodType<Output> })` — drift-prone, ~20-field nested interface per schema), an `ai`/`zod` dependency bump, or per-call `@ts-expect-error`. Each needs AI-flow verification. Left untouched (file reverted) — separate focused commit.

- **`@repo/manifest-runtime` vitest suite: 24 tests red across 9 files (verified 2026-07-12 — pre-existing, NOT a regression from the P1 `_base` role rename).** Failure domains: parent-context field inheritance (catering-order / proposal / revenue-recognition / waste-shipment / facility-work-order — `venueName`/`locationId`/`clientId` inheritance), middleware conformance (container→dish-clear, facility-work-order→asset-status, prep-task→station-count), and datetime coercion (ISO→epoch-ms). **Proof they're pre-existing:** (1) the regenerated `kitchen.ir.json` diff vs HEAD is confined to role `name`/`parent` values + provenance metadata — ZERO entity/relationship/parent-context/middleware/command/property IR changed, so no non-role test could be affected; (2) the suite was already exit-1 on HEAD (baseline run with the P1 change stashed). The P1 rename's own tests pass (11/11 `role-allows-snake-case-resolution` + the fixed `timecard-edit-approved-time-entry-apply-middleware`). These 24 overlap the memory's "manifest-runtime red: 3 causes" (2026-07-11) but are broader — separate investigation(s), out of scope for the Phase-0/P1 iteration. Likely masked historically by turbo `^test` caching + the CI typecheck-red.
