# Uncertainty: [Short Name]

Uncertainty ID: `[area.name.question]`

Related canonical unit:

```text
canonical/[path]/README.md
```

Question:

```text
[Exact uncertainty.]
```

Why this matters:

```text
[What breaks, drifts, duplicates, or becomes harder if the agent guesses.]
```

Evidence found:

```text
[Only facts. Paths, docs, command output, generated files, CI results.]
```

Options:

```text
Option A:
- Meaning:
- Pros:
- Cons:
- App impact:

Option B:
- Meaning:
- Pros:
- Cons:
- App impact:
```

Safest temporary behavior:

```text
[Only if safe. Otherwise write: NO SAFE DEFAULT.]
```

Ryan final decision:

```text
NEEDS-RYAN
```
