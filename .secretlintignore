# Documentation containing example/integration connection strings.
docs/**
**/*.md

# CI workflow files contain shape-valid placeholder DB URLs (postgresql://dummy:dummy@...)
# so that build-time env validation doesn't reject them — they're not real secrets.
.github/workflows/**

# .npmrc registry config (no real credentials live here in this repo).
.npmrc
**/.npmrc

# Lockfiles + generated artifacts.
pnpm-lock.yaml
**/node_modules/**
**/.next/**
**/dist/**
**/generated/**
**/.turbo/**
**/.vercel/**
**/.cache/**
**/.worktrees/**

# Sample env files (templates, not real values).
**/*.env.example
**/*.env.sample
**/.env.example
**/.env.sample
**/.env.vercel
**/.env.vercel.production

# Snapshots and test fixtures occasionally include redacted shapes.
**/__snapshots__/**
**/*.snap
