# SlopScope Rule Implementation Plan

- [ ] skeleton_crud.success_response_with_not_implemented_body — API route returns success HTTP status but response body says feature is not implemented
  - Category: skeleton_crud
  - Severity: medium
  - Detector type: hybrid
  - Source evidence: app/api/command-board/templates/route.ts
  - Future implementation: Add regex detector that flags "not yet implemented" in route files where the companion pattern (manifestSuccessResponse/NextResponse.json) indicates a success response rather than an honest 501
- [ ] feature_claim_mismatch.duplicate_discovery_entry_in_log_file — Discovery log file contains duplicate entry for the same rule with identical timestamp
  - Category: feature_claim_mismatch
  - Severity: low
  - Detector type: hybrid
  - Source evidence: capsule-pro/IMPLEMENTATION_PLAN.md
  - Future implementation: Add regex detector that flags discovery log files where the same rule_id header appears twice, then generalize to parse all headers, group by rule_id, and flag groups with count > 1
- [ ] feature_claim_mismatch.user_color_assigned_by_math_random_not_deterministic — User collaboration color assigned via Math.random instead of deterministic hash
  - Category: feature_claim_mismatch
  - Severity: medium
  - Detector type: regex
  - Source evidence: apps/app/app/actions/users/get.ts
  - Future implementation: Add regex detector that flags `colors[Math.random()]` patterns in server actions/API routes, validated by companion user/avatar/collaborat/Liveblocks pattern to avoid flagging benign random color usage
- [ ] error_handling_theater.silent_catch_empty_body_on_side_effect_calls — Silent error swallowing via empty .catch(() => {}) on async side-effect calls in API routes
  - Category: error_handling_theater
  - Severity: medium
  - Detector type: regex
  - Source evidence: apps/api/app/api/staff/shifts/commands/create/route.ts
  - Future implementation: Replace all empty .catch(() => {}) with a fire-and-forget utility that at minimum logs errors and reports to Sentry
