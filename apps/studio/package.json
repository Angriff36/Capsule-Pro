{
  "name": "studio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "dotenv -e ../../packages/database/.env -- prisma studio --config ../../packages/database/prisma.config.ts --port 2226",
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "typecheck": "tsc --noEmit --emitDeclarationOnly false"
  },
  "devDependencies": {
    "dotenv-cli": "^11.0.0",
    "prisma": "7.3.0"
  }
}
