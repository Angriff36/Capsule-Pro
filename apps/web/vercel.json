{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "basehub && next build",
  "installCommand": "cd ../.. && corepack enable && corepack prepare pnpm@10.22.0 --activate && pnpm install",
  "framework": "nextjs"
}
