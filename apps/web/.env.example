# =============================================================================
# apps/web — Marketing / Landing Website
# =============================================================================
# Extends packages: cms, email, observability, feature-flags, security,
#                   rate-limit, next-config (see each package's .env.example)
# Set SKIP_ENV_VALIDATION=1 to bypass validation during CI/build.

# ------------------------------------------------------------------
# Web-specific (not owned by any shared package)
# ------------------------------------------------------------------
# REVALIDATION_SECRET="..." — secret token for revalidation webhooks
