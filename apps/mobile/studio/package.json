{
  "name": "studio",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "clean": "git clean -xdf .cache .turbo dist node_modules",
    "dev": "echo \"Prisma Studio removed — persistence is Convex. Use npx convex dev and the Convex dashboard.\" && exit 1",
    "typecheck": "exit 0"
  }
}
