# =============================================================================
# apps/mobile — React Native (Expo) Mobile Application
# =============================================================================
# Expo loads .env at build/bundle time. EXPO_PUBLIC_* vars are client-accessible.

EXPO_PUBLIC_CLERK_PUBLISHABLE_KEY="pk_test_..."
EXPO_PUBLIC_API_URL="http://localhost:2223"
# EXPO_PUBLIC_PROJECT_ID="..." — Expo push notifications
