// Navy variant of DarkFeatureBand. Lives in its own file so consumers can
// import either site (deep-green vs navy) by intent rather than knowing the
// tone variant. Both share a single source of truth in dark-feature-band.tsx.
export { NavyFeatureBand } from "./dark-feature-band";
