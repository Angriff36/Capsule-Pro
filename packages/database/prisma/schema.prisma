// ============================================================================
// schema.prisma — HYBRID (generated domain + preserved infra/core).
// DOMAIN MODELS below are GENERATED FROM MANIFEST IR — DO NOT EDIT BY HAND.
// Regenerate: node manifest/scripts/build-live-schema.mjs
// Domain source: manifest/source/*.manifest -> manifest/ir/kitchen.ir.json
// Infra/core partial: manifest/schema-partials/infra-core.prisma (maintained separately,
//   per /integration/prisma — infra tables live outside Manifest entities).
// ============================================================================

generator client {
  provider               = "prisma-client"
  output                 = "../generated"
  moduleFormat           = "esm"
  generatedFileExtension = "ts"
  importFileExtension    = "ts"
}

datasource db {
  provider     = "postgresql"
  relationMode = "prisma"
}

model AdminChatParticipant {
  id String
  tenantId String
  threadId String? @default("")
  userId String? @default("")
  archivedAt DateTime?
  clearedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model AdminTask {
  id String
  tenantId String
  title String? @default("")
  description String? @default("")
  status String? @default("backlog")
  priority String? @default("medium")
  category String? @default("")
  assignedTo String? @default("")
  dueDate String? @default("")
  createdBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model AiEventSetupSession {
  id String
  tenantId String
  userId String
  originalInput String? @default("")
  parsedTitle String? @default("")
  parsedEventType String? @default("")
  parsedEventDate DateTime?
  parsedGuestCount Int? @default(0)
  parsedVenueName String? @default("")
  parsedVenueAddress String? @default("")
  parsedNotes String? @default("")
  parsedTags String? @default("")
  status String? @default("pending")
  confidence Decimal? @db.Decimal(12, 2) @default(0)
  missingFields String? @default("[]")
  suggestions String? @default("[]")
  createdEventId String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  completedAt DateTime?

  @@id([tenantId, id])
}

model AlertsConfig {
  id String
  tenantId String
  channel String? @default("")
  destination String? @default("")

  @@id([tenantId, id])
}

model AllergenWarning {
  id String
  tenantId String
  eventId String? @default("")
  dishId String? @default("")
  warningType String? @default("")
  allergens String? @default("")
  affectedGuests String? @default("")
  severity String? @default("warning")
  isAcknowledged Boolean? @default(false)
  acknowledgedBy String? @default("")
  acknowledgedAt DateTime?
  overrideReason String? @default("")
  resolved Boolean? @default(false)
  resolvedAt DateTime?
  escalatedAt DateTime?
  escalatedTo String? @default("")
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ApiKey {
  id String
  tenantId String
  name String @default("")
  keyPrefix String @default("")
  hashedKey String @default("")
  scopes String[]
  lastUsedAt DateTime?
  expiresAt DateTime?
  revokedAt DateTime?
  createdByUserId String @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model BankAccount {
  id String
  tenantId String
  employeeId String
  accountHolderName String
  accountNumber String
  routingNumber String
  accountType String? @default("checking")
  bankName String? @default("")
  isDefault Boolean? @default(false)
  status String? @default("active")
  verifiedAt DateTime?
  verificationFailedAt DateTime?
  verificationFailureReason String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model BattleBoard {
  id String
  tenantId String
  eventId String? @default("")
  boardName String? @default("")
  boardType String? @default("event-specific")
  schemaVersion String? @default("mangia-battle-board@1")
  boardData String? @default("{}")
  documentUrl String? @default("")
  sourceDocumentType String? @default("")
  documentImportedAt DateTime?
  status String? @default("draft")
  isTemplate Boolean? @default(false)
  description String? @default("")
  notes String? @default("")
  tags String? @default("")
  createdAt DateTime?
  dishCount Int? @default(0)
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Budget {
  id String
  tenantId String
  name String
  description String? @default("")
  fiscalYear Int? @default(0)
  totalAmount Decimal? @db.Decimal(12, 2) @default(0)
  allocatedAmount Decimal? @db.Decimal(12, 2) @default(0)
  spentAmount Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("draft")
  category String? @default("general")
  departmentId String? @default("")
  approvedBy String? @default("")
  approvedAt DateTime?
  lockedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model BulkOrderRule {
  id String
  tenantId String
  catalogEntryId String
  ruleName String? @default("")
  minimumQuantity Int @default(1)
  ruleType String @default("discount")
  thresholdQuantity Int? @default(0)
  action String? @default("discount")
  discountPercent Decimal? @db.Decimal(12, 2) @default(0)
  freeItemQuantity Int? @default(0)
  shippingIncluded Boolean? @default(false)
  priority Int? @default(0)
  effectiveFrom DateTime?
  effectiveTo DateTime?
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CateringOrder {
  id String
  tenantId String
  orderNumber String
  customerId String? @default("")
  eventId String? @default("")
  orderStatus String? @default("draft")
  orderDate DateTime?
  deliveryDate DateTime?
  deliveryTime String? @default("")
  subtotalAmount Decimal? @db.Decimal(12, 2) @default(0)
  taxAmount Decimal? @db.Decimal(12, 2) @default(0)
  discountAmount Decimal? @db.Decimal(12, 2) @default(0)
  serviceChargeAmount Decimal? @db.Decimal(12, 2) @default(0)
  totalAmount Decimal? @db.Decimal(12, 2) @default(0)
  depositRequired Boolean? @default(false)
  depositAmount Decimal? @db.Decimal(12, 2) @default(0)
  depositPaid Boolean? @default(false)
  depositPaidAt DateTime?
  deliveredAt DateTime?
  prepStartedAt DateTime?
  venueName String? @default("")
  venueAddress String? @default("")
  venueCity String? @default("")
  venueState String? @default("")
  venueZip String? @default("")
  venueContactName String? @default("")
  venueContactPhone String? @default("")
  guestCount Int? @default(0)
  specialInstructions String? @default("")
  dietaryRestrictions String? @default("")
  staffRequired Int? @default(0)
  staffAssigned Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ChartOfAccount {
  id String
  tenantId String
  accountNumber String? @default("")
  accountName String? @default("")
  accountType String? @default("")
  parentId String? @default("")
  description String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model ClientInteraction {
  id String
  tenantId String
  clientId String? @default("")
  leadId String? @default("")
  employeeId String? @default("")
  interactionType String? @default("note")
  interactionDate DateTime?
  subject String? @default("")
  description String? @default("")
  followUpDate DateTime?
  followUpCompleted Boolean? @default(false)
  status String? @default("open")
  priority String? @default("normal")
  escalatedAt DateTime?
  escalatedTo String? @default("")
  correlationId String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Client {
  id String
  tenantId String
  clientType String @default("company")
  companyName String? @default("")
  firstName String? @default("")
  lastName String? @default("")
  email String? @default("")
  phone String? @default("")
  website String? @default("")
  addressLine1 String? @default("")
  addressLine2 String? @default("")
  city String? @default("")
  stateProvince String? @default("")
  postalCode String? @default("")
  countryCode String? @default("")
  defaultPaymentTerms Decimal? @db.Decimal(12, 2) @default(30)
  taxExempt Boolean @default(false)
  taxId String? @default("")
  notes String? @default("")
  tags String[]
  source String? @default("")
  assignedTo String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ClientContact {
  id String
  tenantId String
  clientId String
  firstName String? @default("")
  lastName String? @default("")
  title String? @default("")
  email String? @default("")
  phone String? @default("")
  phoneMobile String? @default("")
  isPrimary Boolean? @default(false)
  isBillingContact Boolean? @default(false)
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ClientPreference {
  id String
  tenantId String
  clientId String
  preferenceType String? @default("")
  preferenceKey String? @default("")
  preferenceValue String? @default("")
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CollectionCase {
  id String
  tenantId String
  invoiceId String @default("")
  invoiceNumber String @default("")
  eventId String @default("")
  clientId String @default("")
  clientName String @default("")
  originalAmount Decimal @db.Decimal(12, 2) @default(0)
  outstandingAmount Decimal @db.Decimal(12, 2) @default(0)
  collectedAmount Decimal @db.Decimal(12, 2) @default(0)
  status String @default("ACTIVE")
  priority String @default("MEDIUM")
  dunningStage String @default("CURRENT")
  daysOverdue Int @default(0)
  agingBucket String? @default("")
  assignedTo String? @default("")
  assignedAt DateTime?
  hasPaymentPlan Boolean? @default(false)
  paymentPlanId String? @default("")
  nextPaymentDue DateTime?
  isDisputed Boolean? @default(false)
  disputeReason String? @default("")
  disputeResolvedAt DateTime?
  isEscalatedToLegal Boolean? @default(false)
  legalCaseNumber String? @default("")
  legalFirm String? @default("")
  notes String? @default("")
  internalNotes String? @default("")
  lastActivityAt DateTime?
  metadata String? @default("{}")
  createdAt DateTime
  updatedAt DateTime
  resolvedAt DateTime?
  closedAt DateTime?

  @@id([tenantId, id])
}

model CollectionAction {
  id String
  tenantId String
  caseId String @default("")
  actionType String @default("")
  direction String @default("")
  status String @default("PENDING")
  contactedBy String? @default("")
  contactName String? @default("")
  contactMethod String? @default("")
  subject String? @default("")
  description String? @default("")
  outcome String? @default("")
  nextActionDate DateTime?
  promiseAmount Decimal? @db.Decimal(12, 2) @default(0)
  promiseDate DateTime?
  notes String? @default("")
  metadata String? @default("{}")
  scheduledFor DateTime
  completedAt DateTime?
  createdAt DateTime
  updatedAt DateTime

  @@id([tenantId, id])
}

model CollectionPaymentPlan {
  id String
  tenantId String
  caseId String @default("")
  totalAmount Decimal @db.Decimal(12, 2) @default(0)
  installmentAmount Decimal @db.Decimal(12, 2) @default(0)
  installmentCount Int @default(1)
  completedInstallments Int @default(0)
  status String @default("ACTIVE")
  startDate DateTime
  endDate DateTime
  frequency String? @default("")
  nextPaymentDate DateTime?
  notes String? @default("")
  metadata String? @default("{}")
  createdAt DateTime
  updatedAt DateTime
  completedAt DateTime?
  defaultedAt DateTime?

  @@id([tenantId, id])
}

model CommandBoard {
  id String
  tenantId String
  eventId String? @default("")
  name String? @default("")
  description String? @default("")
  status String? @default("draft")
  isTemplate Boolean? @default(false)
  tags String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CommandBoardCard {
  id String
  tenantId String
  boardId String
  title String? @default("")
  content String? @default("")
  cardType String? @default("task")
  status String? @default("pending")
  positionX Decimal? @db.Decimal(12, 2) @default(0)
  positionY Decimal? @db.Decimal(12, 2) @default(0)
  width Decimal? @db.Decimal(12, 2) @default(200)
  height Decimal? @db.Decimal(12, 2) @default(150)
  zIndex Int? @default(0)
  color String? @default("")
  metadata String? @default("{}")
  groupId String? @default("")
  entityId String? @default("")
  entityType String? @default("")
  version Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CommandBoardGroup {
  id String
  tenantId String
  boardId String
  name String? @default("")
  color String? @default("")
  collapsed Boolean? @default(false)
  positionX Decimal? @db.Decimal(12, 2) @default(0)
  positionY Decimal? @db.Decimal(12, 2) @default(0)
  width Decimal? @db.Decimal(12, 2) @default(300)
  height Decimal? @db.Decimal(12, 2) @default(200)
  zIndex Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CommandBoardConnection {
  id String
  tenantId String
  boardId String
  fromCardId String
  toCardId String
  relationshipType String? @default("generic")
  label String? @default("")
  visible Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CommandBoardLayout {
  id String
  tenantId String
  boardId String
  userId String
  name String? @default("")
  viewport String? @default("{}")
  visibleCards String? @default("")
  gridSize Int? @default(40)
  showGrid Boolean? @default(true)
  snapToGrid Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Container {
  id String
  tenantId String
  name String
  containerType String
  locationId String? @default("")
  sizeDescription String? @default("")
  capacityVolumeMl Decimal? @db.Decimal(12, 2) @default(0)
  capacityWeightG Decimal? @db.Decimal(12, 2) @default(0)
  capacityPortions Int? @default(0)
  isReusable Boolean? @default(true)
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ProposalTemplate {
  id String
  tenantId String
  name String? @default("")
  description String? @default("")
  headerHtml String? @default("")
  footerHtml String? @default("")
  defaultTerms String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model InteractionAttachment {
  id String
  tenantId String
  clientInteractionId String? @default("")
  fileName String? @default("")
  fileUrl String? @default("")
  fileType String? @default("")
  fileSizeBytes Int? @default(0)
  uploadedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CrmScoringRule {
  id String
  tenantId String
  name String? @default("")
  criterion String? @default("")
  points Int? @default(0)
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Report {
  id String
  tenantId String
  name String? @default("")
  reportType String? @default("")
  queryConfig String? @default("{}")
  displayConfig String? @default("{}")
  isShared Boolean? @default(false)
  createdBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Document {
  id String
  tenantId String
  name String? @default("")
  documentType String? @default("")
  fileUrl String? @default("")
  fileSizeBytes Int? @default(0)
  status String? @default("uploaded")
  entityType String? @default("")
  entityId String? @default("")
  uploadedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model AdminChatThread {
  id String
  tenantId String
  subject String? @default("")
  status String? @default("open")
  createdBy String? @default("")
  lastMessageAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model AdminChatMessage {
  id String
  tenantId String
  adminChatThreadId String? @default("")
  senderId String? @default("")
  body String? @default("")
  sentAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CycleCountSession {
  id String
  tenantId String
  locationId String
  sessionId String
  sessionName String
  countType String? @default("ad_hoc")
  scheduledDate DateTime?
  startedAt DateTime?
  completedAt DateTime?
  finalizedAt DateTime?
  status String? @default("draft")
  totalItems Int? @default(0)
  countedItems Int? @default(0)
  totalVariance Decimal? @db.Decimal(12, 2) @default(0)
  variancePercentage Decimal? @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  createdById String? @default("")
  approvedById String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model CycleCountRecord {
  id String
  tenantId String
  sessionId String
  itemId String
  itemNumber String
  itemName String
  storageLocationId String
  expectedQuantity Int? @default(0)
  countedQuantity Int? @default(0)
  variance Decimal? @db.Decimal(12, 2) @default(0)
  variancePct Decimal? @db.Decimal(12, 2) @default(0)
  countDate DateTime?
  countedById String? @default("")
  barcode String? @default("")
  notes String? @default("")
  isVerified Boolean? @default(false)
  verifiedById String? @default("")
  verifiedAt DateTime?
  syncStatus String? @default("synced")
  offlineId String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model VarianceReport {
  id String
  tenantId String
  sessionId String
  reportType String
  itemId String
  itemNumber String
  itemName String
  expectedQuantity Int? @default(0)
  countedQuantity Int? @default(0)
  variance Decimal? @db.Decimal(12, 2) @default(0)
  variancePct Decimal? @db.Decimal(12, 2) @default(0)
  accuracyScore Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("pending")
  adjustmentType String? @default("")
  adjustmentAmount Decimal? @db.Decimal(12, 2) @default(0)
  adjustmentDate DateTime?
  rejectedAt DateTime?
  rejectedBy String? @default("")
  rejectionReason String? @default("")
  notes String? @default("")
  generatedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Deal {
  id String
  tenantId String
  leadId String
  title String
  value Decimal? @db.Decimal(12, 2) @default(0)
  currency String? @default("USD")
  stage String? @default("new")
  status String? @default("open")
  probability Decimal? @db.Decimal(12, 2) @default(0)
  expectedCloseDate DateTime?
  actualCloseDate DateTime?
  assignedTo String? @default("")
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Dish {
  id String
  tenantId String
  recipeId String
  name String
  description String? @default("")
  category String? @default("")
  serviceStyle String? @default("")
  defaultContainerId String? @default("")
  presentationImageUrl String? @default("")
  minPrepLeadDays Int? @default(0)
  maxPrepLeadDays Int? @default(0)
  portionSizeDescription String? @default("")
  dietaryTags String? @default("")
  allergens String? @default("")
  pricePerPerson Decimal? @db.Decimal(12, 2) @default(0)
  costPerPerson Decimal? @db.Decimal(12, 2) @default(0)
  isActive Boolean? @default(true)
  isSeasonal Boolean? @default(false)
  seasonLabel String? @default("")
  seasonStartMonth Int? @default(0)
  seasonEndMonth Int? @default(0)
  isEightySix Boolean? @default(false)
  eightySixReason String? @default("")
  eightySixAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model DocumentVersion {
  id String
  tenantId String
  documentId String
  versionNumber Int
  content String
  changeDescription String? @default("")
  createdBy String? @default("")
  status String? @default("draft")
  approvedBy String? @default("")
  publishedBy String? @default("")
  supersededBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  approvedAt DateTime?
  publishedAt DateTime?
  supersededAt DateTime?

  @@id([tenantId, id])
}

model EmailTemplate {
  id String
  tenantId String
  name String? @default("")
  templateType String? @default("custom")
  subject String? @default("")
  body String? @default("")
  mergeFields String? @default("[]")
  isActive Boolean? @default(true)
  isDefault Boolean? @default(false)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EmailWorkflow {
  id String
  tenantId String
  name String? @default("")
  triggerType String? @default("custom")
  triggerConfig String? @default("{}")
  emailTemplateId String? @default("")
  recipientConfig String? @default("{}")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EmployeeAvailability {
  id String
  tenantId String
  employeeId String? @default("")
  dayOfWeek Int? @default(0)
  startTime String? @default("")
  endTime String? @default("")
  isAvailable Boolean? @default(true)
  effectiveFrom String? @default("")
  effectiveUntil String? @default("")
  isSuspended Boolean? @default(false)
  suspendReason String? @default("")
  suspendedAt DateTime?
  reinstatedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EmployeeCertification {
  id String
  tenantId String
  employeeId String? @default("")
  certificationType String? @default("")
  certificationName String? @default("")
  issuedDate String? @default("")
  expiryDate String? @default("")
  documentUrl String? @default("")
  status String? @default("active")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Equipment {
  id String
  tenantId String
  locationId String
  name String
  type String @default("general")
  status String @default("active")
  serialNumber String? @default("")
  manufacturer String? @default("")
  model String? @default("")
  purchaseDate DateTime?
  warrantyExpiry DateTime?
  lastMaintenanceDate DateTime?
  nextMaintenanceDate DateTime?
  maintenanceIntervalDays Int? @default(90)
  usageHours Int? @default(0)
  maxUsageHours Int? @default(1000)
  condition String? @default("good")
  notes String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model MaintenanceWorkOrder {
  id String
  tenantId String
  equipmentId String
  equipmentName String
  title String
  type String @default("repair")
  priority String @default("medium")
  status String @default("open")
  description String? @default("")
  assignedTo String? @default("")
  estimatedCost Decimal? @db.Decimal(12, 2) @default(0)
  actualCost Decimal? @db.Decimal(12, 2) @default(0)
  scheduledDate DateTime?
  completedDate DateTime?
  partsUsed String? @default("")
  vendorId String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model AutomatedFollowup {
  id String
  tenantId String
  eventId String
  type String? @default("email")
  status String? @default("pending")
  scheduledDate DateTime?
  sentDate DateTime?
  recipientId String? @default("")
  subject String? @default("")
  body String? @default("")
  templateId String? @default("")
  errorMessage String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model EventTimelineItem {
  id String
  tenantId String
  eventId String
  title String
  description String? @default("")
  itemType String? @default("task")
  status String? @default("pending")
  assignedTo String? @default("")
  dueDate DateTime?
  completedAt DateTime?
  sortOrder Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model EventWaitlistEntry {
  id String
  tenantId String
  eventId String
  guestName String
  email String? @default("")
  phone String? @default("")
  partySize Int? @default(1)
  status String? @default("waiting")
  notes String? @default("")
  joinedAt DateTime?
  notifiedAt DateTime?
  seatedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model EventBudget {
  id String
  tenantId String
  eventId String
  version Int? @default(1)
  status String? @default("draft")
  totalBudgetAmount Decimal? @db.Decimal(12, 2) @default(0)
  totalActualAmount Decimal? @db.Decimal(12, 2) @default(0)
  varianceAmount Decimal? @db.Decimal(12, 2) @default(0)
  variancePercentage Decimal? @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  linkedEvent Event @relation(fields: [tenantId, eventId], references: [tenantId, id])

  @@id([tenantId, id])
}

model BudgetLineItem {
  id String
  tenantId String
  budgetId String
  category String
  name String
  description String? @default("")
  budgetedAmount Decimal? @db.Decimal(12, 2) @default(0)
  actualAmount Decimal? @db.Decimal(12, 2) @default(0)
  varianceAmount Decimal? @db.Decimal(12, 2) @default(0)
  sortOrder Int? @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventContract {
  id String
  tenantId String
  eventId String
  clientId String
  contractNumber String? @default("")
  title String? @default("Untitled Contract")
  status String? @default("draft")
  documentUrl String? @default("")
  documentType String? @default("")
  notes String? @default("")
  expiresAt DateTime?
  sentAt DateTime?
  viewedAt DateTime?
  signedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ContractSignature {
  id String
  tenantId String
  contractId String
  signedAt DateTime?
  signatureData String? @default("")
  signerName String? @default("")
  signerEmail String? @default("")
  signerRole String? @default("")
  ipAddress String? @default("")
  verifiedAt DateTime?
  invalidatedAt DateTime?
  invalidationReason String? @default("")
  isValid Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventDish {
  id String
  tenantId String
  eventId String
  dishId String
  quantity Int? @default(1)
  notes String? @default("")
  courseLabel String? @default("")
  sortOrder Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventGuest {
  id String
  tenantId String
  eventId String? @default("")
  guestName String? @default("")
  guestEmail String? @default("")
  guestPhone String? @default("")
  isPrimaryContact Boolean? @default(false)
  dietaryRestrictions String? @default("")
  allergenRestrictions String? @default("")
  notes String? @default("")
  specialMealRequired Boolean? @default(false)
  specialMealNotes String? @default("")
  tableAssignment String? @default("")
  mealPreference String? @default("")
  rsvpStatus String? @default("pending")
  rsvpAt DateTime?
  checkedInAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventImportWorkflow {
  id String
  tenantId String
  eventId String? @default("")
  idempotencyKey String? @default("")
  status String? @default("created")
  currentStep Int? @default(0)
  totalSteps Int? @default(6)
  inputData String? @default("{}")
  outputData String? @default("{}")
  stepResults String? @default("{}")
  errors String? @default("[]")
  warnings String? @default("[]")
  confidence Decimal? @db.Decimal(12, 2) @default(0)
  startedAt DateTime?
  completedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model EventReport {
  id String
  tenantId String
  eventId String
  name String
  version String? @default("2025-01-01")
  status String? @default("draft")
  completion Decimal? @db.Decimal(12, 2) @default(0)
  checklistData String? @default("{}")
  parsedEventData String? @default("")
  reportConfig String? @default("")
  autoFillScore Decimal? @db.Decimal(12, 2) @default(0)
  reviewNotes String? @default("")
  reviewedBy String? @default("")
  reviewedAt DateTime?
  completedAt DateTime?
  rejectedAt DateTime?
  rejectedBy String? @default("")
  rejectionReason String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Event {
  id String
  tenantId String
  clientId String? @default("")
  eventNumber String? @default("")
  title String @default("Untitled Event")
  eventType String @default("general")
  eventDate DateTime
  guestCount Int @default(1)
  status String @default("confirmed")
  budget Decimal? @db.Decimal(12, 2) @default(0)
  ticketPrice Decimal? @db.Decimal(12, 2) @default(0)
  ticketTier String? @default("")
  eventFormat String? @default("")
  accessibilityOptions String[]
  featuredMediaUrl String? @default("")
  locationId String? @default("")
  venueId String? @default("")
  venueEntityId String? @default("")
  assignedTo String? @default("")
  venueName String? @default("")
  venueAddress String? @default("")
  notes String? @default("")
  tags String[]
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  invoices Invoice[]
  payments Payment[]
  budgets EventBudget[]

  @@id([tenantId, id])
}

model EventProfitability {
  id String
  tenantId String
  eventId String
  budgetedRevenue Decimal? @db.Decimal(12, 2) @default(0)
  budgetedFoodCost Decimal? @db.Decimal(12, 2) @default(0)
  budgetedLaborCost Decimal? @db.Decimal(12, 2) @default(0)
  budgetedOverhead Decimal? @db.Decimal(12, 2) @default(0)
  budgetedTotalCost Decimal? @db.Decimal(12, 2) @default(0)
  budgetedGrossMargin Decimal? @db.Decimal(12, 2) @default(0)
  budgetedGrossMarginPct Decimal? @db.Decimal(12, 2) @default(0)
  actualRevenue Decimal? @db.Decimal(12, 2) @default(0)
  actualFoodCost Decimal? @db.Decimal(12, 2) @default(0)
  actualLaborCost Decimal? @db.Decimal(12, 2) @default(0)
  actualOverhead Decimal? @db.Decimal(12, 2) @default(0)
  actualTotalCost Decimal? @db.Decimal(12, 2) @default(0)
  actualGrossMargin Decimal? @db.Decimal(12, 2) @default(0)
  actualGrossMarginPct Decimal? @db.Decimal(12, 2) @default(0)
  revenueVariance Decimal? @db.Decimal(12, 2) @default(0)
  foodCostVariance Decimal? @db.Decimal(12, 2) @default(0)
  laborCostVariance Decimal? @db.Decimal(12, 2) @default(0)
  totalCostVariance Decimal? @db.Decimal(12, 2) @default(0)
  marginVariancePct Decimal? @db.Decimal(12, 2) @default(0)
  calculatedAt DateTime?
  calculationMethod String? @default("auto")
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventSummary {
  id String
  tenantId String
  eventId String
  highlights String? @default("[]")
  issues String? @default("[]")
  financialPerformance String? @default("[]")
  clientFeedback String? @default("[]")
  insights String? @default("[]")
  overallSummary String? @default("")
  generatedAt DateTime?
  generationDurationMs Int? @default(0)
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventStaff {
  id String
  tenantId String
  eventId String
  staffMemberId String
  role String? @default("")
  notes String? @default("")
  shiftStart Int? @default(0)
  shiftEnd Int? @default(0)
  status String? @default("assigned")
  confirmedAt DateTime?
  checkedInAt DateTime?
  checkedOutAt DateTime?
  noShowReason String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Venue {
  id String
  tenantId String
  name String @default("")
  address String? @default("")
  capacity Int? @default(0)
  contactName String? @default("")
  contactPhone String? @default("")
  notes String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventStaffAssignment {
  id String
  tenantId String
  eventId String
  staffMemberId String
  role String? @default("")
  shiftStart DateTime?
  shiftEnd DateTime?
  status String? @default("assigned")
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventTimeline {
  id String
  tenantId String
  eventId String
  title String @default("")
  scheduledAt DateTime?
  durationMinutes Int? @default(0)
  notes String? @default("")
  sortOrder Int? @default(0)
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model TimelineTask {
  id String
  tenantId String
  eventId String
  title String @default("")
  dueAt DateTime?
  status String? @default("pending")
  assignedTo String? @default("")
  notes String? @default("")
  sortOrder Int? @default(0)
  completedAt DateTime?
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventImport {
  id String
  tenantId String
  source String @default("")
  fileName String @default("")
  status String? @default("pending")
  totalRows Int? @default(0)
  importedRows Int? @default(0)
  skippedRows Int? @default(0)
  errorMessage String? @default("")
  createdAt DateTime
  updatedAt DateTime
  completedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EventFollowup {
  id String
  tenantId String
  eventId String
  type String @default("")
  dueAt DateTime?
  status String? @default("pending")
  assignedTo String? @default("")
  notes String? @default("")
  completedAt DateTime?
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model BoardProjection {
  id String
  tenantId String
  boardId String
  entityType String @default("")
  entityId String @default("")
  positionX Decimal? @db.Decimal(12, 2) @default(0)
  positionY Decimal? @db.Decimal(12, 2) @default(0)
  width Decimal? @db.Decimal(12, 2) @default(200)
  height Decimal? @db.Decimal(12, 2) @default(150)
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model BoardAnnotation {
  id String
  tenantId String
  boardId String
  text String? @default("")
  positionX Decimal? @db.Decimal(12, 2) @default(0)
  positionY Decimal? @db.Decimal(12, 2) @default(0)
  color String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Note {
  id String
  tenantId String
  title String @default("")
  content String? @default("")
  color String? @default("")
  tags String[] @default([])
  entityType String? @default("")
  entityId String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Facility {
  id String
  tenantId String
  name String
  type String? @default("venue")
  address String? @default("")
  city String? @default("")
  state String? @default("")
  zip String? @default("")
  phone String? @default("")
  status String? @default("active")
  capacity Int? @default(0)
  description String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model FacilityArea {
  id String
  tenantId String
  venueId String? @default("")
  name String
  code String? @default("")
  areaType String? @default("other")
  floor String? @default("")
  description String? @default("")
  squareFeet Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("active")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model FacilityAsset {
  id String
  tenantId String
  facilityId String? @default("")
  areaId String? @default("")
  name String
  assetType String? @default("other")
  assetClass String? @default("fixed")
  status String? @default("operational")
  purchaseDate DateTime?
  purchasePrice Decimal? @db.Decimal(12, 2) @default(0)
  currentValue Decimal? @db.Decimal(12, 2) @default(0)
  depreciationRate Decimal? @db.Decimal(12, 2) @default(0)
  serialNumber String? @default("")
  lastMaintenanceAt DateTime?
  nextMaintenanceAt DateTime?
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model FacilitySchedule {
  id String
  tenantId String
  facilityId String? @default("")
  areaId String? @default("")
  title String
  description String? @default("")
  scheduleType String? @default("maintenance")
  status String? @default("scheduled")
  startDate DateTime?
  endDate DateTime?
  assignedTo String? @default("")
  completedAt DateTime?
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model FacilityWorkOrder {
  id String
  tenantId String
  facilityId String? @default("")
  areaId String? @default("")
  assetId String? @default("")
  title String
  description String? @default("")
  priority String? @default("medium")
  status String? @default("open")
  category String? @default("repair")
  requestedBy String? @default("")
  assignedTo String? @default("")
  estimatedCost Decimal? @db.Decimal(12, 2) @default(0)
  actualCost Decimal? @db.Decimal(12, 2) @default(0)
  scheduledDate DateTime?
  completedDate DateTime?
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Ingredient {
  id String
  tenantId String
  name String
  category String? @default("")
  defaultUnitId Int? @default(0)
  densityGPerMl Decimal? @db.Decimal(12, 2) @default(0)
  shelfLifeDays Int? @default(0)
  storageInstructions String? @default("")
  allergens String? @default("")
  isActive Boolean? @default(true)
  currentLotNumber String? @default("")
  currentLotReceivedAt DateTime?
  currentLotExpiresAt DateTime?
  isRecalled Boolean? @default(false)
  recallReason String? @default("")
  recalledAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model StorageLocation {
  id String
  tenantId String
  locationId String
  name String
  storageType String
  temperatureZone String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model InventoryStock {
  id String
  tenantId String
  inventoryItemId String
  storageLocationId String
  quantityOnHand Decimal @db.Decimal(12, 2) @default(0)
  unitId String
  lastCountedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model InventoryAlert {
  id String
  tenantId String
  inventoryItemId String
  alertType String
  severity String
  message String
  status String @default("active")
  triggeredAt DateTime?
  acknowledgedAt DateTime?
  resolvedAt DateTime?
  acknowledgedBy String? @default("")
  resolvedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model InventoryForecast {
  id String
  tenantId String
  inventoryItemId String
  forecastDate DateTime
  projectedQuantity Decimal @db.Decimal(12, 2) @default(0)
  confidence Decimal @db.Decimal(12, 2) @default(0)
  status String? @default("draft")
  publishedAt DateTime?
  approvedAt DateTime?
  approvedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model ForecastInput {
  id String
  tenantId String
  inventoryItemId String
  inputDate DateTime
  actualUsage Decimal @db.Decimal(12, 2) @default(0)
  source String
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model ReorderSuggestion {
  id String
  tenantId String
  inventoryItemId String
  suggestedQuantity Decimal @db.Decimal(12, 2) @default(0)
  reason String
  status String @default("pending")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model VendorContact {
  id String
  tenantId String
  inventorySupplierId String
  name String
  email String? @default("")
  phone String? @default("")
  role String? @default("")
  isPrimary Boolean? @default(false)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model VendorRating {
  id String
  tenantId String
  inventorySupplierId String
  category String
  score Decimal @db.Decimal(12, 2) @default(0)
  comment String? @default("")
  ratedAt DateTime?
  ratedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model InventoryTransferItem {
  id String
  tenantId String
  inventoryTransferId String
  inventoryItemId String
  quantity Decimal @db.Decimal(12, 2) @default(0)
  unitId String
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model ProcurementBudget {
  id String
  tenantId String
  name String
  periodStart DateTime
  periodEnd DateTime
  budgetAmount Decimal @db.Decimal(12, 2) @default(0)
  spentAmount Decimal? @db.Decimal(12, 2) @default(0)
  status String @default("active")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model ProcurementBudgetAlert {
  id String
  tenantId String
  procurementBudgetId String
  thresholdPct Decimal @db.Decimal(12, 2) @default(0)
  message String
  triggeredAt DateTime?
  acknowledgedAt DateTime?
  acknowledgedBy String? @default("")
  isAcknowledged Boolean? @default(false)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model AuditSchedule {
  id String
  tenantId String
  name String
  frequency String
  nextRunAt DateTime?
  lastRunAt DateTime?
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model InventoryItem {
  id String
  tenantId String
  item_number String
  name String
  description String? @default("")
  category String
  unitOfMeasure String? @default("each")
  unitCost Decimal? @db.Decimal(12, 2) @default(0)
  quantityOnHand Int? @default(0)
  quantityReserved Int? @default(0)
  parLevel Decimal? @db.Decimal(12, 2) @default(0)
  reorder_level Decimal? @db.Decimal(12, 2) @default(0)
  supplierId String? @default("")
  tags String? @default("")
  fsa_status String? @default("unknown")
  fsa_temp_logged Boolean? @default(false)
  fsa_allergen_info Boolean? @default(false)
  fsa_traceable Boolean? @default(false)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model InventorySupplier {
  id String
  tenantId String
  name String
  supplierNumber String? @default("")
  contactPerson String? @default("")
  email String? @default("")
  phone String? @default("")
  paymentTerms String? @default("NET_30")
  notes String? @default("")
  tags String? @default("")
  isActive Boolean? @default(true)
  openPOCount Int? @default(0)
  approvedAt DateTime?
  approvedBy String? @default("")
  suspendedAt DateTime?
  suspendedReason String? @default("")
  blacklistedAt DateTime?
  blacklistedReason String? @default("")
  qualificationStatus String? @default("pending")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model InventoryTransaction {
  id String
  tenantId String
  itemId String
  transactionType String
  quantity Int? @default(0)
  unitCost Decimal? @db.Decimal(12, 2) @default(0)
  totalCost Decimal? @db.Decimal(12, 2) @default(0)
  reference String? @default("")
  referenceType String? @default("")
  referenceId String? @default("")
  notes String? @default("")
  reason String? @default("")
  employeeId String? @default("")
  storageLocationId String? @default("")
  transactionDate DateTime?
  reversedAt DateTime?
  reversedBy String? @default("")
  reverseOfTransactionId String? @default("")
  isReversed Boolean? @default(false)
  createdAt DateTime?

  @@id([tenantId, id])
}

model InventoryTransfer {
  id String
  tenantId String
  fromLocationId String? @default("")
  toLocationId String? @default("")
  status String? @default("draft")
  requestedBy String? @default("")
  approvedBy String? @default("")
  shippedBy String? @default("")
  receivedBy String? @default("")
  shipDate DateTime?
  receiveDate DateTime?
  discrepancyNotes String? @default("")
  hasDiscrepancy Boolean? @default(false)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  items String? @default("[]")

  @@id([tenantId, id])
}

model Invoice {
  id String
  tenantId String
  invoiceNumber String @default("")
  type String @default("FINAL_PAYMENT")
  status String @default("DRAFT")
  clientId String @default("")
  eventId String @default("")
  subtotal Decimal @db.Decimal(12, 2) @default(0)
  taxAmount Decimal @db.Decimal(12, 2) @default(0)
  discountAmount Decimal @db.Decimal(12, 2) @default(0)
  total Decimal @db.Decimal(12, 2) @default(0)
  amountPaid Decimal @db.Decimal(12, 2) @default(0)
  amountDue Decimal @db.Decimal(12, 2) @default(0)
  paymentTerms Int @default(30)
  dueDate DateTime
  paidAt DateTime?
  depositPercentage Decimal? @db.Decimal(12, 2) @default(0)
  depositRequired Decimal? @db.Decimal(12, 2) @default(0)
  depositPaid Decimal? @db.Decimal(12, 2) @default(0)
  issuedAt DateTime
  sentAt DateTime?
  viewedAt DateTime?
  overdueSince DateTime?
  reminderCount Int? @default(0)
  lastReminderAt DateTime?
  quickBooksId String? @default("")
  goodshuffleId String? @default("")
  externalSyncStatus String? @default("")
  notes String? @default("")
  internalNotes String? @default("")
  lineItems String? @default("[]")
  metadata String @default("{}")

  linkedEvent Event @relation(fields: [tenantId, eventId], references: [tenantId, id])
  payments Payment[]

  @@id([tenantId, id])
}

model TemperatureProbe {
  id String
  tenantId String
  name String
  equipmentId String
  location String
  minThreshold Decimal @db.Decimal(12, 2)
  maxThreshold Decimal @db.Decimal(12, 2)
  isActive Boolean? @default(true)
  lastCalibratedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TemperatureLog {
  id String
  tenantId String
  equipmentId String
  recordedBy String
  temperature Decimal @db.Decimal(12, 2)
  recordedAt DateTime
  withinRange Boolean
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TemperatureReading {
  id String
  tenantId String
  probeId String
  temperature Decimal @db.Decimal(12, 2)
  recordedAt DateTime
  withinRange Boolean
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model IotAlertRule {
  id String
  tenantId String
  equipmentId String
  metric String
  threshold Decimal @db.Decimal(12, 2)
  comparison String
  severity String
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model IoTAlert {
  id String
  tenantId String
  probeId String
  ruleId String
  severity String
  message String
  value Decimal @db.Decimal(12, 2)
  status String @default("active")
  triggeredAt DateTime
  acknowledgedBy String? @default("")
  resolvedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model CorrectiveAction {
  id String
  tenantId String
  sourceType String
  sourceId String
  description String
  assignedTo String? @default("")
  status String @default("open")
  dueAt DateTime?
  resolvedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model QualityCheck {
  id String
  tenantId String
  checkType String
  performedBy String
  status String @default("pending")
  performedAt DateTime?
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model QualityCheckItem {
  id String
  tenantId String
  qualityCheckId String
  criterion String
  passed Boolean? @default(false)
  notes String? @default("")
  sortOrder Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model KitchenTaskClaim {
  id String
  tenantId String
  kitchenTaskId String
  employeeId String
  claimedAt DateTime
  status String @default("active")
  releasedAt DateTime?
  releaseReason String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model KitchenTaskProgress {
  id String
  tenantId String
  kitchenTaskId String
  employeeId String
  status String
  progressPct Decimal @db.Decimal(12, 2)
  note String? @default("")
  recordedAt DateTime
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TaskBundle {
  id String
  tenantId String
  name String
  eventId String? @default("")
  description String? @default("")
  status String @default("active")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TaskBundleItem {
  id String
  tenantId String
  taskBundleId String
  kitchenTaskId String
  sortOrder Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model BulkCombineRule {
  id String
  tenantId String
  name String
  matchCriteria String
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model MethodVideo {
  id String
  tenantId String
  prepMethodId String
  title String
  url String
  durationSeconds Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PrepListImport {
  id String
  tenantId String
  source String
  fileName String
  status String @default("pending")
  importedAt DateTime?
  errorMessage String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model KitchenTask {
  id String
  tenantId String
  title String
  summary String
  status String @default("pending")
  priority Int? @default(5)
  complexity Decimal? @db.Decimal(12, 2) @default(5)
  tags String? @default("")
  assignedTo String? @default("")
  dueDate DateTime?
  completedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model KnowledgeBaseEntry {
  id String
  tenantId String
  title String
  category String? @default("general")
  content String? @default("")
  tags String? @default("")
  status String? @default("draft")
  authorId String? @default("")
  publishedAt DateTime?
  viewCount Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model LaborBudget {
  id String
  tenantId String
  locationId String? @default("")
  periodStart String? @default("")
  periodEnd String? @default("")
  budgetAmount Decimal? @db.Decimal(12, 2) @default(0)
  actualAmount Decimal? @db.Decimal(12, 2) @default(0)
  budgetType String? @default("weekly")
  status String? @default("draft")
  notes String? @default("")
  createdBy String? @default("")
  approvedBy String? @default("")
  approvedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model BudgetAlert {
  id String
  tenantId String
  budgetId String? @default("")
  alertType String? @default("")
  thresholdPct Decimal? @db.Decimal(12, 2) @default(0)
  actualPct Decimal? @db.Decimal(12, 2) @default(0)
  message String? @default("")
  isAcknowledged Boolean? @default(false)
  acknowledgedBy String? @default("")
  acknowledgedAt DateTime?
  resolved Boolean? @default(false)
  resolvedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Lead {
  id String
  tenantId String
  source String? @default("")
  companyName String? @default("")
  contactName String? @default("")
  contactEmail String? @default("")
  contactPhone String? @default("")
  eventType String? @default("")
  eventDate DateTime?
  estimatedGuests Int? @default(0)
  estimatedValue Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("new")
  assignedTo String? @default("")
  notes String? @default("")
  convertedToClientId String? @default("")
  convertedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Driver {
  id String
  tenantId String
  firstName String
  lastName String
  email String? @default("")
  phone String? @default("")
  licenseNumber String? @default("")
  licenseExpiry DateTime?
  status String? @default("active")
  currentVehicleId String? @default("")
  rating Decimal? @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Vehicle {
  id String
  tenantId String
  name String
  type String? @default("delivery")
  plateNumber String? @default("")
  vin String? @default("")
  make String? @default("")
  model String? @default("")
  year Int? @default(0)
  capacity String? @default("")
  status String? @default("available")
  currentDriverId String? @default("")
  lastMaintenanceDate DateTime?
  nextMaintenanceDate DateTime?
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model LogisticsRoute {
  id String
  tenantId String
  name String
  status String? @default("planned")
  vehicleId String? @default("")
  driverId String? @default("")
  startLocation String? @default("")
  endLocation String? @default("")
  distance Decimal? @db.Decimal(12, 2) @default(0)
  estimatedDuration Decimal? @db.Decimal(12, 2) @default(0)
  scheduledStart DateTime?
  scheduledEnd DateTime?
  actualStart DateTime?
  actualEnd DateTime?
  stops Decimal? @db.Decimal(12, 2) @default(0)
  completedStops Int? @default(0)
  delayMinutes Int? @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model LogisticsDispatch {
  id String
  tenantId String
  routeId String
  driverId String? @default("")
  vehicleId String? @default("")
  status String? @default("pending")
  priority String? @default("normal")
  estimatedDeliveryTime DateTime?
  actualDeliveryTime DateTime?
  departedAt DateTime?
  failureReason String? @default("")
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Menu {
  id String
  tenantId String
  name String
  description String? @default("")
  category String? @default("")
  isActive Boolean? @default(true)
  basePrice Decimal? @db.Decimal(12, 2) @default(0)
  pricePerPerson Decimal? @db.Decimal(12, 2) @default(0)
  minGuests Int? @default(0)
  maxGuests Int? @default(0)
  isSeasonal Boolean? @default(false)
  seasonLabel String? @default("")
  seasonYear Int? @default(0)
  status String? @default("draft")
  publishedAt DateTime?
  archivedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model MenuDish {
  id String
  tenantId String
  menuId String
  dishId String
  course String? @default("")
  sortOrder Int? @default(0)
  isOptional Boolean? @default(false)
  priceOverride Decimal? @db.Decimal(12, 2) @default(0)
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Notification {
  id String
  tenantId String
  recipientEmployeeId String
  notificationType String? @default("")
  title String? @default("")
  body String? @default("")
  actionUrl String? @default("")
  isRead Boolean? @default(false)
  readAt DateTime?
  correlationId String? @default("")
  createdAt DateTime?

  @@id([tenantId, id])
}

model OverrideAudit {
  id String
  tenantId String
  entityType String? @default("")
  entityId String? @default("")
  constraintId String? @default("")
  guardExpression String? @default("")
  overriddenBy String? @default("")
  overrideReason String? @default("")
  authorizedBy String? @default("")
  authorizedAt DateTime?
  createdAt DateTime?

  @@id([tenantId, id])
}

model PaymentMethod {
  id String
  tenantId String
  clientId String @default("")
  externalMethodId String? @default("")
  type String @default("CREDIT_CARD")
  cardLastFour String? @default("")
  cardNetwork String? @default("")
  cardExpiryMonth Int? @default(0)
  cardExpiryYear Int? @default(0)
  cardHolderName String? @default("")
  bankAccountLastFour String? @default("")
  bankAccountType String? @default("")
  bankRoutingNumber String? @default("")
  walletProvider String? @default("")
  walletEmail String? @default("")
  status String @default("ACTIVE")
  isDefault Boolean @default(false)
  fraudFlagged Boolean? @default(false)
  verifiedAt DateTime?
  verificationMethod String? @default("")
  nickname String? @default("")
  metadata String? @default("{}")
  createdAt DateTime
  updatedAt DateTime?
  expiresAt DateTime?

  @@id([tenantId, id])
}

model Payment {
  id String
  tenantId String
  amount Decimal @db.Decimal(12, 2) @default(0)
  currency String @default("USD")
  status String @default("PENDING")
  methodType String @default("CREDIT_CARD")
  invoiceId String @default("")
  eventId String @default("")
  clientId String @default("")
  gatewayTransactionId String? @default("")
  gatewayPaymentMethodId String? @default("")
  processor String? @default("")
  processorResponseCode String? @default("")
  processorResponseMessage String? @default("")
  processedAt DateTime?
  completedAt DateTime?
  refundedAt DateTime?
  chargebackAt DateTime?
  fraudStatus String? @default("NOT_CHECKED")
  fraudScore Float? @default(0)
  reviewedAt DateTime?
  reviewedBy String? @default("")
  description String? @default("")
  externalReference String? @default("")
  metadata String? @default("{}")

  invoice Invoice @relation(fields: [tenantId, invoiceId], references: [tenantId, id])
  linkedEvent Event @relation(fields: [tenantId, eventId], references: [tenantId, id])

  @@id([tenantId, id])
}

model PayrollPeriod {
  id String
  tenantId String
  periodStart String? @default("")
  periodEnd String? @default("")
  status String? @default("open")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model EmployeeDeduction {
  id String
  tenantId String
  employeeId String? @default("")
  type String? @default("")
  name String? @default("")
  amount Decimal? @db.Decimal(12, 2) @default(0)
  percentage Decimal? @db.Decimal(12, 2) @default(0)
  isPreTax Boolean? @default(false)
  effectiveDate String? @default("")
  endDate String? @default("")
  maxAnnualAmount Decimal? @db.Decimal(12, 2) @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model PayrollApprovalHistory {
  id String
  tenantId String
  payrollRunId String? @default("")
  action String? @default("")
  previousStatus String? @default("")
  newStatus String? @default("")
  performedBy String? @default("")
  performedAt DateTime?
  reason String? @default("")
  createdAt DateTime?

  @@id([tenantId, id])
}

model PayrollRun {
  id String
  tenantId String
  payrollPeriodId String? @default("")
  runDate String? @default("")
  status String? @default("pending")
  totalGross Decimal? @db.Decimal(12, 2) @default(0)
  totalDeductions Decimal? @db.Decimal(12, 2) @default(0)
  totalNet Decimal? @db.Decimal(12, 2) @default(0)
  approvedBy String? @default("")
  approvedAt DateTime?
  paidAt DateTime?
  rejectReason String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model PrepComment {
  id String
  tenantId String
  taskId String
  employeeId String
  commentText String
  isResolved Boolean? @default(false)
  resolvedAt DateTime?
  resolvedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model PrepList {
  id String
  tenantId String
  eventId String
  name String
  batchMultiplier Decimal? @db.Decimal(12, 2) @default(1)
  dietaryRestrictions String? @default("")
  status String? @default("draft")
  totalItems Int? @default(0)
  totalEstimatedTime Decimal? @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  generatedAt DateTime?
  finalizedAt DateTime?
  isActive Boolean? @default(true)

  @@id([tenantId, id])
}

model PrepListItem {
  id String
  tenantId String
  prepListId String
  stationId String
  stationName String
  ingredientId String
  ingredientName String
  category String? @default("")
  baseQuantity Int? @default(0)
  baseUnit String? @default("")
  scaledQuantity Int? @default(0)
  scaledUnit String? @default("")
  isOptional Boolean? @default(false)
  preparationNotes String? @default("")
  allergens String? @default("")
  dietarySubstitutions String? @default("")
  dishId String? @default("")
  dishName String? @default("")
  recipeVersionId String? @default("")
  sortOrder Int? @default(0)
  isCompleted Boolean? @default(false)
  completedAt DateTime?
  completedBy String? @default("")

  @@id([tenantId, id])
}

model PrepMethod {
  id String
  tenantId String
  name String
  category String? @default("")
  description String? @default("")
  estimatedDurationMinutes Decimal? @db.Decimal(12, 2) @default(0)
  requiresCertification String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model PrepTaskPlanWorkflow {
  id String
  tenantId String
  eventId String? @default("")
  idempotencyKey String? @default("")
  status String? @default("created")
  currentStep Int? @default(0)
  totalSteps Int? @default(5)
  generationOptions String? @default("{}")
  generatedTasks String? @default("[]")
  reviewedTasks String? @default("[]")
  approvedTaskIds String? @default("[]")
  rejectedTaskIds String? @default("[]")
  instantiatedTaskIds String? @default("[]")
  scheduledWindows String? @default("{}")
  constraintOutcomes String? @default("[]")
  errors String? @default("[]")
  warnings String? @default("[]")
  generatedCount Decimal? @db.Decimal(12, 2) @default(0)
  approvedCount Int? @default(0)
  instantiatedCount Int? @default(0)
  reviewedBy String? @default("")
  reviewedAt DateTime?
  approvedBy String? @default("")
  approvedAt DateTime?
  startedAt DateTime?
  completedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PrepTask {
  id String
  tenantId String
  eventId String
  prepListId String? @default("")
  name String
  taskType String @default("prep")
  status String @default("open")
  stationId String? @default("")
  claimedBy String? @default("")
  claimedAt DateTime?
  quantityTotal Int? @default(0)
  quantityCompleted Int? @default(0)
  quantityUnitId String? @default("")
  servingsTotal Int? @default(0)
  startByDate DateTime?
  dueByDate DateTime?
  priority Int? @default(5)
  notes String? @default("")
  ingredients String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PricingTier {
  id String
  tenantId String
  catalogEntryId String
  tierName String? @default("")
  minQuantity Int @default(1)
  maxQuantity Int? @default(0)
  unitCost Decimal @db.Decimal(12, 2) @default(0)
  discountPercent Decimal? @db.Decimal(12, 2) @default(0)
  effectiveFrom DateTime?
  effectiveTo DateTime?
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model PurchaseRequisition {
  id String
  tenantId String
  requisitionNumber String
  requestedBy String @default("")
  requestDate DateTime
  requiredBy DateTime?
  locationId String? @default("")
  department String? @default("")
  justification String? @default("")
  status String @default("draft")
  subtotal Decimal @db.Decimal(12, 2) @default(0)
  estimatedTax Decimal @db.Decimal(12, 2) @default(0)
  estimatedShipping Decimal @db.Decimal(12, 2) @default(0)
  estimatedTotal Decimal @db.Decimal(12, 2) @default(0)
  approvedBy String? @default("")
  approvedAt DateTime?
  managerApprovalBy String? @default("")
  managerApprovalAt DateTime?
  financeApprovalBy String? @default("")
  financeApprovalAt DateTime?
  convertedToPoId String? @default("")
  convertedAt DateTime?
  rejectionReason String? @default("")
  notes String? @default("")
  submittedAt DateTime?
  itemCategory String? @default("")
  priority String @default("normal")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?
  itemCount Int? @default(0)

  items PurchaseRequisitionItem[]

  @@id([tenantId, id])
}

model PurchaseRequisitionItem {
  id String
  tenantId String
  requisitionId String
  itemId String
  itemName String? @default("")
  quantityRequested Decimal @db.Decimal(12, 2) @default(0)
  unitId Int? @default(0)
  estimatedUnitCost Decimal @db.Decimal(12, 2) @default(0)
  estimatedTotalCost Decimal @db.Decimal(12, 2) @default(0)
  suggestedVendorId String? @default("")
  suggestedVendorName String? @default("")
  specifications String? @default("")
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  requisition PurchaseRequisition @relation(fields: [tenantId, requisitionId], references: [tenantId, id], onDelete: Cascade)

  @@id([tenantId, id])
}

model Proposal {
  id String
  tenantId String
  proposalNumber String? @default("")
  clientId String? @default("")
  leadId String? @default("")
  eventId String? @default("")
  title String? @default("")
  eventDate DateTime?
  eventType String? @default("")
  guestCount Int? @default(0)
  venueName String? @default("")
  venueAddress String? @default("")
  subtotal Decimal? @db.Decimal(12, 2) @default(0)
  taxRate Decimal? @db.Decimal(12, 2) @default(0)
  taxAmount Decimal? @db.Decimal(12, 2) @default(0)
  discountAmount Decimal? @db.Decimal(12, 2) @default(0)
  total Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("draft")
  validUntil DateTime?
  sentAt DateTime?
  viewedAt DateTime?
  acceptedAt DateTime?
  rejectedAt DateTime?
  notes String? @default("")
  termsAndConditions String? @default("")
  lineItemCount Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ProposalLineItem {
  id String
  tenantId String
  proposalId String
  itemType String? @default("")
  category String? @default("")
  description String? @default("")
  quantity Int? @default(0)
  unitOfMeasure String? @default("")
  unitPrice Decimal? @db.Decimal(12, 2) @default(0)
  total Decimal? @db.Decimal(12, 2) @default(0)
  totalPrice Decimal? @db.Decimal(12, 2) @default(0)
  sortOrder Int? @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model PurchaseOrder {
  id String
  tenantId String
  poNumber String
  vendorId String
  locationId String
  orderDate DateTime
  expectedDeliveryDate DateTime?
  actualDeliveryDate DateTime?
  status String @default("draft")
  subtotal Decimal @db.Decimal(12, 2) @default(0)
  taxAmount Decimal @db.Decimal(12, 2) @default(0)
  shippingAmount Decimal @db.Decimal(12, 2) @default(0)
  total Decimal @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  submittedBy String? @default("")
  submittedAt DateTime?
  receivedBy String? @default("")
  receivedAt DateTime?
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?
  itemCount Int? @default(0)

  items PurchaseOrderItem[]

  @@id([tenantId, id])
}

model PurchaseOrderItem {
  id String
  tenantId String
  purchaseOrderId String
  itemId String
  quantityOrdered Decimal @db.Decimal(12, 2) @default(0)
  quantityReceived Decimal @db.Decimal(12, 2) @default(0)
  unitId Int @default(0)
  unitCost Decimal @db.Decimal(12, 2) @default(0)
  totalCost Decimal @db.Decimal(12, 2) @default(0)
  qualityStatus String? @default("pending")
  discrepancyType String? @default("")
  discrepancyAmount Decimal? @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  purchaseOrder PurchaseOrder @relation(fields: [tenantId, purchaseOrderId], references: [tenantId, id], onDelete: Cascade)

  @@id([tenantId, id])
}

model QACheck {
  id String
  tenantId String
  location String? @default("")
  checkType String
  result String? @default("pass")
  status String? @default("pending")
  inspector String? @default("")
  notes String? @default("")
  completedAt DateTime?
  reinspectedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model QACorrectiveAction {
  id String
  tenantId String
  checkId String
  description String
  assignedTo String? @default("")
  priority String? @default("medium")
  status String? @default("open")
  resolutionNotes String? @default("")
  escalatedTo String? @default("")
  escalationReason String? @default("")
  dueDate DateTime?
  resolvedAt DateTime?
  escalatedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model QATemperatureLog {
  id String
  tenantId String
  location String
  temperature Decimal @db.Decimal(12, 2)
  unit String? @default("fahrenheit")
  equipment String? @default("")
  recordedBy String? @default("")
  notes String? @default("")
  loggedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model RateLimitConfig {
  id String
  tenantId String
  name String @default("")
  endpointPattern String @default("")
  windowMs Int @default(60000)
  maxRequests Int @default(100)
  burstAllowance Int? @default(0)
  priority Int @default(0)
  isActive Boolean @default(true)
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Recipe {
  id String
  tenantId String
  name String
  category String? @default("")
  cuisineType String? @default("")
  description String? @default("")
  tags String[]
  isActive Boolean @default(true)

  @@id([tenantId, id])
}

model RecipeVersion {
  id String
  recipeId String
  tenantId String
  name String
  versionNumber Int
  category String? @default("")
  cuisineType String? @default("")
  description String? @default("")
  tags String @default("")
  yieldQuantity Int? @default(1)
  yieldUnitId Int? @default(1)
  yieldDescription String? @default("")
  prepTimeMinutes Int? @default(0)
  cookTimeMinutes Int? @default(0)
  restTimeMinutes Int? @default(0)
  difficultyLevel Int? @default(1)
  instructions String? @default("")
  notes String? @default("")
  ingredientCount Int? @default(0)
  stepCount Int? @default(0)
  totalCost Decimal? @db.Decimal(12, 2) @default(0)
  costPerYield Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("draft")
  publishedAt DateTime?
  approvedAt DateTime?
  approvedBy String? @default("")
  createdAt DateTime?

  @@id([tenantId, id])
}

model RecipeIngredient {
  id String
  tenantId String
  recipeVersionId String
  ingredientId String
  quantity Int
  unitId Int
  sortOrder Int
  preparationNotes String? @default("")
  isOptional Boolean? @default(false)
  wasteFactor Decimal? @db.Decimal(12, 2) @default(1)
  deletedAt DateTime?

  @@id([tenantId, id])
}

model RecipeStep {
  id String
  tenantId String
  recipeVersionId String
  stepNumber Int
  instruction String
  durationMinutes Decimal? @db.Decimal(12, 2) @default(0)
  temperatureValue Decimal? @db.Decimal(12, 2) @default(0)
  temperatureUnit String? @default("")
  equipmentNeeded String? @default("")
  tips String? @default("")
  videoUrl String? @default("")
  imageUrl String? @default("")
  deletedAt DateTime?

  @@id([tenantId, id])
}

model RevenueRecognitionSchedule {
  id String
  tenantId String
  invoiceId String
  eventId String
  contractId String
  clientId String
  totalAmount Decimal @db.Decimal(12, 2) @default(0)
  recognizedAmount Decimal @db.Decimal(12, 2) @default(0)
  remainingAmount Decimal @db.Decimal(12, 2) @default(0)
  method String @default("IMMEDIATE")
  status String @default("PENDING")
  startDate DateTime
  endDate DateTime
  recognitionPeriod Int? @default(0)
  serviceStartDate DateTime?
  serviceEndDate DateTime?
  totalMilestones Int? @default(0)
  completedMilestones Int? @default(0)
  description String? @default("")
  notes String? @default("")
  metadata String? @default("")
  createdAt DateTime
  updatedAt DateTime
  completedAt DateTime?

  @@id([tenantId, id])
}

model RevenueRecognitionLine {
  id String
  tenantId String
  scheduleId String
  sequence Int @default(0)
  amount Decimal @db.Decimal(12, 2) @default(0)
  recognizedAmount Decimal @db.Decimal(12, 2) @default(0)
  status String @default("PENDING")
  dueDate DateTime
  recognizedAt DateTime?
  milestoneId String? @default("")
  milestoneName String? @default("")
  milestoneDescription String? @default("")
  description String? @default("")
  notes String? @default("")
  metadata String? @default("")
  createdAt DateTime
  updatedAt DateTime

  @@id([tenantId, id])
}

model RolePolicy {
  id String
  tenantId String
  roleId String
  roleName String? @default("")
  permissions String? @default("[]")
  description String? @default("")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model SampleData {
  id String
  tenantId String
  seededAt DateTime?
  clearedAt DateTime?
  isSeeded Boolean? @default(false)
  eventsCreated Int? @default(0)
  clientsCreated Int? @default(0)
  usersCreated Int? @default(0)
  recipesCreated Int? @default(0)

  @@id([tenantId, id])
}

model Schedule {
  id String
  tenantId String
  locationId String? @default("")
  scheduleDate DateTime?
  status String? @default("draft")
  publishedAt DateTime?
  publishedBy String? @default("")
  approvedBy String? @default("")
  approvedAt DateTime?
  notes String? @default("")
  shiftCount Int? @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ScheduleShift {
  id String
  tenantId String
  scheduleId String
  employeeId String
  locationId String
  shiftStart DateTime?
  shiftEnd DateTime?
  roleDuringShift String? @default("")
  notes String? @default("")
  swapOfferedTo String? @default("")
  swapStatus String? @default("none")
  swapOfferedAt DateTime?
  swapAcceptedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Shipment {
  id String
  tenantId String
  shipmentNumber String
  status String? @default("draft")
  purchaseOrderId String? @default("")
  eventId String? @default("")
  supplierId String? @default("")
  locationId String? @default("")
  scheduledDate DateTime?
  shippedDate DateTime?
  estimatedDeliveryDate DateTime?
  actualDeliveryDate DateTime?
  totalItems Int? @default(0)
  shippingCost Decimal? @db.Decimal(12, 2) @default(0)
  totalValue Decimal? @db.Decimal(12, 2) @default(0)
  trackingNumber String? @default("")
  carrier String? @default("")
  shippingMethod String? @default("")
  deliveredBy String? @default("")
  receivedBy String? @default("")
  signature String? @default("")
  notes String? @default("")
  internalNotes String? @default("")
  reference String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model ShipmentItem {
  id String
  tenantId String
  shipmentId String
  itemId String
  quantityShipped Int? @default(0)
  quantityReceived Int? @default(0)
  quantityDamaged Int? @default(0)
  unitId Int? @default(0)
  unitCost Decimal? @db.Decimal(12, 2) @default(0)
  totalCost Decimal? @db.Decimal(12, 2) @default(0)
  condition String? @default("good")
  conditionNotes String? @default("")
  lotNumber String? @default("")
  expirationDate DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model SmsAutomationRule {
  id String
  tenantId String
  name String @default("")
  description String? @default("")
  triggerType String @default("custom_event")
  triggerConfig String? @default("{}")
  templateId String? @default("")
  customMessage String? @default("")
  recipientType String? @default("employee")
  recipientConfig String? @default("{}")
  isActive Boolean? @default(true)
  priority Int? @default(100)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model TimecardApproval {
  id String
  tenantId String
  employeeId String
  payrollPeriodId String
  status String? @default("pending")
  approvedBy String? @default("")
  approvedAt DateTime?
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PayrollLineItem {
  id String
  tenantId String
  payrollRunId String
  employeeId String
  grossPay Decimal? @db.Decimal(12, 2) @default(0)
  netPay Decimal? @db.Decimal(12, 2) @default(0)
  totalDeductions Decimal? @db.Decimal(12, 2) @default(0)
  hoursWorked Decimal? @db.Decimal(12, 2) @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TipPool {
  id String
  tenantId String
  periodStart DateTime?
  periodEnd DateTime?
  eventId String? @default("")
  totalTips Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("open")
  distributedAmount Decimal? @db.Decimal(12, 2) @default(0)
  distributedAt DateTime?
  distributedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model DisciplinaryAction {
  id String
  tenantId String
  employeeId String
  actionType String? @default("")
  reason String? @default("")
  severity String? @default("low")
  status String? @default("open")
  issuedBy String? @default("")
  escalatedTo String? @default("")
  escalationReason String? @default("")
  escalatedAt DateTime?
  issuedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model ActionMilestone {
  id String
  tenantId String
  disciplinaryActionId String
  title String? @default("")
  dueAt DateTime?
  status String? @default("pending")
  completedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PerformanceReview {
  id String
  tenantId String
  employeeId String
  reviewerId String
  reviewPeriod String? @default("")
  overallRating Decimal? @db.Decimal(12, 2) @default(0)
  status String? @default("draft")
  submittedAt DateTime?
  signedOffAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TrainingCompletion {
  id String
  tenantId String
  employeeId String
  trainingModuleId String
  completedAt DateTime?
  score Decimal? @db.Decimal(12, 2) @default(0)
  passed Boolean? @default(false)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model OnboardingTask {
  id String
  tenantId String
  title String? @default("")
  description String? @default("")
  sortOrder Int? @default(0)
  isRequired Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model OnboardingCompletion {
  id String
  tenantId String
  employeeId String
  onboardingTaskId String
  completedAt DateTime?
  status String? @default("pending")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model OpenShift {
  id String
  tenantId String
  scheduleId String
  role String? @default("")
  shiftStart DateTime?
  shiftEnd DateTime?
  status String? @default("open")
  claimedBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model DeliveryRoute {
  id String
  tenantId String
  name String? @default("")
  driverId String? @default("")
  routeDate DateTime?
  status String? @default("planned")
  totalStops Int? @default(0)
  distanceMiles Decimal? @db.Decimal(12, 2) @default(0)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model RouteStop {
  id String
  tenantId String
  deliveryRouteId String
  eventId String? @default("")
  address String? @default("")
  sequence Int? @default(0)
  scheduledAt DateTime?
  status String? @default("pending")
  arrivedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PaymentRefundAttempt {
  id String
  tenantId String
  paymentId String
  amount Decimal? @db.Decimal(12, 2) @default(0)
  reason String? @default("")
  status String? @default("pending")
  attemptedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model PreventiveMaintenanceSchedule {
  id String
  tenantId String
  equipmentId String
  name String? @default("")
  frequency String? @default("")
  nextDueAt DateTime?
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model StaffMember {
  id String
  tenantId String
  displayName String @default("Unnamed")
  email String? @default("")
  phone String? @default("")
  role String? @default("server")
  status String @default("active")
  notes String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model StaffPerformance {
  id String
  tenantId String
  employeeId String
  reviewType String? @default("quarterly")
  status String? @default("draft")
  rating Decimal? @db.Decimal(12, 2) @default(0)
  reviewerId String? @default("")
  periodStart DateTime?
  periodEnd DateTime?
  strengths String? @default("")
  improvements String? @default("")
  goals String? @default("")
  notes String? @default("")
  acknowledgementNotes String? @default("")
  completedAt DateTime?
  acknowledgedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Station {
  id String
  tenantId String
  locationId String
  name String
  stationType String @default("prep-station")
  capacitySimultaneousTasks Int? @default(1)
  equipmentList String? @default("")
  isActive Boolean? @default(true)
  inMaintenance Boolean? @default(false)
  maintenanceReason String? @default("")
  maintenanceStartAt DateTime?
  currentTaskCount Int? @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TimeEntry {
  id String
  tenantId String
  employeeId String
  locationId String? @default("")
  shiftId String? @default("")
  clockIn DateTime?
  clockOut DateTime?
  breakMinutes Int? @default(0)
  notes String? @default("")
  approvedBy String? @default("")
  approvedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model TimecardEditRequest {
  id String
  tenantId String
  timeEntryId String
  employeeId String
  requestedClockIn DateTime?
  requestedClockOut DateTime?
  requestedBreakMinutes Int? @default(0)
  reason String
  status String? @default("pending")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model TimeOffRequest {
  id String
  tenantId String
  employeeId String? @default("")
  startDate String? @default("")
  endDate String? @default("")
  reason String? @default("")
  requestType String? @default("")
  status String? @default("PENDING")
  processedBy String? @default("")
  processedAt DateTime?
  rejectionReason String? @default("")
  balanceSnapshot Decimal? @db.Decimal(12, 2) @default(0)
  balanceUnit String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model TrainingAssignment {
  id String
  tenantId String
  moduleId String? @default("")
  employeeId String? @default("")
  assignedToAll Boolean? @default(false)
  assignedBy String? @default("")
  dueDate String? @default("")
  status String? @default("assigned")
  score Int? @default(0)
  waiveReason String? @default("")
  assignedAt DateTime?
  startedAt DateTime?
  completedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model TrainingModule {
  id String
  tenantId String
  title String? @default("")
  description String? @default("")
  contentUrl String? @default("")
  contentType String? @default("document")
  durationMinutes Int? @default(0)
  category String? @default("")
  isRequired Boolean? @default(false)
  isActive Boolean? @default(true)
  status String? @default("draft")
  version Int? @default(1)
  publishedAt DateTime?
  archivedAt DateTime?
  notes String? @default("")
  createdBy String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model User {
  id String
  tenantId String
  email String
  firstName String @default("")
  lastName String @default("")
  role String @default("staff")
  authUserId String? @default("")
  employeeNumber String? @default("")
  phone String? @default("")
  employmentType String? @default("full_time")
  hourlyRate Decimal? @db.Decimal(12, 2) @default(0)
  salaryAnnual Decimal? @db.Decimal(12, 2) @default(0)
  hireDate DateTime
  terminationDate DateTime?
  isActive Boolean @default(true)
  avatarUrl String? @default("")
  roleId String? @default("")
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model VendorCatalog {
  id String
  tenantId String
  supplierId String
  itemNumber String @default("")
  itemName String @default("")
  description String? @default("")
  category String? @default("")
  baseUnitCost Decimal @db.Decimal(12, 2) @default(0)
  currency String @default("USD")
  unitOfMeasure String @default("each")
  leadTimeDays Int? @default(0)
  leadTimeMinDays Int? @default(0)
  leadTimeMaxDays Int? @default(0)
  minimumOrderQuantity Int? @default(0)
  orderMultiple Int? @default(0)
  isActive Boolean @default(true)
  effectiveFrom DateTime?
  effectiveTo DateTime?
  supplierSku String? @default("")
  notes String? @default("")
  tags String[]
  lastCostUpdate DateTime?
  createdAt DateTime
  updatedAt DateTime
  deletedAt DateTime?

  @@id([tenantId, id])
}

model VendorContract {
  id String
  tenantId String
  contractNumber String
  vendorId String
  vendorName String? @default("")
  contractType String? @default("purchase")
  status String? @default("draft")
  startDate DateTime?
  endDate DateTime?
  autoRenew Boolean? @default(false)
  renewalTermDays Int? @default(0)
  noticeDaysBeforeRenewal Int? @default(30)
  paymentTerms String? @default("NET_30")
  deliveryTerms String? @default("")
  minimumOrderQuantity Int? @default(0)
  annualSpendCommitment Decimal? @db.Decimal(12, 2) @default(0)
  spendToPeriod Decimal? @db.Decimal(12, 2) @default(0)
  currencyCode String? @default("USD")
  approvedBy String? @default("")
  approvedAt DateTime?
  terminatedBy String? @default("")
  terminatedAt DateTime?
  terminationReason String? @default("")
  contractUrl String? @default("")
  notes String? @default("")
  complianceScore Decimal? @db.Decimal(12, 2) @default(100)
  lastComplianceReview Decimal? @db.Decimal(12, 2) @default(0)
  slaBreachCount Int? @default(0)
  onTimeDeliveryRate Decimal? @db.Decimal(12, 2) @default(0)
  qualityRating Decimal? @db.Decimal(12, 2) @default(0)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model Vendor {
  id String
  tenantId String
  name String
  type String? @default("supplier")
  status String? @default("active")
  email String? @default("")
  phone String? @default("")
  website String? @default("")
  address String? @default("")
  city String? @default("")
  state String? @default("")
  zip String? @default("")
  taxId String? @default("")
  paymentTerms String? @default("net30")
  rating Decimal? @db.Decimal(12, 2) @default(0)
  ratingCount Int? @default(0)
  notes String? @default("")
  approvedAt DateTime?
  approvedBy String? @default("")
  suspendedAt DateTime?
  suspendedReason String? @default("")
  blacklistedAt DateTime?
  blacklistedReason String? @default("")
  lastContactAddedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model VersionedEntity {
  id String
  tenantId String
  entityType String
  entityId String
  entityName String
  isLocked Boolean? @default(false)
  currentVersionId String? @default("")
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model EntityVersion {
  id String
  tenantId String
  versionedEntityId String
  versionNumber Int
  changeReason String? @default("")
  changeSummary String? @default("")
  changeType String
  snapshotData String?
  metadata String?
  isApproved Boolean? @default(false)
  approvedAt DateTime?
  approvedBy String? @default("")
  createdAt DateTime?
  createdBy String? @default("")

  @@id([tenantId, id])
}

model VersionApproval {
  id String
  tenantId String
  entityVersionId String
  approverId String
  status String
  comments String? @default("")
  reviewedAt DateTime?
  createdAt DateTime?

  @@id([tenantId, id])
}

model WasteEntry {
  id String
  tenantId String
  inventoryItemId String? @default("")
  reasonId Int? @default(0)
  quantity Int? @default(0)
  unitId Int? @default(0)
  locationId String? @default("")
  eventId String? @default("")
  loggedBy String? @default("")
  loggedAt DateTime?
  approvedBy String? @default("")
  approvedAt DateTime?
  status String? @default("logged")
  unitCost Decimal? @db.Decimal(12, 2) @default(0)
  totalCost Decimal? @db.Decimal(12, 2) @default(0)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model WorkOrder {
  id String
  tenantId String
  equipmentId String
  equipmentName String? @default("")
  title String
  type String? @default("repair")
  priority String? @default("medium")
  status String? @default("open")
  description String? @default("")
  scheduledDate DateTime?
  assignedTo String? @default("")
  completedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?

  @@id([tenantId, id])
}

model Workflow {
  id String
  tenantId String
  name String? @default("")
  description String? @default("")
  triggerType String? @default("")
  triggerConfig String? @default("{}")
  isActive Boolean? @default(true)
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  @@id([tenantId, id])
}

model WorkforceOptimization {
  id String
  tenantId String
  locationId String? @default("")
  optimizationType String? @default("")
  status String? @default("pending")
  createdAt DateTime?
  updatedAt DateTime?
  completedAt DateTime?

  @@id([tenantId, id])
}

model PerformancePrediction {
  id String
  tenantId String
  employeeId String? @default("")
  predictionType String? @default("")
  predictionHorizon Int? @default(0)
  predictionScore Decimal? @db.Decimal(12, 2) @default(0)
  confidence String? @default("medium")
  factors String? @default("")
  createdAt DateTime?
  expiresAt DateTime?

  @@id([tenantId, id])
}

// ===== PRESERVED NON-IR INFRASTRUCTURE + CORE TABLES =====

// ============================================================================
// INFRA + CORE partial — extracted from pre-regen backup.
// Transforms applied:
//   - Relation fields stripped (object and list relations)
//   - @@schema directives stripped (single-schema datasource)
//   - @@index/@@unique referencing removed fields stripped
// Concatenate with generated domain schema to form hybrid schema.
// ============================================================================

// ── Enums ────────────────────────────────────────────────────────────────────

enum OutboxStatus {
  pending
  published
  failed

}

enum webhook_event_type {
  created
  updated
  deleted

  @@map("webhook_event_type")
}

enum webhook_status {
  active
  inactive
  disabled // Auto-disabled after too many failures

  @@map("webhook_status")
}

enum webhook_delivery_status {
  pending
  success
  failed
  retrying
  dead_letter // Permanently failed, moved to DLQ for manual review

  @@map("webhook_delivery_status")
}

enum ActionType {
  insert
  update
  delete

  @@map("action_type")
}

enum admin_action {
  login
  logout
  create
  update
  delete
  view
  permission_change
  role_change
  account_change
  security_change

}

enum admin_entity_type {
  admin_users
  admin_roles
  admin_permissions
  admin_audit_trail
  users
  roles
  permissions
  tenants
  reports
  settings

}

enum email_status {
  pending
  sent
  delivered
  opened
  failed
  bounced

  @@map("email_status")
}

enum sms_status {
  pending
  sent
  delivered
  failed

}

enum SentryFixJobStatus {
  queued
  running
  succeeded
  failed
  cancelled

}

enum UnitSystem {
  metric
  imperial
  custom

  @@map("unit_system")
}

enum UnitType {
  volume
  weight
  count
  length
  temperature
  time

  @@map("unit_type")
}

enum sms_automation_trigger_type {
  task_assigned
  task_completed
  task_overdue
  shift_assigned
  shift_reminder
  shift_changed
  clock_in_reminder
  clock_out_reminder
  prep_list_published
  inventory_low
  custom_event

  @@map("sms_automation_trigger_type")
}

enum sms_recipient_type {
  employee
  role_based
  custom_phone
  manager

  @@map("sms_recipient_type")
}

enum admin_role {
  super_admin
  tenant_admin
  finance_manager
  operations_manager
  staff_manager
  read_only

}

// ── Models ───────────────────────────────────────────────────────────────────

model ManifestCommandTelemetry {
  tenantId         String   @map("tenant_id") @db.Uuid
  id               String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  commandName      String   @map("command_name")
  entityName       String   @map("entity_name")
  instanceId       String?  @map("instance_id") @db.Uuid
  status           String
  errorMessage     String?  @map("error_message")
  errorCode        String?  @map("error_code")
  durationMs       Int?     @map("duration_ms")
  guardEvalMs      Int?     @map("guard_eval_ms")
  actionExecMs     Int?     @map("action_exec_ms")
  guardsEvaluated  Int?     @default(0) @map("guards_evaluated")
  guardsPassed     Int?     @default(0) @map("guards_passed")
  guardsFailed     Int?     @default(0) @map("guards_failed")
  failedGuards     Json?    @map("failed_guards")
  idempotencyKey   String?  @map("idempotency_key")
  wasIdempotentHit Boolean  @default(false) @map("was_idempotent_hit")
  eventsEmitted    Int?     @default(0) @map("events_emitted")
  performedBy      String?  @map("performed_by") @db.Uuid
  correlationId    String?  @map("correlation_id")
  causationId      String?  @map("causation_id")
  requestId        String?  @map("request_id")
  ipAddress        String?  @map("ip_address")
  executedAt       DateTime @default(now()) @map("executed_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, commandName, executedAt])
  @@index([tenantId, entityName, executedAt])
  @@index([tenantId, status, executedAt])
  @@index([tenantId, performedBy, executedAt])
  @@index([tenantId, correlationId])
  @@index([executedAt])
  @@map("manifest_command_telemetry")
}

model ManifestEntity {
  tenantId   String   @map("tenant_id") @db.Uuid
  entityType String   @map("entity_type")
  id         String   @db.Uuid
  data       Json
  version    Int      @default(1)
  createdAt  DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt  DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, entityType, id])
  @@index([tenantId, entityType])
  @@map("manifest_entity")
}

model ManifestIdempotency {
  tenantId  String   @map("tenant_id") @db.Uuid
  key       String
  result    Json
  createdAt DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  expiresAt DateTime @map("expires_at") @db.Timestamptz(6)

  @@id([tenantId, key])
  @@index([expiresAt])
  @@map("manifest_idempotency")
}

model OutboxEvent {
  id            String       @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenantId      String       @map("tenant_id") @db.Uuid
  eventType     String       @map("event_type")
  payload       Json
  status        OutboxStatus @default(pending)
  error         String?
  createdAt     DateTime     @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt     DateTime     @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  publishedAt   DateTime?    @map("published_at") @db.Timestamptz(6)
  aggregateId   String       @map("aggregate_id")
  aggregateType String       @map("aggregate_type")

  @@index([status, createdAt])
  @@index([tenantId])
  @@index([aggregateType, aggregateId])
  @@map("OutboxEvent")
}

model OutboundWebhook {
  tenantId            String               @map("tenant_id") @db.Uuid
  id                  String               @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                String
  url                 String               @db.VarChar(2048)
  secret              String?              @map("secret") // For signing payloads
  apiKey              String?              @map("api_key") // For authentication header
  eventTypeFilters    webhook_event_type[] @default([]) @map("event_type_filters")
  entityFilters       String[]             @default([]) @map("entity_filters") // e.g., ["event", "task", "employee"]
  status              webhook_status       @default(active)
  retryCount          Int                  @default(3) @map("retry_count")
  retryDelayMs        Int                  @default(1000) @map("retry_delay_ms") // Initial delay, exponential backoff
  timeoutMs           Int                  @default(30000) @map("timeout_ms")
  customHeaders       Json?                @map("custom_headers") // Additional headers to send
  lastTriggeredAt     DateTime?            @map("last_triggered_at") @db.Timestamptz(6)
  lastSuccessAt       DateTime?            @map("last_success_at") @db.Timestamptz(6)
  lastFailureAt       DateTime?            @map("last_failure_at") @db.Timestamptz(6)
  consecutiveFailures Int                  @default(0) @map("consecutive_failures")
  createdAt           DateTime             @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime             @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt           DateTime?            @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, status])
  @@index([tenantId, createdAt(sort: Desc)])
  @@map("outbound_webhooks")
}

model WebhookDeliveryLog {
  tenantId           String                  @map("tenant_id") @db.Uuid
  id                 String                  @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  webhookId          String                  @map("webhook_id") @db.Uuid
  eventType          webhook_event_type
  entityType         String                  @map("entity_type") // event, task, employee, etc.
  entityId           String                  @map("entity_id") @db.Uuid
  payload            Json                    @map("payload")
  status             webhook_delivery_status @default(pending)
  attemptNumber      Int                     @default(1) @map("attempt_number")
  httpResponseStatus Int?                    @map("http_response_status")
  responseBody       String?                 @map("response_body") @db.Text
  errorMessage       String?                 @map("error_message") @db.Text
  nextRetryAt        DateTime?               @map("next_retry_at") @db.Timestamptz(6)
  deliveredAt        DateTime?               @map("delivered_at") @db.Timestamptz(6)
  failedAt           DateTime?               @map("failed_at") @db.Timestamptz(6)
  createdAt          DateTime                @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime                @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, webhookId])
  @@index([tenantId, status])
  @@index([tenantId, entityType, entityId])
  @@index([tenantId, createdAt(sort: Desc)])
  @@index([tenantId, nextRetryAt])
  @@map("webhook_delivery_logs")
}

model WebhookDeadLetterQueue {
  tenantId           String             @map("tenant_id") @db.Uuid
  id                 String             @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  webhookId          String?            @map("webhook_id") @db.Uuid // Null if webhook was deleted
  originalDeliveryId String             @map("original_delivery_id") @db.Uuid // Reference to original log
  eventType          webhook_event_type
  entityType         String             @map("entity_type")
  entityId           String             @map("entity_id") @db.Uuid
  payload            Json               @map("payload")
  finalErrorMessage  String?            @map("final_error_message") @db.Text
  totalAttempts      Int                @default(1) @map("total_attempts")
  originalUrl        String             @map("original_url") @db.VarChar(2048) // URL at time of failure
  movedToDlqAt       DateTime           @default(now()) @map("moved_to_dlq_at") @db.Timestamptz(6)
  reviewedAt         DateTime?          @map("reviewed_at") @db.Timestamptz(6)
  reviewedBy         String?            @map("reviewed_by") @db.Uuid // User who reviewed
  resolution         String?            @map("resolution") @db.Text // Notes on resolution
  resolvedAt         DateTime?          @map("resolved_at") @db.Timestamptz(6)
  retriedAt          DateTime?          @map("retried_at") @db.Timestamptz(6) // If manually retried
  createdAt          DateTime           @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime           @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, webhookId])
  @@index([tenantId, entityType, entityId])
  @@index([tenantId, movedToDlqAt(sort: Desc)])
  @@index([tenantId, reviewedAt])
  @@map("webhook_dead_letter_queue")
}

model GoodshuffleConfig {
  tenantId           String    @map("tenant_id") @db.Uuid
  id                 String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  apiKey             String    @map("api_key")
  apiSecret          String    @map("api_secret")
  webhookSecret      String?   @map("webhook_secret")
  syncEnabled        Boolean   @default(true) @map("sync_enabled")
  syncDirection      String    @default("one_way") // one_way or two_way
  conflictResolution String    @default("convoy_wins") // convoy_wins, goodshuffle_wins, manual
  lastSyncAt         DateTime? @map("last_sync_at") @db.Timestamptz(6)
  lastSyncStatus     String?   @map("last_sync_status")
  lastSyncError      String?   @map("last_sync_error") @db.Text
  autoSyncInterval   Int?      @map("auto_sync_interval") // in minutes
  createdAt          DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId])
  @@map("goodshuffle_config")
}

model GoodshuffleEventSync {
  tenantId             String    @map("tenant_id") @db.Uuid
  id                   String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  goodshuffleEventId   String    @map("goodshuffle_event_id")
  convoyEventId        String?   @map("convoy_event_id") @db.Uuid
  eventName            String?   @map("event_name")
  eventDate            DateTime? @map("event_date") @db.Timestamptz(6)
  status               String    @default("pending") // pending, synced, conflict, error
  syncError            String?   @map("sync_error") @db.Text
  conflictData         Json?     @map("conflict_data")
  conflictResolvedAt   DateTime? @map("conflict_resolved_at") @db.Timestamptz(6)
  lastSyncedAt         DateTime? @map("last_synced_at") @db.Timestamptz(6)
  goodshuffleUpdatedAt DateTime? @map("goodshuffle_updated_at") @db.Timestamptz(6)
  createdAt            DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, goodshuffleEventId])
  @@index([tenantId, status])
  @@index([tenantId, eventDate])
  @@index([tenantId, convoyEventId])
  @@map("goodshuffle_event_syncs")
}

model GoodshuffleInventorySync {
  tenantId              String    @map("tenant_id") @db.Uuid
  id                    String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  goodshuffleItemId     String    @map("goodshuffle_item_id")
  convoyInventoryItemId String?   @map("convoy_inventory_item_id") @db.Uuid
  itemName              String?   @map("item_name")
  itemSku               String?   @map("item_sku")
  status                String    @default("pending") // pending, synced, conflict, error
  syncError             String?   @map("sync_error") @db.Text
  conflictData          Json?     @map("conflict_data")
  conflictResolvedAt    DateTime? @map("conflict_resolved_at") @db.Timestamptz(6)
  lastSyncedAt          DateTime? @map("last_synced_at") @db.Timestamptz(6)
  goodshuffleUpdatedAt  DateTime? @map("goodshuffle_updated_at") @db.Timestamptz(6)
  createdAt             DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt             DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, goodshuffleItemId])
  @@index([tenantId, status])
  @@index([tenantId, convoyInventoryItemId])
  @@map("goodshuffle_inventory_syncs")
}

model GoodshuffleInvoiceSync {
  tenantId             String    @map("tenant_id") @db.Uuid
  id                   String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  goodshuffleInvoiceId String    @map("goodshuffle_invoice_id")
  convoyInvoiceId      String?   @map("convoy_invoice_id") @db.Uuid
  invoiceNumber        String?   @map("invoice_number")
  invoiceTotal         Decimal?  @map("invoice_total") @db.Decimal(12, 2)
  status               String    @default("pending") // pending, synced, conflict, error
  syncError            String?   @map("sync_error") @db.Text
  conflictData         Json?     @map("conflict_data")
  conflictResolvedAt   DateTime? @map("conflict_resolved_at") @db.Timestamptz(6)
  lastSyncedAt         DateTime? @map("last_synced_at") @db.Timestamptz(6)
  goodshuffleUpdatedAt DateTime? @map("goodshuffle_updated_at") @db.Timestamptz(6)
  createdAt            DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt            DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, goodshuffleInvoiceId])
  @@index([tenantId, status])
  @@index([tenantId, convoyInvoiceId])
  @@map("goodshuffle_invoice_syncs")
}

model NowstaConfig {
  tenantId         String    @map("tenant_id") @db.Uuid
  id               String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  apiKey           String    @map("api_key")
  apiSecret        String    @map("api_secret")
  organizationId   String?   @map("organization_id")
  syncEnabled      Boolean   @default(true) @map("sync_enabled")
  syncDirection    String    @default("one_way") // one_way or two_way
  lastSyncAt       DateTime? @map("last_sync_at") @db.Timestamptz(6)
  lastSyncStatus   String?   @map("last_sync_status")
  lastSyncError    String?   @map("last_sync_error") @db.Text
  autoSyncInterval Int?      @map("auto_sync_interval") // in minutes
  createdAt        DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId])
  @@map("nowsta_config")
}

model NowstaEmployeeMapping {
  tenantId            String    @map("tenant_id") @db.Uuid
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nowstaEmployeeId    String    @map("nowsta_employee_id")
  convoyEmployeeId    String    @map("convoy_employee_id") @db.Uuid
  nowstaEmployeeName  String?   @map("nowsta_employee_name")
  nowstaEmployeeEmail String?   @map("nowsta_employee_email")
  autoMapped          Boolean   @default(false) @map("auto_mapped")
  confirmedAt         DateTime? @map("confirmed_at") @db.Timestamptz(6)
  createdAt           DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, nowstaEmployeeId])
  @@index([tenantId, convoyEmployeeId])
  @@map("nowsta_employee_mappings")
}

model NowstaShiftSync {
  tenantId         String    @map("tenant_id") @db.Uuid
  id               String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  nowstaShiftId    String    @map("nowsta_shift_id")
  convoyShiftId    String?   @map("convoy_shift_id") @db.Uuid
  nowstaEmployeeId String    @map("nowsta_employee_id")
  locationId       String?   @map("location_id") @db.Uuid
  shiftStart       DateTime  @map("shift_start") @db.Timestamptz(6)
  shiftEnd         DateTime  @map("shift_end") @db.Timestamptz(6)
  roleDuringShift  String?   @map("role_during_shift")
  status           String    @default("pending") // pending, synced, error
  syncError        String?   @map("sync_error") @db.Text
  lastSyncedAt     DateTime? @map("last_synced_at") @db.Timestamptz(6)
  nowstaUpdatedAt  DateTime? @map("nowsta_updated_at") @db.Timestamptz(6)
  createdAt        DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, nowstaShiftId])
  @@index([tenantId, status])
  @@index([tenantId, shiftStart])
  @@index([tenantId, nowstaEmployeeId])
  @@map("nowsta_shift_syncs")
}

model ProviderSync {
  tenantId       String    @map("tenant_id") @db.Uuid
  id             String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  provider       String // "google" | "outlook"
  providerUserId String?   @map("provider_user_id") // external account ID
  accessToken    String?   @map("access_token") @db.Text
  refreshToken   String?   @map("refresh_token") @db.Text
  tokenExpiry    DateTime? @map("token_expiry") @db.Timestamptz(6)
  calendarId     String?   @map("calendar_id") // primary calendar ID from provider
  calendarName   String?   @map("calendar_name")
  status         String    @default("disconnected") // disconnected | connected | error
  lastSyncAt     DateTime? @map("last_sync_at") @db.Timestamptz(6)
  lastSyncStatus String?   @map("last_sync_status")
  lastSyncError  String?   @map("last_sync_error") @db.Text
  syncDirection  String    @default("import") // import | export | two_way
  enabled        Boolean   @default(true)
  createdAt      DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt      DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt      DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, provider])
  @@index([tenantId, status])
  @@map("provider_syncs")
}

model SupplierSyncLog {
  tenantId            String    @map("tenant_id") @db.Uuid
  id                  String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  supplierId          String    @map("supplier_id") @db.Uuid
  connectorId         String    @map("connector_id")
  status              String    @default("pending")
  productsSynced      Int       @default(0) @map("products_synced")
  productsCreated     Int       @default(0) @map("products_created")
  productsUpdated     Int       @default(0) @map("products_updated")
  productsDeactivated Int       @default(0) @map("products_deactivated")
  errors              Json      @default("[]") @db.JsonB
  durationMs          Int       @default(0) @map("duration_ms")
  triggeredBy         String?   @map("triggered_by")
  createdAt           DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt           DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt           DateTime? @map("deleted_at") @db.Timestamptz(6)


  @@id([tenantId, id])
  @@index([tenantId, supplierId], map: "supplier_sync_logs_tenant_supplier_idx")
  @@index([connectorId], map: "supplier_sync_logs_connector_idx")
  @@index([status], map: "supplier_sync_logs_status_idx")
  @@index([createdAt], map: "supplier_sync_logs_created_at_idx")
  @@map("supplier_sync_logs")
}

model audit_log {
  id           String     @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String?    @db.Uuid
  table_schema String
  table_name   String
  record_id    String     @db.Uuid
  action       ActionType
  old_values   Json?
  new_values   Json?
  performed_by String?    @db.Uuid
  ip_address   String?    @db.Inet
  user_agent   String?
  created_at   DateTime   @default(now()) @db.Timestamptz(6)

  @@id([id, created_at])
  @@index([table_name, record_id], map: "audit_log_table_record_idx")
  @@index([tenant_id, created_at], map: "audit_log_tenant_created_idx")
}

model audit_archive {
  id           String     @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id    String?    @db.Uuid
  table_schema String
  table_name   String
  record_id    String     @db.Uuid
  action       ActionType
  old_values   Json?
  new_values   Json?
  performed_by String?    @db.Uuid
  ip_address   String?    @db.Inet
  user_agent   String?
  created_at   DateTime   @db.Timestamptz(6)
  archived_at  DateTime   @default(now()) @db.Timestamptz(6)

  @@id([id, created_at])
  @@index([tenant_id], map: "audit_archive_tenant_idx")
}

model audit_config {
  table_schema     String
  table_name       String
  audit_level      String   @default("full")
  excluded_columns String[]

  @@id([table_schema, table_name])
}

model admin_audit_trail {
  tenant_id     String            @db.Uuid
  id            String            @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  admin_user_id String            @db.Uuid
  entity_type   admin_entity_type
  entity_id     String?           @db.Uuid
  action        admin_action
  description   String?
  changes       Json?
  old_values    Json?
  new_values    Json?
  ip_address    String?           @db.Inet
  user_agent    String?
  created_at    DateTime          @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime?         @db.Timestamptz(6)

  @@id([tenant_id, id])
}

model CycleCountAuditLog {
  tenantId      String   @map("tenant_id") @db.Uuid
  id            String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sessionId     String   @map("session_id") @db.Uuid
  recordId      String?  @map("record_id") @db.Uuid
  action        String
  entityType    String   @map("entity_type")
  entityId      String   @map("entity_id") @db.Uuid
  oldValue      Json?    @map("old_value")
  newValue      Json?    @map("new_value")
  performedById String   @map("performed_by_id") @db.Uuid
  ipAddress     String?  @map("ip_address") @db.Inet
  userAgent     String?  @map("user_agent")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, sessionId], map: "cycle_count_audit_tenant_session_idx")
  @@index([tenantId, performedById], map: "cycle_count_audit_tenant_performer_idx")
  @@index([tenantId, createdAt(sort: Desc)], map: "cycle_count_audit_tenant_created_idx")
  @@map("cycle_count_audit_log")
}

model PayrollAuditLog {
  tenantId      String   @map("tenant_id") @db.Uuid
  id            String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  periodId      String   @map("period_id") @db.Uuid
  action        String
  userId        String?  @map("user_id") @db.Uuid
  inputSnapshot Json?    @map("input_snapshot") @db.JsonB
  rulesVersion  String?  @map("rules_version")
  resultSummary Json?    @map("result_summary") @db.JsonB
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)


  @@id([tenantId, id])
  @@unique([tenantId, id])
  @@index([tenantId, periodId])
  @@index([tenantId, action])
  @@index([tenantId, createdAt])
  @@map("payroll_audit_log")
}

model ApprovalHistory {
  tenantId       String   @map("tenant_id") @db.Uuid
  id             String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  entityType     String   @map("entity_type") // "payroll_run" or "timecard_approval"
  entityId       String   @map("entity_id") @db.Uuid
  action         String // "submitted", "approved", "rejected", "reviewed"
  performedBy    String   @map("performed_by") @db.Uuid
  performedAt    DateTime @default(now()) @map("performed_at") @db.Timestamptz(6)
  previousStatus String?  @map("previous_status")
  newStatus      String   @map("new_status")
  notes          String?
  metadata       Json?    @default("{}")
  createdAt      DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([entityType, entityId], map: "approval_history_entity_idx")
  @@index([performedBy], map: "approval_history_performer_idx")
  @@map("approval_history")
}

model ActivityFeed {
  tenantId      String   @map("tenant_id") @db.Uuid
  id            String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  activityType  String   @map("activity_type")
  entityType    String?  @map("entity_type")
  entityId      String?  @map("entity_id") @db.Uuid
  action        String
  title         String
  description   String?
  metadata      Json?
  performedBy   String?  @map("performed_by") @db.Uuid
  performerName String?  @map("performer_name")
  correlationId String?  @map("correlation_id") @db.Uuid
  parentId      String?  @map("parent_id") @db.Uuid
  sourceType    String?  @map("source_type")
  sourceId      String?  @map("source_id") @db.Uuid
  importance    String   @default("normal")
  visibility    String   @default("all")
  createdAt     DateTime @default(now()) @map("created_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, createdAt])
  @@index([tenantId, activityType, createdAt])
  @@index([tenantId, entityType, entityId])
  @@index([tenantId, performedBy, createdAt])
  @@index([tenantId, correlationId])
  @@map("ActivityFeed")
}

model EmployeePinAccessLog {
  tenant_id      String   @db.Uuid
  id             String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id    String   @map("employee_id") @db.Uuid
  accessed_by_id String   @map("accessed_by_id") @db.Uuid
  access_type    String   @map("access_type") // VIEW, UPDATE, DELETE, UNAUTHORIZED_ATTEMPT
  ip_address     String?  @map("ip_address")
  user_agent     String?  @map("user_agent")
  success        Boolean  @default(true)
  created_at     DateTime @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([employee_id], map: "employee_pin_access_logs_employee_idx")
  @@index([accessed_by_id], map: "employee_pin_access_logs_accessor_idx")
  @@index([created_at], map: "employee_pin_access_logs_created_idx")
  @@map("employee_pin_access_logs")
}

model EmailLog {
  tenantId         String       @map("tenant_id") @db.Uuid
  id               String       @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflowId       String?      @map("workflow_id") @db.Uuid
  recipientEmail   String       @map("recipient_email")
  recipientId      String?      @map("recipient_id") @db.Uuid
  recipientType    String?      @map("recipient_type") // employee, client, vendor
  subject          String
  notificationType String       @map("notification_type")
  status           email_status @default(pending)
  resendId         String?      @map("resend_id")
  errorMessage     String?      @map("error_message") @db.Text
  sentAt           DateTime?    @map("sent_at") @db.Timestamptz(6)
  deliveredAt      DateTime?    @map("delivered_at") @db.Timestamptz(6)
  openedAt         DateTime?    @map("opened_at") @db.Timestamptz(6)
  failedAt         DateTime?    @map("failed_at") @db.Timestamptz(6)
  createdAt        DateTime     @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime     @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, workflowId])
  @@index([tenantId, recipientEmail])
  @@index([tenantId, status])
  @@index([tenantId, createdAt(sort: Desc)])
  @@map("email_logs")
}

model sms_logs {
  tenant_id         String     @db.Uuid
  id                String     @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id       String?    @db.Uuid
  phone_number      String
  message           String
  notification_type String
  status            sms_status @default(pending)
  twilio_sid        String?
  error_message     String?
  sent_at           DateTime?  @db.Timestamptz(6)
  delivered_at      DateTime?  @db.Timestamptz(6)
  failed_at         DateTime?  @db.Timestamptz(6)
  created_at        DateTime   @default(now()) @db.Timestamptz(6)
  updated_at        DateTime   @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([tenant_id, status])
  @@index([tenant_id, employee_id])
  @@index([tenant_id, created_at(sort: Desc)])
}

model sent_emails {
  id              String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tenant_id       String   @db.Uuid
  recipient_email String
  subject         String
  correlation_id  String?
  sent_at         DateTime @default(now()) @db.Timestamptz(6)

  @@index([tenant_id], map: "sent_emails_tenant_idx")
}

model report_history {
  tenant_id       String   @db.Uuid
  id              String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id       String   @db.Uuid
  schedule_id     String?  @db.Uuid
  generated_by    String?  @db.Uuid
  generated_at    DateTime @default(now()) @db.Timestamptz(6)
  output_format   String
  file_url        String?
  file_size_bytes BigInt?
  parameters      Json     @default("{}")
  created_at      DateTime @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([tenant_id, generated_at(sort: Desc)], map: "report_history_generated_at_idx")
  @@index([tenant_id, report_id], map: "report_history_tenant_report_idx")
}

model SentryFixJob {
  id               String             @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  sentryIssueId    String             @map("sentry_issue_id")
  sentryEventId    String?            @map("sentry_event_id")
  organizationSlug String             @map("organization_slug")
  projectSlug      String             @map("project_slug")
  environment      String?
  release          String?
  issueTitle       String             @map("issue_title")
  issueUrl         String             @map("issue_url")
  status           SentryFixJobStatus @default(queued)
  payloadSnapshot  Json               @map("payload_snapshot")
  branchName       String?            @map("branch_name")
  prUrl            String?            @map("pr_url")
  prNumber         Int?               @map("pr_number")
  errorMessage     String?            @map("error_message")
  retryCount       Int                @default(0) @map("retry_count")
  maxRetries       Int                @default(3) @map("max_retries")
  startedAt        DateTime?          @map("started_at") @db.Timestamptz(6)
  completedAt      DateTime?          @map("completed_at") @db.Timestamptz(6)
  createdAt        DateTime           @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime           @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@unique([sentryIssueId])
  @@index([status, createdAt])
  @@index([sentryIssueId, createdAt])
  @@map("sentry_fix_jobs")
}

model RateLimitEvent {
  tenantId         String   @map("tenant_id") @db.Uuid
  id               String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  endpoint         String
  method           String
  allowed          Boolean
  windowStart      DateTime @map("window_start") @db.Timestamptz(6)
  windowEnd        DateTime @map("window_end") @db.Timestamptz(6)
  requestsInWindow Int      @map("requests_in_window")
  limit            Int
  userId           String?  @map("user_id") @db.Uuid
  userAgent        String?  @map("user_agent")
  ipHash           String?  @map("ip_hash")
  responseTime     Int?     @map("response_time")
  timestamp        DateTime @default(now()) @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, timestamp])
  @@index([tenantId, endpoint])
  @@index([tenantId, allowed])
  @@index([tenantId, userId])
  @@map("rate_limit_events")
}

model RateLimitUsage {
  tenantId        String   @map("tenant_id") @db.Uuid
  id              String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  endpoint        String
  method          String
  bucketStart     DateTime @map("bucket_start") @db.Timestamptz(6)
  requestCount    Int      @default(0) @map("request_count")
  blockedCount    Int      @default(0) @map("blocked_count")
  avgResponseTime Int?     @map("avg_response_time")
  maxResponseTime Int?     @map("max_response_time")
  userHashes      String[] @map("user_hashes")
  createdAt       DateTime @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, endpoint, method, bucketStart])
  @@index([tenantId, bucketStart])
  @@index([tenantId, endpoint])
  @@map("rate_limit_usage")
}

model settings {
  tenant_id     String   @db.Uuid
  id            String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  setting_key   String
  setting_value Json
  created_at    DateTime @default(now()) @db.Timestamptz(6)
  updated_at    DateTime @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@unique([tenant_id, setting_key])
  @@index([tenant_id, setting_key], map: "settings_tenant_key_idx")
}

model WasteReason {
  id           Int          @id @default(autoincrement()) @db.SmallInt
  code         String       @unique
  name         String
  description  String?
  colorHex     String?      @map("color_hex") @db.Char(7)
  isActive     Boolean      @default(true) @map("is_active")
  sortOrder    Int          @default(0) @map("sort_order") @db.SmallInt

  @@map("waste_reasons")
}

model status_types {
  id          Int     @id @db.SmallInt
  category    String
  code        String
  label       String
  description String?
  color_hex   String? @db.Char(7)
  sort_order  Int     @default(0) @db.SmallInt
  is_terminal Boolean @default(false)
  is_default  Boolean @default(false)
  is_active   Boolean @default(true)

  @@unique([category, code])
}

model status_transitions {
  id               BigInt   @id @default(autoincrement())
  category         String
  from_status_code String?
  to_status_code   String
  requires_role    String[]
  is_automatic     Boolean  @default(false)

  @@unique([category, from_status_code, to_status_code])
}

model units {
  id           Int        @id @db.SmallInt
  code         String     @unique
  name         String
  name_plural  String
  unit_system  UnitSystem
  unit_type    UnitType
  is_base_unit Boolean    @default(false)

}

model unit_conversions {
  from_unit_id Int     @db.SmallInt
  to_unit_id   Int     @db.SmallInt
  multiplier   Decimal @db.Decimal(20, 10)

  @@id([from_unit_id, to_unit_id])
}

model workflow_executions {
  tenant_id       String    @db.Uuid
  id              String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id     String    @db.Uuid
  triggered_by    String?   @db.Uuid
  trigger_data    Json?
  status          String    @default("running")
  current_step_id String?   @db.Uuid
  started_at      DateTime  @default(now()) @db.Timestamptz(6)
  completed_at    DateTime? @db.Timestamptz(6)
  error_message   String?
  execution_log   Json      @default("[]")
  created_at      DateTime  @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([tenant_id, status], map: "workflow_executions_status_idx")
  @@index([tenant_id, workflow_id], map: "workflow_executions_workflow_idx")
}

model workflow_steps {
  tenant_id          String    @db.Uuid
  id                 String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  workflow_id        String    @db.Uuid
  step_number        Int       @db.SmallInt
  step_type          String
  step_config        Json      @default("{}")
  on_success_step_id String?   @db.Uuid
  on_failure_step_id String?   @db.Uuid
  created_at         DateTime  @default(now()) @db.Timestamptz(6)
  updated_at         DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at         DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@unique([tenant_id, workflow_id, step_number], map: "workflow_steps_workflow_number_idx")
  @@index([tenant_id, workflow_id], map: "workflow_steps_workflow_idx")
}

model sms_automation_rules {
  tenant_id        String                      @db.Uuid
  id               String                      @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name             String
  description      String?                     @db.Text
  trigger_type     sms_automation_trigger_type
  trigger_config   Json                        @default("{}")
  template_id      String?                     @db.Uuid
  custom_message   String?                     @db.Text
  recipient_type   sms_recipient_type          @default(employee)
  recipient_config Json                        @default("{}")
  is_active        Boolean                     @default(true) @map("is_active")
  priority         Int                         @default(100)
  created_at       DateTime                    @default(now()) @db.Timestamptz(6)
  updated_at       DateTime                    @default(now()) @updatedAt @db.Timestamptz(6)
  deleted_at       DateTime?                   @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([tenant_id, is_active])
  @@index([tenant_id, trigger_type])
  @@index([tenant_id, priority])
  @@map("sms_automation_rules")
}

model report_schedules {
  tenant_id     String    @db.Uuid
  id            String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  report_id     String    @db.Uuid
  schedule_cron String
  output_format String    @default("pdf")
  recipients    Json      @default("[]")
  is_active     Boolean   @default(true)
  last_run_at   DateTime? @db.Timestamptz(6)
  next_run_at   DateTime? @db.Timestamptz(6)
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([tenant_id, report_id], map: "report_schedules_tenant_report_idx")
}

model notification_preferences {
  tenant_id         String   @db.Uuid
  id                String   @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id       String   @db.Uuid
  notification_type String
  channel           String
  is_enabled        Boolean  @default(true)
  created_at        DateTime @default(now()) @db.Timestamptz(6)
  updated_at        DateTime @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@unique([tenant_id, employee_id, notification_type, channel])
}

model Account {
  id                             String                          @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                           String
  slug                           String                          @unique
  defaultTimezone                String                          @default("UTC") @map("default_timezone")
  weekStart                      Int                             @default(1) @map("week_start") @db.SmallInt
  subscriptionTier               String                          @default("trial") @map("subscription_tier")
  subscriptionStatus             String                          @default("active") @map("subscription_status")
  maxLocations                   Int                             @default(1) @map("max_locations") @db.SmallInt
  maxEmployees                   Int                             @default(10) @map("max_employees") @db.SmallInt
  createdAt                      DateTime                        @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                      DateTime                        @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt                      DateTime?                       @map("deleted_at") @db.Timestamptz(6)

  @@map("accounts")
}

model Tenant {
  id        String   @id @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name      String
  slug      String   @unique
  createdAt DateTime @default(now()) @db.Timestamptz(6)
  updatedAt DateTime @default(now()) @db.Timestamptz(6)

  @@map("Tenant")
}

model Location {
  tenantId         String     @map("tenant_id") @db.Uuid
  id               String     @unique @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name             String
  addressLine1     String?    @map("address_line1")
  addressLine2     String?    @map("address_line2")
  city             String?
  stateProvince    String?    @map("state_province")
  postalCode       String?    @map("postal_code")
  countryCode      String?    @map("country_code") @db.Char(2)
  timezone         String?
  isPrimary        Boolean    @default(false) @map("is_primary")
  isActive         Boolean    @default(true) @map("is_active")
  createdAt        DateTime   @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt        DateTime   @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt        DateTime?  @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, id])
  @@map("locations")
}

model Role {
  tenantId               String    @map("tenant_id") @db.Uuid
  id                     String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name                   String
  baseRate               Decimal   @map("base_rate") @db.Decimal(10, 2)
  overtimeMultiplier     Decimal   @default(1.5) @map("overtime_multiplier") @db.Decimal(4, 2)
  overtimeThresholdHours Int       @default(40) @map("overtime_threshold_hours") @db.SmallInt
  description            String?
  isActive               Boolean   @default(true) @map("is_active")
  createdAt              DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt              DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt              DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, name], map: "roles_name_unique")
  @@map("roles")
}

model Department {
  tenantId  String    @map("tenant_id") @db.Uuid
  id        String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name      String
  code      String?
  isActive  Boolean   @default(true) @map("is_active")
  createdAt DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, name], map: "departments_name_unique")
  @@index([tenantId, isActive], map: "departments_active_idx")
  @@map("departments")
}

model TaxConfiguration {
  tenant_id    String    @db.Uuid
  id           String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  tax_type     String // federal | state | local
  jurisdiction String
  state_code   String?
  is_active    Boolean   @default(true)
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  updated_at   DateTime  @default(now()) @updatedAt @db.Timestamptz(6)
  deleted_at   DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([tenant_id, is_active])
  @@index([tenant_id, tax_type])
  @@map("tax_configurations")
}

model EmployeeBankAccount {
  tenantId           String    @map("tenant_id") @db.Uuid
  id                 String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employeeId         String    @map("employee_id") @db.Uuid
  bankName           String    @map("bank_name")
  accountType        String    @default("checking") @map("account_type")
  routingNumber      String    @map("routing_number")
  accountNumber      String    @map("account_number")
  /// Generated column: GENERATED ALWAYS AS (RIGHT(account_number, 4)) STORED.
  /// Managed by Postgres; never set from application code.
  accountNumberLast4 String?   @map("account_number_last4")
  accountHolderName  String    @map("account_holder_name")
  isDefault          Boolean   @default(false) @map("is_default")
  status             String    @default("pending")
  verifiedAt         DateTime? @map("verified_at") @db.Timestamptz(6)
  verificationMethod String?   @map("verification_method")
  depositHistory     Json      @default("[]") @map("deposit_history")
  notes              String?
  createdAt          DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt          DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@index([tenantId, employeeId], map: "employee_bank_accounts_employee_idx")
  @@index([tenantId, status], map: "employee_bank_accounts_status_idx")
  @@map("employee_bank_accounts")
}

model EmployeePayrollPrefs {
  tenantId           String    @map("tenant_id") @db.Uuid
  id                 String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employeeId         String    @map("employee_id") @db.Uuid
  payPeriodFrequency String    @default("biweekly") @map("pay_period_frequency")
  roundingRule       String    @default("none") @map("rounding_rule")
  createdAt          DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt          DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt          DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, employeeId], map: "employee_payroll_prefs_employee_unique")
  @@map("employee_payroll_prefs")
}

model EmployeeTaxInfo {
  tenantId                     String    @map("tenant_id") @db.Uuid
  id                           String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employeeId                   String    @map("employee_id") @db.Uuid
  jurisdiction                 String    @default("FL")
  filingStatus                 String    @default("single") @map("filing_status")
  federalWithholdingAllowances Int       @default(0) @map("federal_withholding_allowances")
  stateWithholdingAllowances   Int       @default(0) @map("state_withholding_allowances")
  additionalWithholding        Decimal   @default(0) @map("additional_withholding") @db.Decimal(10, 2)
  createdAt                    DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt                    DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt                    DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, employeeId], map: "employee_tax_info_employee_unique")
  @@map("employee_tax_info")
}

model EmployeePin {
  tenant_id     String    @db.Uuid
  id            String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id   String    @unique @map("employee_id") @db.Uuid
  pin_encrypted String    @map("pin_encrypted")
  pin_hint      String?   @map("pin_hint")
  created_at    DateTime  @default(now()) @db.Timestamptz(6)
  updated_at    DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at    DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@map("employee_pins")
}

model EmployeeLocation {
  tenantId   String    @map("tenant_id") @db.Uuid
  employeeId String    @map("employee_id") @db.Uuid
  locationId String    @map("location_id") @db.Uuid
  isPrimary  Boolean   @default(false) @map("is_primary")
  createdAt  DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updated_at DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at DateTime? @db.Timestamptz(6)

  @@id([tenantId, employeeId, locationId])
  @@index([employeeId], map: "employee_locations_employee_idx")
  @@index([locationId], map: "employee_locations_location_idx")
  @@map("employee_locations")
}

model employee_seniority {
  tenant_id    String    @db.Uuid
  id           String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  employee_id  String    @db.Uuid
  level        String // e.g., "junior", "mid", "senior", "lead", "executive"
  rank         Int // Numeric rank for sorting (1 = lowest, higher = more senior)
  effective_at DateTime  @db.Timestamptz(6)
  created_at   DateTime  @default(now()) @db.Timestamptz(6)
  updated_at   DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at   DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@index([employee_id], map: "employee_seniority_employee_idx")
  @@index([tenant_id, employee_id, effective_at(sort: Desc)], map: "employee_seniority_current_idx")
}

model employee_skills {
  tenant_id         String    @db.Uuid
  employee_id       String    @db.Uuid
  skill_id          String    @db.Uuid
  proficiency_level Int       @default(1) @db.SmallInt
  verified_by       String?   @db.Uuid
  verified_at       DateTime? @db.Timestamptz(6)
  created_at        DateTime  @default(now()) @db.Timestamptz(6)
  updated_at        DateTime  @default(now()) @db.Timestamptz(6)

  @@id([tenant_id, employee_id, skill_id])
}

model skills {
  tenant_id   String    @db.Uuid
  id          String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  name        String
  category    String?
  description String?
  created_at  DateTime  @default(now()) @db.Timestamptz(6)
  updated_at  DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at  DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
  @@unique([tenant_id, name], map: "skills_name_unique")
}

model UserPreference {
  tenantId        String    @map("tenant_id") @db.Uuid
  id              String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  userId          String    @map("user_id") @db.Uuid
  preferenceKey   String    @map("preference_key")
  preferenceValue Json      @map("preference_value")
  category        String?
  notes           String?
  createdAt       DateTime  @default(now()) @map("created_at") @db.Timestamptz(6)
  updatedAt       DateTime  @default(now()) @updatedAt @map("updated_at") @db.Timestamptz(6)
  deletedAt       DateTime? @map("deleted_at") @db.Timestamptz(6)

  @@id([tenantId, id])
  @@unique([tenantId, userId, preferenceKey, category])
  @@index([tenantId, category])
  @@map("user_preferences")
}

model admin_users {
  tenant_id             String    @db.Uuid
  id                    String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  auth_user_id          String    @db.Uuid
  role_id               String    @db.Uuid
  is_active             Boolean   @default(true)
  last_login            DateTime? @db.Timestamptz(6)
  last_failed_login     DateTime? @db.Timestamptz(6)
  failed_login_attempts Int?      @default(0) @db.SmallInt
  locked_until          DateTime? @db.Timestamptz(6)
  two_factor_enabled    Boolean   @default(false)
  two_factor_secret     String?
  login_ip              String?
  created_at            DateTime  @default(now()) @db.Timestamptz(6)
  updated_at            DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at            DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
}

model admin_roles {
  tenant_id   String     @db.Uuid
  id          String     @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  role_name   admin_role
  description String?
  permissions Json?      @default("[]")
  created_at  DateTime   @default(now()) @db.Timestamptz(6)
  updated_at  DateTime   @default(now()) @db.Timestamptz(6)
  deleted_at  DateTime?  @db.Timestamptz(6)

  @@id([tenant_id, id])
}

model admin_permissions {
  tenant_id       String    @db.Uuid
  id              String    @default(dbgenerated("gen_random_uuid()")) @db.Uuid
  permission_name String
  resource        String
  action          String
  description     String
  created_at      DateTime  @default(now()) @db.Timestamptz(6)
  updated_at      DateTime  @default(now()) @db.Timestamptz(6)
  deleted_at      DateTime? @db.Timestamptz(6)

  @@id([tenant_id, id])
}
