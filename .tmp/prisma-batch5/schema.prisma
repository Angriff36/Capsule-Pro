// Auto-generated by @manifest/projection-prisma
// DO NOT EDIT — regenerate with the projection.

model PurchaseRequisition {
  id String @id
  tenantId String
  requisitionNumber String
  requestedBy String? @default("")
  requestDate DateTime?
  requiredBy DateTime?
  locationId String? @default("")
  department String? @default("")
  justification String? @default("")
  status String? @default("draft")
  subtotal Decimal? @default(0) @db.Decimal(12, 2)
  estimatedTax Decimal? @default(0) @db.Decimal(12, 2)
  estimatedShipping Decimal? @default(0) @db.Decimal(12, 2)
  estimatedTotal Decimal? @default(0) @db.Decimal(12, 2)
  approvedBy String? @default("")
  approvedAt DateTime?
  managerApprovalBy String? @default("")
  managerApprovalAt DateTime?
  financeApprovalBy String? @default("")
  financeApprovalAt DateTime?
  convertedToPoId String? @default("")
  convertedAt DateTime?
  rejectionReason String? @default("")
  notes String? @default("")
  submittedAt DateTime?
  itemCategory String? @default("")
  priority String? @default("normal")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?
  itemCount Int? @default(0)

  items PurchaseRequisitionItem[]
}

model PurchaseRequisitionItem {
  id String @id
  tenantId String
  requisitionId String
  itemId String
  itemName String? @default("")
  quantityRequested Decimal? @default(0) @db.Decimal(12, 2)
  unitId Int? @default(0)
  estimatedUnitCost Decimal? @default(0) @db.Decimal(12, 2)
  estimatedTotalCost Decimal? @default(0) @db.Decimal(12, 2)
  suggestedVendorId String? @default("")
  suggestedVendorName String? @default("")
  specifications String? @default("")
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  requisition PurchaseRequisition @relation(fields: [requisitionId], references: [id])
}

model PurchaseOrder {
  id String @id
  tenantId String
  poNumber String
  vendorId String
  locationId String
  orderDate DateTime?
  expectedDeliveryDate DateTime?
  actualDeliveryDate DateTime?
  status String? @default("draft")
  subtotal Decimal? @default(0) @db.Decimal(12, 2)
  taxAmount Decimal? @default(0) @db.Decimal(12, 2)
  shippingAmount Decimal? @default(0) @db.Decimal(12, 2)
  total Decimal? @default(0) @db.Decimal(12, 2)
  notes String? @default("")
  submittedBy String? @default("")
  submittedAt DateTime?
  receivedBy String? @default("")
  receivedAt DateTime?
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?
  itemCount Int? @default(0)
  items String? @default("")

  items PurchaseOrderItem[]
}

model PurchaseOrderItem {
  id String @id
  tenantId String
  purchaseOrderId String
  itemId String
  quantityOrdered Decimal? @default(0) @db.Decimal(12, 2)
  quantityReceived Decimal? @default(0) @db.Decimal(12, 2)
  unitId Int? @default(0)
  unitCost Decimal? @default(0) @db.Decimal(12, 2)
  totalCost Decimal? @default(0) @db.Decimal(12, 2)
  qualityStatus String? @default("pending")
  discrepancyType String? @default("")
  discrepancyAmount Decimal? @default(0) @db.Decimal(12, 2)
  notes String? @default("")
  createdAt DateTime?
  updatedAt DateTime?
  deletedAt DateTime?

  purchaseOrder PurchaseOrder @relation(fields: [purchaseOrderId], references: [id])
}
