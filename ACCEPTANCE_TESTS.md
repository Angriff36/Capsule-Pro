<!--
Completion contract. Seeded from the success criteria of manifest/NATIVE-REWRITE-PLAN.md;
PLAN mode maintains it from specs/manifest-native/*. BUILD mode gates completion on this
file: every non-RETIRED criterion needs its real check passing — no deleting, skipping,
or weakening checks to fake a pass.

Ids are stable: assigned once, never renumbered. Status: PENDING | PASS | RETIRED.
Counts show plan-time → re-verified 2026-07-11 (bare rg over manifest/source/*.manifest,
reference .bak/.example excluded). Re-count every iteration — never trust these.
-->

| Id | Spec | Outcome to verify (WHAT, not HOW) | Required check | Status |
| ------ | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ------- |
| AC-001 | specs/manifest-native/native-rewrite.md | WS0: zero `uuid … = ""` defaults remain in `manifest/source` (188 plan-time → **8 / 6 files** on 2026-07-12; IR-only sweep of nullable/param-seeded fields COMPLETE — incl. inventory `createdById`/`countedById` trusted-param port 2026-07-12. 2026-07-11 accounting sub-batch: dropped 2 dead `collectionCaseId` defaults on no-create-command entities (CollectionAction, CollectionPaymentPlan — audit skips them); the other 5 accounting body-seeded FK fields (CollectionCase.{eventId,clientId}, Payment.{eventId,clientId}, Invoice.clientId) are BLOCKED by the schema-drift audit's body-seed blind spot (NEEDS-RYAN — audit only covers create-param / property-default / create-mutate-target, not body-seed). **2026-07-12 kitchen+logistics nullable-migration batch (commit ca25e2aac): PrepTask.locationId, QACorrectiveAction.{relatedCheckId,escalatedTo}, LogisticsRoute.{vehicleId,driverId} → uuid?; migration 20260712074235 DROP-NOT-NULL ×5 awaiting human deploy. The 8 remaining are all fork/defer/body-seed-blocked (api-key createdByUserId DEFER, admin-chat-participant userId FORK, 5 accounting body-seeded FK BLOCKED, event-import-workflow eventId DEFER).**) | `rg 'uuid.*= ""' manifest/source -g '*.manifest'` → 0 matches | PENDING |
| AC-002 | specs/manifest-native/native-rewrite.md | WS0: every entity touched by a uuid-default fix has a passing Postgres-backed create smoke-test (in-memory stores hide the bug). TrainingAssignment pilot already proven. | per-entity create smoke-test against Postgres | PENDING |
| AC-003 | specs/manifest-native/native-rewrite.md | WS1: zero `user.role in [...]` literals (464 plan-time → **461** on 2026-07-11); each replacement capability resolves the identical role set. P1 RESOLVED (snake_case roles). ⛔ **2026-07-12 FORK (NEEDS-RYAN): literals are role SUBSETS, capabilities are subtree-CLOSURES — dominant `{"admin","manager"}` (~90 sites) → `manageAccess` is a 16-role widening the plan's own diff-guard rejects; no literal exactly equals any closure. WS1 cannot proceed until Ryan decides widen-to-closure vs preserve-exactly; until then NO site may be migrated (silent widening = privilege escalation).** Details: `canonical/unresolved/manifest.ws1.capability-closure-fork.md`. | `rg 'user\.role in \[' manifest/source -g '*.manifest'` → 0 + effectivePermissions IR-diff script per batch | PENDING |
| AC-004 | specs/manifest-native/native-rewrite.md | WS7: zero entity `property (required )?status: string` on true closed sets; each domain batch fully migrated to `enum` with its redundant `validStatus` constraint deleted. **✓ 2026-07-12 opus reconciliation DONE** — verified via bare `rg` over `manifest/source/**/*.manifest`. Corrected figures: **27 `enum` declarations** in source (26 used as entity-status types + `RecipeStepPhase`); **26 entity status properties already retyped to enum** (+sales 2026-07-12 commit d53c212dc: LeadStatus/CallPlanningSessionStatus/ClientInteractionStatus/ProposalDraftStatus/DocumentStatus; +EquipmentStatus +MaintenanceWorkOrderStatus commit de08278d8); **71 entity `property (required )?status: string` remain** (prior "76" — sales' 5 fields retyped 2026-07-12 commit d53c212dc). Of the 71, **63 are ENUM-CANDIDATES** (carry a `validStatus` → true closed set) and **8 are FREE-FORM** (no `validStatus`: `PreventiveMaintenanceSchedule`, `CollectionCase`/`CollectionAction`/`CollectionPaymentPlan`, `Invoice`, `PaymentMethod`, `Payment`, `InventoryForecast`). **79 `validStatus` constraints** remain. **P2/P3 preflights CLEARED 2026-07-12**: pilot `KnowledgeBaseEntryStatus` enum ran end-to-end green (commit `99a4fd631`); enum-typed status is runtime-proven FSM-safe by new `knowledge-base-entry-status-fsm.test.ts` (5/5 green, deploy-free). Pilot migration `20260712035457_kb_entry_status_enum` **awaiting human deploy** (`pnpm db:deploy && pnpm db:check`); AC stays PENDING until deploy + domain batches complete. **2026-07-12 platform batch DONE**: `DocumentVersionStatus` + `VersionApprovalStatus` enums (migration `20260712055817_platform_status_enums` awaiting deploy — see IMPLEMENTATION_PLAN WS7 for the DROP/ADD→USING-cast deploy caveat). **2026-07-12 sales batch DONE** (commit d53c212dc): `LeadStatus` + `CallPlanningSessionStatus` + `ClientInteractionStatus` + `ProposalDraftStatus` + `DocumentStatus` enums, all `validStatus` deleted, migration `20260712155449_sales_status_enums` hand-rewritten to data-preserving `ALTER COLUMN TYPE ... USING` casts (REVIEW-2), awaiting human deploy; gates: manifest:ci green, `pnpm check` exit 0, `db:check` exit 0, `migrate status` clean (41 migrations). ⚠ **REVIEW-2 REGRESSED**: the 3 prior enum migrations (kb/platform/equipment) are STILL DROP+ADD at HEAD — commit `a822772c7` reverted the USING-cast rewrites + re-introduced the empty `20260712085252` dup; see IMPLEMENTATION_PLAN REVIEW-2-REGRESSED. All 4 enum migrations deploy together via one `db:deploy`, so none deploys to a non-empty DB until that is fixed. **2026-07-12 equipment batch DONE** (commit de08278d8): `EquipmentStatus` + `MaintenanceWorkOrderStatus` enums, both `validStatus` deleted, migration `20260712084329` awaiting deploy (same DROP/ADD→USING-cast caveat). **DB state (NOT a drift emergency):** the original ~17 status enums were DB-aligned by migrations `20260705063000_enum_column_alignment` + `20260705120000_enum_namespace_and_insert_unblock` (they convert live columns → pg enum types). Only **5 enums have pending-deploy migrations**: `20260712035457_kb_entry_status_enum` (kb pilot) + `20260712055817_platform_status_enums` (DocumentVersionStatus + VersionApprovalStatus) + `20260712084329_equipment_status_enums` (EquipmentStatus + MaintenanceWorkOrderStatus) — all **awaiting human deploy** (`pnpm db:deploy && pnpm db:check`). Per-domain enum-candidate vs free-form breakdown + recommended next batch (operations/facilities + procurement + logistics): see `IMPLEMENTATION_PLAN.md` WS7. **NEEDS-RYAN flags:** (1) `Invoice.status`/`Payment.status` are free-form (no `validStatus`) yet have clear de-facto lifecycles — decide enumerate-vs-leave before migrating; (2) UPPERCASE-vocab convention (time-off-request, revenue-recognition ×2, collections ×3, invoice, payment, payment-method) — enum members must match the DB's current casing or a data-migration lowercases them first; (3) non-empty-table DROP/ADD→USING-cast deploy caveat applies per prod batch (already hit on platform) | `rg 'property (required )?status: string' manifest/source -g '*.manifest'` → 0 (entity props only; the old `'status: string'` both over-counts command params AND misses `required status` variants) + `pnpm manifest:ci` green | PENDING |
| AC-005 | specs/manifest-native/native-rewrite.md | WS3: every portable middleware ported to native reaction/fanOut/count with its middleware wiring deleted and its test ported and passing — never both live at once (0 `fanOut` in source on 2026-07-11) | ported reaction conformance tests + `manifest-runtime-factory.ts` wiring count reduced | PENDING |
| AC-006 | specs/manifest-native/native-rewrite.md | WS5: 10/10 `apps/api/vercel.json` crons declared as `schedule` (0 native `schedule` in source on 2026-07-11), generated route proven firing before any hand route deleted | route-drift audit + manual cron trigger test per migrated entry | PENDING |
| AC-007 | specs/manifest-native/native-rewrite.md | `pnpm manifest:ci` green at every commit; no new governance-allowlist entries added | `pnpm manifest:ci` | PENDING |
| AC-008 | specs/manifest-native/native-rewrite.md | Every Phase-3 / fork-gated item (WS6, WS9-if-FK, WS11, WS12–WS14) has a written NEEDS-RYAN proposal instead of a silent implementation | review of IMPLEMENTATION_PLAN.md / canonical/ entries | PENDING |
