<!--
Completion contract. Seeded from the success criteria of manifest/NATIVE-REWRITE-PLAN.md;
PLAN mode maintains it from specs/manifest-native/*. BUILD mode gates completion on this
file: every non-RETIRED criterion needs its real check passing — no deleting, skipping,
or weakening checks to fake a pass.

Ids are stable: assigned once, never renumbered. Status: PENDING | PASS | RETIRED.
Counts show plan-time → re-verified 2026-07-11 (bare rg over manifest/source/*.manifest,
reference .bak/.example excluded). Re-count every iteration — never trust these.
-->

| Id | Spec | Outcome to verify (WHAT, not HOW) | Required check | Status |
| ------ | ---------------------------------------- | ------------------------------------------------------------------------------------------------------------ | ------------------------------------------------------------------------------- | ------- |
| AC-001 | specs/manifest-native/native-rewrite.md | WS0: zero `uuid … = ""` defaults remain in `manifest/source` (188 plan-time → **13 / 9 files** on 2026-07-12; IR-only sweep of nullable/param-seeded fields COMPLETE — incl. inventory `createdById`/`countedById` trusted-param port 2026-07-12. 2026-07-11 accounting sub-batch: dropped 2 dead `collectionCaseId` defaults on no-create-command entities (CollectionAction, CollectionPaymentPlan — audit skips them); the other 5 accounting body-seeded FK fields (CollectionCase.{eventId,clientId}, Payment.{eventId,clientId}, Invoice.clientId) are BLOCKED by the schema-drift audit's body-seed blind spot (NEEDS-RYAN — audit only covers create-param / property-default / create-mutate-target, not body-seed). All 13 remaining are required DEFER cases needing schema/fork decisions) | `rg 'uuid.*= ""' manifest/source -g '*.manifest'` → 0 matches | PENDING |
| AC-002 | specs/manifest-native/native-rewrite.md | WS0: every entity touched by a uuid-default fix has a passing Postgres-backed create smoke-test (in-memory stores hide the bug). TrainingAssignment pilot already proven. | per-entity create smoke-test against Postgres | PENDING |
| AC-003 | specs/manifest-native/native-rewrite.md | WS1: zero `user.role in [...]` literals (464 plan-time → **461** on 2026-07-11); each replacement capability resolves the identical role set. P1 RESOLVED (snake_case roles). ⛔ **2026-07-12 FORK (NEEDS-RYAN): literals are role SUBSETS, capabilities are subtree-CLOSURES — dominant `{"admin","manager"}` (~90 sites) → `manageAccess` is a 16-role widening the plan's own diff-guard rejects; no literal exactly equals any closure. WS1 cannot proceed until Ryan decides widen-to-closure vs preserve-exactly; until then NO site may be migrated (silent widening = privilege escalation).** Details: `canonical/unresolved/manifest.ws1.capability-closure-fork.md`. | `rg 'user\.role in \[' manifest/source -g '*.manifest'` → 0 + effectivePermissions IR-diff script per batch | PENDING |
| AC-004 | specs/manifest-native/native-rewrite.md | WS7: zero `status: string` on true closed sets (82 plan-time; **140 raw incl. params / 92 `validStatus` constraints** on 2026-07-11); each domain batch fully migrated to `enum` with its redundant `validStatus` constraint deleted. **P2/P3 preflights CLEARED 2026-07-12**: pilot `KnowledgeBaseEntryStatus` enum ran end-to-end green (commit `99a4fd631`); enum-typed status is runtime-proven FSM-safe by new `knowledge-base-entry-status-fsm.test.ts` (5/5 green, deploy-free). Pilot migration `20260712035457_kb_entry_status_enum` **awaiting human deploy** (`pnpm db:deploy && pnpm db:check`); AC stays PENDING until deploy + domain batches complete. **2026-07-12 platform batch DONE**: `DocumentVersionStatus` + `VersionApprovalStatus` enums (migration `20260712055817_platform_status_enums` awaiting deploy — see IMPLEMENTATION_PLAN WS7 for the DROP/ADD→USING-cast deploy caveat). **⚠ WS7 docs stale repo-wide** — bare `rg 'enum ' manifest/source` shows ~17 `// U7:` status enums exist beyond the pilot (BudgetStatus, AdminTaskStatus, PayrollStatus×2, WorkOrderStatus, ShipmentStatus, ProposalStatus, DealStatus, ScheduleStatus, PrepListStatus, MenuStatus, KitchenTaskStatus, PrepTaskStatus, QACheckStatus, PrepTaskPlanWorkflowStatus, RecipeVersionStatus, RecipeStepPhase); the counts/checkboxes above are UNVERIFIED pending an opus reconciliation audit. The raw `status: string` count also mixes in command params + event fields + AGENTS.md doc files — the true closed-set scope is entity `property status: string` only | `rg 'status: string' manifest/source -g '*.manifest'` audit + `pnpm manifest:ci` green | PENDING |
| AC-005 | specs/manifest-native/native-rewrite.md | WS3: every portable middleware ported to native reaction/fanOut/count with its middleware wiring deleted and its test ported and passing — never both live at once (0 `fanOut` in source on 2026-07-11) | ported reaction conformance tests + `manifest-runtime-factory.ts` wiring count reduced | PENDING |
| AC-006 | specs/manifest-native/native-rewrite.md | WS5: 10/10 `apps/api/vercel.json` crons declared as `schedule` (0 native `schedule` in source on 2026-07-11), generated route proven firing before any hand route deleted | route-drift audit + manual cron trigger test per migrated entry | PENDING |
| AC-007 | specs/manifest-native/native-rewrite.md | `pnpm manifest:ci` green at every commit; no new governance-allowlist entries added | `pnpm manifest:ci` | PENDING |
| AC-008 | specs/manifest-native/native-rewrite.md | Every Phase-3 / fork-gated item (WS6, WS9-if-FK, WS11, WS12–WS14) has a written NEEDS-RYAN proposal instead of a silent implementation | review of IMPLEMENTATION_PLAN.md / canonical/ entries | PENDING |
